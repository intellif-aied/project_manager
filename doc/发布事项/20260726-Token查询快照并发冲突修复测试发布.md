# 2026-07-26 Token 查询快照并发冲突修复测试发布

## 发布信息

- 日期：2026-07-26
- 环境：测试服
- 源码版本：`f7df10c`
- 改动范围：Token Analytics 查询快照事务、有限重试、稳定错误响应及测试
- 生产环境：未涉及

## 部署组件

| 组件 | 部署结果 | 版本 |
| --- | --- | --- |
| API | 已部署 | `sha256:1222b31fb6404e219fa0bcfdc2479a3edb5ea73c0494adf48fc21b1bc01f753c` |
| Web | 未部署、未重启 | 保持原版本 |
| DB | 无迁移、未重启 | 保持原版本 |
| MinIO | 未重启 | 保持原版本 |
| CLI | 无改动、未发布 | 保持原版本 |

替换前 API 镜像：`sha256:823fff366100e80690eb76ab32b8ced6f631335acbba4586b3a7502e705464ac`。

## 构建与部署

执行：

```text
cd api && go test ./... -count=1
docker compose up -d --build --no-deps api
```

结果：后端全量测试通过，API 镜像构建成功，仅 API 容器被替换。

## 健康检查

- API 容器：运行中
- `GET http://192.168.14.157:18090/health`：HTTP 200，返回 `{"status":"ok"}`
- API 启动日志：migration 完成，MinIO、Token Rollup、Digest 和 Report Run 服务正常启动
- 部署后日志：未发现 `40001`、`could not serialize`、`TOKEN_SNAPSHOT_BUSY` 或 HTTP 500

## 真实流程验收

使用测试服 director 账号 `t02`，查询日期为 2026-07-24 至 2026-07-26。

1. `/api/v1/auth/me` 返回 HTTP 200，身份和角色正确。
2. 同时发起 24 个管理 Summary，轮流覆盖全局日期范围、当天、测试部门和小组 A：24/24 返回 HTTP 200。
3. Summary 响应生成有效 `query_snapshot_token`。
4. Trends 复用该 Token：HTTP 200。
5. Rankings 按用户分组复用该 Token：HTTP 200。
6. Sessions 分页复用该 Token：HTTP 200。
7. Summary 返回 `data_freshness=ready`，Rollup 与 Session 数量均为 13。

## 提交与发布结论

- 修复提交已推送到 `origin/main`：`f7df10c fix(tokens): prevent snapshot serialization failures`
- 测试服真实并发和快照复用链路通过。
- 尚未发布生产。
