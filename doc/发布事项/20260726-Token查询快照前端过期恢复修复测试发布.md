# 2026-07-26 Token 查询快照前端过期恢复修复测试发布

## 发布信息

- 日期：2026-07-26
- 环境：测试服
- 源码版本：`7fe76cc`
- 改动范围：Token Analytics 快照过期后的前端刷新顺序、错误提示边界及契约测试
- 生产环境：未涉及

## 部署组件

| 组件 | 部署结果 | 版本 |
| --- | --- | --- |
| Web | 已部署 | `sha256:6f5b2d986d056339ba50e76c72ca8a3ad760b2af0f5d6c85815ca152e14bddcc` |
| API | 未部署、未重启 | 保持原版本 |
| DB | 无迁移、未重启 | 保持原版本 |
| MinIO | 未重启 | 保持原版本 |
| CLI | 无改动、未发布 | 保持原版本 |

## 构建与部署

执行：

```text
pnpm test
pnpm lint
pnpm typecheck
pnpm build
docker compose up -d --build --no-deps web
```

结果：

- 全部前端契约测试通过；
- TypeScript 类型检查和生产构建通过；
- 本次改动文件 ESLint 为 0 error，存在 2 条原有 Hook warning；
- 全量 ESLint 被未改动的 `src/layouts/HelpCenter/HelpCenter.tsx:120` 既有 `react-hooks/set-state-in-effect` 错误阻挡；
- Web 镜像构建成功，仅 Web 容器被替换。

## 健康检查

- Web 容器：运行中，RestartCount 为 0；
- `HEAD http://192.168.14.157:13000/`：HTTP 200；
- API、DB、MinIO 未重启，运行时长保持不变。

## 真实浏览器验收

使用测试服 director 账号 `t02`，通过无头 Chromium 打开 `/token-analytics`。

1. 初次 Summary、Rankings 等真实接口返回 HTTP 200；
2. 浏览器时钟推进 16 分钟并触发页面恢复；
3. 恢复时旧 `query_snapshot_token` 的子查询请求数为 0；
4. 前端先刷新 Summary，再使用新 token 发起 6 个子查询；
5. 注入一次与后端一致的 `QUERY_SNAPSHOT_EXPIRED` HTTP 410；
6. 前端自动重新获取 Summary 并恢复子查询；
7. 页面未显示底层 `query snapshot expired; restart from summary` 错误。

## 提交与发布结论

- 修复提交已推送到 `origin/main`：`7fe76cc fix(tokens): refresh expired snapshots without stale requests`；
- 测试服 Web 已更新，页面恢复和 410 兜底链路均通过；
- 尚未发布生产。
