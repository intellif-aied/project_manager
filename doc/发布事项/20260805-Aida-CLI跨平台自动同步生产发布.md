# Aida CLI 跨平台自动同步生产发布

## 1. 发布元数据

| 项目 | 内容 |
| --- | --- |
| 发布编号 | `20260805-01-aida-cli-cross-platform-auto-sync` |
| 目标环境 | 生产 |
| 发布负责人 | Codex |
| 变更目标 | Windows、Apple Silicon Mac 支持自动同步；Windows 定时运行不再闪现控制台窗口；Claude/Codex 支持本机 Ignore |
| Git 基线 | `944247bfb5c486f776f1b85dd681cf763edc5ab4` |
| CLI 版本 | `0.1.27` |
| 开始/结束时间 | 2026-08-05 02:20 UTC / 02:49 UTC |

## 2. 完整范围清单

| 范围 | 本次状态 | 版本/提交/迁移 | 验收证据 | 影响与依赖 |
| --- | --- | --- | --- | --- |
| API/后台 | 不涉及 | 无代码或镜像变更 | 生产 `/health` 返回成功 | 未重启 |
| Web | 不涉及 | 无代码或镜像变更 | 容器 ID 保持不变 | 未重启 |
| PostgreSQL migration | 不涉及 | 无 migration | DB 容器保持 healthy | 无数据库备份需求 |
| 历史数据回填 | 不涉及 | 无 | 无写入 | 无 |
| 数据清理/删除 | 不涉及 | 无 | 未执行 | 无 |
| Report Skill | 不涉及 | 无 | 无依赖变更 | 无 |
| MCP/默认 Agent | 不涉及 | 无 | 无依赖变更 | 无 |
| CLI/安装包 | 发布 | `0.1.27` / `944247b` | 三平台产物及安装脚本回下载 SHA256 全部通过 | Windows、Apple Silicon Mac、Linux |
| MinIO/对象存储 | 发布 | `statics-live/aida` 从 `0.1.26` 切换至 `0.1.27` | 公网版本文件与完整校验通过 | 只替换 CLI 七个对象 |
| Digest/报告链路 | 不涉及 | 无 | 无代码或配置变更 | 无 |
| 文档/配置 | 发布 | 测试版本后缀规则及本记录 | 已提交发布手册更新 | 不修改生产配置 |

## 3. 构建与验收

- `go test ./... -count=1`：通过。
- `go vet ./...`：通过。
- Windows 测试03：定时上传 4 个 Session，prepare/chunk/finalize 全部返回 200，未出现控制台闪窗。
- 生产包由 `release-prod-dir` 构建，内置生产静态地址、公网 API 和内网 API。
- 公网七个文件按新 `SHA256SUMS.txt` 回下载校验全部通过。
- 公网 Linux 二进制输出 `aida 0.1.27`，版本发现文件返回 `0.1.27`。
- `SHA256SUMS.txt` SHA256：`1892de207092ca173d8fdd5afc3e4add20fa2e6acbb4b681cbb9a540bdf90eac`。

## 4. 生产状态

- API：`275ab32cf25b`，保持运行，未重启。
- Web：`281eccbbe901`，保持运行，未重启。
- DB：`fa736db37a85`，保持 healthy，未重启。
- MinIO：`1c958a2bcc90`，保持 healthy，未重启。
- 健康检查：`{"status":"ok"}`。

## 5. 备份与回退

- 原生产 CLI `0.1.26`：`/home/luoxian/aida/backups/aida-cli-before-0.1.27-20260805T024840Z`。
- 本次生产暂存：`/home/luoxian/aida/releases/aida-cli-0.1.27-20260805T024840Z`。
- 公网回下载验证：`/home/luoxian/aida/releases/aida-cli-0.1.27-20260805T024840Z-verify`。
- 回退时先恢复六个主体文件并校验，最后恢复 `aida-latest.txt`。已升级客户端不会自动降级，异常优先发布更高版本 forward fix。

## 6. 最终发布判定

```text
本次范围已完整列出：是
所有发布项均有证据：是
所有阻断项均为 0：是
生产可以发布：是
代码已提交并推送：是
AIDA CLI 0.1.27 已发布：是
数据库备份或变更：否
生产服务重启：否
最终状态：已发布
```
