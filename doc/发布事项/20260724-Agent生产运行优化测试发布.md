# Agent 生产运行优化测试发布记录

> 发布日期：2026-07-24
> 环境：14.157 测试服
> 状态：已发布 P0 修复；Aida 提交契约验证通过，真实 Agent 执行被平台基础设施故障阻断

## 1. 发布范围

| 项目 | 结论 |
| --- | --- |
| API | 已部署 Report Context、默认 Agent、MCP 工具集和报告写回优化 |
| Report Skill | 已发布 `100866/aida-report@1.0.47` |
| MCP | 沿用 `aida-report-mcp@report-v1`，未发布新版本 |
| Web | 不涉及，未部署 |
| Aida CLI | 不涉及，未发布 |
| 数据库迁移 | 不涉及 |

本次没有修改模型选择，也没有改变旧 Report Context 和自定义 Agent 的兼容路径。

## 2. 构建来源

- 工作树：`/home/intellif/dev/project_manager-worktrees/agent-runtime-optimization`
- 分支：`feat/agent-runtime-optimization`
- 源码提交：`f2455ad`
- API 镜像：`project_manager-api:latest`
- API 镜像 ID：`sha256:3c3dfc4dfb14934d9651993e86527f21d8321dc0c90f84d090392507f8f2f5ec`
- Skill：`100866/aida-report@1.0.47`
- Skill SHA256：`b74aac2ae262ef100f7936f9ad6331e083fea76b57e41ed18651e369fe22d6fc`
- Skill 基础版本：`1.0.45`

`1.0.46` 已存在且正文不同，本次没有覆盖，按不可变版本规则发布为 `1.0.47`。

## 3. 回退点

- API 回退镜像：`project_manager-api:rollback-20260724-08cf10d`
- API 回退镜像 ID：`sha256:67f3aba23c6adb71f0f33a63e656401c1291360bbbaa3375fc3c5eec8cd69beb`
- Skill 回退版本：`100866/aida-report@1.0.45`
- P0 `run_id` 修复回退镜像：`project_manager-api:rollback-before-runid-fix-20260724`
- P0 `run_id` 修复回退镜像 ID：`sha256:aafba2e9593d73e44b605a59de123b5d45c0c70a0b5579db4ffd9e6e7d96baae`

回退时把 API 镜像恢复为上述回退镜像，将测试环境 `MANAGED_AGENT_REPORT_SKILL_VERSION` 恢复为 `1.0.45`，然后只重建 `api` 服务。

## 4. 发布前验证

- `cd api && go test ./...`：通过。
- `cd api && go vet ./...`：通过。
- `python3 -m py_compile scripts/test_default_report_assets.py`：通过。
- `git diff --check`：通过。

## 5. 部署结果

- 只重建 `project_manager-api-1`；DB、MinIO、Web 未重建。
- API 启动日志显示迁移检查、MinIO、Digest Worker、Report Run Worker 和 HTTP 服务正常启动。
- `http://192.168.14.157:18090/health` 返回 `{"status":"ok"}`。
- 运行容器使用镜像 ID `sha256:3c3dfc4dfb14934d9651993e86527f21d8321dc0c90f84d090392507f8f2f5ec`。
- 运行环境已确认 `MANAGED_AGENT_REPORT_SKILL_OWNER=100866`、`MANAGED_AGENT_REPORT_SKILL_VERSION=1.0.47`。
- Registry 中 `1.0.47` 的 `SKILL.md` 与 API 生成正文逐字节一致，SHA256 如上。
- 未修改、推送、合并或发布生产环境。

## 6. 待人工验收

1. 新建个人日报 Run，确认默认 Agent 只接收 `run_id`，正常读取 Context 并写回。
2. 检查结果包含一段工作总结和完整正文，前端仍读取一个最终内容字段。
3. 验证内容按工作流聚合，不使用固定“重点工作”，Git 操作不单独形成工作项。
4. 验证团队、部门、周报结构仍由各自 `presentation_profile` 控制。
5. 检查 Agent 实际加载 `100866/aida-report@1.0.47`，并且托管报告 MCP 只暴露三个工具。

## 7. P0 `run_id` 修复与更新验收

### 7.1 修复内容

- 原因：Agent 平台在非空 `message` 存在时优先使用 `message`，不再渲染 Start Prompt。Aida 传递的“生成本次 Aida 报告”因此覆盖了含 `run_id` 的模板。
- 修复：Aida 构建最终 Agent 消息时直接写入 `/aida-report\nrun_id=<UUID>`。存在用户补充说明时只追加说明，不传报告类型、周期、目标或 Selection。
- 未修改 Agent 配置、Skill、模型、Web、CLI 和数据库。

### 7.2 自动化验证

- 新增默认消息、旧兜底消息和用户补充说明三类回归，均断言最终消息含当前 `run_id`。
- 断言旧架构的报告类型、周期、日历、目标、Selection 和 MCP 协议描述不会进入最终消息。
- `cd api && go test ./...`：通过。
- `cd api && go vet ./...`：通过。
- `git diff --check`：通过。

### 7.3 真实 Agent 闸门验收

- Aida Run：`fc1b8cfc-6dd8-4af0-ae4a-2d161231a74a`。
- Agent 平台 Session：`271a41d3-aa2e-47d4-a746-f623a98f8dce`。
- 实际首条消息精确为 `/aida-report\nrun_id=fc1b8cfc-6dd8-4af0-ae4a-2d161231a74a`；未携带旧架构参数。
- Agent、模型和 Skill 沿用现有配置：`aida-agent-djyzif23h6pp` / `GLM-5.2` / `aida-report@1.0.47`。
- Agent 平台完成 Sandbox、Skill 和 MCP 挂载后，597 秒内未产生任何执行事件（`event_cursor=0`），最终返回 `infrastructure_failure`。
- 因 Agent 平台执行引擎未进入 Skill/MCP 阶段，本轮无法验证 Context 读取和报告写回；该项未误报为通过。
- 按闸门规则未再创建第二条 Codex 来源 Run，避免重复消耗模型和平台容量。
- 证据目录：`tmp/runid-fix-acceptance-20260724-090214/`。
