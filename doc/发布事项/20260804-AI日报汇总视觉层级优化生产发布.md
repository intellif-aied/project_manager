# AI 日报汇总视觉层级优化生产发布

## 1. 发布元数据

| 项目 | 内容 |
| --- | --- |
| 发布编号 | `20260804-02-daily-summary-layout` |
| 目标环境 | Aida 生产服 |
| Git 基线 | `8c5fa1bec02b70f62211e5b3e6e39544ee001731` |
| Web 镜像 | `20260804-8c5fa1b-daily-summary-layout` |
| Web digest | `sha256:65da6a45850a1a9ea030d148219d283eb143e0d0d39bd190e4511656fe4fc7e5` |
| 变更目标 | 让 Leader 一眼看懂当天日报填写方式和 AI 日报修改情况 |

## 2. 发布范围

| 范围 | 本次状态 | 内容 |
| --- | --- | --- |
| Web | 发布 | 当日一句话摘要、两组比例数据、菜单文案和明细颜色 |
| API | 不涉及 | 保持 `20260804-2474d15-daily-report-leader`，容器未重启 |
| PostgreSQL | 不涉及 | 无 migration、无数据写入，容器未重启 |
| MinIO | 不涉及 | 容器未重启 |
| Skill/MCP/Agent | 不涉及 | 无版本或配置变更 |
| CLI | 不涉及 | 无构建或发布 |

## 3. 修改内容

- 顶部使用一句话汇总当天填写人数、AI 生成人数以及修改数量。
- 取消五个无层级的等宽统计格，改为“日报填写方式”和“AI日报修改情况”两组数据。
- “日报填写方式”使用较窄区域，“AI日报修改情况”作为主要信息区。
- 比例条直接表达 AI/手工填写及未修改/修改较少/修改较多的数量关系。
- 移除“未留存AI原稿”等技术状态，不在 Leader 页面分析采集异常。
- “未修改”改为中性色，不表达认可或质量判断。
- 菜单名称同步调整为“AI日报当日汇总”；明细对比功能保持不变。

## 4. 验证与发布结果

- ESLint：通过。
- TypeScript 检查：通过。
- 前端生产构建：通过。
- `git diff --check`：通过。
- 生产页面 `/admin/daily-report-value`：HTTP 200。
- 生产 Web 产物包含“AI日报修改情况”文案。
- 生产 `/health` 返回 `{"status":"ok"}`。
- API、DB、MinIO 容器 ID 保持不变；仅 Web 被替换。
- 发布窗口 Web 日志未发现错误。

## 5. 备份与回退

- 发布前备份：`/home/luoxian/aida/backups/daily-summary-layout-20260804T054134Z`。
- 原 Web 镜像：`20260804-2474d15-daily-report-leader`。
- 回退时恢复备份 `.env` 中的 Web 标签，只执行 `docker compose up -d --no-deps web`。

## 6. 最终判定

```text
本次范围已完整列出：是
所有发布项均有证据：是
所有阻断项均为 0：是
生产可以发布：是
最终状态：已发布
```
