# Aida 上传成功提示 0.1.20 测试发布记录

> 日期：2026-07-22
>
> 环境：14.157 测试服
>
> 状态：测试分发部署及真实 OpenClaw 重复同步验证完成

## 1. 源码与范围

- CLI 代码基线：`main` 中与 `81b5715` 一致的 CLI 代码，加本次隔离工作树修改；`81b5715..main` 在 `daemon/`、`VERSION`、`Makefile` 和安装脚本中无差异；
- CLI 版本：`0.1.20`；
- 修改范围：新增客户端同步成功提示；
- 不再向用户显示 `generation`、`chunks`、`finalized` 和 Token fixture 等内部协议信息；
- API 不重新部署，继续运行基于 `main@ed1c148` 构建的测试镜像；Web、DB、MinIO 容器和生产环境不涉及。

## 2. 构建与测试

| 项目 | 结果 |
| --- | --- |
| 输出回归测试 | 先失败后通过 |
| `cd daemon && go test ./... -count=1` | 通过 |
| `cd daemon && go vet ./...` | 通过 |
| `make release-test-dir` | 通过 |
| 包内 `sha256sum -c SHA256SUMS.txt` | 七项全部通过 |
| 包内 Linux 版本 | `aida 0.1.20` |
| 安装脚本地址 | 测试 CLI 和测试 API 地址正确 |

## 3. 测试分发

| 项目 | 结果 |
| --- | --- |
| 分发地址 | `http://192.168.14.157:9000/statics-live/aida` |
| 发布前版本 | `0.1.19` |
| 发布后版本 | `0.1.20` |
| 切换前 payload 校验 | 三平台二进制和两个安装脚本全部通过 |
| 最终下载校验 | 七项 SHA256 全部通过 |
| 最终 Linux 版本 | `aida 0.1.20` |
| 最终复验目录 | `/home/intellif/aida-release-verifications/20260722-0.1.20-final-ko1D7k` |

发布顺序为三个二进制、两个安装脚本、`SHA256SUMS.txt`，从测试 HTTP 地址校验通过后，最后将 `aida-latest.txt` 从 `0.1.19` 切换为 `0.1.20`。

## 4. 真实验证

- 按用户授权，将 14.157 当前账号已安装的 Aida 从 `0.1.19` 更新到 `0.1.20`；
- 对 `openclaw-cc2547d867b424f73a6ce1691100aa90` 执行重复同步，退出码为 `0`；
- 输出为“同步完成”和“共 1 个 openclaw Session 同步完成”，没有内部协议字段；
- 测试 API `/health` 返回成功，运行镜像 ID 为 `sha256:a872fd4cb4a45026dc2d81ba1395cd68e08cf4fd69ba787130e4c88037de371b`。

## 5. 发布边界

- 未提交、未推送、未合并；
- 未发布生产环境；
- 未重新部署 API、Web、DB 或 MinIO。
