# 团队上传统计口径优化测试发布

## 1. 发布元数据

| 字段 | 内容 |
| --- | --- |
| 发布编号 | 20260728-04-team-upload-counts |
| 目标环境 | 测试服 |
| 发布负责人 | Codex |
| 发布目标 | 团队上传同时展示上传组数与实际 Session 数 |
| Git 基线 | CLI：`f8750b1` 加当前未提交工作区 |
| 版本 | AIDA CLI：`0.1.24` |
| 时间 | 开始：2026-07-28 13:43 UTC；结束：2026-07-28 13:51 UTC |

## 2. 发布范围

| 范围 | 状态 | 内容 |
| --- | --- | --- |
| API/后台 | 不涉及 | API 协议未变更 |
| Web | 不涉及 | 本次 CLI 发布不替换 Web |
| AIDA CLI | 发布 | `0.1.24` 三平台产物、安装脚本、校验文件和版本发现文件 |
| PostgreSQL migration | 不涉及 | 无数据库结构变更 |
| 历史数据回填 | 不涉及 | 未执行 |
| 数据清理/删除 | 不涉及 | 未执行 |
| Report Skill | 不涉及 | 无依赖变更 |
| MCP/默认 Agent | 不涉及 | 无依赖变更 |
| MinIO/对象存储 | 发布 | `statics-live/aida` 更新为 `0.1.24` |
| Digest/报告链路 | 不涉及 | 无相关变更 |
| Token/Session | 仅核对 | 上传协议和归属规则保持不变 |
| 监控/配置/文档 | 发布 | 保存本次测试发布记录 |

## 3. 版本与代码证据

| 组件 | 证据 |
| --- | --- |
| 仓库 | 分支 `main`，HEAD `f8750b1`，包含当前未提交工作区 |
| CLI | `VERSION=0.1.24`；测试分发版本文件返回 `0.1.24` |
| 发布地址 | `http://192.168.14.157:9000/statics-live/aida` |

## 4. 变更项清单

| 变更项 | 文件/提交/版本 | 状态 | 验证命令或运行证据 | 影响范围 |
| --- | --- | --- | --- | --- |
| 上传前统计口径 | `daemon/device_client.go` | 已发布 | 显示本地 Session 数与上传组数 | 团队上传 CLI 输出 |
| 上传结果统计口径 | `daemon/device_client.go` | 已发布 | 成功、待配置、失败均显示组数与 Session 数 | 团队上传 CLI 输出 |
| 输出单元测试 | `daemon/team_sync_test.go` | 已增加 | `go test ./... -count=1` 通过 | CLI 自动化验证 |
| CLI 版本 | `VERSION` | 已发布 | 下载产物输出 `aida 0.1.24` | 测试 CLI 更新 |

## 5. 发布前检查

- [x] 工作区状态和 Git 基线已记录。
- [x] daemon 全量测试和 `go vet` 通过。
- [x] CLI 三平台产物构建完成。
- [x] 构建目录内 SHA256 校验通过。
- [x] 测试 API 与测试 CLI 地址已核对。
- [x] API、Web、数据库和生产环境不涉及。

## 6. 执行步骤

| 步骤 | 执行主机 | 操作/命令 | 预期结果 | 实际结果 |
| --- | --- | --- | --- | --- |
| 1 | 测试服 157 | `go test ./... -count=1` | daemon 测试通过 | 通过 |
| 2 | 测试服 157 | `go vet ./...` | 无静态检查错误 | 通过 |
| 3 | 测试服 157 | `make release-test-dir` | 生成 0.1.24 三平台产物 | 通过 |
| 4 | 测试服 157 | `sha256sum -c SHA256SUMS.txt` | 构建产物校验一致 | 通过 |
| 5 | 测试服 157 | 上传六个主体文件到 `statics-live/aida` | 主体文件更新 | 通过 |
| 6 | 测试服 157 | 回下载主体文件并校验 | SHA256 一致 | 通过 |
| 7 | 测试服 157 | 最后上传 `aida-latest.txt` | 版本发现切换到 0.1.24 | 通过 |
| 8 | 测试服 157 | 回下载全部七个文件复验 | 版本和 SHA256 一致 | 通过 |

## 7. 发布后验收

| 验收类别 | 用例 | 账号/数据 | 预期结果 | 实际结果 | 证据 |
| --- | --- | --- | --- | --- | --- |
| 自动化 | 团队上传统计格式 | 固定计数样例 | 同时展示组数和 Session 数 | 通过 | daemon 单元测试 |
| AIDA CLI | 三平台发布文件 | 0.1.24 | SHA256 全部一致 | 通过 | 回下载七文件校验 |
| AIDA CLI | Linux 版本 | 下载产物 | 输出 `aida 0.1.24` | 通过 | 版本命令 |
| Token/Session | 上传协议兼容 | 不涉及新数据 | 不改变归属和上传行为 | 通过 | 仅修改本地统计计数与文案 |
| API/接口 | 不涉及 | 不涉及 | 不涉及 | 不涉及 | 无 API 发布 |
| Web 浏览器 | 不涉及 | 不涉及 | 不涉及 | 不涉及 | 无 Web 发布 |

## 8. 回滚与停止条件

| 范围 | 回滚方式 |
| --- | --- |
| CLI | 使用 `/tmp/aida-024-final.h2Rvtv` 中已回下载的 0.1.23 七个文件，先恢复主体文件并校验，最后恢复版本发现文件 |
| API/Web/migration/数据 | 不涉及 |

停止条件：版本发现文件、二进制版本或 SHA256 任一不一致时停止发布。本次未触发。

## 9. 最终结果

```text
发布范围已完整列出：是
发布项执行结果已记录：是
发布后验收已完成：是
阻断项数量：0
最终状态：已发布
```
