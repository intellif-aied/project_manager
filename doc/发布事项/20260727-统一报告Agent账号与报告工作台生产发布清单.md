# 统一报告 Agent 账号与报告工作台生产发布清单

## 1. 发布信息

| 项目 | 内容 |
| --- | --- |
| 发布编号 | `20260727-02-report-agent-system-account-prod` |
| 目标环境 | Aida 生产服 `/home/luoxian/aida` |
| 发布状态 | 已发布 |
| 代码基线 | `main@9e11641`（功能代码截至 `ae1d1b4`） |
| 目标结果 | 报告统一使用专用 Agent 平台账号及模型额度运行，MCP 仍以当前 Aida 用户身份访问，同时上线统一报告工作台与日期展示优化。 |

## 2. 发布范围

| 项目 | 状态 | 内容 |
| --- | --- | --- |
| API | 发布 | 专用 Agent 平台客户端、报告模型配置、默认 Agent 修复及 Skill 调用约束 |
| Web | 发布 | 日报/周报统一工作台、隐藏“我的 Skill / 我的 MCP”、报告交互及日期展示优化 |
| 数据库迁移 | 不涉及 | 无表结构或数据迁移 |
| 历史数据回填 | 不涉及 | 无历史数据改写 |
| 数据清理 | 不涉及 | 无删除操作 |
| Report Skill | 仅核对 | Skill 内容未改；确认生产专用账号 `10086` 下使用当前不可变版本 |
| MCP / 默认 Agent | 发布 | MCP 协议与地址不变；更新默认 Agent 的账号、模型、共享额度和指令配置 |
| CLI | 不涉及 | 无 CLI 改动 |
| MinIO | 不涉及 | 无对象存储改动 |
| Digest / 报告链路 | 仅核对 | 发布后执行真实报告生成与回写验证 |
| 配置 | 发布 | 新增并显式设置 `MANAGED_AGENT_REPORT_MODEL_ID=deepseek-v4-flash`；生产 Skill owner 必须显式设置为 `10086` |

## 3. 已冻结验证证据

- API：`go test ./... -count=1` 通过。
- 默认 Agent 指令回归：`TestDefaultReportAgentInstructionsContainProtocolOnly` 通过。
- Web：报告工作流测试、定向 ESLint、TypeScript 类型检查通过，生产 Docker 镜像构建通过。
- 全量前端 lint 仍有既存无关错误：`src/layouts/HelpCenter/HelpCenter.tsx` 的 `react-hooks/set-state-in-effect`；不由本次变更引入。
- 并发验证：测试服 t03-t08 共 6 个账号同时生成日报，6/6 成功，耗时 43-54 秒，真实重叠约 42 秒。
- 修复后真实链路：Aida 用户 307，Run `da9637ae-29e3-4035-84fd-c9258e56fb0e`，Agent Session `03d9fcbe-79b2-46b1-8472-d860a5f1f897`，44.637 秒成功；首次工具调用为 `Skill(skill="aida-report", ...)`，无 `Unknown skill`，平台账号为 332，MCP 凭证元数据用户为 307，报告成功回写。
- 测试服 Agent：`aida-agent-dk98hc136bzm` Version 3，模型 `deepseek-v4-flash`，`share_model_access=true`，Skill `100866/aida-report@1.0.50`。

## 4. 发布前必须关闭的事项

- [x] 只读核对生产专用 Agent 平台账号 `10086`、凭证归属及可用状态，不记录明文 Token。
- [x] 通过真实运行确认生产账号 `10086` 可以使用 `deepseek-v4-flash`。
- [x] 记录生产当前 API/Web 镜像、Compose 配置、默认 Agent、Skill owner/version 与 MCP 地址。
- [x] 从 `9e11641` 构建并登记 API/Web 不可变镜像标签和 digest，未使用 `latest`。
- [x] 按生产手册备份 PostgreSQL、环境配置、Compose/Nginx 配置和当前镜像信息。
- [x] 使用生产验收账号 t03（UID 305）及既有生产验收 Session 完成冒烟测试。

以上事项未全部关闭前，不执行生产替换。

## 5. 生产配置目标

```dotenv
MANAGED_AGENT_TOKEN=<生产专用账号 10086 的凭证>
MANAGED_AGENT_REPORT_SKILL_OWNER=10086
MANAGED_AGENT_REPORT_SKILL_VERSION=<只读核对后的生产不可变版本>
MANAGED_AGENT_REPORT_MCP_URL=<生产现值>
MANAGED_AGENT_REPORT_MODEL_ID=deepseek-v4-flash
```

注意：仓库 Compose 的 Skill owner 默认值用于测试环境，生产必须通过 `.env` 显式写入 `10086`，不得依赖默认值。

## 6. 发布步骤

1. 在生产目录执行只读盘点，保存当前分支、提交、容器、镜像、健康状态和关键配置快照。
2. 完成 PostgreSQL、配置文件和当前镜像信息备份，并记录恢复命令及备份位置。
3. 构建并推送基于 `9e11641` 的 API/Web 不可变镜像，核对 digest。
4. 对比更新生产 `.env`，仅加入或修改本清单列出的 Agent 报告配置，不覆盖其他配置。
5. 替换 API 容器并检查健康状态和启动日志；本次不执行数据库迁移。
6. 使用生产专用账号修复或升级默认报告 Agent，核对 owner、模型、`share_model_access=true`、SkillRef、MCP 当前用户凭证槽及 Agent 指令。
7. 替换 Web 容器并检查健康状态、静态资源和 `/reports` 页面。
8. 使用约定的生产测试账号生成一份真实报告，确认：首个 Skill 参数正确、无 `Unknown skill`、平台额度属于专用账号、MCP 用户属于当前 Aida 用户、报告成功保存并可查看。
9. 观察 API/Web 日志和报告运行状态，确认无异常后结束发布窗口。

## 7. 回退方案

- API/Web：恢复发布前记录的不变镜像标签并重新创建对应容器。
- 配置：恢复发布前 `.env`、Compose/Nginx 备份，不手工拼接旧值。
- 默认 Agent：恢复发布前记录的 Agent 版本及模型、Skill、MCP、共享额度配置。
- 数据库：本次无结构和数据变更，正常回退不应操作数据库；仅在意外数据损坏时按备份恢复流程处理。

## 8. 停止条件

出现以下任一情况立即停止并回退：

- 生产专用账号、Token 归属或模型权限无法确认；
- API/Web 新容器健康检查失败或持续重启；
- 默认 Agent owner、模型、SkillRef、MCP 用户身份任一不符合目标；
- 真实报告运行出现身份串用、额度归属错误、Skill 加载错误或报告无法回写；
- 当前生产配置或镜像无法可靠恢复。

## 9. 当前结论

| 判断项 | 结论 |
| --- | --- |
| 发布范围是否完整 | 是 |
| 测试证据是否具备 | 是 |
| 生产前置事项是否全部关闭 | 是 |
| 当前是否可直接发布生产 | 已完成发布 |
| 是否已发布生产 | 是 |

## 10. 生产发布结果

- 发布时间：2026-07-27。
- 生产目录：`/home/luoxian/aida`，经 `192.168.14.157` 跳板发布至 `192.168.14.182`。
- 发布前 API：`20260726-f7df10c-token-snapshot`。
- 发布前 Web：`20260727-a66ffdb-report-actions`。
- 发布后 API：`20260727-9e11641-report-agent-v3`，digest `sha256:3a6e5f362bed4840797a6451818f491d55034d22ca6d3b530ed1228acaab4b18`。
- 发布后 Web：`20260727-9e11641-report-agent-v3`，digest `sha256:c891021a2158bfee88c94171ec90198c3aea95b2a2e173e9319e87c4daea4a87`。
- 备份目录：`/home/luoxian/aida/backups/20260727T-report-agent-9e11641`；包含约 2 GB PostgreSQL custom-format dump、环境和部署配置、旧容器/镜像与旧 Agent 配置，dump 已通过目录读取和 SHA256 校验。
- 未执行数据库 migration、历史数据回填、清理、Skill 新版本发布、CLI 或 MinIO 更新。
- 默认 Agent：`aida-agent-dk9dxzv7b0fe`，owner `10086`（平台 UID 331），模型 `deepseek-v4-flash`，`share_model_access=true`，Skill `10086/aida-report@1.0.16`，凭据槽 `AIDA_REPORT_MCP_AUTH`。
- 生产冒烟：t03（UID 305）使用 Session slice `85f28853-bf8d-4aa5-aed7-0106bbbc51c6` 生成 2026-07-27 日报；Run `8d0ad6e6-f3d9-492d-8eb4-730763d94e99`、Agent Session `479c1c23-9eeb-4f8e-a764-234c5c72df8e`，状态 `succeeded`，模型正确，无 `Unknown skill` 或标记错误，日报 `96f41c11-8b87-40ec-98fb-f48ce8775c14` 已保存。
- 发布后状态：db/minio 健康，API `/health` 正常，公网 `/` 与 `/reports` 均返回 200，观察窗口未发现 API/Web panic、fatal、error 或 5xx 日志。
