# 报告 Run 身份绑定修复生产发布

## 1. 发布元数据

| 项目 | 内容 |
| --- | --- |
| 发布编号 | `20260728-03-report-run-bound-prod` |
| 目标环境 | 生产，`192.168.14.182:/home/luoxian/aida`，经14.157跳板 |
| 变更目标 | 将报告Run身份绑定到签名MCP凭证，移除模型重复传递run_id的依赖，并补齐Brief有界重试和可见错误码 |
| Git基线 | `2c6442a6a5b3ca17b8caca71d27d4882c29a6ec8`，main与origin/main一致 |
| 发布时间 | 2026-07-28 UTC |

## 2. 完整范围清单

| 范围 | 状态 | 版本/证据 | 影响 |
| --- | --- | --- | --- |
| API | 已发布 | `20260728-2c6442a-report-run-bound`；Registry digest `sha256:85c4099ecf0a7e4c16c351c861161b5088f419e4b49ca3dc2c963936991f9a82` | 仅替换API容器 |
| Web | 不涉及 | 保持`20260727-9e11641-report-agent-v3`，容器ID未变化 | 无前端发布 |
| migration | 不涉及 | 最高版本仍为29 | 无数据库结构变化 |
| 历史数据/清理 | 不涉及 | 未执行回填、删除、DROP或TRUNCATE | 无 |
| Report Skill | 已发布 | `10086/aida-report@1.0.18`；SkillID`7738d058-ac92-4cf5-b3f3-986a01169f6e`；正文SHA256`2f1166d36ca4534c93667ca0c0b1450c874927521684989e6b4ac17c9afc0358` | 从1.0.17派生 |
| MCP/默认Agent | 已发布 | MCP保持`report-v1`；默认Agent实际加载Skill 1.0.18 | managed工具改为服务端绑定Run；legacy契约保留 |
| CLI/MinIO/Digest | 不涉及或仅核对 | CLI未构建；MinIO持续healthy；Digest V2正常启动 | 无变更 |

## 3. 备份与回退

本次无migration、无数据库结构变更、无数据处理，按用户确认不备份PostgreSQL。仅保存配置与容器回退信息：

- 回退资料：`/home/luoxian/aida/backups/20260728T0320-report-run-bound-2c6442a`；
- API回退镜像：`20260728-626763b-report-two-pass`，digest `sha256:a755a565d1bc888b2fdf2e8f747cfa3b9ba75e5b8674fce96c45aa7df7181fdb`；
- Skill回退版本：`10086/aida-report@1.0.17`；
- 回退时恢复`.env`中的API标签和Skill版本，仅重建API容器；Web、DB和MinIO不动。

## 4. 验收结果

- 发布前全量`go test ./...`、`go vet ./...`与`git diff --check`通过；
- API、DB、MinIO、Web均正常，`/health`、公网`/`和`/reports`返回200；
- 生产API实际镜像digest与Registry一致，Web容器未替换；
- production Skill公共Registry唯一，owner、slug、version和正文哈希一致；
- 真实Run `9e458475-aba1-47de-9a9e-40c515971258`为`succeeded/completed`；Agent Session `d3e3e054-a517-401c-853f-534b5a1bfa70`实际加载Skill 1.0.18；
- 工具顺序为Skill → Context → Brief → Brief → Result；Context参数为空，Brief只含`brief_json`，Result只含`brief_hash`、`summary`和`content`，均无`run_id`；
- 首次Brief返回可见`REPORT_BRIEF_INVALID`，第二次修正成功；Brief无效次数1、Result无效次数0；
- 日报`96f41c11-8b87-40ec-98fb-f48ce8775c14`归属用户305和当前Run，Brief与最终内容均已落库；
- 发布后API日志无5xx、panic、fatal或启动错误。

## 5. 最终判定

生产发布完成，未触发回退条件。数据库未备份且未发生结构或数据维护操作。
