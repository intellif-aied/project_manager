# 团队模式 Session 同步测试发布

## 1. 发布元数据

| 字段 | 内容 |
| --- | --- |
| 发布编号 | 20260728-01-team-session-sync |
| 目标环境 | 测试服 |
| 发布负责人 | Codex |
| 发布目标 | 在测试服提供团队目录映射、团队模式上传与自动同步，并发布 Aida CLI 0.1.22 供人工验收 |
| Git 基线 | API：`f8750b1` 加当前未提交工作区；Web：`f8750b1` 加当前未提交工作区；CLI：`f8750b1` 加当前未提交工作区 |
| 版本 | API：未单独编号；Web：`0.1.29`；AIDA CLI：`0.1.22` |
| 时间 | 开始：2026-07-28 10:14 UTC；结束：2026-07-28 10:26 UTC |

## 2. 发布范围

| 范围 | 状态 | 内容 |
| --- | --- | --- |
| API/后台 | 发布 | 测试服 API 镜像 `sha256:4c1be0b72932a547fb328fcf58cefd13fe9fd2229f6cb99dc76f7c05089ec376`，团队目录配置接口和团队上传分发逻辑 |
| Web | 发布 | 测试服 Web 镜像 `sha256:5f34e8fc584f4ee694feaa6da946ace37ba85b8b9ef351f979000e88b2fd564d`，My Token 页面团队目录配置 |
| AIDA CLI | 发布 | `0.1.22`，Linux amd64、macOS arm64、Windows amd64、安装脚本、校验文件和版本发现文件 |
| PostgreSQL migration | 发布 | `api/db/migrations/030_team_session_sync.sql`，由 API 启动迁移器幂等执行 |
| 历史数据回填 | 不涉及 | 本次不运行 `team-session-reassign`，留待单独授权执行 |
| 数据清理/删除 | 不涉及 | 本次没有数据清理或删除 |
| Report Skill | 不涉及 | 本次变更不依赖 Report Skill |
| MCP/默认 Agent | 不涉及 | 本次变更不依赖 MCP 或默认 Agent |
| MinIO/对象存储 | 发布 | `statics-live/aida` 下 AIDA CLI 0.1.22 七个发布文件 |
| Digest/报告链路 | 不涉及 | 本次没有 Digest 或报告链路变更 |
| 监控/配置/文档 | 发布 | 团队模式需求、开发方案和历史归属拆分说明；测试服 API/Web/CLI 状态核对 |

## 3. 版本与代码证据

| 组件 | 证据 |
| --- | --- |
| 仓库 | 分支 `main`，HEAD `f8750b1`，发布内容包含当前工作区未提交变更 |
| API | 容器镜像 `sha256:4c1be0b72932a547fb328fcf58cefd13fe9fd2229f6cb99dc76f7c05089ec376` |
| Web | 容器镜像 `sha256:5f34e8fc584f4ee694feaa6da946ace37ba85b8b9ef351f979000e88b2fd564d` |
| CLI | 根目录 `VERSION=0.1.22`；测试分发版本文件返回 `0.1.22` |

## 4. 变更项清单

| 变更项 | 文件/提交/版本 | 状态 | 验证命令或运行证据 | 影响范围 |
| --- | --- | --- | --- | --- |
| 团队目录映射 API | `api/handler/team_sync_path.go`、`api/main.go` | 已发布 | 未认证请求 `/api/v1/me/team-sync-paths` 返回 HTTP 401 | API |
| 团队上传归属分发 | `api/internal/sessionsync/` | 已发布 | `go test ./...` 通过 | API、Session 同步 |
| 团队同步表结构 | `030_team_session_sync.sql` | 已发布 | `schema_migrations` 查询返回 `30` | PostgreSQL |
| 团队目录配置界面 | `TokensPage.tsx`、`TokensPage.css`、`client.ts` | 已发布 | 定向 ESLint、typecheck、test、build 通过 | Web |
| 团队模式 CLI | `daemon/`、`VERSION=0.1.22` | 已发布 | daemon test、vet、三平台构建及 SHA256 通过 | AIDA CLI |
| 历史归属拆分工具 | `api/cmd/team-session-reassign/` | 未执行 | 仅随 API 源码构建，不运行命令 | 历史数据 |

## 5. 发布前检查

- [x] 工作区状态和发布 SHA 已记录；发布内容来自 `f8750b1` 加当前未提交工作区。
- [x] API/Web/CLI 构建结果已记录。
- [ ] migration 空库和生产数据副本验证不涉及本次测试服发布；迁移单测和测试服正向执行通过。
- [ ] 生产容器、镜像、配置和 migration 不涉及；本次未访问生产环境。
- [x] 测试服原有 db、minio 容器保持运行；API/Web 仅替换各自容器。
- [x] Skill/MCP/Agent 不涉及，已按变更范围核对。
- [x] 本次使用测试服地址和测试 CLI 分发地址。
- [x] 不涉及组件已完成依赖检查。
- [x] 测试服回滚边界和停止条件已填写。
- [ ] Web 全量 ESLint 未通过：既有无关文件 `web/src/layouts/HelpCenter/HelpCenter.tsx:120` 存在 `react-hooks/set-state-in-effect`；本次三个改动文件定向 ESLint 通过。

## 6. 执行步骤

| 步骤 | 执行主机 | 操作/命令 | 预期结果 | 实际结果 |
| --- | --- | --- | --- | --- |
| 1 | 测试服 157 | `cd api && go test ./...` | API 测试通过 | 通过 |
| 2 | 测试服 157 | `docker compose up -d --build --no-deps api` | 仅替换 API | API 已替换，db/minio/web 未重启 |
| 3 | 测试服 157 | 查询 `schema_migrations` 和 `/health` | migration 30、健康成功 | 返回 `30` 和 `{"status":"ok"}` |
| 4 | 测试服 157 | Web test、lint、typecheck、build | 完成发布前验证 | test/typecheck/build 和改动文件 lint 通过；全量 lint 存在一项既有无关错误 |
| 5 | 测试服 157 | `docker compose up -d --build --no-deps web` | 仅替换 Web | Web 已替换，HTTP 200 |
| 6 | 测试服 157 | `go test ./... -count=1`、`go vet ./...` | CLI 校验通过 | 通过 |
| 7 | 测试服 157 | `make release-test-dir` | 生成 CLI 0.1.22 七个文件 | 三平台产物和发布文件齐全 |
| 8 | 测试服 157 | 按固定顺序上传六个主体文件 | 先不改变版本发现 | 上传成功 |
| 9 | 测试服 157 | 从 HTTP 地址回下载并校验六个主体文件 | SHA256 全部匹配 | 全部通过，Linux 二进制输出 `aida 0.1.22` |
| 10 | 测试服 157 | 最后上传 `aida-latest.txt` | 更新版本发现为 0.1.22 | 上传成功 |
| 11 | 测试服 157 | 回下载七个文件并执行完整校验 | 七个文件一致 | SHA256 全部通过，版本为 `0.1.22` |

## 7. 发布后验收

| 验收类别 | 用例 | 账号/数据 | 预期结果 | 实际结果 | 证据 |
| --- | --- | --- | --- | --- | --- |
| 自动化 | API 全量测试 | 无业务数据写入 | 通过 | 通过 | `go test ./...` |
| 自动化 | Web 测试、类型检查、构建 | 无业务数据写入 | 通过 | 通过 | `pnpm test`、`pnpm typecheck`、`pnpm build` |
| API/接口 | 健康检查 | 无账号 | HTTP 200 | HTTP 200 | `/health` 返回 `{"status":"ok"}` |
| API/接口 | 团队目录接口认证 | 无 token | 拒绝访问 | HTTP 401 | `/api/v1/me/team-sync-paths` |
| AIDA CLI | 发布地址和产物 | 0.1.22 | 版本与 SHA 一致 | 通过 | 七个文件 `sha256sum -c` 全部 OK；Linux 输出 `aida 0.1.22` |
| Report Skill/MCP/Agent | 不涉及 | 不涉及 | 不涉及 | 不涉及 | 变更范围核对 |
| Digest/报告 | 不涉及 | 不涉及 | 不涉及 | 不涉及 | 变更范围核对 |
| Token/Session | migration 可用 | 测试数据库 | schema 版本 30 | 返回 `30` | `schema_migrations` 查询 |
| Web 浏览器 | 团队目录配置人工验收 | 用户测试账号 | 用户人工验证 | 待用户执行 | 测试 Web `http://192.168.14.157:13000/` |

## 8. 回滚与停止条件

| 范围 | 回滚方式 |
| --- | --- |
| API | 使用发布前代码重新构建并仅替换 API；migration 采用 forward fix，不执行 DROP、TRUNCATE 或删除数据 |
| Web | 使用发布前代码重新构建并仅替换 Web |
| CLI | 将 `statics-live/aida` 六个主体文件恢复为 0.1.21，校验后最后恢复 `aida-latest.txt` |
| Skill/MCP/Agent | 不涉及 |
| migration | 保留已执行的 030；异常时使用 forward fix |
| 数据回填 | 未执行，无需回滚 |
| 数据清理 | 未执行，无需回滚 |

停止条件：API 持续 5xx、migration 异常、数据对账不一致、CLI 版本或 SHA256 不一致时停止后续发布。本次未触发停止条件。

## 9. 最终结果

```text
发布范围已完整列出：是
发布项执行结果已记录：是
发布后验收已完成：否（团队模式业务流程待用户人工验收）
阻断项数量：0
最终状态：已发布
```
