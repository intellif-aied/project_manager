# 系统与个人报告 Agent 双通道生产发布清单

## 1. 发布元数据

| 字段 | 内容 |
| --- | --- |
| 发布编号 | `20260729-01-report-agent-dual-channel-prod` |
| 目标环境 | Aida 生产服，经 `192.168.14.157` 跳板进入 `192.168.14.182:/home/luoxian/aida` |
| 发布负责人 | 23:00 窗口实际执行人填写 |
| 发布目标 | 系统默认报告继续使用统一系统 Agent；用户明确选择个人 Agent 时保留个人模型、Prompt、Skill 和 MCP，并通过系统 Report MCP 写回报告。 |
| Git 基线 | API：`main@1b7298e` 加发布前阻断修复提交；Web：`main@1b7298e`；最终 SHA 在冻结版本后填写 |
| 版本 | API/Web 使用本次不可变镜像标签；Report Skill 仅核对 `10086/aida-report@1.0.20`；Report MCP URL 保持生产现值 |
| 计划时间 | 2026-07-29 23:00（Asia/Shanghai）；开始、结束时间由执行人填写 |
| 当前状态 | 未发布；等待阻断修复、测试服回归和 23:00 发布确认 |

## 2. 发布范围

| 范围 | 状态 | 版本/对象 | 影响与依赖 |
| --- | --- | --- | --- |
| API/后台 | 阻断 | `1b7298e` 加待提交修复 | 发布系统/个人 Agent 分流；发布前必须关闭第 5 节阻断项 |
| Web | 发布 | `main@1b7298e` | 展示只读系统 Agent，允许用户选择系统或个人默认 Agent |
| AIDA CLI | 不涉及 | 无 CLI 代码变更 | 已检查本次调用链不依赖 CLI 升级 |
| PostgreSQL migration | 不涉及 | 无 migration | 不修改表结构 |
| 历史数据回填 | 发布 | `managed_agent_profiles` 中 30 条生产默认配置 | 28 条旧系统默认配置改指向统一系统 Agent；UID 305 已指向系统 Agent；UID 322 保留个人默认 |
| 数据清理/删除 | 发布 | 旧版系统生成的用户侧报告 Agent | 使用平台归档能力，不物理删除；个人 Agent 不处理 |
| Report Skill | 仅核对 | `10086/aida-report@1.0.20` | 不发布新版本；核对 owner、slug、version 和可加载状态 |
| MCP/默认 Agent | 发布 | 系统 Agent `aida-agent-dk9dxzv7b0fe`；生产 Report MCP | 更新默认映射；系统 Agent 走标准链路，个人 Agent 仅注入绑定 Run 的 Report MCP |
| MinIO/对象存储 | 不涉及 | 无对象变更 | API/Web 和 Agent 配置不写 MinIO 发布对象 |
| Digest/报告链路 | 仅核对 | Digest → Context → Run → Report | 分别用系统默认和个人默认完成真实报告写回 |
| 监控/配置/文档 | 发布 | API/Web 镜像标签、定向数据快照、本清单 | 不新增环境变量；记录执行结果和回滚证据 |

## 3. 版本与代码证据

### 3.1 当前生产基线

| 组件 | 当前生产版本 |
| --- | --- |
| API | `20260728-06b832a-team-session-sync`，Git `06b832a` |
| Web | `20260729-d6ec87c-aida-guide`，Git `d6ec87c` |
| 系统默认 Agent | `aida-agent-dk9dxzv7b0fe`，系统账号 `10086` |
| Report Skill | `10086/aida-report@1.0.20` |
| Report MCP | `http://192.168.14.182/api/v1/mcp/reports` |

### 3.2 测试服证据

- 个人默认流程：Run `d6d30b04-9691-4f2f-9f78-d05f29dd5847`、Session `f62ab3b0-493a-4122-a5f0-efa923821ed6` 成功；加载个人 Skill，通过系统 Report MCP 读写，无系统 Brief，个人 Markdown 原样保存。
- 系统默认流程：Run `20ece927-6cc9-47fa-92b4-b48116bd7471`、Session `ada527a0-fc3e-44a5-bc77-e1c9d25d9ced` 成功；使用 `deepseek-v4-flash`、Report Brief 和标准格式。
- `cd api && go test ./...`：通过。
- `cd web && pnpm test && pnpm typecheck && pnpm build`：通过；仅有既存 Vite chunk size warning。

### 3.3 发布目标版本

| 组件 | 不可变标签 | Git SHA | Digest |
| --- | --- | --- | --- |
| API | 23:00 冻结后填写 | 待填写 | 待填写 |
| Web | 23:00 冻结后填写 | 待填写 | 待填写 |

## 4. 变更项清单

| 变更项 | 文件/提交/版本 | 状态 | 验证命令或运行证据 | 影响范围 |
| --- | --- | --- | --- | --- |
| 系统/个人 Agent 解析 | `33fc696`、`1b7298e` | 已实现，待阻断修复 | 两条测试服真实 Run | 报告生成入口 |
| 个人 Agent 原样输出 | `1b7298e` | 已实现 | 个人默认 Run 成功 | 个人报告 Markdown |
| 系统格式规范 | `1b7298e` | 已实现 | 系统默认 Run 成功 | 系统报告格式 |
| 旧默认 Agent 收敛 | 生产定向数据操作 | 待执行 | 第 5.2 节只读盘点 | 30 条默认配置、旧 Agent 归档 |
| 用户资产保护 | 待提交修复 | 阻断 | 精确 owner/slug/version 和自定义 Prompt 回归测试 | 个人 Agent、Skill、Prompt |
| 普通 Agent 更新隔离 | 待提交修复 | 阻断 | 普通 Agent 部分更新回归测试 | 报告外 Agent 业务 |

## 5. 发布前检查

### 5.1 必须关闭的代码阻断

- [ ] 个人 Agent 修复只移除可精确识别的系统托管 Skill，不得仅按 `aida-report` slug 删除用户同名 Skill。
- [ ] 启动 Prompt 仅在精确识别为系统托管残留时清理，不得改写用户自定义 Prompt。
- [ ] 显式清空 `instructions/start_prompt_template` 仅用于专用清理请求，不改变普通 Agent 部分更新语义。
- [ ] 增加同 slug 不同 owner、用户自定义 Prompt、普通 Agent 部分更新回归测试。
- [ ] 测试服重新验证系统默认、个人默认、用户同名 Skill 三条链路。

任一项未关闭，23:00 不得发布。

### 5.2 生产默认 Agent 只读盘点结果

- 默认配置共 30 条。
- 27 条默认配置明确指向带系统标记的旧版系统 Agent。
- UID 198 当前默认 Agent 无完整标记，但名称、系统 Skill 和 `/aida-report` 启动模板与旧版系统模板一致，按旧系统 Agent 处理。
- UID 305 当前配置已指向统一系统 Agent；个人 Agent `sheep` 保留。
- UID 322 当前默认 Agent 无系统标记且配置经过自定义，按个人默认保留；其“口头汇报 Agent”同时保留。
- 受影响用户下发现 38 个旧版系统报告 Agent，生产操作使用归档，不物理删除。
- 明确保留 3 个个人 Agent：UID 305 的 `sheep`，UID 322 的“口头汇报 Agent”和当前个人“报告生成 Agent”。
- 禁止仅按 Agent 名称、Skill slug 或模型判断删除范围；执行时使用发布前冻结的 owner + agent_id 白名单。

### 5.3 版本、快照与依赖检查

- [ ] 工作区状态、目标提交和发布差异已冻结，未混入 `team-session-reassign` 等无关改动。
- [ ] API/Web 测试、构建和不可变镜像 digest 已记录。
- [ ] 当前 API/Web 镜像、`.env`、Compose/Nginx 配置已记录。
- [ ] 导出 `managed_agent_profiles` 中 30 条默认配置，保存 SQL/CSV、行数和 SHA256。
- [ ] 导出 38 个待归档 Agent 的 owner、agent_id、当前归档状态及 3 个保留 Agent 白名单。
- [ ] 复用最近一次完整 PostgreSQL 备份；本次无 migration，不新做全库备份。数据回退以定向快照为准。
- [ ] 核对系统 Agent、Report Skill、Report MCP、凭据槽和生产测试账号。

## 6. 执行步骤

| 步骤 | 执行主机 | 操作 | 预期结果 | 实际结果 |
| --- | --- | --- | --- | --- |
| 1 | `ssh 157` / 项目目录 | 冻结 main SHA，确认仅包含本次功能、阻断修复和发布文档 | 发布范围固定 | 待填写 |
| 2 | 生产主机 | 记录当前容器、镜像 digest、健康状态和关键配置 | 可恢复当前生产状态 | 待填写 |
| 3 | 生产数据库/Agent 平台 | 生成默认配置定向快照、待归档白名单和个人保留白名单，核对 `30/38/3` 数量 | 快照有路径、SHA256 和对账数量 | 待填写 |
| 4 | 构建机/镜像仓库 | 从冻结 SHA 构建并推送 API/Web 不可变镜像 | 标签与 digest 唯一 | 待填写 |
| 5 | 生产主机 | 仅更新 API 镜像标签并重建 API；不重启 db、minio | API `/health` 成功，无持续 5xx | 待填写 |
| 6 | 生产接口 | 先用生产测试账号验证系统默认和个人默认各一次 | 两条 Run 均成功写回报告 | 待填写 |
| 7 | 生产数据库 | 在事务中将 28 条旧系统默认映射改为统一系统 Agent；UID 305 保持系统，UID 322 保持个人；提交前核对数量 | 最终 29 个用户使用系统默认，UID 322 使用个人默认 | 待填写 |
| 8 | Agent 平台 | 按 owner + agent_id 白名单分批归档 38 个旧系统 Agent；每批后核对，不处理 3 个保留 Agent | 旧系统 Agent 已归档，个人 Agent 未变化 | 待填写 |
| 9 | 生产主机 | 更新 Web 不可变镜像并重建 Web | `/reports` 与 Agent 列表可访问 | 待填写 |
| 10 | 生产接口/浏览器 | 再次执行系统默认、UID 322 个人默认及个人 Agent 权限验收 | 路由、资产和写回均正确 | 待填写 |
| 11 | 生产主机 | 观察 API/Web/Agent 运行日志至少 15 分钟 | 无身份串用、误删资产、持续 5xx | 待填写 |

Agent 归档和默认映射更新必须分开执行：先完成 API 冒烟，再更新映射，最后归档旧 Agent。任一阶段失败立即停止，不继续下一阶段。

## 7. 发布后验收

| 验收类别 | 用例 | 账号/数据 | 预期结果 | 实际结果 | 证据 |
| --- | --- | --- | --- | --- | --- |
| 自动化 | API/Web 回归 | 冻结 SHA | 全部通过 | 待填写 | 待填写 |
| API/接口 | `/health` 和报告查询 | 生产测试账号 | 200，报告可查询 | 待填写 | 待填写 |
| Report Skill/MCP/Agent | 系统默认报告 | 生产测试账号和既有 Session | 系统账号、Brief、标准格式、成功写回 | 待填写 | Run/Session 待填写 |
| Report Skill/MCP/Agent | 个人默认报告 | UID 322 或指定生产测试账号 | 个人模型/Prompt/Skill，系统 Report MCP，成功写回 | 待填写 | Run/Session 待填写 |
| 用户资产 | 保留白名单核对 | 3 个个人 Agent | 全部存在、未归档、配置未变 | 待填写 | 快照 hash 待填写 |
| 历史数据 | 默认映射对账 | 30 条配置 | 29 条系统、1 条个人，无悬空 Agent ID | 待填写 | SQL 结果待填写 |
| Web 浏览器 | Agent 列表与 `/reports` | 生产测试账号 | 系统 Agent 只读；个人 Agent 可选择和运行 | 待填写 | 截图待填写 |
| 观察窗口 | 错误日志和运行状态 | 发布后 15 分钟 | 无持续 5xx、身份串用或资产误处理 | 待填写 | 日志时间段待填写 |

## 8. 回滚与停止条件

### 8.1 回滚方式

- API：恢复 `20260728-06b832a-team-session-sync` 及发布前 digest，仅重建 API。
- Web：恢复 `20260729-d6ec87c-aida-guide` 及发布前 digest，仅重建 Web。
- 默认映射：使用第 3 步的定向快照，在事务中恢复 30 条 `managed_agent_profiles` 记录并核对数量。
- Agent：按归档前白名单将本次归档的 Agent 恢复为未归档；不创建新副本，不修改个人 Agent。
- Skill/MCP：本次不发布新版本；如运行异常，恢复 API 后继续使用生产现有 `10086/aida-report@1.0.20` 和 Report MCP。
- migration：不涉及。
- 数据清理：无物理删除；禁止通过 DROP、TRUNCATE 或按名称批量删除回滚。

### 8.2 立即停止条件

- 第 5.1 节任一代码阻断未关闭；
- 定向快照、待归档数量 `38` 或个人保留数量 `3` 无法对账；
- 任一个人 Agent 内容、Skill、Prompt 或归档状态发生非预期变化；
- 系统默认被解析为个人 Agent，或个人默认被解析为系统 Agent；
- Report MCP 身份与当前 Aida 用户不一致，Run ID 丢失或报告无法写回；
- API/Web 持续 5xx、容器反复重启或当前镜像无法恢复；
- 默认映射出现悬空 Agent ID，或 30 条配置对账不一致。

## 9. 最终结果

```text
发布范围已完整列出：是
发布项执行结果已记录：否
发布后验收已完成：否
阻断项数量：2
最终状态：阻断（等待代码修复、测试服回归和 2026-07-29 23:00 发布确认）
```

发布完成后必须补充：最终 API/Web SHA、镜像标签与 digest、定向快照路径与 SHA256、映射和归档实际数量、生产 Run/Session/Report ID、观察窗口及最终状态。
