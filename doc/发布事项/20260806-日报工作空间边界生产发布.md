# 日报工作空间边界生产发布

> 日期：2026-08-06  
> 生产目标：`192.168.14.182:/home/luoxian/aida`

## 发布元数据

| 项目 | 内容 |
| --- | --- |
| Git 基线 | `main@1c75cb4` |
| API 镜像 | `20260806-1c75cb4-workspace-context` |
| Registry digest | `sha256:fee1d9e1f5057298f172f5535b16ef5d3e9c89345508c84f4e631efffb2306e2` |
| Report Skill | `10086/aida-report@1.1.32` |
| 默认模型 | 保持生产现有 `deepseek-v4-flash` |

## 发布范围

| 范围 | 状态 | 说明 |
| --- | --- | --- |
| API | 发布 | Report Context 增加匿名 Workspace Fact 分组，并修复复用 Evidence 的关联读取 |
| Report Skill | 发布 | 同一 Workspace 默认归并，跨 Workspace 不因 MCP 等共同技术词合并 |
| Web、CLI、MCP | 不涉及 | 未构建、未替换 |
| migration、历史数据 | 不涉及 | 无 Schema 变化、无回填、无清理 |
| DB、MinIO | 仅核对 | 容器未替换、未重启 |

## 验证与执行

- API 全量 Go 测试与 `git diff --check` 通过。
- 测试服真实双 Workspace 样本确认 AIDA 与 Knowledge Map Facts 已隔离；MiniMax 对同 Workspace 父级命名仍可能拆分，本期不增加失败型硬校验。
- 用户确认本次无 migration、无数据写入或清理，不执行新的 PostgreSQL dump；保留既有完整备份，仅保存发布前 `.env`、Compose 与 API inspect。
- 配置证据目录：`/home/luoxian/aida/backups/workspace-context-20260806T1625CST`。
- 仅重建 API；DB、Web、MinIO 容器 ID 保持不变。
- `/health` 返回 200；API 启动、migration 检查、MinIO 初始化及后台 Worker 正常，无 panic、fatal 或 migration 错误。
- 系统 Report Agent `aida-agent-dk9dxzv7b0fe` 已绑定 `10086/aida-report@1.1.32`。
- Registry `SKILL.md` 为 10,997 字节，文件 SHA256 `d9ab8b7d21db113dd3e98a38ed4c8a4d7ee79bf096f76505f772644611b28288`。

## 回退

1. `API_IMAGE_TAG` 恢复为 `20260806-ab8f50f-project-association`；
2. `MANAGED_AGENT_REPORT_SKILL_VERSION` 恢复为 `1.1.31`；
3. 执行 `docker compose up -d --no-deps api`；
4. 无 migration 和数据变更，不需要数据库回退。

## 最终判定

```text
生产状态：已发布
阻断项：0
```
