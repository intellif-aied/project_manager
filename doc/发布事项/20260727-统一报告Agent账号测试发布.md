# 统一报告 Agent 账号测试发布记录

## 1. 发布元数据

| 字段 | 内容 |
| --- | --- |
| 发布编号 | `20260727-01-report-agent-system-account` |
| 目标环境 | 14.157 测试服 |
| 发布目标 | 默认报告使用专用平台账号和 `deepseek-v4-flash`，Report MCP 保持当前 AIDA 用户身份；前端屏蔽个人 Skill/MCP 管理 |
| Git 基线 | 功能提交 `249da4f`；UI 集成提交 `0e23133`；修复提交 `30c5590` |
| API 镜像 | `sha256:3f93e6fec07d57ac5c213f866c0aab18b5f378eefca0a7948487d67d95950d3b` |
| Web 镜像 | `sha256:85ba10dbf567a7b0bdddf8a51f2c027dacba21f653eca6dcd5d014794252f1fd` |
| 时间 | 2026-07-27 10:45—10:51 UTC |

## 2. 发布范围

| 范围 | 状态 | 内容 |
| --- | --- | --- |
| API/后台 | 已发布 | 默认 Agent、Credential、Session 使用专用平台账号；用户 Token 仅作为 Report MCP Credential 值 |
| Web | 已发布 | 隐藏个人 Skill/MCP 页签、指标和创建入口；旧创建地址重定向；保留既有日报/周报新 UI |
| 配置 | 已更新 | `MANAGED_AGENT_TOKEN` 切换到测试专用账号；`MANAGED_AGENT_REPORT_MODEL_ID=deepseek-v4-flash` |
| 默认 Agent | 已更新 | owner `100866`，模型 `deepseek-v4-flash`，`share_model_access=true` |
| Report Skill | 仅核对 | `100866/aida-report@1.0.50` |
| PostgreSQL migration | 不涉及 | 无 migration、回填、数据清理或结构变更 |
| CLI | 不涉及 | 未修改、未发布 |
| 生产环境 | 不涉及 | 未操作生产 |

## 3. 发布前验证

| 验证 | 结果 |
| --- | --- |
| API `go test ./...` | 通过 |
| Web 工作流测试 | 通过 |
| Web 变更文件定向 ESLint | 通过 |
| Web typecheck | 通过 |
| Web production build | 通过 |
| 专用账号身份 | Agent 平台用户名 `100866`，内部 UID `332` |
| 专用账号模型目录 | 包含 `deepseek-v4-flash` |
| 全仓前端 lint / 模板校验 | 基线已有 `HelpCenter` effect 与无关页面 Table scroll 规则错误；本次变更文件无新增错误 |

## 4. 执行结果

| 步骤 | 实际结果 |
| --- | --- |
| 配置回退准备 | 保存 `/home/intellif/dev/project_manager/.env.rollback-report-system-account-20260727-1038` |
| API 回退准备 | `project_manager-api:rollback-before-report-system-account-20260727`，原镜像 `sha256:1222b31fb6404e219fa0bcfdc2479a3edb5ea73c0494adf48fc21b1bc01f753c` |
| Web 回退准备 | `project_manager-web:rollback-before-report-system-account-20260727`，原镜像 `sha256:6f5b2d986d056339ba50e76c72ca8a3ad760b2af0f5d6c85815ca152e14bddcc` |
| API 部署 | 仅替换 `project_manager-api-1`，`/health` 返回成功 |
| Web 部署 | 仅替换 `project_manager-web-1`，`http://192.168.14.157:13000/` 返回 HTTP 200 |
| 默认 Agent 初始化 | Aida 接口返回 owner `100866`、默认模型 `deepseek-v4-flash`、共享模型状态为 true |
| 其他服务 | db、minio 和 13001 前端开发进程未重启 |

## 5. 验收与未执行项

- API 与 Web 健康检查通过；
- 默认 Agent 当前部署标记、Report Skill owner、MCP Credential Slot、默认模型和共享模型状态通过；
- 未创建真实报告 Run，避免覆盖测试用户现有日报数据；真实双用户 MCP 数据隔离验收后续使用专门测试数据执行；
- 未提交 Token 明文，未写入 Git 或本文档。

## 6. 回滚

1. 恢复 `.env.rollback-report-system-account-20260727-1038` 为测试服 `.env`；
2. 使用已标记的 API/Web 回退镜像只替换对应服务；
3. 将测试默认 Agent 模型恢复为 `GLM-5.2` 并关闭 `share_model_access`；
4. 复查 API `/health` 和 Web 13000 首页；数据库无需回滚。

## 7. 最终结果

```text
发布范围已完整列出：是
发布项执行结果已记录：是
API/Web 健康检查：通过
专用账号与目标模型：通过
生产环境：未操作
最终状态：测试发布完成
```
