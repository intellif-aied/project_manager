# 报告Agent两阶段生成生产发布

## 1. 发布元数据

| 项目 | 内容 |
| --- | --- |
| 发布编号 | `20260728-01-report-agent-two-pass-prod` |
| 目标环境 | 生产，`192.168.14.182:/home/luoxian/aida`，经`192.168.14.157`跳板 |
| 发布负责人 | Codex，用户已确认生产发布 |
| 变更目标 | 默认个人日报先生成并校验Report Brief，再只基于Brief写入最终日报；纠错次数由服务端限制 |
| Git基线 | API `626763b3d8bf1a93c019a0e085eef570cc890d69`；Web/CLI不变 |
| 开始/结束时间 | 2026-07-28 01:25～01:51 UTC |

## 2. 完整范围清单

| 范围 | 本次状态 | 版本/提交/迁移 | 验收证据 | 影响与依赖 |
| --- | --- | --- | --- | --- |
| API/后台 | 发布 | `626763b`；镜像`20260728-626763b-report-two-pass` | 全量Go测试与vet；容器、健康接口、真实Run | 仅替换API容器 |
| Web | 不涉及 | 保持`20260727-9e11641-report-agent-v3` | 容器ID未替换，公网`/`与`/reports`返回200 | 无前端代码变化 |
| PostgreSQL migration | 发布 | 028、029 | 测试服已应用；生产启动日志成功；`schema_migrations=29`且两张表存在 | 仅新增按run级联的Brief和纠错计数表 |
| 历史数据回填 | 不涉及 | 无 | 未执行回填命令 | 新表仅由新Run写入 |
| 数据清理/删除 | 不涉及 | 无 | 未执行删除、DROP或TRUNCATE | 无 |
| Report Skill | 发布 | `10086/aida-report@1.0.17`；SkillID`a5953060-d819-498d-825a-85ff7521d225` | owner-qualified derive；公共Registry唯一；正文hash一致 | 从生产1.0.16派生 |
| MCP/默认Agent | 发布 | MCP保持`report-v1`；Agent`aida-agent-dk9dxzv7b0fe`引用Skill 1.0.17 | 实际Session工具顺序Context→Brief→Result；跨用户读取返回`RUN_NOT_FOUND` | MCP继续使用当前Aida用户身份 |
| CLI/安装包 | 不涉及 | 保持现状 | 未构建、未上传 | 无CLI变化 |
| MinIO/对象存储 | 仅核对 | 保持现状 | 容器持续healthy | 无对象写入或配置修改 |
| Digest/报告链路 | 仅核对 | Digest V2与Projection确定性链不变 | t03生产冒烟成功；上线后真实用户Run成功 | 两阶段只作用于默认个人日报 |
| 文档/配置 | 发布 | Skill 1.0.17、API新标签、`REPORT_TWO_PASS_ENABLED=true` | 容器内环境变量与生产资源登记一致 | Compose仅增加两阶段开关透传 |

## 3. 代码、迁移和数据证据

- [x] main与origin/main均为`626763b`；构建上下文仅`api/`，既有未跟踪回滚文件和UI原型未纳入镜像或提交；
- [x] API提交SHA、镜像不可变标签和Registry digest已冻结；
- [x] 028/029已在测试服数据库验证，生产在完整备份后正向应用成功；
- [x] migration只新增表、约束和索引，不修改或扫描历史业务数据；回退保留已执行结构；
- [x] 未执行历史回填、数据清理或删除；
- [x] 生产备份目录、dump目录清单和SHA256均已验证；
- [x] 测试服11组真实Session结果与生产真实Run分别记录，不混用环境结论。

## 4. 功能、接口和性能验收

- [x] Report Brief模块、MCP工具、写回门禁、有界重试和其他报告兼容测试通过；
- [x] 全量`go test ./...`与`go vet ./...`通过，Standards与Spec Review无上线阻断；
- [x] 测试服同批25个Session、11组Run全部成功；
- [x] 生产t03手动Run成功，约39.7秒完成；实际顺序为Skill→Context→Brief→Brief→Result；
- [x] 生产上线后的真实用户Run成功，Brief存在且未触发重试耗尽；
- [x] t05读取t03 Run的Context返回`RUN_NOT_FOUND`，未暴露跨用户数据；
- [x] 个人日报最终写回包含`report-brief/v1`与`brief_hash`；
- [x] 本次不涉及Web、上传、CLI、MinIO或数据清理性能变更。

## 5. 构建、部署和备份

- [x] 生产完整备份：`/home/luoxian/aida/backups/20260728T-report-two-pass-626763b/postgres.dump`，约2.0GB；
- [x] dump SHA256：`72cc6bc356591ed1fbd4597d1bf667b7bf94578f36a09231ee92b0638bbb49c3`，`pg_restore -l`目录727行；
- [x] `.env`、Compose、Nginx、旧容器与旧镜像信息已保存在同一备份目录；
- [x] API镜像Registry digest：`sha256:a755a565d1bc888b2fdf2e8f747cfa3b9ba75e5b8674fce96c45aa7df7181fdb`；
- [x] Skill正文SHA256：`952690582aa2cce79e1451930da2038a2fc6375ef246f7f61820b158ea69f061`；
- [x] 新API容器ID`612f45e3506ca159779670c7ab20f9596339342e1ed7f11ca32dd73884454174`，旧容器ID已记录在备份；
- [x] `/health`、公网`/`与`/reports`正常；仓库当前未提供`/health/ready`，以真实业务Run补充验证；
- [x] 观察窗口无API 5xx、panic、fatal、migration错误或数据库锁等待。

## 6. 回滚与停止条件

### 回滚方案

- API：恢复`API_IMAGE_TAG=20260727-9e11641-report-agent-v3`并仅重建API容器；
- Web：未发布，无需回滚；
- Skill/MCP/Agent：恢复`MANAGED_AGENT_REPORT_SKILL_VERSION=1.0.16`，关闭`REPORT_TWO_PASS_ENABLED`并重启API；MCP仍为`report-v1`；
- migration：保留028/029结构，不执行DROP；旧API不读取新表；
- 数据回填：未执行；
- 数据清理：未执行。

立即停止条件为持续5xx、API重启、数据库锁等待、SkillRef或MCP身份错误、Report Context跨用户暴露、日报无法写回。观察窗口内均未触发。

## 7. 最终发布判定

```text
本次范围已完整列出：是
所有发布项均有证据：是
所有阻断项均为0：是
生产可以发布：是
```

## 8. 上线结果

| 项目 | 实际结果 | 证据 |
| --- | --- | --- |
| API/Web容器 | API已更新，Web未替换；四个生产服务运行 | Compose状态、容器ID、镜像digest |
| migration | 028、029成功，最高版本29 | API启动日志、表存在性查询 |
| 数据回填/清理 | 未执行 | 无相关命令和作业 |
| Skill/MCP/Agent | Skill 1.0.17唯一可解析；MCP report-v1；默认Agent实际加载新Skill | Registry hash、Session SkillRef与工具事件 |
| 健康检查 | API、生产入口和报告页正常 | `/health`、`/`、`/reports`均200 |
| 关键接口 | 个人日报两阶段成功，跨用户隔离正常 | Run`0b9bcd96-f464-4bf4-a54b-f0e386b4882f`、Session`d838bbb1-21e6-45c4-a636-69cce3bb3164` |
| 观察窗口 | 无5xx、panic、fatal、migration错误或锁等待；上线后真实用户Run成功 | API日志、`pg_stat_activity`、Run`b44abd6e-26c6-4023-84e5-db9c2cffb881` |
| 未完成项与后续任务 | 无发布阻断；定时报告沿用同一Run提交链路，本窗口未等待下一次定时触发 | 现有调度代码与回归测试 |
