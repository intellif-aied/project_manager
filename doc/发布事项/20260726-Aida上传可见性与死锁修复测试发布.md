# Aida 上传可见性与死锁修复测试发布

## 1. 发布元数据

| 字段 | 内容 |
| --- | --- |
| 发布编号 | `20260726-01-aida-upload-consistency` |
| 目标环境 | 测试服 `192.168.14.157` |
| 发布负责人 | Codex（用户授权执行） |
| 发布目标 | 历史 Session 增量上传完成后进入当天列表，并消除 Session 上传与 Usage Worker 的反向锁序 |
| Git 基线 | API：`f1a3b63` 加当前未提交 P0 差异；Web：`f1a3b63`，未变更；CLI：`f1a3b63`，未变更 |
| 版本 | API 镜像 `sha256:823fff366100e80690eb76ab32b8ced6f631335acbba4586b3a7502e705464ac`；Web/CLI/Skill/MCP：不涉及 |
| 时间 | 2026-07-26 01:42 至 01:49 UTC |

## 2. 发布范围

| 范围 | 状态 | 内容 |
| --- | --- | --- |
| API/后台 | 发布 | 重建并替换测试服 API；健康接口 `http://192.168.14.157:18090/health` |
| Web | 不涉及 | `web/` 无差异，未构建、未重启 |
| AIDA CLI | 仅核对 | `daemon/` 无差异，使用当前源码执行真实上传；未发布 CLI 产物 |
| PostgreSQL migration | 不涉及 | 无 migration 文件和结构变化 |
| 历史数据回填 | 不涉及 | 未执行回填 |
| 数据清理/删除 | 不涉及 | 未删除业务数据 |
| Report Skill | 不涉及 | 无 Skill 变更 |
| MCP/默认 Agent | 不涉及 | 无 MCP 或 Agent 变更 |
| MinIO/对象存储 | 仅核对 | 真实上传沿用现有对象写入链路，无配置变化 |
| Digest/报告链路 | 不涉及 | 无 Digest、Context、Run 或报告写回变更 |
| 监控/配置/文档 | 发布 | 更新 P0 Bug 文档、未修复清单和本发布记录；无运行配置变化 |

## 3. 版本与代码证据

| 项目 | 发布前 | 发布后 |
| --- | --- | --- |
| API 容器 | `c1d76cd3adad59533c36cdd1e954a43bb9fb75baaa20f42a932ff236d8f4b6bf` | `9ecef30a0d8db5992efee53b4e58260517aba61ef02b935ecd337ec52d7d4d1c` |
| API 镜像 | `sha256:9c04f7fc372c9daf22d56f6cbe8f6e0c32375829ca2039d8956fba398bffa1d2` | `sha256:823fff366100e80690eb76ab32b8ced6f631335acbba4586b3a7502e705464ac` |
| Git HEAD | `f1a3b63` | `f1a3b63`，代码尚未提交 |
| 数据库结构 | 未变更 | 未变更 |

## 4. 变更项清单

| 变更项 | 文件/提交/版本 | 状态 | 验证命令或运行证据 | 影响范围 |
| --- | --- | --- | --- | --- |
| 统一 Session 行锁顺序 | `api/internal/sessionsync/session_lock.go`、`api/internal/usage/processor.go` | 已部署 | Usage 锁顺序 PostgreSQL 集成测试通过；部署后死锁日志为 0 | Usage Chunk 与 Revision Activation |
| 数据库冲突有限重试 | `prepare_retry.go`、`postgres_repository.go`、`sync_service.go` | 已部署 | `40P01/40001` 单元测试通过 | Prepare、CommitChunk、Finalize |
| 增量上传时间更新 | `content_projection_processor.go` | 已部署 | 两条历史 Session 增量上传后 `uploaded_at` 更新到 2026-07-26 | Session 日期筛选与排序 |
| 自动化和真实上传 | API 镜像 `823fff3661…` | 已通过 | API 全量测试、两条真实 Session 首次/增量/重复上传 | 测试服 |

## 5. 发布前检查

- [ ] 工作区状态和发布 SHA 已冻结；代码尚未提交，实际部署镜像 SHA256 已记录。
- [x] API `go test ./...` 通过，镜像构建成功。
- [x] PostgreSQL migration 不涉及；代码差异无 migration。
- [x] 发布前 API 容器和镜像已记录。
- [x] 回退镜像 `sha256:9c04f7fc372c9daf22d56f6cbe8f6e0c32375829ca2039d8956fba398bffa1d2` 已记录。
- [x] Skill/MCP/Agent 不涉及，代码与配置均未修改。
- [x] 使用测试账号 `304/t02`，未使用生产账号。
- [x] Web、CLI、数据库结构和运行配置已完成无差异核对。
- [x] 本次仅测试服发布，回滚边界已填写。

## 6. 执行步骤

| 步骤 | 执行主机 | 操作/命令 | 预期结果 | 实际结果 |
| ---: | --- | --- | --- | --- |
| 1 | `ssh 157` | `git status`、记录 API 容器和镜像 | 范围明确且可回退 | 仅本次 API、测试和文档差异；旧镜像已记录 |
| 2 | `ssh 157` | `cd api && go test ./...` | API 全量测试通过 | 通过 |
| 3 | `ssh 157` | `docker compose up -d --build --no-deps api` | 只替换 API | API 容器替换为 `9ecef30a…`，db/minio/web 未重启 |
| 4 | `ssh 157` | `curl -fsS http://192.168.14.157:18090/health` | HTTP 200 | 返回 `{"status":"ok"}` |
| 5 | `ssh 157` | 隔离 HOME，测试账号 `304/t02`，`aida upload --all` | 两条真实 Codex Session 成功 | 成功 2 个 |
| 6 | `ssh 157` | 给隔离副本各追加一条非 Token 事件，再次上传 | 两条增量上传成功 | 成功 2 个 |
| 7 | `ssh 157` | 查询数据库、当天 Session 列表和日志 | cursor 追平、列表可见、Token 不变、无死锁 | 全部满足 |
| 8 | `ssh 157` | 不修改内容重复上传 | 无重复 Chunk、时间和 Token 不漂移 | 全部满足 |

## 7. 发布后验收

| 验收类别 | 用例 | 账号/数据 | 预期结果 | 实际结果 | 证据 |
| --- | --- | --- | --- | --- | --- |
| 自动化 | API 全量测试 | 当前源码 | 全部通过 | 通过 | `go test ./...` |
| API/接口 | 健康检查 | 测试服 API | HTTP 200 | HTTP 200 | `{"status":"ok"}` |
| AIDA CLI | 首次、增量和无变化重复上传 | `304/t02`；Session `019f8d73…`、`019f83f1…` | 每轮成功 2 个 | 三轮均成功 2 个 | CLI 输出 |
| Report Skill/MCP/Agent | 不涉及 | 无相关变更 | 不执行 | 未执行 | 代码范围核对 |
| Digest/报告 | 不涉及 | 无相关变更 | 不执行 | 未执行 | 代码范围核对 |
| Token/Session | 增量后 Token、cursor、列表 | 两条真实 Codex Session | Token 不变、cursor 追平、当天可见 | Token 为 `37291/414129`；两条 cursor 追平；当天列表完整 | PostgreSQL 查询和 `/sessions?date=2026-07-26` HTTP 200 |
| Token/Session | 重复上传幂等 | 同上 | 时间、Chunk、Token 不漂移 | `uploaded_at` 未变化；每条 2 个 Chunk；Token 未变化 | PostgreSQL 查询 |
| Web 浏览器 | 不涉及 | Web 无改动 | 不执行 | 未执行 | 代码范围核对 |
| 运行日志 | Chunk 500 和死锁 | 部署后日志 | 0 条 | API 0 条，PostgreSQL 0 条 | Compose 日志 |

## 8. 回滚与停止条件

| 范围 | 回滚方式 |
| --- | --- |
| API | 将 `sha256:9c04f7fc372c9daf22d56f6cbe8f6e0c32375829ca2039d8956fba398bffa1d2` 重新标记为 `project_manager-api`，执行 `docker compose up -d --no-build --no-deps --force-recreate api`，复验 `/health` |
| Web | 不涉及，未发布 |
| CLI | 不涉及，未发布 |
| Skill/MCP/Agent | 不涉及，未发布 |
| migration | 不涉及，未执行 |
| 数据回填 | 不涉及，未执行 |
| 数据清理 | 不涉及，未执行 |

出现 API 持续 5xx、Token 对账变化、cursor 不追平、当天列表缺失、数据库新增死锁或旧客户端上传失败时立即停止并回滚 API。

## 9. 最终结果

```text
发布范围已完整列出：是
发布项执行结果已记录：是
发布后验收已完成：是
阻断项数量：0
最终状态：已发布
```

代码未提交、未推送；生产环境未修改。
