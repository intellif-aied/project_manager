# 日报概览格式统一生产发布

## 发布范围

| 项目 | 内容 |
| --- | --- |
| 环境 | 生产，`192.168.14.182:/home/luoxian/aida` |
| Git | `c64812f` |
| API | `20260728-c64812f-summary-canonical`；`sha256:dd0c88c9bd006ee256dd285ef1e95204a8db27cbe82be32ab754bec8783a2801` |
| Web/Skill/MCP | 不涉及，保持当前版本 |
| 数据库 | 无 migration、回填或清理；最高版本保持 29；未新增数据库备份 |

## 变更与验收

- 在 `normalizeReportSummary` seam 内统一解析 Agent 概览，不再按单个线上样本追加分支；
- 支持真实 LF/CRLF、空白折叠、字面量 `\\n`/`\\r\\n` 及紧邻标点等展示形式；
- 最终统一输出连续编号、逐项换行的标准 Markdown；
- `2.0` 等正文版本号不会被误拆；
- 用户 317 的生产样本被用于最小复现，确认根因是 Agent 工具参数包含字面量换行；
- 格式族回归、后端全量 `go test ./...`、`go vet ./...`、测试服部署和健康检查通过；
- 生产 API、DB、MinIO 正常，公网 `/health` 与 `/reports` 返回 200，日志无 5xx、panic、fatal 或锁异常。

## 回退

- 配置备份：`.env.rollback-20260728-c64812f-summary-canonical`；
- API 回退到 `20260728-557e103-summary-lines`；
- 无数据库、Web、Skill 或 MCP 回退动作。

本次发布仅影响新写入日报，历史日报内容不自动改写。
