# 个人日报单层正文测试发布

> 日期：2026-08-05  
> 环境：AIDA 测试服（192.168.14.157）  
> 源码基线：`main@a87ce0f` 加本记录所列未提交改动

## 发布范围

| 组件 | 结果 | 说明 |
| --- | --- | --- |
| API | 已部署 | 测试镜像 `sha256:e6caf035a34188e6249e33053e1e6c97a28576ba8fa4448dc8037eb82d72763b` |
| System Report Skill | 已发布并切换 | `100866/aida-report@1.1.30`，SkillID `0bec72d6-409c-487b-b792-f0a1a5a0a3bf` |
| Report MCP | 沿用 | `aida-report-mcp@report-v1`，工具接口未变化 |
| Web / CLI / DB / MinIO | 未涉及 | 未部署、未迁移、未备份、未重启 |

## 变更行为

- 系统个人日报正文由“工作概览 + 工作详情”改成唯一的 Markdown 有序列表。
- 最终列表按已接受 Brief 的 Workstream Title 确定性生成，`content` 与内部快照 `summary` 一致。
- 个人自定义 Agent、周报和历史日报不改写。
- Project Memory 与最近日报连续性上下文继续通过已有无标题列表回退读取新日报。

## 验证

- `cd api && go test ./... -count=1`：通过。
- API `/health` 返回 `{"status":"ok"}`，运行配置为 `100866/aida-report@1.1.30`。
- Skill 由测试当前版本 `1.1.26` owner-qualified derive 为 `1.1.30`；公共 Registry 唯一且未归档。
- Registry `SKILL.md` 与 API 生成正文一致，SHA256 均为 `cbeb0dbe5522e00f428f3c5aa542028cb480c5bf1784c89e23a3e21fa7dd2f66`。
- 测试用户 305、生产样本日期 2026-07-31：Run `1448cdb3-816e-478a-9748-86995aa345bc` 成功，4 个 Brief Workstream 对应 4 条正文，Report `014475a3-1d4a-41ad-96ba-9bfdb550c3e8`。
- 测试用户 305、生产样本日期 2026-07-30：Run `89dbdc9e-7010-445a-a7de-c2c3129fac49` 成功，5 个 Brief Workstream 对应 5 条正文，Report `da94e317-56ed-4abb-906b-45d175fc06f3`。
- 两份正文均无“工作概览”“工作详情”、标题层级、详情段和重复 Summary；Agent Session 实际加载 Skill `1.1.30`。
- 第二份样本仍出现“儿童睡前卡通视频”被选为工作主题的一部分，属于 Brief 选材与长期上下文质量问题，不属于本次单层格式改动；留作 Project Memory 下一步优化样本。

## 回退

1. 将测试服 `MANAGED_AGENT_REPORT_SKILL_VERSION` 恢复为 `1.1.26`；
2. 恢复上一 API 镜像并只替换 API；
3. Web、DB、MinIO 和历史日报不处理。

## 仓库状态

- 本记录生成时改动尚未提交、尚未推送；
- 当前仍为 `main`，未涉及生产环境。
