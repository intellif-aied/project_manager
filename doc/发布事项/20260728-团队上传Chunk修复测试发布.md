# 团队上传 Chunk SQL 修复测试发布

## 1. 发布元数据

| 字段 | 内容 |
| --- | --- |
| 发布编号 | 20260728-02-team-upload-chunk-fix |
| 目标环境 | 测试服 |
| 发布负责人 | Codex |
| 发布目标 | 修复团队模式 Chunk 上传 500 和 generation 状态查询错误 |
| Git 基线 | API：`f8750b1` 加当前未提交工作区；Web/CLI：不涉及 |
| 版本 | API：未单独编号；Web/CLI：不涉及 |
| 时间 | 开始：2026-07-28 11:20 UTC；结束：2026-07-28 11:37 UTC |

## 2. 发布范围

| 范围 | 状态 | 内容 |
| --- | --- | --- |
| API/后台 | 发布 | 测试服 API 镜像 `sha256:7c05676fa357ea586e641770dff84fa6c7d52e4cb7824433ae4a5caf1778fe49` |
| Web | 不涉及 | 无 Web 代码变更 |
| AIDA CLI | 不涉及 | CLI 继续使用 0.1.22 |
| PostgreSQL migration | 仅核对 | 无新增 migration；当前版本 30 |
| 历史数据回填 | 不涉及 | 未执行 |
| 数据清理/删除 | 不涉及 | 未执行 |
| Report Skill | 不涉及 | 无依赖变更 |
| MCP/默认 Agent | 不涉及 | 无依赖变更 |
| MinIO/对象存储 | 不涉及 | 未修改发布对象 |
| Digest/报告链路 | 不涉及 | 无相关代码变更 |
| 监控/配置/文档 | 发布 | 保存本次测试发布记录 |

## 3. 版本与代码证据

| 组件 | 证据 |
| --- | --- |
| 仓库 | 分支 `main`，HEAD `f8750b1`，包含当前未提交团队模式工作区 |
| API | 镜像 `sha256:7c05676fa357ea586e641770dff84fa6c7d52e4cb7824433ae4a5caf1778fe49` |
| migration | `schema_migrations` 最大版本为 `30` |

## 4. 变更项清单

| 变更项 | 文件/提交/版本 | 状态 | 验证命令或运行证据 | 影响范围 |
| --- | --- | --- | --- | --- |
| 修复 Chunk SQL 参数编号 | `api/internal/sessionsync/postgres_repository.go` | 已发布 | 回归测试由 42P18 失败转为通过 | 团队 Chunk 上传 |
| 修复 generation 状态 SQL 别名 | `api/internal/sessionsync/sync_service.go` | 已发布 | sessionsync 数据库集成测试通过 | 团队状态查询 |
| 处理可空 active generation | `api/internal/sessionsync/sync_service.go` | 已发布 | sessionsync 数据库集成测试通过 | 新建/重建状态查询 |
| 团队 Chunk 回归测试 | `api/internal/sessionsync/team_sync_integration_test.go` | 已增加 | `TestTeamActorCanCommitChunkForMappedOwnerIntegration` 通过 | API 测试 |

## 5. 发布前检查

- [x] 工作区状态和 Git 基线已记录。
- [x] 团队同步数据库集成测试通过。
- [x] API 标准全量测试通过。
- [x] migration 无新增，测试服当前版本为 30。
- [x] 本次只替换 API，不重启 db、minio 或 web。
- [x] 生产环境不涉及。

## 6. 执行步骤

| 步骤 | 执行主机 | 操作/命令 | 预期结果 | 实际结果 |
| --- | --- | --- | --- | --- |
| 1 | 测试服 157 | 运行新增团队 Chunk 集成测试 | 复现参数错误 | 失败并返回 PostgreSQL 42P18 |
| 2 | 测试服 157 | 修正 SQL 参数、别名和可空布尔值 | 同一测试转绿 | 通过 |
| 3 | 测试服 157 | 带数据库运行 `./internal/sessionsync` 测试 | 团队同步集成测试通过 | 通过 |
| 4 | 测试服 157 | 标准 `go test ./... -count=1` | API 全量测试通过 | 通过 |
| 5 | 测试服 157 | `docker compose up -d --build --no-deps api` | 仅替换 API | API 已替换 |
| 6 | 测试服 157 | 健康、migration、路由检查 | 服务可用 | health 成功、migration 30、未认证接口 401 |

## 7. 发布后验收

| 验收类别 | 用例 | 账号/数据 | 预期结果 | 实际结果 | 证据 |
| --- | --- | --- | --- | --- | --- |
| 自动化 | 团队成员向映射归属人提交 Chunk | 临时集成测试数据 | Chunk accepted | 通过 | `TestTeamActorCanCommitChunkForMappedOwnerIntegration` |
| 自动化 | sessionsync 数据库集成测试 | 临时集成测试数据 | 全部通过 | 通过 | 包测试结果 `ok` |
| 自动化 | API 标准全量测试 | 无外部数据库集成 | 全部通过 | 通过 | `go test ./... -count=1` |
| API/接口 | 健康检查 | 无账号 | HTTP 200 | 通过 | `/health` 返回 `{"status":"ok"}` |
| Token/Session | 团队上传真实 CLI 复验 | 测试03 | 上传成功 | 待用户人工执行 | `aida upload --team` |
| Web 浏览器 | 不涉及 | 不涉及 | 不涉及 | 不涉及 | 无 Web 发布 |

## 8. 回滚与停止条件

| 范围 | 回滚方式 |
| --- | --- |
| API | 恢复发布前 API 镜像 `sha256:4c1be0b72932a547fb328fcf58cefd13fe9fd2229f6cb99dc76f7c05089ec376` 或以前一工作区重新构建，仅替换 API |
| Web/CLI | 不涉及 |
| migration | 无新增 migration，无需回滚 |
| 数据回填/清理 | 未执行，无需回滚 |

停止条件：团队 Chunk 再次持续 500、数据归属错误或健康检查失败时停止测试并回滚 API。本次未触发。

## 9. 最终结果

```text
发布范围已完整列出：是
发布项执行结果已记录：是
发布后验收已完成：否（真实 CLI 团队上传待用户复验）
阻断项数量：0
最终状态：已发布
```
