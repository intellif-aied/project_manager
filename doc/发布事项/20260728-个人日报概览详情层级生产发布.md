# 个人日报概览详情层级生产发布

## 1. 发布元数据

| 项目 | 内容 |
| --- | --- |
| 发布编号 | `20260728-04-report-hierarchy-prod` |
| 目标环境 | 生产，`192.168.14.182:/home/luoxian/aida`，经14.157跳板 |
| 发布负责人 | Codex，用户已确认生产发布且明确本次不新增数据库备份 |
| 变更目标 | 个人日报使用“工作概览/工作详情”层级并保持主题一一对应；侧边栏临时隐藏AI资产入口 |
| Git基线 | `c7208efc`，main与origin/main一致 |
| 开始/结束时间 | 2026-07-28 06:12～06:20 UTC |

## 2. 完整范围清单

| 范围 | 本次状态 | 版本/提交/迁移 | 验收证据 | 影响与依赖 |
| --- | --- | --- | --- | --- |
| API/后台 | 发布 | `20260728-c7208ef-report-hierarchy`；digest `sha256:295dddecbfee61683a66b1abec4bd1d1ca923ce30fa938a79b7b6fa7394a1121` | 全量Go测试、vet、生产容器与健康接口 | 仅替换API容器 |
| Web | 发布 | `20260728-c7208ef-report-hierarchy`；digest `sha256:d44da61aa9933e4c6f2fdeea9f7a8ed32b7fccf22b30fcfca0993e4b46faef21` | Web测试、类型检查、构建、变更文件eslint、公网页面 | 仅隐藏菜单，页面和路由保留 |
| PostgreSQL migration | 不涉及 | 最高版本仍为29 | 生产查询为29，启动日志Migrations complete | 无结构变化 |
| 历史数据回填 | 不涉及 | 无 | 未运行回填 | 无 |
| 数据清理/删除 | 不涉及 | 无 | 未执行删除、DROP或TRUNCATE | 无 |
| Report Skill | 发布 | `10086/aida-report@1.0.19`；SkillID `77c50ef7-05ca-44df-aa12-c45afce12327` | owner-qualified derive、公共Registry唯一、正文hash一致 | 从1.0.18派生 |
| MCP/默认 Agent | 仅核对 | MCP保持`report-v1`；API配置Skill 1.0.19 | 容器环境变量与Registry一致 | 新报告Run按配置解析1.0.19 |
| CLI/安装包 | 不涉及 | 保持现状 | 未构建、未上传 | 无CLI变化 |
| MinIO/对象存储 | 仅核对 | 保持现状 | 容器持续healthy | 无对象操作 |
| Digest/报告链路 | 仅核对 | Digest V2、Projection和Brief协议不变 | 测试Run `de138b53-17a3-4137-b1bd-ed0e39f92f10`真实16个Session切片通过；生产API正常启动 | 不新增Digest处理 |
| 文档/配置 | 发布 | 生产镜像标签、Skill版本、v3需求和发布记录 | 实际容器配置与本文一致 | 无其他配置变化 |

## 3. 代码、迁移和数据证据

- [x] `c7208ef`已推送main，构建范围与提交一致；用户原有回滚文件和UI原型未纳入提交或镜像；
- [x] API/Web不可变标签和Registry digest已冻结；
- [x] 本次无migration文件、历史回填、数据清理或删除；生产migration版本保持29；
- [x] 用户明确本次不新增PostgreSQL备份，沿用现有历史备份；本次没有需要恢复的数据写入；
- [x] 生产发布仅修改API/Web镜像标签和Report Skill版本。

## 4. 功能、接口和前端验收

- [x] `go test ./...`、`go vet ./...`和`git diff --check`通过；
- [x] `pnpm test`、`pnpm typecheck`、`pnpm build`以及`eslint src/router/routes.tsx`通过；
- [x] 全量`pnpm lint`仍被未改动的`HelpCenter.tsx:120`既有规则错误阻断，并包含两条`TokenAnalyticsPage.tsx`既有warning；本次变更文件lint通过，未扩大处理；
- [x] 测试服真实Run生成4条概览和4个详情主题，数量、顺序、二级/三级标题层级均通过；
- [x] 个人周报与历史报告格式回归通过；AI资产仅从菜单隐藏，直接路由、页面源码及报告内部Agent接口保持不变；
- [x] 生产未额外生成用户日报，避免为发布验收写入用户业务数据。

## 5. 构建、部署和备份

- [x] 按用户确认未创建新的数据库备份；回退使用上一不可变镜像、Skill版本和现有历史数据库备份；
- [x] API/Web均使用不可变镜像标签和记录的Registry digest；
- [x] Skill公共Registry匹配数为1，owner `10086`，版本`1.0.19`，未归档；
- [x] Skill正文SHA256 `6949379fdedb905435351258ecc1c78f10c868b77782c508502f50b6d7cc759b`，平台包SHA256 `03b12705b3e032935a776e0b7cc1b4e0509d2cd4e0cd3e0cdcb08913014dcbde`；
- [x] API容器 `7c71fa41ebf4872db496b43b00aeaf99998c08c45bb01dac74ba346cf30653ca`；Web容器 `01feaa107ddf46438110b4532b1726556af610883c3a66a8562921a56146adc8`；
- [x] DB、MinIO持续healthy，API和Web正常运行；
- [x] 公网`/health`、`/`、`/reports`均返回200，API日志无5xx、panic、fatal、dead或锁等待异常。

## 6. 回滚与停止条件

### 回滚方案

- API：恢复`API_IMAGE_TAG=20260728-2c6442a-report-run-bound`并仅替换API容器；
- Web：恢复`WEB_IMAGE_TAG=20260727-9e11641-report-agent-v3`并仅替换Web容器；
- Skill/MCP/Agent：恢复`MANAGED_AGENT_REPORT_SKILL_VERSION=1.0.18`并仅重启API；MCP保持`report-v1`；
- migration、数据回填、数据清理：均未执行，无回滚动作。

立即停止条件为持续5xx、API重启、报告写回失败、Skill无法解析、数据库锁等待或页面不可访问。发布窗口未触发。

## 7. 最终发布判定

```text
本次范围已完整列出：是
所有发布项均有证据：是
所有阻断项均为0：是
生产可以发布：是
```

## 8. 上线结果

| 项目 | 实际结果 | 证据 |
| --- | --- | --- |
| API/Web容器 | 均已更新为`20260728-c7208ef-report-hierarchy` | 容器ID、镜像名和digest |
| migration | 保持29 | 生产数据库查询、API启动日志 |
| 数据回填/清理 | 未执行 | 无相关命令或作业 |
| Skill/MCP/Agent | Skill 1.0.19唯一可解析；MCP report-v1不变；API配置已切换 | Registry、正文hash、容器环境变量 |
| 健康检查 | API、生产入口和报告页正常 | `/health`、`/`、`/reports`均200 |
| 关键功能 | 测试服真实数据通过；生产部署不写入用户日报 | 测试Run和前后端回归结果 |
| 观察窗口 | 无5xx、panic、fatal、dead或数据库锁等待 | 生产API日志和容器状态 |
| 未完成项与后续任务 | 全量前端lint的既有HelpCenter错误未纳入本次修复 | 变更文件lint与生产构建均通过 |
