# Aida 新增客户端统一选择器测试发布记录

> 发布日期：2026-07-22

> 环境：14.157 测试服

> 状态：测试分发部署完成

## 1. 发布范围

- Aida CLI：`0.1.18`；
- OpenCode、Kimi Code、OpenClaw 无 ref 时复用 Claude Code/Codex Session Picker；
- OpenClaw 保留逐项选择，禁止 CLI 和页面全选；
- 新增客户端名称完整显示；
- `upload-client --help` 和客户端级帮助可用；
- API、Web、数据库、MinIO 服务、Adapter 内容解析、canonical 协议、Claude Code/Codex uploader 均不修改。

## 2. 源码快照

| 项目 | 值 |
| --- | --- |
| 构建目录 | `/home/intellif/dev/project_manager_worktrees/p0-multiclient-picker-20260722` |
| Git 基线 | `81b5715` detached worktree + 本 Bug 列出的 CLI/测试/文档修改 |
| 发布代码补丁 SHA256 | `0fbf3d931eb02fa55288367917ee763d2be514e221ae3ee90e627eef9587d41d` |
| 版本 | `0.1.18` |
| Bug | [AIDA-BUG-20260722-011](../v2/bug清单/AIDA-BUG-20260722-011-新增客户端上传未复用统一Session选择器.md) |

## 3. 发布前验证

- `cd daemon && go test ./... -count=1`：通过；
- `cd daemon && go vet ./...`：通过；
- 临时 Linux `0.1.18` 构建：通过；
- node157 真实 OpenClaw：TUI 与非 TTY 选择页均打开成功，取消后未上传；
- node157 当前安装 CLI：仍为 `0.1.17`。

## 4. 正式测试分发

| 项目 | 实际结果 |
| --- | --- |
| 发布前测试版本 | `0.1.16.4` |
| 发布前旧分发副本 | `/home/intellif/aida-release-backups/20260722T032456Z-0.1.16.4`；副本内全部 SHA256 校验通过 |
| 构建产物目录 | `/home/intellif/dev/project_manager_worktrees/p0-multiclient-picker-20260722/aida-releases-test` |
| 构建目录 SHA256 校验 | Linux、macOS、Windows、两个安装脚本和版本指针全部通过 |
| 测试 MinIO payload 上传 | 通过；先上传五个 payload 和校验清单，下载验证期间版本指针保持 `0.1.16.4` |
| 下载验证目录 | `/home/intellif/aida-release-verifications/20260722T033347Z-0.1.18` |
| 下载 SHA256 校验 | `sha256sum -c SHA256SUMS.txt` 六项全部通过；下载的 Linux CLI 输出 `aida 0.1.18` |
| `aida-latest.txt` 切换 | payload 验证后最后切换；`20260722T033548Z` 确认为 `0.1.18` |
| 测试 API `/health` | 通过：`{"status":"ok"}` |
| node157 本地 CLI 是否安装 | 否；仍为 `aida 0.1.17`，文件时间和大小未改变 |

首次上传尝试错误地假设主项目 `.env` 包含 MinIO root 凭据，实际注入为空，第一个 payload 被 MinIO 以权限不足拒绝。该尝试没有写入任何文件，测试版本发现仍为 `0.1.16.4`。长期测试手册已改为从运行 MinIO 容器读取临时凭据且禁止空值继续。

正式重试从运行中的测试 MinIO 容器临时读取凭据，仅注入发布命令且未输出、未写入文档。测试分发文件、版本指针和下载校验均已完成。

## 5. 部署结论

- 测试 CLI 分发地址的版本指针为 `0.1.18`；
- 从测试 HTTP 地址下载的发布文件 SHA256 全部通过；
- 下载的 Linux CLI 输出 `aida 0.1.18`；
- 本次没有部署 API、Web、数据库或生产环境；
- 本记录只记录部署结果，不包含功能测试流程。
