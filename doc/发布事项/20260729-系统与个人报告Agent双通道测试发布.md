# 系统与个人报告 Agent 双通道测试发布

## 发布信息

| 字段 | 内容 |
| --- | --- |
| 日期 | 2026-07-29 |
| 环境 | Aida 测试服（192.168.14.157） |
| 源码版本 | `33fc69658cc441b0f377214e38a5ed8bb7c38991` |
| 改动范围 | 系统 Agent 只读投影、个人/系统默认执行分流、恢复个人 Agent/Skill/MCP 前端入口 |
| 数据库 | 无 migration、无数据变更、未备份 |
| 生产环境 | 未涉及 |

## 部署组件

| 组件 | 镜像 | 结果 |
| --- | --- | --- |
| API | `sha256:31bdf4adcd7699900a9c8d8085ca7f163848f1fcedb441d9dd62074acf2aa234` | 已更新，容器运行正常 |
| Web | `sha256:e7119d0a8a13c93c9bad6f82b4ba2b73e745caf2694dc2e5db9f6c95defd836f` | 已更新，容器运行正常 |

## 构建与检查

- API：`go test ./...` 通过；`docker compose build api` 通过。
- Web：`pnpm test`、变更文件 ESLint、`pnpm typecheck`、`pnpm build` 通过。
- Web 全量 `pnpm lint` 未通过，原因是既有未改动文件 `src/layouts/HelpCenter/HelpCenter.tsx:120` 的 `react-hooks/set-state-in-effect`；本次所有变更文件单独 ESLint 通过。
- `git diff --check` 通过。

## 发布后检查

- API：`http://192.168.14.157:18090/health` 返回 `{"status":"ok"}`。
- Web：`http://192.168.14.157:13000/` 返回 HTTP 200。
- 仅重建 API、Web，未重启 db、minio，未发布 CLI。
- 代码已提交并推送 `main`，功能提交：`33fc696`。

## 回滚

- 源码回滚基线：`d6ec87c`。
- API/Web 可按该提交重新构建并执行 `docker compose up -d --no-deps api web`。
