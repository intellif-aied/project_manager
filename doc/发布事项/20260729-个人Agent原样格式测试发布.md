# 个人 Agent 原样格式测试发布

## 发布信息

| 字段 | 内容 |
| --- | --- |
| 日期 | 2026-07-29 |
| 环境 | Aida 测试服（192.168.14.157） |
| 源码版本 | `main@9f77148` 加当前未提交的系统/个人 Report Agent 分流改动 |
| 改动范围 | 系统 Agent 固定标准格式；个人 Agent 原样保存个人 Skill Markdown；个人启动消息只要求 Report MCP 读写 |
| 数据库 | 无 migration、无数据变更、未备份 |
| 前端 | 未改动、未部署 |
| 生产环境 | 未涉及 |

## 部署组件

| 组件 | 镜像 | 结果 |
| --- | --- | --- |
| API | `sha256:9adff619bc7c2ea7300e283b91aaec658c5fee30b4a86a8d57c3a8c0e4c041dc` | 已更新，容器运行正常 |

## 构建与检查

- 个人 Markdown 原样保存、系统标准格式和 MCP tool schema 定向回归测试通过。
- `cd api && go test ./...`：通过。
- `git diff --check`：通过。
- `docker compose up -d --build --no-deps api`：通过。
- 工作区另有 `api/cmd/team-session-reassign` 协作者改动；该独立命令不在 API Dockerfile 的构建及运行产物列表中，未回退、未提交。

## 发布后检查

- API 容器运行正常。
- `http://192.168.14.157:18090/health` 返回 `{"status":"ok"}`。
- 容器运行产物包含本次 `format_mode` 系统格式策略。
- 仅替换 API，未重启 db、minio、web，未发布 CLI。
- 本次未提交、未推送。

## 回滚

- 发布前 API 镜像：`project_manager-api:rollback-20260729-before-personal-preserve`。
- 回退镜像 ID：`sha256:2168d0f3c3c05cd438f93a089c1874143c5672506c7630fe46a8de24112ef756`。
