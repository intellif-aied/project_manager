# 日报项目成果聚合与可读性优化测试发布

## 发布信息

| 字段 | 内容 |
| --- | --- |
| 日期 | 2026-07-31 |
| 环境 | Aida 测试服（192.168.14.157） |
| 源码版本 | `bb1614d6eb0a2789531852ae473e0d92a71cc115`（`main`） |
| 生产差异基线 | `31e1ea7`；本次待验收业务差异为 `4e7b325` 所含日报生成优化 |
| 改动范围 | 系统默认个人日报的项目关联、主题收敛、项目成果 Brief、工作概览通俗化与稳定降级 |
| 生产环境 | 未涉及 |

## 本次发布清单

| 组件 | 是否发布 | 版本或结果 | 说明 |
| --- | --- | --- | --- |
| API | 是 | `sha256:e125176fbe5e775a0e8d73cb56bcfa3ee1edc4e27e23b4cf3b5bee76d4bd4630` | 只重建并替换 `api` |
| 系统 Report Skill | 是 | `100866/aida-report@1.1.13` | SkillID `776ece15-0766-44a8-88b4-6b6733c92398` |
| Report MCP | 否 | 继续使用 `aida-report-mcp@report-v1` | 接口和鉴权方式未变化 |
| Web | 否 | 保持原容器 | 本次无前端代码差异 |
| CLI | 否 | 未发布 | 本次无 CLI 代码差异 |
| DB 容器 | 否 | 保持运行 | 未重启、未替换数据库容器 |
| MinIO | 否 | 保持运行 | 未重启、未替换 |

## 功能范围

1. 把 Git、目录、文件、测试、构建、合并和部署信息只作为项目关联线索，不直接写成日报事项。
2. 同一项目的实现、方案、修复、验证等工作优先归并，默认保留一至三个主要主题，避免按 Session 或操作步骤拆散。
3. 系统默认个人日报的 Report Brief 只保留可读的项目成果，不从局部 Session 推导“尚未合并”“未发布”“不建议上线”等外部状态。
4. 工作概览使用普通读者能理解的中文表达，不直接照搬内部架构名、方法名或新造术语。
5. 保持失败降级：表达质量不足不能扩大为用户生成失败。
6. 个人 Agent、周报、团队/部门报告、系统/个人 Agent 路由、MCP 鉴权及前端交互不在本次改动范围。

## 系统资源核对

- 测试服切换前配置：`100866/aida-report@1.1.10`。
- 新 Skill 从当前生效版本按 owner-qualified derive 发布，未覆盖已有版本。
- `1.1.13` 公共 Registry 精确匹配 1 条、未归档。
- 生成文件和 Registry 回读文件 SHA256 均为 `f6a7896f78ea0c4e9813b10457f3fec4c49fc40236ecbeb21017bced1f51643e`。
- 运行配置、默认系统 Agent 与真实 Agent Session 均核对为 `100866/aida-report@1.1.13`。

## 数据库说明

- 本次日报项目成果优化本身不新增 migration。
- 测试服此前只应用到 `031`；本次 API 同步带入已合并的日报评测模块，启动时正常执行 `032_report_run_variant_manifests.sql`，只新增 `report_run_variant_manifests` 表。
- migration 当前最大版本为 `32`；数据库容器未重启。

## 构建与部署结果

- `cd api && go test ./...`：通过。
- `docker compose up -d --build --no-deps api`：通过。
- API 旧容器：`9b13e376bc03...`；旧镜像：`sha256:9c74433110ca707fb0a6976509c864b12385a09f7fe87cd834ed5bb185fd42c1`。
- API 新容器：`d86ae57daa5d...`；新镜像：`sha256:e125176fbe5e775a0e8d73cb56bcfa3ee1edc4e27e23b4cf3b5bee76d4bd4630`。
- `http://192.168.14.157:18090/health` 返回 `{"status":"ok"}`。
- Web、DB、MinIO 容器运行时长连续，未被重启。

## 真实流程冒烟

- 测试用户：`305`。
- 报告日期：`2026-07-29`。
- 默认系统 Agent：`aida-agent-dk98hc136bzm`。
- Run ID：`625476db-b37b-4668-80c8-d8e1da84f262`。
- Agent Session：`0a57afb0-9678-451e-aa47-e490d82725ca`。
- Session 实际加载：`aida-report@1.1.13` 与 `aida-report-mcp`。
- `get_report_context`、`write_report_brief`、`write_report_result` 均正常调用。
- Run 状态：`succeeded`；Brief 修正 `0` 次，写稿修正 `0` 次，未触发降级。
- 写回日报：`ccf4c747-bece-42ab-a74e-07efa7e49b23`，生成 4 个工作主题；Markdown 编号与三级标题结构正常。
- 结果中未出现“尚未合并”“尚未发布”“暂不建议上线”“可以合并/发布”等外部状态推导。

## 人工验收重点

1. 同一项目是否不再按 Session、测试、文档、合并和发布动作拆成多个事项。
2. 日报是否只写用户实际推进的项目成果，不写 AI 建议或外部状态猜测。
3. 工作概览是否自然、通俗，第一次阅读即可理解。
4. Markdown 编号、标题和换行是否正常。
5. 个人自定义 Agent、周报、团队日报和部门日报是否保持原行为。

人工入口：`http://192.168.14.157:13000/reports`

## 回退

1. 恢复 `/home/intellif/dev/report-evaluation/test-deploy-20260731-bb1614d/project-manager.env.before` 中的测试服配置，使系统 Skill 回到 `1.1.10`。
2. 将 API 恢复为旧镜像 `sha256:9c74433110ca707fb0a6976509c864b12385a09f7fe87cd834ed5bb185fd42c1`，只重建或替换 API。
3. 验证 `/health` 和默认 Agent SkillRef；Web、DB、MinIO 不处理。
4. `032` 为独立新增表，旧 API 不依赖该表，回退 API 时无需删除或回退数据库结构。
