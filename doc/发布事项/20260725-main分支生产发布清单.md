# 20260725 main 分支生产发布清单

## 1. 发布元数据

| 字段 | 内容 |
| --- | --- |
| 发布编号 | `20260725-04-main-report-release` |
| 目标环境 | 生产 |
| 发布负责人 | 待填写实际执行人 |
| 发布目标 | 发布报告编辑与图片体验、Report Projection 完整性修复及稳定错误展示 |
| Git 基线 | API `main@9c95c34`；Web `main@9c95c34`；CLI 不涉及 |
| 版本 | API 使用本次构建的不可变镜像标签；Web `0.1.29` 并使用本次构建的不可变镜像标签；CLI、Skill、MCP 不变 |
| 时间 | 待执行时填写开始时间、结束时间和时区 |

## 2. 发布范围

| 范围 | 状态 | 内容与核对依据 |
| --- | --- | --- |
| API/后台 | 发布 | 新增报告图片上传接口；修复 Report Projection 围栏、Git 元数据和失败回退；收敛模型、超时及缺失写回错误展示 |
| Web | 发布 | 报告 Markdown 编辑器与展示优化、粘贴图片、报告记录视图、保存后刷新和周报弹窗关闭修复 |
| AIDA CLI | 不涉及 | `origin/main..9c95c34` 无 `daemon/`、安装脚本或 `VERSION` 变化 |
| PostgreSQL migration | 不涉及 | `origin/main..9c95c34` 无 `api/db/migrations` 变化 |
| 历史数据回填 | 不涉及 | 无数据结构变更和回填命令 |
| 数据清理/删除 | 不涉及 | 本次不删除生产数据 |
| Report Skill | 仅核对 | 继续使用生产当前已配置的不可变 Skill；本次不发布新版本 |
| MCP/默认 Agent | 仅核对 | MCP Tool、默认 Agent 和模型配置不变；核对现有配置可解析 |
| MinIO/对象存储 | 仅核对 | 报告图片接口依赖现有 MinIO；不新增 Bucket，不改变对象存储配置 |
| Digest/报告链路 | 发布 | Digest 内容和 Worker 不变；更新 Digest 到 Report Context 的 Projection 及 Run 错误归一 |
| 监控/配置/文档 | 发布 | 发布文档更新；运行并发、模型、Skill 和 MCP 配置不变 |

## 3. 版本与代码证据

- `origin/main..main` 的功能提交范围：`9185924` 至 `9c95c34`，共 9 个提交，包含报告前端改动和 Projection 最终修复。
- Report Projection 功能提交：`2475c83`。
- 合并提交：`9c95c34`。
- API 变更文件：报告图片 Handler、Report Context Projection、托管 Agent 状态同步及对应测试。
- Web 变更文件：报告页面、日报生成弹窗、成员报告浏览、Markdown Editor/Viewer、报告 Summary 工具及依赖锁文件。
- 无 migration、CLI、Skill 或 MCP 版本变更。
- 测试服最终 API 验证镜像：`sha256:9c04f7fc372c9daf22d56f6cbe8f6e0c32375829ca2039d8956fba398bffa1d2`；该测试标签不得直接作为生产不可变标签。

## 4. 变更项清单

| 变更项 | 文件/提交/版本 | 状态 | 验证命令或运行证据 | 影响范围 |
| --- | --- | --- | --- | --- |
| 报告图片上传 | `api/handler/report_image.go`、`api/main.go` | 待发布 | API 单元测试通过；生产发布后验证鉴权、大小错误和对象读取 | API、MinIO、报告编辑 |
| 报告编辑与展示 | `web/src/shared/components/MarkdownEditor`、`MarkdownViewer`、报告页面 | 待发布 | Web 业务测试、变更文件 lint、typecheck、build 通过 | 日报、周报记录与编辑 |
| 保存与弹窗交互 | `ReportsPage.tsx`、`WeeklyReportsPage.tsx`、`DailyReportGenerateModal.tsx` | 待发布 | Web 构建及变更文件 lint 通过 | 保存刷新、周报弹窗 |
| Projection 完整性 | `2475c83` | 待发布 | API 全量测试；真实个人 Run Context 48,969 bytes、28 条事实并成功写回 | 个人报告生成 |
| Git 元数据降噪 | `projection.go`、`service_test.go` | 待发布 | 中文冒号、换行、提交 SHA、分支名回归通过 | Report Context 内容 |
| Agent 错误归一 | `managed_agent_run_status_syncer.go`、测试 | 待发布 | 模型 500、超时、缺失写回测试通过；真实团队 Run 超时映射通过 | 报告失败展示 |

## 5. 发布前检查

- [x] API/Web 发布代码基线冻结为 `9c95c34`；最终生产构建必须确认该提交仍为待发布代码祖先。
- [x] `cd api && go test ./... -count=1`：通过。
- [x] `cd api && go vet ./...`：通过。
- [x] `cd web && pnpm test`：通过。
- [x] Web 本次变更 TS/TSX 文件 lint：通过。
- [x] `cd web && pnpm typecheck`、`pnpm build`：通过；构建仅有既有大 Chunk warning。
- [ ] Web 全仓 lint：存在本次未修改的 `HelpCenter.tsx` 一项 error 和 `TokenAnalyticsPage.tsx` 两项 warning；发布前确认该基线问题不由本次差异引入。
- [x] migration、CLI、Skill/MCP 版本变更均为不涉及。
- [ ] 记录生产现有 API/Web 容器 ID、镜像 ID、配置摘要和数据库 migration 最高版本。
- [ ] 为生产现有 API/Web 镜像创建明确回退标签；记录配置备份位置。
- [ ] 核对生产 MinIO 可用性、报告图片对象前缀和 API 服务凭据，不打印凭据。
- [ ] 核对生产默认 Report Skill owner/version、MCP、Agent 和模型配置，不做版本切换。
- [ ] 确认生产测试账号、个人日报测试数据和报告图片测试文件。

## 6. 执行步骤

| 步骤 | 执行主机 | 操作/命令 | 预期结果 | 实际结果 |
| --- | --- | --- | --- | --- |
| 1 | 生产部署主机 | 拉取并确认包含 `9c95c34` 的最终 `main` SHA，记录工作区状态 | 工作区干净，SHA 与发布清单一致 | 待执行 |
| 2 | 生产部署主机 | 记录 API/Web 当前容器和镜像，为当前镜像创建回退标签 | 回退目标可唯一定位 | 待执行 |
| 3 | 生产部署主机 | 从最终 `main` 分别构建 API、Web 不可变镜像 | 两个镜像构建成功并记录 ID | 待执行 |
| 4 | 生产部署主机 | 只更新 API 服务 | DB、MinIO、Web 不重启；API 健康检查通过 | 待执行 |
| 5 | 生产测试账号 | 验证现有 Session、Token、报告查询及一条个人日报 Run | 旧主链路正常；Run 最多一个外部 Session | 待执行 |
| 6 | 生产测试账号 | 上传一张允许格式的小图片并验证报告引用；再验证超限文件错误 | 上传、读取和友好错误正常 | 待执行 |
| 7 | 生产部署主机 | 只更新 Web 服务 | API、DB、MinIO 不重启；Web 首页返回成功 | 待执行 |
| 8 | 生产测试账号 | 浏览器验证日报/周报编辑、粘贴图片、保存刷新、记录视图和生成弹窗 | 页面行为与测试服一致 | 待执行 |
| 9 | 生产部署主机 | 核对日志和容器 ID，填写发布后结果 | 无持续 5xx、panic、写回异常或对象存储错误 | 待执行 |

## 7. 发布后验收

| 验收类别 | 用例 | 账号/数据 | 预期结果 | 实际结果 | 证据 |
| --- | --- | --- | --- | --- | --- |
| 自动化 | API/Web 构建门禁 | 发布 SHA | 与发布前结果一致 | 待执行 | 构建日志 |
| API/接口 | API 健康、报告图片上传与读取 | 生产测试账号、小图片、超限图片 | 200/预期错误码；无底层存储错误泄露 | 待执行 | 请求时间、状态码、对象引用 |
| AIDA CLI | 现有 CLI 回归 | 生产测试账号 | 上传和状态查询不受影响 | 待执行 | CLI 版本、Session ID |
| Report Skill/MCP/Agent | 个人日报生成 | 生产测试账号、可核对 Session | Skill/Context/写回各一次；不反复询问 | 待执行 | Run ID、Agent Session、Report ID |
| Digest/报告 | Projection 与错误展示 | 同上 | 不回退原始 Digest；失败时只展示稳定文案 | 待执行 | Context bytes/hash、Run 终态 |
| Token/Session | Codex、Claude Code 既有链路 | 生产测试账号 | Session 和 Token 统计无回归 | 待执行 | Session ID、Token 查询结果 |
| Web 浏览器 | 日报/周报编辑与记录 | 生产测试账号 | 粘贴图片、编辑、保存刷新、弹窗关闭和记录展示正常 | 待执行 | 浏览器人工记录 |

## 8. 回滚与停止条件

- API：恢复发布前记录的 API 不可变镜像，只重建 API，确认健康接口和报告查询恢复。
- Web：恢复发布前记录的 Web 不可变镜像，只重建 Web，确认页面可访问。
- CLI、Skill/MCP/Agent、migration、数据回填和数据清理：本次未修改，无回滚动作。
- Report Context 已按 Run 冻结，不修改历史 Context；终态 Run 不原地重跑。
- 出现以下任一情况立即停止：API/Web 持续 5xx；报告图片越权或对象不可读；个人报告无法创建或重复创建 Agent Session；Projection 回退原始 Digest；Codex/Claude Code 上传或 Token 统计回归；报告保存丢内容；无法确认回退镜像。
- 生产数据库不执行 DROP、TRUNCATE 或数据删除；本次没有 migration，禁止临时增加数据库操作。

## 9. 最终结果

```text
发布范围已完整列出：是
发布项执行结果已记录：否，当前仅完成发布准备
发布后验收已完成：否
阻断项数量：0；Web 全仓 lint 既有基线问题需在发布前确认
最终状态：未发布
```
