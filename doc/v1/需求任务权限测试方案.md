# 需求 / 任务权限测试方案

## 1. 测试目标

本方案用于让 Claude 或自动化脚本使用当前服务中的真实账号，对需求 / 任务权限改造做端到端验证。

测试目标：

1. 验证后端接口权限兜底，不能只依赖前端按钮显隐。
2. 验证前端 route、按钮、可操作入口与后端权限一致。
3. 验证不同角色的资源可见范围符合《需求任务权限设计.md》。
4. 验证 employee 不是只读用户：可以管理自己的任务，但不能影响别人的任务或需求主数据。
5. 验证 TL 只能管理自己团队范围或自己创建范围内的需求 / 任务。
6. 验证 PM / director / admin 在 P0 阶段具备全量管理能力。
7. 覆盖越权、跨团队、状态、依赖、删除关联数据等边界。

本方案优先以 **API 自动化测试** 作为权限验收基准，再用 **浏览器真实 UI 测试** 验证入口和按钮一致性。

---

## 2. 测试范围

### 2.1 覆盖模块

- 登录鉴权
- 用户 / 团队辅助数据
- 需求列表 / 详情 / 创建 / 编辑 / 状态流转 / 取消 / 恢复 / 删除 / AC
- 任务列表 / 详情 / 创建 / 编辑 / 负责人变更 / 状态 / 进度 / 依赖 / 删除
- 前端路由权限
- 前端按钮显隐和只读态

### 2.2 不覆盖模块

本次不以以下模块为主验收对象：

- 日报 / 周报
- Token 明细统计
- session 上传链路
- 风险通知完整链路
- 组织用户管理后台
- 归档字段实现；归档只保留产品语义，不纳入本轮 P0 权限测试

但如果需求 / 任务删除会影响 session / token / documents 关联字段，应覆盖删除副作用。

### 2.3 统一状态码规则

本方案所有 API 用例统一使用以下状态码口径，后续用例必须写成明确、唯一的预期状态码：

| 场景 | 明确预期 |
|---|---|
| 未登录、缺 token、token 过期、token 被篡改 | 401 |
| 列表接口越权范围 | 200，但只返回当前用户可见范围内的数据；越权数据不得出现 |
| 详情 GET 无查看权限 | 404，避免暴露资源存在性 |
| 修改 / 删除 / 状态变更 / 依赖维护无操作权限 | 403 |
| 删除因有关联任务 / session / token_usage / document 失败 | 409 |
| 参数非法、字段非法、状态非法、依赖非法 | 400 |
| 资源真实不存在 | 404 |

重点收敛：

- `GET /requirements/{id}` 跨团队不可见：404。
- `GET /tasks/{id}` 跨团队不可见：404。
- `PUT /requirements/{id}` 越权：403。
- `PUT /tasks/{id}` 越权：403。
- `DELETE` 越权：403。
- 删除有关联数据：409。
- 权限判断必须优先于业务副作用；无权限请求不能因为 AI 未配置、有关联数据、依赖非法等原因泄露资源状态。

---

## 3. 测试环境要求

### 3.1 服务要求

建议使用本地 docker compose 环境：

```bash
docker compose up -d db api web
```

默认地址：

```text
API: http://localhost:8080/api/v1
Web: http://localhost:5173 或 docker 暴露的 Web 地址
```

本文档中的接口路径默认都是相对 `AIDA_API_BASE` 的路径。`AIDA_API_BASE` 默认是 `http://localhost:8080/api/v1`。自动化脚本拼接 URL 时只能使用 `AIDA_API_BASE + 本文档路径`，例如 `AIDA_API_BASE + /users`，不要再额外拼接 `/api/v1`。

如果端口不同，执行测试前设置：

```bash
export AIDA_API_BASE="http://localhost:8080/api/v1"
export AIDA_WEB_BASE="http://localhost:5173"
```

### 3.2 数据库状态

建议使用干净数据库执行完整权限验收：

```bash
docker compose down -v
docker compose up -d db api web
```

如果不能重置数据库，自动化脚本必须：

1. 使用唯一测试前缀创建数据，例如 `PERM-E2E-<timestamp>`。
2. 只清理自己创建的测试数据。
3. 不修改非测试前缀的生产 / 手工数据。

### 3.3 测试账号

当前种子账号使用 `employee_id + password` 登录。默认密码为：

```text
123
```

| 角色 | 姓名 | employee_id | team |
|---|---|---|---|
| admin | 管理员 | admin | 无 |
| director | 李总监 | li_director | 无 |
| pm | 陈PM | chen_pm | 无 |
| team_leader | 刘TL | liu_tl | AI工程 |
| team_leader | 赵TL | zhao_tl | 推理加速 |
| team_leader | 孙TL | sun_tl | 模型训练 |
| employee | 张三 | zhangsan | AI工程 |
| employee | 李四 | lisi | AI工程 |
| employee | 王五 | wangwu | AI工程 |
| employee | 赵六 | zhaoliu | AI工程 |
| employee | 钱七 | qianqi | AI工程 |
| employee | 孙八 | sunba | 推理加速 |
| employee | 周九 | zhoujiu | 推理加速 |
| employee | 吴十 | wushi | 模型训练 |

团队 ID：

| 团队 | id |
|---|---|
| AI工程 | `a0000000-0000-0000-0000-000000000001` |
| 推理加速 | `a0000000-0000-0000-0000-000000000002` |
| 模型训练 | `a0000000-0000-0000-0000-000000000003` |

---

## 4. 自动化执行原则

### 4.1 Claude 执行要求

让 Claude 执行本方案时，应明确要求：

```text
请使用真实服务和真实账号执行，不要 mock API，不要只读代码推断。
优先用 API 自动化脚本完成权限矩阵验证，再用浏览器检查关键 UI 入口和按钮。
所有测试数据必须带唯一前缀，执行结束后清理。
遇到失败不要跳过，记录请求、角色、资源、期望状态码、实际状态码和响应体。
```

### 4.2 自动化脚本建议

推荐写一个临时测试脚本，例如：

```text
/tmp/aida-permission-e2e.<timestamp>.js
```

脚本职责：

1. 登录全部测试账号并保存 token。
2. 创建跨角色、跨团队测试夹具。
3. 按矩阵执行 API 请求。
4. 断言 HTTP 状态码和关键响应字段。
5. 输出 Markdown 或 JSON 报告。
6. 按依赖顺序清理测试数据。

禁止：

- 禁止使用 mock 数据。
- 禁止修改非测试前缀数据。
- 禁止为了通过测试而放宽断言。
- 禁止在测试失败后继续错误清理导致证据丢失。

---

## 5. 测试数据设计

### 5.1 命名前缀

每轮测试生成唯一前缀：

```text
PERM-E2E-YYYYMMDD-HHmmss
```

示例：

```text
PERM-E2E-20260625-153000
```

所有需求 / 任务 title 都必须包含该前缀。

### 5.2 需求夹具

至少创建以下需求：

| 代号 | 创建者 | team_ids | 用途 |
|---|---|---|---|
| REQ_ADMIN_AI | admin | AI工程 | 验证全量管理和 AI工程可见 |
| REQ_PM_ACCEL | pm | 推理加速 | 验证 PM 全量、跨团队可见 |
| REQ_TL_AI | 刘TL | AI工程 | 验证 TL 自己团队 / 自己创建需求 |
| REQ_TL_ACCEL | 赵TL | 推理加速 | 验证跨团队 TL 越权 |
| REQ_MULTI | director | AI工程 + 推理加速 | 验证多团队需求可见 |
| REQ_EMPTY_AC | pm | AI工程 | 验证需求 AC 可选且为空不影响权限 |
| REQ_DELETE_EMPTY | pm | AI工程 | 验证无关联需求可硬删除 |
| REQ_DELETE_LINKED | pm | AI工程 | 验证有关联任务时硬删除返回 409 |
| REQ_DELETE_SESSION | pm | AI工程 | 验证仅有关联 session 时硬删除返回 409 |
| REQ_DELETE_TOKEN | pm | AI工程 | 验证仅有关联 token_usage 时硬删除返回 409 |
| REQ_DELETE_DOC | pm | AI工程 | 验证仅有关联 document 时硬删除返回 409 |
| REQ_DELETE_FOLLOW_ONLY | pm | AI工程 | 验证仅有 follow 时可硬删除且清理 follow |
| REQ_ATOMIC_PARTIAL_TEAMS | admin | AI工程 + 非法 team_id | 验证创建需求 team_ids 部分非法时事务整体失败 |

需求基础字段建议：

```json
{
  "title": "PERM-E2E-... REQ_ADMIN_AI",
  "description": "权限自动化测试需求",
  "priority": "medium",
  "deadline": "2026-12-31",
  "team_ids": ["a0000000-0000-0000-0000-000000000001"],
  "acceptance_criteria": ["需求级 AC 1", "需求级 AC 2"]
}
```

### 5.3 任务夹具

至少创建以下任务：

| 代号 | 所属需求 | 创建者 | assignee | 用途 |
|---|---|---|---|---|
| TASK_AI_ZHANGSAN | REQ_ADMIN_AI | admin | 张三 | AI工程 employee 自己任务 |
| TASK_AI_LISI | REQ_ADMIN_AI | 刘TL | 李四 | 同团队别人任务 |
| TASK_ACCEL_SUNBA | REQ_PM_ACCEL | pm | 孙八 | 跨团队任务 |
| TASK_MULTI_ZHANGSAN | REQ_MULTI | pm | 张三 | 多团队需求下 AI工程任务 |
| TASK_MULTI_SUNBA | REQ_MULTI | pm | 孙八 | 多团队需求下 推理加速任务 |
| TASK_DEP_BASE | REQ_ADMIN_AI | 刘TL | 张三 | 依赖基准任务 |
| TASK_DEP_CHILD | REQ_ADMIN_AI | 刘TL | 张三 | 依赖子任务 |
| TASK_DELETE_OWN | REQ_ADMIN_AI | 张三或 admin 创建且 assignee 张三 | 张三 | employee 删除自己任务 |
| TASK_DELETE_OTHER | REQ_ADMIN_AI | admin | 李四 | employee 删除别人任务越权 |
| TASK_REASSIGN_ZHANGSAN | REQ_ADMIN_AI | 张三 | 张三，后续由 TL 改派李四 | 验证“自己的任务”按当前 assignee 判断，不按 creator 判断 |
| TASK_CROSS_DEP_TARGET | REQ_PM_ACCEL | pm | 孙八 | 验证不可见跨团队任务不能作为 employee 依赖目标 |
| TASK_ATOMIC_DEP_VALID | REQ_ADMIN_AI | 刘TL | 张三 | 验证创建任务依赖列表部分非法时整体失败 |
| TASK_ATOMIC_DEP_INVALID | REQ_PM_ACCEL | pm | 孙八 | 验证跨需求非法依赖 |

任务基础字段建议：

```json
{
  "requirement_id": "<REQ_ID>",
  "title": "PERM-E2E-... TASK_AI_ZHANGSAN",
  "acceptance_criteria": ["任务级 AC 1"],
  "assignee_id": "c0000000-0000-0000-0000-000000000001",
  "priority": "medium",
  "due_date": "2026-12-31"
}
```

---

## 6. API 测试基础流程

### 6.1 登录并保存 token

对所有测试账号执行：

```http
POST /auth/login
Content-Type: application/json

{
  "employee_id": "zhangsan",
  "password": "123"
}
```

断言：

- HTTP 200
- 返回 token
- 返回 user.role 正确
- 返回 team_id 与种子数据一致

负向登录：

| 场景 | 请求 | 期望 |
|---|---|---|
| 错误密码 | `password=wrong` | 401 |
| 不存在 employee_id | `employee_id=no_such_user` | 401 |
| 缺少 token 访问 `/requirements` | 无 Authorization | 401 |
| 非 Bearer 格式 | `Authorization: token xxx` | 401 |

### 6.2 辅助数据接口

| 接口 | 角色 | 期望 |
|---|---|---|
| `GET /users` 未登录 | anonymous | 401 |
| `GET /users` 已登录 | admin / li_director / chen_pm / liu_tl / zhangsan | 200，返回用户基础列表；该接口只做登录鉴权，不作为任务负责人候选范围断言 |
| `GET /task-assignees` admin/director/pm | admin / li_director / chen_pm | 200，仅返回全部 employee 用户 |
| `GET /task-assignees` TL | liu_tl | 200，仅返回自己团队 employee |
| `GET /task-assignees` employee | zhangsan | 200，仅返回当前登录用户自己 |
| `GET /teams` 未登录 | anonymous | 401 |
| `GET /teams` 登录用户 | 任意 | 200，返回全部团队基础信息 |

`/users` 只验证登录鉴权，不用于负责人选择器；`/task-assignees` 的范围是强断言，前端负责人选择器只能使用 `/task-assignees`。

---

## 7. 需求权限 API 测试矩阵

### 7.1 需求列表可见范围

接口：

```http
GET /requirements
```

| 角色 | 账号 | 必须能看到 | 必须不能看到 |
|---|---|---|---|
| admin | admin | 全部测试需求 | 无 |
| director | li_director | 全部测试需求 | 无 |
| pm | chen_pm | 全部测试需求 | 无 |
| TL-AI | liu_tl | AI工程相关需求、REQ_MULTI、自己创建需求 | 纯推理加速 REQ_TL_ACCEL / REQ_PM_ACCEL |
| TL-推理 | zhao_tl | 推理加速相关需求、REQ_MULTI、自己创建需求 | 纯 AI工程 REQ_ADMIN_AI / REQ_TL_AI |
| employee-AI | zhangsan | AI工程相关需求、REQ_MULTI | 纯推理加速需求 |
| employee-推理 | sunba | 推理加速相关需求、REQ_MULTI | 纯 AI工程需求 |
| employee-模型 | wushi | 模型训练相关需求 | AI工程 / 推理加速需求 |

边界：

```http
GET /requirements?team_id=<其他团队ID>
```

断言：

- employee / TL 不能通过 `team_id` 参数扩大到其他团队。
- TL 传其他团队 ID 时，返回 200 空列表。

### 7.2 需求详情可见范围

接口：

```http
GET /requirements/{id}
```

| 场景 | 账号 | 资源 | 期望 |
|---|---|---|---|
| admin 看任意需求 | admin | 任意测试需求 | 200 |
| PM 看任意需求 | chen_pm | 任意测试需求 | 200 |
| TL 看自己团队需求 | liu_tl | REQ_ADMIN_AI | 200 |
| TL 看自己创建需求 | liu_tl | REQ_TL_AI | 200 |
| TL 跨团队看无关需求 | liu_tl | REQ_PM_ACCEL | 404 |
| employee 看本团队需求 | zhangsan | REQ_ADMIN_AI | 200 |
| employee 看多团队需求 | zhangsan | REQ_MULTI | 200 |
| employee 跨团队看需求 | zhangsan | REQ_PM_ACCEL | 404 |
| 不存在 ID | admin | random uuid | 404 |

### 7.3 创建需求

接口：

```http
POST /requirements
```

| 场景 | 账号 | team_ids | 期望 |
|---|---|---|---|
| admin 创建任意团队需求 | admin | AI工程 | 201 |
| director 创建任意团队需求 | li_director | 推理加速 | 201 |
| PM 创建任意团队需求 | chen_pm | 模型训练 | 201 |
| TL 创建自己团队需求 | liu_tl | AI工程 | 201 |
| TL 创建不含自己团队需求 | liu_tl | 推理加速 | 403 |
| TL 创建多团队且包含自己团队 | liu_tl | AI工程 + 推理加速 | 403；P0 默认 TL 只能选择自己团队。[待确认] 如业务必须允许 TL 发起跨团队需求，可改为 201，但仍必须包含自己 team_id |
| employee 创建需求 | zhangsan | AI工程 | 403 |
| 缺 title | admin | AI工程 | 400 |
| 缺 description | admin | AI工程 | 400 |
| 缺 team_ids | admin | 空数组 | 400 |
| 不存在 team_id | admin | random uuid | 400 |
| acceptance_criteria 为空 | pm | AI工程 | 201，且返回空数组 |

### 7.4 编辑需求基础信息 / 状态

接口：

```http
PUT /requirements/{id}
```

字段分别测试：

- title
- description
- priority
- deadline
- feishu_doc_url
- acceptance_criteria
- status

| 场景 | 账号 | 资源 | 期望 |
|---|---|---|---|
| admin 编辑任意需求 | admin | 任意 | 200 |
| director 编辑任意需求 | li_director | 任意 | 200 |
| PM 编辑任意需求 | chen_pm | 任意 | 200 |
| TL 编辑本团队需求 | liu_tl | REQ_ADMIN_AI | 200 |
| TL 编辑自己创建需求 | liu_tl | REQ_TL_AI | 200 |
| TL 编辑跨团队无关需求 | liu_tl | REQ_PM_ACCEL | 403 |
| employee 编辑本团队需求 | zhangsan | REQ_ADMIN_AI | 403 |
| employee 取消本团队需求 | zhangsan | status=cancelled | 403 |
| 非法 status | admin | status=invalid | 400 |
| status=completed | admin | 任意 | 200；如果需求模型返回 `completed_at`，断言非空；如果当前模型没有该字段，则不断言 `completed_at` |
| status 从 completed 改回 active/todo | admin | 任意 | 200；如果需求模型返回 `completed_at`，断言清空；如果当前模型没有该字段，则不断言 `completed_at` |

### 7.5 取消 / 恢复需求

取消当前使用：

```http
PUT /requirements/{id}
{"status":"cancelled"}
```

恢复接口：

```http
PUT /requirements/{id}/restore
```

| 场景 | 账号 | 资源 | 期望 |
|---|---|---|---|
| admin 取消任意需求 | admin | 任意 | 200 |
| PM 取消任意需求 | chen_pm | 任意 | 200 |
| TL 取消本团队需求 | liu_tl | REQ_ADMIN_AI | 200 |
| TL 取消跨团队需求 | liu_tl | REQ_PM_ACCEL | 403 |
| employee 取消需求 | zhangsan | REQ_ADMIN_AI | 403 |
| admin 恢复 cancelled 需求 | admin | cancelled | 200 |
| PM 恢复 cancelled 需求 | chen_pm | cancelled | 200 |
| TL 恢复本团队 cancelled 需求 | liu_tl | 本团队 cancelled | 200 |
| TL 恢复跨团队 cancelled 需求 | liu_tl | 跨团队 | 403 |
| employee 恢复需求 | zhangsan | cancelled | 403 |
| 恢复非 cancelled 需求 | admin | active/todo | 409 |

### 7.6 删除需求

接口：

```http
DELETE /requirements/{id}
```

| 场景 | 账号 | 资源 | 期望 |
|---|---|---|---|
| admin 删除无关联需求 | admin | REQ_DELETE_EMPTY | 200 |
| PM 删除无关联需求 | chen_pm | 新建空需求 | 200 |
| TL 删除本团队无关联需求 | liu_tl | 本团队空需求 | 200 |
| TL 删除跨团队需求 | liu_tl | 推理加速空需求 | 403 |
| employee 删除需求 | zhangsan | 本团队空需求 | 403 |
| 删除有关联任务的需求 | admin | REQ_DELETE_LINKED | 409 |
| 删除不存在需求 | admin | random uuid | 404 |

断言删除副作用：

- 删除成功后 `GET /requirements/{id}` 返回 404。
- 删除失败 409 时，需求仍可查询。

### 7.7 需求 AC

接口：

```http
GET /requirements/{id}/ac
POST /requirements/{id}/regenerate-ac
```

| 场景 | 账号 | 资源 | 期望 |
|---|---|---|---|
| employee 查看本团队 AC | zhangsan | REQ_ADMIN_AI | 200 |
| employee 查看跨团队 AC | zhangsan | REQ_PM_ACCEL | 404 |
| admin regenerate AC，AI 正常 | admin | 任意 | 200 |
| admin regenerate AC，AI 未配置 | admin | 任意 | 503，不能返回 403 |
| director regenerate AC，AI 正常 | li_director | 任意 | 200 |
| director regenerate AC，AI 未配置 | li_director | 任意 | 503，不能返回 403 |
| PM regenerate AC，AI 正常 | chen_pm | 任意 | 200 |
| PM regenerate AC，AI 未配置 | chen_pm | 任意 | 503，不能返回 403 |
| TL regenerate 本团队 AC，AI 正常 | liu_tl | REQ_ADMIN_AI | 200 |
| TL regenerate 本团队 AC，AI 未配置 | liu_tl | REQ_ADMIN_AI | 503，不能返回 403 |
| TL regenerate 跨团队 AC | liu_tl | REQ_PM_ACCEL | 403，不触发 AI |
| employee regenerate 本团队 AC | zhangsan | REQ_ADMIN_AI | 403，不触发 AI |
| employee regenerate 跨团队 AC | zhangsan | REQ_PM_ACCEL | 404，不触发 AI |

regenerate AC 的强断言：有权限且 AI 正常返回 200；有权限但 AI 未配置返回 503；有权限用户不能返回 403。无权限用户必须先被权限拦截，禁止因为 AI 未配置返回 503。

---

## 8. 任务权限 API 测试矩阵

### 8.1 任务列表可见范围

接口：

```http
GET /tasks
GET /tasks?scope=requirements
GET /tasks?requirement_id=<id>
GET /tasks?assignee_id=<id>
GET /tasks?status=todo
```

| 角色 | 账号 | 必须能看到 | 必须不能看到 |
|---|---|---|---|
| admin | admin | 全部测试任务 | 无 |
| director | li_director | 全部测试任务 | 无 |
| pm | chen_pm | 全部测试任务 | 无 |
| TL-AI | liu_tl | AI工程成员任务、AI工程需求下任务、自己创建任务 | 纯推理加速任务 |
| TL-推理 | zhao_tl | 推理加速成员任务、推理需求下任务、自己创建任务 | 纯 AI工程任务 |
| employee-AI | zhangsan | AI工程成员任务、AI工程参与需求下任务、REQ_MULTI 下全部任务（含 TASK_MULTI_SUNBA） | 纯推理加速且 AI工程未参与需求的任务 |
| employee-推理 | sunba | 推理加速成员任务、推理加速参与需求下任务、REQ_MULTI 下全部任务（含 TASK_MULTI_ZHANGSAN） | 纯 AI工程且推理加速未参与需求的任务 |

边界强断言：

```http
GET /tasks?scope=requirements
```

employee 不得因为 `scope=requirements` 看到当前可见集合之外的任务；当前可见集合为：负责人属于自己团队，或任务所属 requirement 的 `requirement_teams` 包含自己团队。

### 8.2 任务详情可见范围

接口：

```http
GET /tasks/{id}
```

| 场景 | 账号 | 资源 | 期望 |
|---|---|---|---|
| admin 看任意任务 | admin | 任意 | 200 |
| PM 看任意任务 | chen_pm | 任意 | 200 |
| TL 看本团队任务 | liu_tl | TASK_AI_ZHANGSAN | 200 |
| TL 看跨团队任务 | liu_tl | TASK_ACCEL_SUNBA | 404 |
| employee 看自己任务 | zhangsan | TASK_AI_ZHANGSAN | 200 |
| employee 看同团队别人任务 | zhangsan | TASK_AI_LISI | 200 |
| employee 看纯跨团队任务 | zhangsan | TASK_ACCEL_SUNBA | 404 |
| employee 看多团队需求下其他团队任务 | zhangsan | TASK_MULTI_SUNBA | 200，只读；所有编辑类 `can_xxx=false` |
| 不存在任务 | admin | random uuid | 404 |

### 8.3 创建任务

接口：

```http
POST /tasks
```

| 场景 | 账号 | requirement | assignee | 期望 |
|---|---|---|---|---|
| admin 给任意 employee 创建任务 | admin | 任意 | 任意 employee | 201 |
| director 给任意 employee 创建任务 | li_director | 任意 | 任意 employee | 201 |
| PM 给任意 employee 创建任务 | chen_pm | 任意 | 任意 employee | 201 |
| TL 给本团队 employee 创建任务 | liu_tl | AI工程需求 | 张三 | 201 |
| TL 给跨团队 employee 创建任务 | liu_tl | AI工程需求 | 孙八 | 403 |
| TL 在跨团队需求下创建任务 | liu_tl | 推理加速需求 | 张三 | 403 |
| employee 给自己创建任务 | zhangsan | AI工程需求 | 张三 | 201 |
| employee 给别人创建任务 | zhangsan | AI工程需求 | 李四 | 403 |
| employee 在跨团队需求下给自己创建 | zhangsan | 推理加速需求 | 张三 | 403 |
| 缺 requirement_id | admin | 空 | 张三 | 400 |
| 缺 title | admin | AI工程需求 | 张三 | 400 |
| admin 缺 assignee_id | admin | AI工程需求 | 空 | 400 |
| employee 缺 assignee_id | zhangsan | AI工程需求 | 空 | 201，默认 assignee 为当前登录用户 |
| assignee 不存在 | admin | AI工程需求 | random uuid | 400 |
| priority 为空 | admin | AI工程需求 | 张三 | 201，默认 medium |
| acceptance_criteria 为空 | admin | AI工程需求 | 张三 | 201，返回空数组 |
| dependency_task_ids 合法 | admin | 同需求已有任务 | 张三 | 201 |
| dependency_task_ids 跨需求 | admin | AI工程需求 | 依赖其他需求任务 | 400 |

### 8.4 编辑任务元数据

接口：

```http
PUT /tasks/{id}
```

字段分别测试：

- title
- priority
- due_date
- acceptance_criteria
- assignee_id
- status
- progress

| 场景 | 账号 | 资源 | 字段 | 期望 |
|---|---|---|---|---|
| admin 编辑任意任务 | admin | 任意 | title/priority/due_date/AC | 200 |
| PM 编辑任意任务 | chen_pm | 任意 | title/priority/due_date/AC | 200 |
| TL 编辑本团队任务 | liu_tl | TASK_AI_ZHANGSAN | title/priority/due_date/AC | 200 |
| TL 编辑跨团队任务 | liu_tl | TASK_ACCEL_SUNBA | title | 403 |
| employee 编辑自己任务 | zhangsan | TASK_AI_ZHANGSAN | title/priority/due_date/AC | 200 |
| employee 编辑同团队别人任务 | zhangsan | TASK_AI_LISI | title | 403 |
| employee 编辑纯跨团队不可见任务 | zhangsan | TASK_ACCEL_SUNBA | title | 404 |
| employee 编辑多团队需求下其他团队任务 | zhangsan | TASK_MULTI_SUNBA | title | 403 |
| employee 修改自己任务 assignee | zhangsan | TASK_AI_ZHANGSAN | assignee_id=李四 | 403 |
| TL 改派给本团队 employee | liu_tl | TASK_AI_ZHANGSAN | assignee_id=李四 | 200 |
| TL 改派给跨团队 employee | liu_tl | TASK_AI_ZHANGSAN | assignee_id=孙八 | 403 |
| admin 改派给任意 employee | admin | 任意 | assignee_id=孙八 | 200 |
| 设置非法 status | admin | 任意 | status=invalid | 400 |
| 设置 blocked | admin | 任意 | status=blocked | 400 |
| progress < 0 | admin | 任意 | progress=-1 | 400 |
| progress > 100 | admin | 任意 | progress=101 | 400 |
| status=done | admin | 任意 | status=done | 200，progress=100，completed_at 有值 |

### 8.5 修改任务状态

接口：

```http
PUT /tasks/{id}/status
```

| 场景 | 账号 | 资源 | status | 期望 |
|---|---|---|---|---|
| admin 改任意任务状态 | admin | 任意 | in_progress | 200 |
| PM 改任意任务状态 | chen_pm | 任意 | done | 200 |
| TL 改本团队任务状态 | liu_tl | TASK_AI_ZHANGSAN | done | 200 |
| TL 改跨团队任务状态 | liu_tl | TASK_ACCEL_SUNBA | done | 403 |
| employee 改自己任务状态 | zhangsan | TASK_AI_ZHANGSAN | in_progress | 200 |
| employee 改同团队别人任务状态 | zhangsan | TASK_AI_LISI | done | 403 |
| employee 改纯跨团队不可见任务状态 | zhangsan | TASK_ACCEL_SUNBA | done | 404 |
| employee 改多团队需求下其他团队任务状态 | zhangsan | TASK_MULTI_SUNBA | done | 403 |
| 设置 blocked | admin | 任意 | blocked | 400 |
| 设置非法状态 | admin | 任意 | invalid | 400 |
| 设置 done | admin | 任意 | done | 200，progress=100，completed_at 非空 |
| done 后 reopen 到 todo | admin | 任意 | todo | 200，completed_at 清空 |
| done 后 reopen 到 in_progress | admin | 任意 | in_progress | 200，completed_at 清空 |

### 8.6 修改任务进度

接口：

```http
PUT /tasks/{id}/progress
```

| 场景 | 账号 | 资源 | progress | 期望 |
|---|---|---|---|---|
| admin 改任意任务进度 | admin | 任意 | 50 | 200 |
| PM 改任意任务进度 | chen_pm | 任意 | 50 | 200 |
| TL 改本团队任务进度 | liu_tl | TASK_AI_ZHANGSAN | 60 | 200 |
| TL 改跨团队任务进度 | liu_tl | TASK_ACCEL_SUNBA | 60 | 403 |
| employee 改自己任务进度 | zhangsan | TASK_AI_ZHANGSAN | 70 | 200 |
| employee 改同团队别人任务进度 | zhangsan | TASK_AI_LISI | 70 | 403 |
| employee 改多团队需求下其他团队任务进度 | zhangsan | TASK_MULTI_SUNBA | 70 | 403 |
| progress=-1 | admin | 任意 | -1 | 400 |
| progress=101 | admin | 任意 | 101 | 400 |
| progress=0 | admin | 任意 | 0 | 200 |
| progress=100 | admin | 任意 | 100 | 200 |

同时断言：

- 父需求 progress 被重算。
- 多任务情况下 progress 为任务 progress 聚合值，符合当前实现。

### 8.7 任务依赖

接口：

```http
POST /tasks/{id}/dependencies
DELETE /tasks/{id}/dependencies/{dep_id}
```

| 场景 | 账号 | 任务 | 依赖任务 | 期望 |
|---|---|---|---|---|
| admin 添加合法依赖 | admin | TASK_DEP_CHILD | TASK_DEP_BASE | 200 |
| PM 添加合法依赖 | chen_pm | 任意 | 同需求任务 | 200 |
| TL 给本团队任务添加依赖 | liu_tl | TASK_DEP_CHILD | TASK_DEP_BASE | 200 |
| TL 给跨团队任务添加依赖 | liu_tl | TASK_ACCEL_SUNBA | 同需求任务 | 403 |
| employee 给自己任务添加依赖 | zhangsan | 自己任务 | 同需求任务 | 200 |
| employee 给别人任务添加依赖 | zhangsan | TASK_AI_LISI | 同需求任务 | 403 |
| 依赖自己 | admin | TASK_DEP_CHILD | TASK_DEP_CHILD | 400 |
| 跨需求依赖 | admin | TASK_DEP_CHILD | 其他需求任务 | 400 |
| 重复添加依赖 | admin | TASK_DEP_CHILD | TASK_DEP_BASE | 200，幂等成功且不能生成重复数据 |
| 形成环 | admin | TASK_DEP_BASE | TASK_DEP_CHILD | 400 |
| 删除合法依赖 | admin | TASK_DEP_CHILD | TASK_DEP_BASE | 200 |
| employee 删除自己任务依赖 | zhangsan | 自己任务 | dep | 200 |
| employee 删除别人任务依赖 | zhangsan | TASK_AI_LISI | dep | 403 |

阻塞状态断言：

1. 给子任务添加未完成上游依赖。
2. 查询子任务详情。
3. 断言 `display_status` 或展示状态为 `blocked`。
4. 尝试直接设置 `status=blocked`，断言 400。
5. 将上游任务设为 done。
6. 再查子任务，断言不再 blocked，`display_status` 回到存储状态。

### 8.8 删除任务

接口：

```http
DELETE /tasks/{id}
```

| 场景 | 账号 | 资源 | 期望 |
|---|---|---|---|
| admin 删除任意任务 | admin | 任意测试任务 | 200 |
| PM 删除任意任务 | chen_pm | 任意测试任务 | 200 |
| TL 删除本团队任务 | liu_tl | AI工程任务 | 200 |
| TL 删除跨团队任务 | liu_tl | 推理加速任务 | 403 |
| employee 删除自己任务 | zhangsan | assignee=张三 | 200 |
| employee 删除别人任务 | zhangsan | assignee=李四 | 403 |
| 删除不存在任务 | admin | random uuid | 404 |

删除副作用断言：

- 删除成功后 `GET /tasks/{id}` 返回 404。
- 父需求 progress 被重算。
- 删除任务成功后 `sessions.task_id` 置空。
- 删除任务成功后 `token_usage.task_id` 置空。
- 删除任务成功后 `documents.task_id` 置空。
- 删除任务成功后 task follow 被清理。
- 删除任务成功后 task 本体删除。

### 8.9 多团队需求下任务可见边界

最新产品口径：

- 需求可见按 `requirement_teams` 判断。
- employee 可见任务集合为：负责人属于自己团队，或任务所属 requirement 的 `requirement_teams` 包含自己团队。
- 多团队需求下，参与团队 employee 可以看到该需求下所有拆解任务，包括其他团队 assignee 的任务。
- 可见不等于可管理；employee 对同团队别人任务、同一多团队需求下其他团队任务都只能只读。
- 如果任务负责人为空，按所属需求团队判断；只要需求包含当前 employee 团队即可见。

| 场景 | 测试账号 | 资源 | 请求 / 操作 | 预期结果 |
|---|---|---|---|---|
| AI 工程员工查看多团队需求 | zhangsan | REQ_MULTI | `GET /requirements/{REQ_MULTI}` | 200 |
| AI 工程员工查看多团队需求下本团队任务 | zhangsan | TASK_MULTI_ZHANGSAN | `GET /tasks/{TASK_MULTI_ZHANGSAN}` | 200 |
| AI 工程员工查看多团队需求下推理加速任务 | zhangsan | TASK_MULTI_SUNBA | `GET /tasks/{TASK_MULTI_SUNBA}` | 200，只读；所有编辑类 `can_xxx=false` |
| 推理加速员工查看多团队需求 | sunba | REQ_MULTI | `GET /requirements/{REQ_MULTI}` | 200 |
| 推理加速员工查看多团队需求下推理加速任务 | sunba | TASK_MULTI_SUNBA | `GET /tasks/{TASK_MULTI_SUNBA}` | 200 |
| 推理加速员工查看多团队需求下 AI 工程任务 | sunba | TASK_MULTI_ZHANGSAN | `GET /tasks/{TASK_MULTI_ZHANGSAN}` | 200，只读；所有编辑类 `can_xxx=false` |
| AI 工程员工拉取多团队需求任务列表 | zhangsan | REQ_MULTI | `GET /tasks?requirement_id={REQ_MULTI}` | 200，包含 TASK_MULTI_ZHANGSAN 和 TASK_MULTI_SUNBA；TASK_MULTI_SUNBA 只读 |
| 推理加速员工拉取多团队需求任务列表 | sunba | REQ_MULTI | `GET /tasks?requirement_id={REQ_MULTI}` | 200，包含 TASK_MULTI_SUNBA 和 TASK_MULTI_ZHANGSAN；TASK_MULTI_ZHANGSAN 只读 |

### 8.10 employee 任务归属变化边界

P0 明确口径：`自己的任务 = task.assignee_id = 当前用户 ID`。不能因为任务最初由某 employee 创建，就永久保留该 employee 的编辑 / 删除权限。

| 场景 | 测试账号 | 资源 | 请求 / 操作 | 预期结果 |
|---|---|---|---|---|
| employee 创建自己的任务 | zhangsan | REQ_ADMIN_AI | `POST /tasks`，`assignee_id=zhangsan` | 201 |
| 创建后编辑自己的任务 | zhangsan | TASK_REASSIGN_ZHANGSAN | `PUT /tasks/{id}` 修改 title | 200 |
| 创建后删除自己的任务 | zhangsan | 临时自建任务 | `DELETE /tasks/{id}` | 200 |
| TL 改派张三任务给李四 | liu_tl | TASK_REASSIGN_ZHANGSAN | `PUT /tasks/{id}`，`assignee_id=lisi` | 200 |
| 改派后原负责人编辑任务 | zhangsan | TASK_REASSIGN_ZHANGSAN | `PUT /tasks/{id}` 修改 title | 403 |
| 改派后原负责人删除任务 | zhangsan | TASK_REASSIGN_ZHANGSAN | `DELETE /tasks/{id}` | 403 |
| 改派后原负责人改状态 | zhangsan | TASK_REASSIGN_ZHANGSAN | `PUT /tasks/{id}/status` | 403 |
| 改派后原负责人改进度 | zhangsan | TASK_REASSIGN_ZHANGSAN | `PUT /tasks/{id}/progress` | 403 |
| 改派后原负责人查看任务 | zhangsan | TASK_REASSIGN_ZHANGSAN | `GET /tasks/{id}` | 200，只读；因为任务仍属于 AI 工程团队 |
| 改派后新负责人编辑任务 | lisi | TASK_REASSIGN_ZHANGSAN | `PUT /tasks/{id}` 修改 title | 200 |
| 改派后新负责人删除任务 | lisi | TASK_REASSIGN_ZHANGSAN | `DELETE /tasks/{id}` | 200 |

### 8.11 查询参数组合越权

核心规则：所有查询参数只能在当前用户可见范围内做交集过滤，不能扩大权限。列表接口统一返回 200；越权资源不得出现在结果中。

| 场景 | 测试账号 | 资源 / 参数 | 请求 / 操作 | 预期结果 |
|---|---|---|---|---|
| employee 查询纯跨团队负责人任务 | zhangsan | `assignee_id=sunba_id` | `GET /tasks?assignee_id={sunba_id}` | 200，只能返回当前可见集合内且 assignee=sunba 的任务；可包含 TASK_MULTI_SUNBA，不得包含 TASK_ACCEL_SUNBA |
| employee 查询跨团队需求下任务 | zhangsan | REQ_PM_ACCEL | `GET /tasks?requirement_id={REQ_PM_ACCEL}` | 200，空列表 |
| employee 使用 scope + 跨团队负责人 | zhangsan | `scope=requirements&assignee_id=sunba_id` | `GET /tasks?scope=requirements&assignee_id={sunba_id}` | 200，空列表 |
| employee 使用状态 + 跨团队负责人 | zhangsan | `status=todo&assignee_id=sunba_id` | `GET /tasks?status=todo&assignee_id={sunba_id}` | 200，空列表 |
| employee 使用 scope + 跨团队需求 | zhangsan | REQ_PM_ACCEL | `GET /tasks?scope=requirements&requirement_id={REQ_PM_ACCEL}` | 200，空列表 |
| TL 使用 team_id 查询其他团队 active 需求 | liu_tl | 推理加速 team_id | `GET /requirements?team_id={ACCEL_TEAM}&status=active` | 200，空列表 |
| employee 使用 team_id 查询其他团队需求 | zhangsan | 推理加速 team_id | `GET /requirements?team_id={ACCEL_TEAM}` | 200，空列表 |
| employee 使用 team_id + status 查询其他团队需求 | zhangsan | 推理加速 team_id | `GET /requirements?team_id={ACCEL_TEAM}&status=todo` | 200，空列表 |

### 8.12 employee 创建任务字段越权

最新产品口径：employee 可以创建自己的任务；创建接口不得接受调用方伪造身份字段。employee 创建任务不传 `assignee_id` 时，后端默认使用当前登录用户 ID，返回 201。

| 场景 | 测试账号 | 资源 / 请求体 | 请求 / 操作 | 预期结果 |
|---|---|---|---|---|
| employee 创建自己的任务 | zhangsan | `requirement_id=REQ_ADMIN_AI, assignee_id=zhangsan` | `POST /tasks` | 201 |
| employee 创建任务不传 assignee_id | zhangsan | `requirement_id=REQ_ADMIN_AI`，无 `assignee_id` | `POST /tasks` | 201，后端默认 assignee 为当前用户 zhangsan |
| employee 创建同团队别人任务 | zhangsan | `assignee_id=lisi` | `POST /tasks` | 403 |
| employee 创建跨团队别人任务 | zhangsan | `assignee_id=sunba` | `POST /tasks` | 403 |
| employee 在自己团队需求下创建任务 | zhangsan | REQ_ADMIN_AI | `POST /tasks` | 201 |
| employee 在跨团队需求下创建任务 | zhangsan | REQ_PM_ACCEL | `POST /tasks` | 403 |
| employee 创建任务伪造 creator_id | zhangsan | body 带 `creator_id=admin` | `POST /tasks` | 201，响应和数据库使用当前用户身份，不使用请求体 creator_id |
| employee 创建任务伪造 creator_role | zhangsan | body 带 `creator_role=admin` | `POST /tasks` | 201，后端忽略伪造字段 |
| employee 创建任务伪造 creator_tl_id | zhangsan | body 带 `creator_tl_id=liu_tl` | `POST /tasks` | 201，后端忽略伪造字段 |
| employee 创建任务时传 done 状态 | zhangsan | body 带 `status=done` | `POST /tasks` | 400；创建接口不接受初始状态覆盖 |
| employee 创建任务时传 progress=100 | zhangsan | body 带 `progress=100` | `POST /tasks` | 400；创建接口不接受初始进度覆盖 |

### 8.13 employee 编辑任务字段白名单

employee 编辑自己的任务允许字段：

- `title`
- `priority`
- `due_date`
- `acceptance_criteria`
- `status`
- `progress`
- `dependencies`

employee 禁止修改字段：

- `assignee_id`
- `requirement_id`
- `creator_id`
- `creator_role`
- `creator_tl_id`
- `team_id`
- `created_at`
- `updated_at`
- `completed_at`

| 场景 | 测试账号 | 资源 / 字段 | 请求 / 操作 | 预期结果 |
|---|---|---|---|---|
| employee 修改自己任务 title | zhangsan | TASK_AI_ZHANGSAN | `PUT /tasks/{id}` | 200 |
| employee 修改自己任务 priority | zhangsan | TASK_AI_ZHANGSAN | `PUT /tasks/{id}` | 200 |
| employee 修改自己任务 due_date | zhangsan | TASK_AI_ZHANGSAN | `PUT /tasks/{id}` | 200 |
| employee 修改自己任务 acceptance_criteria | zhangsan | TASK_AI_ZHANGSAN | `PUT /tasks/{id}` | 200 |
| employee 修改自己任务 assignee_id | zhangsan | TASK_AI_ZHANGSAN | `PUT /tasks/{id}` | 403 |
| employee 修改自己任务 requirement_id 到跨团队需求 | zhangsan | REQ_PM_ACCEL | `PUT /tasks/{id}` | 403 |
| employee 修改自己任务 creator_id | zhangsan | TASK_AI_ZHANGSAN | `PUT /tasks/{id}` | 400，非法字段不允许出现在请求体 |
| employee 修改自己任务 creator_tl_id | zhangsan | TASK_AI_ZHANGSAN | `PUT /tasks/{id}` | 400，非法字段不允许出现在请求体 |
| employee 修改自己任务 created_at / updated_at / completed_at | zhangsan | TASK_AI_ZHANGSAN | `PUT /tasks/{id}` | 400 |
| employee 修改别人任务 title | zhangsan | TASK_AI_LISI | `PUT /tasks/{id}` | 403 |
| employee 修改别人任务 status | zhangsan | TASK_AI_LISI | `PUT /tasks/{id}/status` | 403 |
| employee 修改别人任务 progress | zhangsan | TASK_AI_LISI | `PUT /tasks/{id}/progress` | 403 |

### 8.14 依赖权限和依赖目标边界

依赖维护必须同时满足：操作者可管理当前任务、依赖目标在当前用户可见范围内、依赖关系本身合法。权限不足时优先返回 403；资源不可见详情语义为 404；依赖参数非法为 400。

| 场景 | 测试账号 | 任务 | 依赖目标 | 请求 / 操作 | 预期结果 |
|---|---|---|---|---|
| employee 给自己任务添加同团队任务依赖 | zhangsan | TASK_DEP_CHILD | TASK_DEP_BASE | `POST /tasks/{id}/dependencies` | 200 |
| employee 给自己任务添加纯跨团队不可见任务依赖 | zhangsan | TASK_DEP_CHILD | TASK_CROSS_DEP_TARGET | `POST /tasks/{id}/dependencies` | 404 |
| employee 给自己任务添加同一多团队需求下其他团队任务依赖 | zhangsan | TASK_MULTI_ZHANGSAN | TASK_MULTI_SUNBA | `POST /tasks/{id}/dependencies` | 200 |
| employee 给自己任务添加可见但跨需求任务依赖 | zhangsan | TASK_DEP_CHILD | TASK_MULTI_SUNBA | `POST /tasks/{id}/dependencies` | 400 |
| employee 给别人任务添加依赖 | zhangsan | TASK_AI_LISI | TASK_DEP_BASE | `POST /tasks/{id}/dependencies` | 403 |
| employee 删除自己任务上的依赖 | zhangsan | TASK_DEP_CHILD | TASK_DEP_BASE | `DELETE /tasks/{id}/dependencies/{dep_id}` | 200 |
| employee 删除别人任务上的依赖 | zhangsan | TASK_AI_LISI | 任意 dep | `DELETE /tasks/{id}/dependencies/{dep_id}` | 403 |
| TL 给本团队任务添加依赖 | liu_tl | TASK_DEP_CHILD | TASK_DEP_BASE | `POST /tasks/{id}/dependencies` | 200 |
| TL 给跨团队无关任务添加依赖 | liu_tl | TASK_ACCEL_SUNBA | 同需求任务 | `POST /tasks/{id}/dependencies` | 403 |
| 添加重复依赖 | admin | TASK_DEP_CHILD | TASK_DEP_BASE | `POST /tasks/{id}/dependencies` | 200，幂等且不产生重复数据 |
| 添加循环依赖 | admin | TASK_DEP_BASE | TASK_DEP_CHILD | `POST /tasks/{id}/dependencies` | 400 |
| 添加自己依赖自己 | admin | TASK_DEP_CHILD | TASK_DEP_CHILD | `POST /tasks/{id}/dependencies` | 400 |
| 添加跨需求依赖 | admin | TASK_DEP_CHILD | 其他需求任务 | `POST /tasks/{id}/dependencies` | 400 |
| employee 给别人任务添加非法依赖 | zhangsan | TASK_AI_LISI | TASK_AI_LISI | `POST /tasks/{id}/dependencies` | 403，权限先拦截，不返回 400 |

### 8.15 需求删除关联类型边界

需求硬删除必须区分业务强关联和 follow。强关联存在时返回 409 且事务不应产生部分清理；仅有 follow 时允许删除并清理 follow。

| 场景 | 测试账号 | 资源 | 请求 / 操作 | 预期结果 |
|---|---|---|---|---|
| 需求仅有关联任务 | admin | REQ_DELETE_LINKED | `DELETE /requirements/{id}` | 409 |
| 需求仅有关联 session | admin | REQ_DELETE_SESSION | `DELETE /requirements/{id}` | 409 |
| 需求仅有关联 token_usage | admin | REQ_DELETE_TOKEN | `DELETE /requirements/{id}` | 409 |
| 需求仅有关联 document | admin | REQ_DELETE_DOC | `DELETE /requirements/{id}` | 409 |
| 需求仅有 follow | admin | REQ_DELETE_FOLLOW_ONLY | `DELETE /requirements/{id}` | 200，删除需求并清理 follow |
| 删除成功后查需求 | admin | REQ_DELETE_FOLLOW_ONLY | `GET /requirements/{id}` | 404 |
| 删除成功后查 requirement_teams | admin | REQ_DELETE_FOLLOW_ONLY | 数据库或 API 间接断言 | 已清理 |
| 删除成功后查 user_follows | admin | REQ_DELETE_FOLLOW_ONLY | `GET /follows` | 不包含该 requirement follow |
| 删除 409 后查需求 | admin | REQ_DELETE_LINKED | `GET /requirements/{id}` | 200 |
| 删除 409 后查 follows | admin | REQ_DELETE_LINKED | `GET /follows` | 原 follow 仍保留 |
| 删除 409 后查 requirement_teams | admin | REQ_DELETE_LINKED | 列表 / 详情断言 team_ids | 原 team_ids 仍保留 |

### 8.16 取消 / 删除 / 归档差异

P0 明确口径：取消不是删除；取消保留详情、历史任务和 follow，但取消后的需求不允许继续创建新任务。归档字段 P0 不实现，只保留产品语义，不纳入本轮测试。

| 场景 | 测试账号 | 资源 | 请求 / 操作 | 预期结果 |
|---|---|---|---|---|
| 取消需求后打开详情 | pm | 已取消需求 | `GET /requirements/{id}` | 200 |
| 取消需求后查看历史任务 | zhangsan | 已取消需求下本团队任务 | `GET /tasks?requirement_id={id}` | 200，仍返回当前用户可见历史任务 |
| 取消需求后继续创建任务 | pm | 已取消需求 | `POST /tasks` | 409；取消需求不可继续拆任务 |
| 取消需求后 follow 保留 | zhangsan | 已关注需求 | `GET /follows` | 仍包含该需求 follow |
| 有权限用户恢复取消需求 | pm | 已取消需求 | `PUT /requirements/{id}/restore` | 200 |
| employee 恢复取消需求 | zhangsan | 已取消需求 | `PUT /requirements/{id}/restore` | 403 |
| 删除需求后打开详情 | admin | 已删除需求 | `GET /requirements/{id}` | 404 |
| 删除需求后 follow 清理 | admin | 已删除需求 | `GET /follows` | 不包含该需求 follow |
| 取消需求不清理 requirement_teams | admin | 已取消需求 | `GET /requirements/{id}` | 200，team_ids 仍保留 |

### 8.17 负责人列表 / 用户列表边界

`/users` 和 `/task-assignees` 路径均相对 AIDA_API_BASE。`/users` 只要求登录鉴权，不作为任务负责人候选接口；`/task-assignees` 必须按下表返回负责人候选。即使前端错误拿到其他用户 ID，后端也必须拒绝越权创建 / 改派。

| 角色 | `/task-assignees` 负责人列表预期 |
|---|---|
| admin | 全部 employee |
| director | 全部 employee |
| pm | 全部 employee |
| team_leader | 自己团队 employee |
| employee | 仅自己 |

| 场景 | 测试账号 | 资源 / 参数 | 请求 / 操作 | 预期结果 |
|---|---|---|---|---|
| 未登录访问用户列表 | anonymous | 无 token | `GET /users` | 401 |
| employee 获取负责人列表 | zhangsan | task assignees | `GET /task-assignees` | 200，仅包含 zhangsan |
| TL 获取负责人列表 | liu_tl | task assignees | `GET /task-assignees` | 200，仅 AI工程 employee |
| PM 获取负责人列表 | chen_pm | task assignees | `GET /task-assignees` | 200，全部 employee |
| 前端拿到跨团队 ID 后 employee 创建任务 | zhangsan | assignee_id=sunba | `POST /tasks` | 403 |
| 前端拿到跨团队 ID 后 TL 改派任务 | liu_tl | assignee_id=sunba | `PUT /tasks/{id}` | 403 |

### 8.18 异常账号和 token 边界

需要通过临时测试数据或直接构造 token 覆盖异常账号。若不允许直接改数据库，可在报告中标记为需测试环境支持，但不应从验收集中删除。

| 场景 | 测试账号 / token | 请求 / 操作 | 预期结果 |
|---|---|---|---|
| TL 的 team_id 为空访问需求列表 | 临时 TL_NO_TEAM | `GET /requirements` | 200，空列表，不能退化为全量 |
| TL 的 team_id 为空访问任务列表 | 临时 TL_NO_TEAM | `GET /tasks` | 200，空列表，不能退化为全量 |
| employee 的 team_id 为空访问需求列表 | 临时 EMP_NO_TEAM | `GET /requirements` | 200，空列表，不能退化为全量 |
| employee 的 team_id 为空访问任务列表 | 临时 EMP_NO_TEAM | `GET /tasks` | 200，空列表，不能退化为全量 |
| token 过期 | expired token | 任意受保护接口 | 401 |
| token 被篡改 | tampered token | 任意受保护接口 | 401 |
| body 伪造 creator_id / creator_role 创建需求 | pm | `POST /requirements` | 201，后端使用当前登录用户身份，忽略伪造字段 |
| body 伪造 creator_id / creator_role 创建任务 | zhangsan | `POST /tasks` | 201，后端使用当前登录用户身份，忽略伪造字段 |
| 非法 role 用户访问需求列表 | 临时 role=unknown 或伪造 token | `GET /requirements` | 403；如果无法生成合法 token，则登录被禁止 |

### 8.19 权限判断顺序

无权限必须先拦截，避免先触发业务逻辑导致信息泄露或副作用。

| 场景 | 测试账号 | 资源 | 请求 / 操作 | 预期结果 |
|---|---|---|---|---|
| employee 调用 regenerate AC | zhangsan | REQ_ADMIN_AI | `POST /requirements/{id}/regenerate-ac` | 403，即使 AI 未配置也不能返回 503 |
| TL 对跨团队需求 regenerate AC | liu_tl | REQ_PM_ACCEL | `POST /requirements/{id}/regenerate-ac` | 403，不触发 AI |
| employee 删除别人有关联数据的任务 | zhangsan | TASK_AI_LISI 且有关联数据 | `DELETE /tasks/{id}` | 403，不返回 409 |
| employee 删除跨团队有关联数据需求 | zhangsan | REQ_PM_ACCEL 且有关联数据 | `DELETE /requirements/{id}` | 403，不返回 409 |
| employee 给别人任务添加非法依赖 | zhangsan | TASK_AI_LISI | `POST /tasks/{id}/dependencies` | 403，不返回依赖非法 400 |
| employee 更新不存在但格式合法的跨团队 ID | zhangsan | random uuid | `PUT /tasks/{id}` | 404；不存在资源仍返回 404 |

### 8.20 事务 / 原子性测试

| 场景 | 测试账号 | 资源 / 请求体 | 请求 / 操作 | 预期结果 |
|---|---|---|---|---|
| 创建需求 team_ids 部分合法部分非法 | admin | AI工程 team_id + random uuid | `POST /requirements` | 400；不创建 requirement，不创建 requirement_teams 半成品 |
| TL 创建需求 team_ids 包含自己团队和非法团队 | liu_tl | AI工程 team_id + random uuid | `POST /requirements` | 400；整体失败 |
| 创建任务依赖列表部分合法部分非法 | admin | valid dep + 跨需求 dep | `POST /tasks` | 400；不创建半成品任务，不创建任何依赖 |
| 添加重复依赖 | admin | TASK_DEP_CHILD -> TASK_DEP_BASE | `POST /tasks/{id}/dependencies` 两次 | 200；依赖记录只有一条 |
| 删除任务失败 | zhangsan | TASK_AI_LISI | `DELETE /tasks/{id}` | 403；任务仍存在，依赖不变，父需求 progress 不变 |
| 删除需求返回 409 | admin | REQ_DELETE_LINKED | `DELETE /requirements/{id}` | 409；不清理 follows，不清理 requirement_teams |

### 8.21 follow / 关注边界

follow 是需求看板核心能力，纳入本轮权限边界测试。关注不授予额外编辑权限。

| 场景 | 测试账号 | 资源 | 请求 / 操作 | 预期结果 |
|---|---|---|---|---|
| employee 关注本团队需求 | zhangsan | REQ_ADMIN_AI | `POST /follows` | 200 |
| employee 关注跨团队需求 | zhangsan | REQ_PM_ACCEL | `POST /follows` | 404 |
| employee 关注本团队别人任务 | zhangsan | TASK_AI_LISI | `POST /follows` | 200，只获得关注，不获得编辑权限 |
| employee 关注纯跨团队不可见任务 | zhangsan | TASK_ACCEL_SUNBA | `POST /follows` | 404 |
| employee 关注多团队需求下其他团队任务 | zhangsan | TASK_MULTI_SUNBA | `POST /follows` | 200，只获得关注，不获得编辑权限 |
| 关注需求后编辑需求 | zhangsan | REQ_ADMIN_AI | `PUT /requirements/{id}` | 403 |
| 关注别人任务后编辑别人任务 | zhangsan | TASK_AI_LISI | `PUT /tasks/{id}` | 403 |
| 关注别人任务后删除别人任务 | zhangsan | TASK_AI_LISI | `DELETE /tasks/{id}` | 403 |
| 删除需求后 follow 清理 | admin | REQ_DELETE_FOLLOW_ONLY | `DELETE /requirements/{id}` 后 `GET /follows` | 不再返回该需求 follow |
| 删除任务后 follow 清理 | admin | 已关注任务 | `DELETE /tasks/{id}` 后 `GET /follows` | 不再返回该任务 follow |
| 取消需求后 follow 保留 | pm | 已关注需求 | `PUT /requirements/{id}` status=cancelled 后 `GET /follows` | 仍返回该需求 follow |

---

## 9. 前端 UI 自动化测试方案

API 测试通过后，再做浏览器真实模拟。推荐使用 Playwright 或 Claude 浏览器能力。

### 9.1 路由权限

| 账号 | 路由 | 期望 |
|---|---|---|
| admin | `/requirements` | 可访问 |
| employee | `/requirements` | 可访问 |
| employee | `/requirements/create` | 跳转 `/403` |
| PM | `/tasks` | 可访问 |
| PM | `/tasks/create` | 可访问 |
| PM | `/tasks/:id` | 可访问 |
| employee | `/tasks` | 可访问 |
| employee | `/tasks/create` | 可访问 |
| anonymous | `/requirements` | 跳转 login |

### 9.2 需求看板按钮

| 账号 | 页面 | 断言 |
|---|---|---|
| admin | 需求看板 | 显示新建需求、编辑、取消、恢复、删除、添加任务 |
| PM | 需求看板 | 显示需求管理和任务管理入口 |
| TL-AI | AI工程需求抽屉 | 显示本团队需求可管理入口 |
| TL-AI | 推理加速需求 | 不显示管理按钮或无法打开 |
| employee | 需求看板 | 不显示新建需求、编辑需求、取消、恢复、删除需求 |
| employee | 本团队需求任务区 | 可以看到任务；只在可创建自己任务时显示添加任务入口 |

### 9.3 任务按钮

| 账号 | 任务 | 断言 |
|---|---|---|
| employee 张三 | 张三自己的任务 | 显示编辑、状态、进度、依赖、删除 |
| employee 张三 | 李四任务 | 只读，不显示编辑、状态、进度、依赖、删除 |
| employee 张三 | 纯跨团队不可见任务 | 列表不可见；直接打开详情显示 404 页面 |
| employee 张三 | 多团队需求下其他团队任务 | 可见但只读，不显示编辑、状态、进度、依赖、删除 |
| TL-AI | AI工程任务 | 显示编辑、改派、状态、进度、依赖、删除 |
| TL-AI | 推理加速任务 | 不可见或无管理按钮 |
| PM | 任意任务 | 显示任务管理按钮 |

### 9.4 UI 与 API 一致性

对每个 UI 隐藏的关键操作，必须额外用 API 直接请求验证后端也拒绝。

示例：

1. employee 页面不显示“编辑需求”。
2. 用 employee token 直接请求 `PUT /requirements/{id}`。
3. 必须返回 403。

### 9.5 UI 直接访问和刷新边界

| 场景 | 测试账号 | 页面 / 资源 | 操作 | 预期结果 |
|---|---|---|---|---|
| employee 直接访问跨团队需求详情 URL | zhangsan | REQ_PM_ACCEL | 打开 `/requirements/{id}` | 显示 404 页面，不展示需求内容 |
| employee 直接访问纯跨团队任务详情 URL | zhangsan | TASK_ACCEL_SUNBA | 打开 `/tasks/{id}` | 显示 404 页面，不展示任务内容 |
| employee 直接访问多团队需求下其他团队任务详情 URL | zhangsan | TASK_MULTI_SUNBA | 打开 `/tasks/{id}` | 显示任务详情但只读，不显示编辑 / 删除 / 状态 / 进度 / 依赖按钮 |
| employee 从任务列表点击同团队别人任务 | zhangsan | TASK_AI_LISI | 点击任务详情 | 可查看，但只读，不显示编辑 / 删除 / 状态 / 进度 / 依赖按钮 |
| employee 刷新同团队别人任务详情页 | zhangsan | TASK_AI_LISI | 浏览器刷新 | 仍然只读 |
| employee 自己任务被 TL 改派后刷新 | zhangsan | TASK_REASSIGN_ZHANGSAN | TL 改派给李四后刷新页面 | 编辑 / 删除 / 状态 / 进度 / 依赖按钮消失，只读可见 |
| employee 进入创建任务页 | zhangsan | `/tasks/create` | 打开页面 | 可以进入；负责人固定为自己或负责人选择器只包含自己 |
| PM 进入任务列表 | chen_pm | `/tasks` | 打开页面 | 正常访问 |
| PM 进入创建任务页 | chen_pm | `/tasks/create` | 打开页面 | 正常访问，可选择全部 employee |
| PM 进入任务详情 | chen_pm | `/tasks/{id}` | 打开任意任务 | 正常访问并显示管理按钮 |

### 9.6 UI 负责人列表边界

| 场景 | 测试账号 | 页面 | 操作 | 预期结果 |
|---|---|---|---|---|
| employee 创建任务负责人列表 | zhangsan | `/tasks/create` | 打开负责人控件 | 只包含 zhangsan，或不展示负责人控件并默认自己 |
| TL 创建任务负责人列表 | liu_tl | `/tasks/create` | 打开负责人控件 | 只包含 AI工程 employee，不包含 sunba / zhoujiu / wushi |
| TL 改派任务负责人列表 | liu_tl | 任务编辑弹窗 | 打开负责人控件 | 只包含 AI工程 employee |
| PM 创建 / 改派任务负责人列表 | chen_pm | 任务创建 / 编辑 | 打开负责人控件 | 包含全部 employee |
| 前端构造跨团队负责人 ID | zhangsan / liu_tl | 创建 / 编辑任务 | 绕过 UI 直接请求 API | 后端返回 403 |

---

## 10. 自动化断言规范

### 10.1 状态码断言

| 场景 | 明确状态码 |
|---|---|
| 未登录 / token 过期 / token 被篡改 | 401 |
| 列表接口越权范围 | 200，结果按当前用户权限过滤，越权数据不返回 |
| GET 详情无查看权限 | 404 |
| 修改 / 删除 / 状态变更 / 依赖维护无操作权限 | 403 |
| 资源真实不存在 | 404 |
| 参数非法 / 字段非法 / 状态非法 / 依赖非法 | 400 |
| 删除存在强关联 | 409 |
| 有权限调用 regenerate AC 且 AI 正常 | 200 |
| 有权限调用 regenerate AC 但 AI 未配置 | 503 |

权限判断顺序强断言：无权限请求必须优先返回 403 或详情 GET 的 404，不能先返回 409、400、503 等业务错误。

### 10.2 响应字段断言

需求对象至少断言：

- `id`
- `title`
- `creator_id`
- `creator_role`
- `status`
- `priority`
- `team_ids`
- `team_names`
- `acceptance_criteria`
- `progress`
- `can_update`
- `can_change_status`
- `can_cancel`
- `can_restore`
- `can_delete`
- `can_manage_ac`
- `can_create_task`

任务对象至少断言：

- `id`
- `requirement_id`
- `title`
- `acceptance_criteria`
- `assignee_id`
- `assignee_name`
- `status`
- `display_status`
- `priority`
- `progress`
- `dependencies`
- `blocking`
- `risk_types`
- `can_update_meta`
- `can_reassign`
- `can_update_status`
- `can_update_progress`
- `can_manage_dependencies`
- `can_delete`

### 10.3 列表断言方式

不要只断言列表 HTTP 200。必须断言：

1. 应该可见的测试资源 ID 存在。
2. 不应该可见的测试资源 ID 不存在。
3. `team_id`、`scope`、`assignee_id`、`status` 等查询参数不能扩大权限范围。

### 10.4 can_xxx 权限字段断言

后端必须在需求 / 任务列表项和详情中返回 `can_xxx` 权限字段，前端按钮显隐以这些字段为验收基准，不再把权限字段视为可选增强。

#### 10.4.1 需求 can_xxx

| 场景 | 测试账号 | 资源 | 请求 / 操作 | 预期字段 |
|---|---|---|---|---|
| employee 查看本团队需求 | zhangsan | REQ_ADMIN_AI | `GET /requirements/{id}` | `can_update=false`, `can_change_status=false`, `can_cancel=false`, `can_restore=false`, `can_delete=false`, `can_manage_ac=false`, `can_create_task=true` |
| employee 查看多团队需求 | zhangsan | REQ_MULTI | `GET /requirements/{id}` | 同本团队需求；可创建任务但只能创建给自己 |
| TL 查看本团队需求 | liu_tl | REQ_ADMIN_AI | `GET /requirements/{id}` | `can_update=true`, `can_change_status=true`, `can_cancel=true`, `can_restore` 按状态返回, `can_manage_ac=true`, `can_create_task=true` |
| TL 查看跨团队需求 | liu_tl | REQ_PM_ACCEL | `GET /requirements/{id}` | 404，无字段泄露 |
| PM 查看任意需求 | chen_pm | 任意 | `GET /requirements/{id}` | 管理相关 can_xxx 为 true；`can_delete` 仍受关联数据约束 |

#### 10.4.2 任务 can_xxx

| 场景 | 测试账号 | 资源 | 请求 / 操作 | 预期字段 |
|---|---|---|---|---|
| employee 查看自己任务 | zhangsan | TASK_AI_ZHANGSAN | `GET /tasks/{id}` | `can_update_meta=true`, `can_delete=true`, `can_update_status=true`, `can_update_progress=true`, `can_manage_dependencies=true`, `can_reassign=false` |
| employee 查看同团队别人任务 | zhangsan | TASK_AI_LISI | `GET /tasks/{id}` | `can_update_meta=false`, `can_delete=false`, `can_update_status=false`, `can_update_progress=false`, `can_manage_dependencies=false`, `can_reassign=false` |
| employee 查看多团队需求下其他团队任务 | zhangsan | TASK_MULTI_SUNBA | `GET /tasks/{id}` | `can_update_meta=false`, `can_delete=false`, `can_update_status=false`, `can_update_progress=false`, `can_manage_dependencies=false`, `can_reassign=false` |
| TL 查看本团队任务 | liu_tl | TASK_AI_ZHANGSAN | `GET /tasks/{id}` | 管理相关 can_xxx 为 true，`can_reassign=true` 但只能改派给本团队 employee |
| TL 查看跨团队任务 | liu_tl | TASK_ACCEL_SUNBA | `GET /tasks/{id}` | 404，无字段泄露 |
| PM 查看任意任务 | chen_pm | 任意 | `GET /tasks/{id}` | 管理相关 can_xxx 为 true |
| employee 自己任务被改派后查看 | zhangsan | TASK_REASSIGN_ZHANGSAN | `GET /tasks/{id}` | 200，但所有编辑类 can_xxx 变为 false |

---

## 11. 建议自动化报告格式

每条用例输出：

```json
{
  "case_id": "REQ-LIST-EMP-001",
  "role": "employee",
  "account": "zhangsan",
  "method": "GET",
  "path": "/requirements",
  "resource": "REQ_PM_ACCEL",
  "expect": "not_visible",
  "actual": "not_visible",
  "status": "passed"
}
```

最终报告汇总：

```text
总用例数：N
通过：N
失败：N
跳过：N
失败详情：...
测试数据前缀：PERM-E2E-...
开始时间：...
结束时间：...
服务地址：...
```

失败时必须保留：

- 请求 method/path/body
- 使用账号
- 期望结果
- 实际状态码
- 实际响应体
- 相关资源 ID

---

## 12. 清理策略

清理顺序：

1. 删除测试任务依赖。
2. 删除测试任务。
3. 恢复被取消但仍需删除的测试需求到可删除状态，或直接删除无关联需求。
4. 删除无关联测试需求。
5. 对因为关联数据无法硬删除的需求，改为 `cancelled`，并在报告中列为残留测试数据。

清理要求：

- 只清理 title 包含本轮 `PERM-E2E-...` 前缀的数据。
- 如果删除返回 409，不要强制破坏业务关联；改为取消需求并记录。
- 如果测试失败，应优先保留现场，除非用户明确要求清理。

---

## 13. 最小冒烟用例集

如果时间有限，至少执行以下用例：

1. 登录所有角色成功，错误密码失败。
2. employee 不能创建需求。
3. employee 不能编辑 / 取消本团队需求。
4. employee 不能查看跨团队需求详情。
5. TL 不能创建不含自己团队的需求。
6. TL 不能编辑跨团队需求。
7. PM 可以进入并调用任务创建。
8. employee 可以创建 assignee 为自己的任务。
9. employee 不能给别人创建任务。
10. employee 可以编辑自己的任务。
11. employee 不能编辑同团队别人的任务。
12. employee 不能更新别人任务状态 / 进度 / 依赖。
13. `GET /tasks?scope=requirements` 不能让 employee 看到可见集合之外的任务；多团队参与需求下其他团队任务可见但只读。
14. TL 只能给本团队 employee 分配任务。
15. `blocked` 不能直接设置。
16. 依赖不能跨需求、不能自依赖、不能形成环。
17. 有关联任务的需求删除返回 409。
18. 无关联需求可被有权限角色删除。
19. admin 可以 regenerate AC 或在 AI 未配置时返回非权限错误。
20. 前端 employee 不显示需求管理按钮，自己的任务显示操作，别人的任务只读。

---

## 14. Claude 执行提示词模板

可直接把下面提示词交给 Claude 执行：

```text
请按照 doc/v1/需求任务权限测试方案.md 执行真实账号自动化测试。
要求：
1. 使用当前运行中的真实 API / Web 服务，不要 mock。
2. 使用种子账号 employee_id + password=123 登录。
3. 生成唯一测试前缀 PERM-E2E-<timestamp>。
4. 先执行 API 权限矩阵测试，再执行关键 UI route/按钮测试。
5. 每个失败用例记录 method/path/body、账号、资源 ID、期望和实际响应。
6. 不要修改非测试前缀数据。
7. 测试结束后按方案清理测试数据；如果发生失败，保留现场并报告残留 ID。
8. 输出最终 Markdown 测试报告。
```

---

## 15. 验收通过标准

本轮权限验收通过需要同时满足：

1. 所有 P0 权限矩阵 API 用例通过。
2. 所有越权请求被后端拒绝。
3. 所有列表接口不会通过查询参数扩大可见范围。
4. employee 可以管理自己的任务，但不能管理别人的任务。
5. TL 不能跨团队管理无关需求 / 任务。
6. PM 可以访问和管理任务相关页面与接口。
7. admin / director / PM 全量管理能力正常。
8. 需求删除 409、任务删除副作用、blocked 推导等边界符合本方案明确预期。
9. 前端 route 和按钮显隐与后端权限一致。
10. 测试报告无未解释失败；有权限调用 regenerate AC 时，AI 正常必须返回 200，AI 未配置必须返回 503；无权限调用必须返回 403 或详情不可见的 404。

---

## 16. 待产品最终确认但 P0 已给默认值

以下事项如产品未另行确认，自动化验收按 P0 默认值执行：

| 事项 | P0 默认值 | 是否待确认 |
|---|---|---|
| 多团队需求下 employee 是否能看到其他团队 assignee 的任务 | 能；参与团队 employee 可见该需求下全部拆解任务，但只读非自己任务 | 否 |
| 多团队需求下未分配任务是否可见 | 当前用户团队在 requirement_teams 中即可见 | 否 |
| employee 创建任务不传 assignee_id | 后端默认当前登录用户 | 否 |
| employee 创建任务时传 status / progress | 返回 400，不允许创建时覆盖初始状态 / 进度 | 否 |
| TL 创建需求是否可选择其他团队 | P0 默认不允许，只能选择自己团队 | [待确认] |
| 取消需求后是否允许继续创建任务 | 不允许，返回 409 | [待确认] |
| 依赖是否允许跨需求 | 不允许，返回 400 | 否 |
| employee 是否可添加跨团队不可见任务作为依赖 | 不允许，返回 404 | 否 |
| 归档字段 | P0 不实现，不纳入测试 | 否 |
