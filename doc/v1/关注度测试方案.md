# 关注度 P0 第一阶段接口测试方案

> 本方案用于后续执行「关注 / 自动关注 / 关注度 P0 第一阶段」完整接口测试。当前步骤只梳理测试方案，不执行测试，不修改业务代码。

## 1. 测试范围与原则

### 1.1 测试目标

验证以下产品口径是否成立：

1. 创建需求后，创建人自动关注需求。
2. 创建任务后，任务创建人自动关注任务。
3. 创建任务时有 `assignee_id`，被指派人自动关注任务。
4. 任务改派时，新 assignee 自动关注任务。
5. 任务普通编辑不恢复用户已取消的关注。
6. 改派不自动删除老 assignee 的关注。
7. 被指派也算关注，并计入关注度。
8. `admin` 权重为 0，不影响关注度。
9. 关注度按当前用户 role 实时计算。
10. `dashboard/follows` 返回 `attentionScore`、`attentionLevel`、`riskPriority`，并按规则排序。
11. `dashboard/risks` 返回 `attentionScore`、`attentionLevel`，不返回 `followCount`。
12. `dashboard/risks` 的任务风险关注度使用 `max(task_attention_score, requirement_attention_score)`，不是两者相加。
13. 前端关注列表排序不再依赖风险文案字符串推断；如仅测接口，也要检查前端代码是否残留旧排序逻辑。

### 1.2 测试约束

- 只做测试，不改业务代码。
- 使用当前服务已有账号测试；不要新建测试用户，除非当前环境完全没有可用账号。
- 测试数据不要删除，保留给人工 review。
- 所有测试数据名称必须带唯一前缀。
- 脚本只能调用接口，不要直接写数据库。
- 允许只读查询数据库辅助验证 `user_follows` 与关注度，不允许数据库写入。
- 不要清理需求、任务、`user_follows` 或数据库。

### 1.3 建议测试前缀

执行测试时生成唯一前缀：

```text
ATTN_E2E_YYYYMMDD_HHMM
```

示例：

```text
ATTN_E2E_20260626_1630
ATTN_E2E_20260626_1630_需求关注度测试
```

后续所有需求 / 任务标题统一以该前缀开头。

## 2. 当前代码确认结果

### 2.1 服务地址

默认本地接口地址：

```text
http://localhost:8080/api/v1
```

如通过前端 Vite 代理测试，可使用：

```text
http://localhost:5173/api/v1
```

实际执行时以当前启动服务为准。

### 2.2 登录接口与 Token

接口：

```http
POST /api/v1/auth/login
Content-Type: application/json

{
  "employee_id": "chen_pm",
  "password": "123"
}
```

响应关键字段：

```json
{
  "token": "<jwt>",
  "user": {
    "id": "...",
    "employee_id": "chen_pm",
    "name": "陈PM",
    "role": "pm",
    "team_id": null,
    "team_name": null
  }
}
```

后续请求头：

```http
Authorization: Bearer <token>
```

说明：当前迁移 `005_user_auth.sql` 为种子用户和 admin 回填的默认密码是 `123`。如果环境中密码已被改动，以当前环境实际账号为准。

### 2.3 当前种子账号

优先使用以下已有账号：

| 测试角色 | employee_id | 姓名 | role | team |
| --- | --- | --- | --- | --- |
| directorUser | `li_director` | 李总监 | `director` | 无 |
| pmUser | `chen_pm` | 陈PM | `pm` | 无 |
| tlUser | `liu_tl` | 刘TL | `team_leader` | AI工程 |
| employeeA | `zhangsan` | 张三 | `employee` | AI工程 |
| employeeB | `lisi` | 李四 | `employee` | AI工程 |
| adminUser | `admin` | 管理员 | `admin` | 无 |

测试开始后必须通过登录响应记录每个账号的实际：

- `id`
- `employee_id`
- `name`
- `role`
- `team_id`
- `team_name`
- token 保存位置或 token 摘要

如果缺少某类账号，停止相关用例并在报告中说明，不要自行改代码造假。

### 2.4 已确认接口路径

| 能力 | 方法与路径 | 请求体 / 参数 | 关键响应 |
| --- | --- | --- | --- |
| 登录 | `POST /api/v1/auth/login` | `employee_id`, `password` | `token`, `user` |
| 当前用户 | `GET /api/v1/auth/me` | Bearer token | 当前用户 |
| 用户列表 | `GET /api/v1/users` | Bearer token | `User[]` |
| 团队列表 | `GET /api/v1/teams` | Bearer token | `Team[]` |
| 创建需求 | `POST /api/v1/requirements` | `title`, `description`, `priority`, `team_ids`, `deadline?`, `acceptance_criteria?` | `Requirement` |
| 查询需求 | `GET /api/v1/requirements/{id}` | path id | `Requirement`，含 `is_followed` |
| 创建任务 | `POST /api/v1/tasks` | `requirement_id`, `title`, `assignee_id`, `priority`, `due_date?`, `acceptance_criteria?`, `depends_on_ids?` | `{id,status}` |
| 查询任务 | `GET /api/v1/tasks/{id}` | path id | `Task`，含 `is_followed`, `risk_types`, `display_status` |
| 更新任务 | `PUT /api/v1/tasks/{id}` | `title?`, `assignee_id?`, `status?`, `priority?`, `due_date?`, `progress?`, `acceptance_criteria?` | `Task` |
| 更新任务状态 | `PUT /api/v1/tasks/{id}/status` | `status` | `Task` |
| 更新任务进度 | `PUT /api/v1/tasks/{id}/progress` | `progress` | `Task` |
| 添加依赖 | `POST /api/v1/tasks/{id}/dependencies` | `depends_on_id` | `Task` |
| 关注 | `POST /api/v1/follows` | `target_type`, `target_id` | `favorited`, `target_type`, `target_id` |
| 取消关注 | `DELETE /api/v1/follows/{target_type}/{target_id}` | path params | `favorited=false` |
| 我的关注关系 | `GET /api/v1/follows` | Bearer token | `UserFollow[]` |
| 首页关注列表 | `GET /api/v1/dashboard/follows` | Bearer token | `DashboardFollowItem[]` |
| 首页风险列表 | `GET /api/v1/dashboard/risks` | Bearer token | `DashboardRiskItem[]` |

### 2.5 请求字段与枚举

#### 需求创建字段

```json
{
  "title": "ATTN_E2E_xxxx_REQ_PM_CREATE",
  "description": "关注度 P0 接口测试需求",
  "priority": "medium",
  "team_ids": ["<AI工程 team id>"],
  "deadline": "2026-07-10",
  "acceptance_criteria": ["验证关注度规则"]
}
```

需求优先级枚举：

```text
low / medium / high / urgent
```

需求状态枚举：

```text
todo / review / active / completed / cancelled
```

#### 任务创建字段

```json
{
  "requirement_id": "<requirementId>",
  "title": "ATTN_E2E_xxxx_TASK_TL_ASSIGN_A",
  "assignee_id": "<employeeA.id>",
  "priority": "medium",
  "due_date": "2026-07-01",
  "acceptance_criteria": ["验证任务自动关注"]
}
```

任务优先级枚举：

```text
low / medium / high
```

任务存储状态枚举：

```text
todo / in_progress / done
```

注意：`blocked` 不是可写入的任务状态，`PUT /tasks/{id}` 和 `PUT /tasks/{id}/status` 都不接受 `blocked`。`blocked` 是由未完成依赖派生出的 `display_status` / `risk_types`。

### 2.6 风险制造方式

当前风险由后端实时派生：

1. **超期风险**：任务 `status != done`，且 `due_date` 早于当前 UTC 日期，派生 `risk_types` 包含 `overdue`。
2. **阻塞风险**：任务依赖的上游任务状态不是 `done`，派生 `risk_types` 包含 `blocked`，展示状态为 `display_status=blocked`。

推荐测试优先使用超期风险，造数最稳定：

```json
{
  "due_date": "<昨天 yyyy-mm-dd>",
  "status": "todo"
}
```

如需制造阻塞风险：先创建一个未完成上游任务，再创建或更新目标任务依赖该上游任务。

### 2.7 关注度权重与等级

权重：

| role | 权重 |
| --- | ---: |
| `director` | 100 |
| `team_leader` | 50 |
| `pm` | 40 |
| `employee` | 10 |
| `admin` | 0 |

等级：

| 分数区间 | attentionLevel |
| --- | --- |
| 0-39 | `normal` |
| 40-79 | `notable` |
| 80-149 | `important` |
| 150+ | `high` |

注意：不做“总监一票高关注”。

### 2.8 Dashboard DTO 关键字段

#### dashboard/follows

`GET /api/v1/dashboard/follows` 返回数组元素包含：

```json
{
  "key": "requirement:<id> 或 task:<id>",
  "type": "需求 或 任务",
  "title": "...",
  "requirementId": "...",
  "taskId": "...",
  "owner": "...",
  "status": "...",
  "deadline": "...",
  "risk": "...",
  "activity": "...",
  "attentionScore": 40,
  "attentionLevel": "notable",
  "riskPriority": 0,
  "navigation": {
    "requirementId": "...",
    "taskId": "...",
    "url": "/requirements?..."
  }
}
```

排序规则应为：

1. `riskPriority DESC`
2. `attentionScore DESC`
3. `dueDate ASC NULLS LAST`，接口中展示字段为 `deadline`，后端内部用真实 `SortDueDate`
4. `updatedAt DESC`，接口中不直接暴露，后端内部用真实 `SortUpdatedAt`

说明：`dashboard/follows` 本身就是“我的关注列表”，当前没有 `isFollowedByMe` 字段；如需等价验证，可用该 item 是否出现在当前用户 `dashboard/follows` / `GET /follows` 中判断。

#### dashboard/risks

`GET /api/v1/dashboard/risks` 返回数组元素包含：

```json
{
  "key": "<taskId>:overdue",
  "riskType": "deadline",
  "title": "任务已超过截止日期",
  "source": "已超期",
  "target": "...",
  "relatedObjectType": "task",
  "requirementId": "...",
  "taskId": "...",
  "owner": "...",
  "deadline": "...",
  "reason": "...",
  "level": "高",
  "tone": "red",
  "actionText": "查看任务",
  "targetUrl": "/requirements?...",
  "attentionScore": 190,
  "attentionLevel": "high",
  "navigation": {
    "requirementId": "...",
    "taskId": "...",
    "url": "/requirements?..."
  }
}
```

`DashboardRiskItem` 不返回 `followCount`，也不返回 `riskLevelPriority`。排序验证时可按代码规则从 `riskType` 推导：

| riskType | 内部 riskLevelPriority |
| --- | ---: |
| `deadline` | 100 |
| `dependency_blocker` | 90 |

排序规则应为：

1. `riskLevelPriority DESC`
2. `attentionScore DESC`
3. `dueDate ASC NULLS LAST`
4. `updatedAt DESC`

### 2.9 前端排序检查点

当前代码未发现名为 `sortFollowItems` 的函数；Dashboard 中关注列表使用接口返回的 `followsQuery.data` 顺序。需要在报告中记录：

- 是否仍存在 `sortFollowItems` 或类似前端二次排序逻辑。
- 如存在，是否只使用结构化字段排序：`riskPriority`、`attentionScore`、`deadline/dueDate`、`updatedAt`。
- 不允许从 `risk` 文案字符串推断排序。

注意：当前 `getFollowTone()` 仍根据 `risk` 文案 / `status` 推断卡片色调，这不是排序逻辑；接口测试报告中可作为前端展示观察项单独说明，不作为接口排序失败。

## 3. 测试前准备

### 3.1 环境检查

1. 确认 API 服务可访问：

```http
GET /health
```

2. 确认前缀：

```text
ATTN_E2E_<执行时间>
```

3. 登录并记录账号信息：

- directorUser
- pmUser
- tlUser
- employeeA
- employeeB
- adminUser，可选但当前种子数据应存在

4. 获取团队 ID：

```http
GET /api/v1/teams
```

选择 `AI工程` 团队，确保 TL、employeeA、employeeB 属于同一团队，便于 TL 创建 / 改派任务。

### 3.2 建议本地输出目录

执行测试时保存请求响应：

```text
tmp/attention-e2e-YYYYMMDD-HHMM/
```

最终报告建议输出：

```text
docs/test-reports/attention-e2e-YYYYMMDD-HHMM.md
```

本仓库当前已有 `doc/` 目录；如果实际项目统一用 `tmp/test-reports/`，执行时按项目目录习惯调整。

### 3.3 可选只读数据库核对

允许执行只读 SQL 辅助验证，不允许写库：

```sql
SELECT f.user_id, u.employee_id, u.name, u.role, f.target_type, f.target_id, f.created_at
FROM user_follows f
JOIN users u ON u.id = f.user_id
WHERE f.target_id IN ('<requirementId>', '<taskId>')
ORDER BY f.target_type, f.target_id, u.role, u.name;
```

关注度只读核对 SQL：

```sql
SELECT COALESCE(SUM(CASE u.role
  WHEN 'director' THEN 100
  WHEN 'team_leader' THEN 50
  WHEN 'pm' THEN 40
  WHEN 'employee' THEN 10
  ELSE 0
END), 0) AS attention_score
FROM user_follows f
JOIN users u ON u.id = f.user_id
WHERE f.target_type = '<requirement|task>' AND f.target_id = '<id>';
```

## 4. 测试用例

### Case 0：账号与环境确认

操作：

1. 分别登录 director、PM、TL、employeeA、employeeB、admin。
2. 调用 `GET /api/v1/auth/me` 或使用登录响应确认用户信息。
3. 调用 `GET /api/v1/teams` 记录团队。
4. 如果某类账号登录失败，停止依赖该角色的用例。

预期：

- 至少获得 directorUser、tlUser、pmUser、employeeA、employeeB 的 token。
- adminUser 如缺失则后续 admin 用例标记为 skipped。
- TL、employeeA、employeeB 属于同一团队。

记录：

- userId
- employee_id
- name
- role
- team_id / team_name
- token 保存文件或 token 前后若干字符

### Case 1：PM 创建需求后自动关注

操作：

1. PM 创建需求：

```http
POST /api/v1/requirements
```

请求体摘要：

```json
{
  "title": "ATTN_E2E_xxxx_REQ_PM_CREATE",
  "description": "关注度 P0 接口测试：PM 创建需求自动关注",
  "priority": "medium",
  "team_ids": ["<AI工程 team id>"],
  "deadline": "<未来日期>",
  "acceptance_criteria": ["PM 创建需求后自动关注"]
}
```

2. 记录 `requirementId`。
3. PM 请求：

```http
GET /api/v1/dashboard/follows
```

4. 找到该需求 item。

预期：

- 该需求出现在 PM 的 `dashboard/follows`。
- `attentionScore = 40`。
- `attentionLevel = notable`。
- 需求状态不是 `completed` / `cancelled`。
- 如查询 `GET /requirements/{id}`，`is_followed = true`。

记录：

- `requirementId`
- PM `dashboard/follows` 中该 item JSON
- `attentionScore` / `attentionLevel`

### Case 2：director 手动关注需求，关注度提升

操作：

1. director 手动关注 Case 1 需求：

```http
POST /api/v1/follows

{
  "target_type": "requirement",
  "target_id": "<requirementId>"
}
```

2. PM 或 director 请求 `GET /api/v1/dashboard/follows`。
3. 找到该需求。

预期：

- 关注接口返回 `favorited = true`。
- `attentionScore = 40 + 100 = 140`。
- `attentionLevel = important`。
- 不应为 `high`，因为没有“总监一票高关注”。

记录：

- follow 接口响应
- dashboard/follows 中该需求 item JSON

### Case 3：admin 关注需求，但不影响关注度

操作：

1. 如果 admin 存在，admin 关注 Case 1 需求。
2. PM 或 director 请求 `GET /api/v1/dashboard/follows`，找到该需求。

预期：

- admin 可以成功关注。
- `attentionScore` 仍为 `140`。
- `attentionLevel` 仍为 `important`。
- admin 不贡献业务关注权重。

如果 admin 不存在：

- 该 Case 标记为 `SKIPPED`，不算失败。

记录：

- admin follow 响应
- dashboard/follows 中该需求 item JSON

### Case 4：TL 手动关注需求，关注度变 high

操作：

1. TL 关注 Case 1 需求。
2. PM、director 或 TL 请求 `GET /api/v1/dashboard/follows`。
3. 找到该需求。

预期：

- `attentionScore = 40 + 100 + 50 = 190`。
- `attentionLevel = high`。
- `dashboard/follows` item 包含明显关注度字段：`attentionScore=190`、`attentionLevel=high`。

记录：

- follow 响应
- item JSON

### Case 5：TL 创建任务并指派 employeeA，创建人和被指派人自动关注

操作：

1. TL 在 Case 1 需求下创建任务：

```http
POST /api/v1/tasks
```

请求体摘要：

```json
{
  "requirement_id": "<requirementId>",
  "title": "ATTN_E2E_xxxx_TASK_TL_ASSIGN_A",
  "assignee_id": "<employeeA.id>",
  "priority": "medium",
  "due_date": "<未来日期>",
  "acceptance_criteria": ["TL 创建任务并指派 employeeA 后双方自动关注"]
}
```

2. 记录 `taskId`。
3. TL 请求 `GET /api/v1/dashboard/follows`。
4. employeeA 请求 `GET /api/v1/dashboard/follows`。
5. 分别找到该任务。

预期：

- TL dashboard/follows 中能看到该任务。
- employeeA dashboard/follows 中能看到该任务。
- 任务 `attentionScore = 50 + 10 = 60`。
- `attentionLevel = notable`。
- 若只读查询 `user_follows`，该任务对 TL 和 employeeA 各一条关注记录。

记录：

- `taskId`
- TL item JSON
- employeeA item JSON

### Case 6：employeeA 取消关注后，普通编辑不能恢复关注

操作：

1. employeeA 取消关注 Case 5 任务：

```http
DELETE /api/v1/follows/task/<taskId>
```

2. employeeA 请求 `GET /api/v1/dashboard/follows`，确认该任务消失。
3. TL 对该任务做普通编辑，不改 `assignee_id`：

```http
PUT /api/v1/tasks/<taskId>

{
  "title": "ATTN_E2E_xxxx_TASK_TL_ASSIGN_A_EDITED",
  "progress": 20
}
```

4. employeeA 再次请求 `GET /api/v1/dashboard/follows`。
5. TL 请求 `GET /api/v1/dashboard/follows`，查看该任务关注度。

预期：

- employeeA 取消关注后，该任务不再出现在 employeeA `dashboard/follows`。
- TL 普通编辑后，该任务仍然不出现在 employeeA `dashboard/follows`。
- 任务 `attentionScore` 从 60 降为 50。
- `attentionLevel` 仍为 `notable`。
- 普通 `PUT /tasks/{id}` 不恢复用户已取消的关注。

记录：

- unfollow 响应
- 普通编辑响应
- employeeA 两次 follows 查询结果
- TL item JSON

### Case 7：任务改派给 employeeB，新 assignee 自动关注

操作：

1. TL 将 Case 5 任务从 employeeA 改派给 employeeB：

```http
PUT /api/v1/tasks/<taskId>

{
  "assignee_id": "<employeeB.id>"
}
```

2. employeeB 请求 `GET /api/v1/dashboard/follows`。
3. employeeA 请求 `GET /api/v1/dashboard/follows`。
4. TL 请求 `GET /api/v1/dashboard/follows`。

预期：

- employeeB 自动关注该任务。
- employeeB `dashboard/follows` 出现该任务。
- employeeA 因之前手动取消过关注，仍不出现该任务。
- TL 仍然关注该任务。
- `attentionScore = 50 + 10 = 60`。
- `attentionLevel = notable`。

记录：

- 改派响应
- employeeB / employeeA / TL 的 dashboard/follows 结果

### Case 8：再次改派回 employeeA，employeeA 应重新自动关注

操作：

1. TL 将任务从 employeeB 改派回 employeeA：

```http
PUT /api/v1/tasks/<taskId>

{
  "assignee_id": "<employeeA.id>"
}
```

2. employeeA 请求 `GET /api/v1/dashboard/follows`。
3. employeeB 请求 `GET /api/v1/dashboard/follows`。
4. TL 请求 `GET /api/v1/dashboard/follows`。

预期：

- employeeA 因再次被明确指派，重新自动关注。
- employeeB 的关注不被系统自动删除。
- TL 仍然关注。
- `attentionScore = 50 + 10 + 10 = 70`。
- `attentionLevel = notable`。

该用例验证：

- 改派不删除老 assignee 关注。
- 再次指派可以重新自动关注。

记录：

- 改派响应
- employeeA / employeeB / TL 的 follows item JSON
- 可选只读 `user_follows` 查询结果

### Case 9：制造任务风险，验证 dashboard/risks 使用 max 关注度

操作：

1. 在 Case 1 高关注需求下，TL 创建风险任务：

```http
POST /api/v1/tasks
```

请求体摘要：

```json
{
  "requirement_id": "<requirementId>",
  "title": "ATTN_E2E_xxxx_TASK_RISK_HIGH_REQ",
  "assignee_id": "<employeeA.id>",
  "priority": "medium",
  "due_date": "<昨天 yyyy-mm-dd>",
  "acceptance_criteria": ["验证风险关注度 max 规则"]
}
```

2. 如创建时 due_date 未生效，可 TL 更新任务：

```http
PUT /api/v1/tasks/<riskTaskId>

{
  "due_date": "<昨天 yyyy-mm-dd>",
  "status": "todo"
}
```

3. 请求 `GET /api/v1/tasks/<riskTaskId>`，确认：

- `risk_types` 包含 `overdue`
- `display_status` 不是 `done`

4. 请求 `GET /api/v1/dashboard/risks`。
5. 找到该风险任务对应的风险项。
6. 分别通过 `dashboard/follows` 或只读 SQL 记录父需求关注度和任务自身关注度。

预期：

- 风险项出现在 `dashboard/risks`。
- 风险项返回 `attentionScore` 和 `attentionLevel`。
- 风险项不返回 `followCount`。
- 如果父需求 `attentionScore = 190`，任务自身 `attentionScore = 60`，则风险项：
  - `attentionScore = 190`
  - `attentionLevel = high`
- 不允许返回 `250`，即不能把任务关注度和需求关注度简单相加。

记录：

- `riskTaskId`
- `requirementAttentionScore`
- `taskAttentionScore`
- `riskAttentionScore`
- 是否符合 `max(task, requirement)` 规则
- dashboard/risks item JSON

### Case 10：创建一个低关注风险任务，验证风险排序

操作：

1. 使用 PM 创建一个低关注需求：

```http
POST /api/v1/requirements
```

标题示例：

```text
ATTN_E2E_xxxx_REQ_LOW_RISK_SORT
```

2. 使用 PM 或 TL 在该低关注需求下创建风险任务。
3. 让该任务 `due_date` 为昨天，且 `status != done`。
4. 确保该低关注风险项 `attentionScore` 低于 Case 9 风险项。
5. 请求 `GET /api/v1/dashboard/risks`。

预期排序：

1. `riskLevelPriority` 更高的风险优先。接口不返回该字段，按 `riskType` 推导：`deadline=100`、`dependency_blocker=90`。
2. `riskLevelPriority` 相同时，`attentionScore` 更高的风险优先。
3. 排序方向必须是 `riskLevelPriority DESC`，不是 ASC。

推荐造数方式：

- Case 9 和 Case 10 都制造超期风险，使两者 `riskType=deadline`，内部 `riskLevelPriority=100` 相同。
- 这样排序主要验证同风险级别下 `attentionScore DESC`。

记录：

- 低关注 `requirementId`
- 低关注 `taskId`
- dashboard/risks 返回顺序中本轮 `ATTN_E2E` 项的位置
- 每项 `riskType`、推导的 `riskLevelPriority`、`attentionScore`、`attentionLevel`

### Case 11：dashboard/follows 排序验证

操作：

1. 使用 PM、TL、employeeA 分别请求：

```http
GET /api/v1/dashboard/follows
```

2. 找到本轮创建的多个 `ATTN_E2E` 项。
3. 检查返回顺序。
4. 检查前端代码是否还存在旧的文本排序逻辑。

预期：

`dashboard/follows` 排序符合：

1. `riskPriority DESC`
2. `attentionScore DESC`
3. `dueDate ASC NULLS LAST`
4. `updatedAt DESC`

接口返回字段中：

- `riskPriority` 直接返回。
- `attentionScore` 直接返回。
- due date 展示为 `deadline`，后端内部使用真实日期排序。
- `updatedAt` 不直接返回在 dashboard item 中，若要严格验证兜底排序，可通过创建 / 编辑时间和返回顺序间接核对，或只读 DB 辅助核对。

前端检查预期：

- 不存在 `sortFollowItems` 或类似基于 `risk` 文案字符串的排序函数。
- 如存在前端排序，只能使用结构化字段。
- 若仅有 `getFollowTone()` 基于风险文案决定卡片颜色，不判定为接口排序失败，但应在报告中作为前端展示观察项记录。

记录：

- PM / TL / employeeA 各自 follows 返回顺序
- 每项 `key`、`type`、`title`、`riskPriority`、`attentionScore`、`attentionLevel`、`deadline`
- 前端排序检查结果

## 5. 关注度计算核对矩阵

执行报告中按以下格式记录：

| 对象 | 关注用户 | 角色 | 权重 | 预期分 | 实际分 | 等级 |
| --- | --- | --- | ---: | ---: | ---: | --- |
| Case 1 需求 | 陈PM | pm | 40 | 40 |  | notable |
| Case 1 需求 | 李总监 | director | 100 | 140 |  | important |
| Case 1 需求 | 管理员 | admin | 0 | 140 |  | important |
| Case 1 需求 | 刘TL | team_leader | 50 | 190 |  | high |
| Case 5 任务 | 刘TL | team_leader | 50 | 60 |  | notable |
| Case 5 任务 | 张三 | employee | 10 | 60 |  | notable |
| Case 6 任务 | 张三取消关注 | employee | -10 | 50 |  | notable |
| Case 7 任务 | 李四 | employee | 10 | 60 |  | notable |
| Case 8 任务 | 张三 + 李四 | employee | 20 | 70 |  | notable |
| Case 9 风险 | max(需求190, 任务60) | - | - | 190 |  | high |

## 6. 自动化执行建议

可以编写临时脚本，例如：

```text
scripts/manual-test-attention-e2e.sh
scripts/manual-test-attention-e2e.ts
scripts/manual-test-attention-e2e.py
```

脚本要求：

- 只调用 HTTP 接口。
- 不直接写数据库。
- 可执行只读 SQL，但必须和 HTTP 流程分离并明确标注为辅助核对。
- 保存每个请求和响应到本地目录。
- 不删除任何测试数据。

建议脚本流程：

1. 生成测试前缀。
2. 登录全部账号，保存 token。
3. 查询 teams，选择 AI工程。
4. 顺序执行 Case 1-11。
5. 每个 Case 输出 PASS / FAIL / SKIPPED 和关键 JSON。
6. 生成 Markdown 测试报告。

## 7. 测试报告模板

执行完成后输出以下 Markdown 报告。

```markdown
# 关注度 P0 第一阶段接口测试报告

## 1. 测试环境

* 服务地址：
* 测试时间：
* 测试前缀：
* 使用账号列表：

| 变量名 | userId | employee_id | name | role | team_id | team_name |
| --- | --- | --- | --- | --- | --- | --- |
| directorUser |  |  |  |  |  |  |
| pmUser |  |  |  |  |  |  |
| tlUser |  |  |  |  |  |  |
| employeeA |  |  |  |  |  |  |
| employeeB |  |  |  |  |  |  |
| adminUser |  |  |  |  |  |  |

## 2. 测试数据

* 数据命名前缀：
* requirementId 列表：
* taskId 列表：

| 类型 | 名称 | ID | 创建账号 | 说明 |
| --- | --- | --- | --- | --- |
| requirement |  |  |  |  |
| task |  |  |  |  |

## 3. 用例结果汇总

| Case | 名称 | 结果 | 说明 |
| --- | --- | --- | --- |
| Case 0 | 账号与环境确认 | PASS/FAIL/SKIPPED |  |
| Case 1 | PM 创建需求自动关注 | PASS/FAIL |  |
| Case 2 | director 手动关注需求 | PASS/FAIL |  |
| Case 3 | admin 关注不影响关注度 | PASS/FAIL/SKIPPED |  |
| Case 4 | TL 关注需求变 high | PASS/FAIL |  |
| Case 5 | TL 创建任务并指派 employeeA | PASS/FAIL |  |
| Case 6 | 取消关注后普通编辑不恢复 | PASS/FAIL |  |
| Case 7 | 改派 employeeB 自动关注 | PASS/FAIL |  |
| Case 8 | 改派回 employeeA 重新自动关注 | PASS/FAIL |  |
| Case 9 | 风险关注度使用 max 规则 | PASS/FAIL |  |
| Case 10 | dashboard/risks 排序 | PASS/FAIL |  |
| Case 11 | dashboard/follows 排序 | PASS/FAIL |  |

## 4. 详细验证过程

### Case 0：账号与环境确认

* 操作账号：
* 请求接口：
* 请求体摘要：
* 响应关键字段：
* 预期：
* 实际：
* 结果：PASS/FAIL/SKIPPED

### Case 1：PM 创建需求后自动关注

* 操作账号：
* 请求接口：
* 请求体摘要：
* 响应关键字段：
* 预期：
* 实际：
* 结果：PASS/FAIL

<!-- Case 2-11 同结构记录 -->

## 5. 关注度计算核对

| 对象 | 关注用户 | 角色 | 权重 | 预期分 | 实际分 | 等级 |
| --- | --- | --- | ---: | ---: | ---: | --- |
|  |  |  |  |  |  |  |

## 6. dashboard/follows 排序核对

| 查询账号 | 顺序 | key | title | riskPriority | attentionScore | attentionLevel | deadline | 结论 |
| --- | ---: | --- | --- | ---: | ---: | --- | --- | --- |
|  | 1 |  |  |  |  |  |  |  |

前端排序代码检查：

* 是否存在 `sortFollowItems`：
* 是否存在基于风险文案字符串的排序：
* 结论：

## 7. dashboard/risks 排序核对

| 顺序 | key | riskType | 推导 riskLevelPriority | attentionScore | attentionLevel | deadline | 结论 |
| ---: | --- | --- | ---: | ---: | --- | --- | --- |
| 1 |  |  |  |  |  |  |  |

max 规则核对：

| 风险任务 | requirementAttentionScore | taskAttentionScore | riskAttentionScore | 预期 max | 是否符合 |
| --- | ---: | ---: | ---: | ---: | --- |
|  |  |  |  |  |  |

## 8. 发现的问题

### Blocker

* 无 / 问题描述

### Major

* 无 / 问题描述

### Minor

* 无 / 问题描述

### Suggestion

* 无 / 建议描述

## 9. 保留数据说明

以下测试数据按要求保留，不做清理，供人工 review：

| 类型 | ID | 名称 | 说明 |
| --- | --- | --- | --- |
| requirement |  |  |  |
| task |  |  |  |

## 10. 最终结论

1. 是否通过：PASS / FAIL
2. 失败用例：
3. 失败原因分类：
   * 产品口径不一致：
   * 后端接口问题：
   * 前端展示问题：
   * 测试数据问题：
   * 账号权限问题：
4. 是否建议进入下一阶段：需求/任务卡片关注度展示：是 / 否
```

## 8. 不执行事项清单

测试执行过程中禁止：

1. 删除测试需求。
2. 删除测试任务。
3. 删除 `user_follows`。
4. 清理数据库。
5. backfill 历史数据。
6. 新增 `follow_source`。
7. 新增 tombstone。
8. 修改业务代码。
9. 为测试修改角色权重。
10. 使用 mock 数据替代真实接口。

## 9. 当前方案结论

本测试方案覆盖关注自动创建、手动关注、取消关注、改派自动关注、admin 零权重、关注度等级、`dashboard/follows` 排序、`dashboard/risks` max 规则与排序，以及前端排序逻辑检查。后续执行时应保留全部 `ATTN_E2E` 测试数据，并在测试报告中明确给出 PASS / FAIL、失败用例和是否建议进入下一阶段。
