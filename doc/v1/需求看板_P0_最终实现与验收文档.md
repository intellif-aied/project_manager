# 需求架构设计文档

> 状态：P0 已落地实施口径
> 基线日期：2026-06-24
> 适用范围：`requirements` 主数据域 + `dashboard` 投影域
> 依据：`doc/v1/需求看板_P0_需求文档.md`、`doc/v1/需求看板_P0_架构文档.md`，以及当前仓库 `api/`、`web/` 的实际代码

---

## 0. 文档目标

本文档**不是摘要**，而是对 P0 已实现状态的完整结构化描述，便于：

1. 让新进入项目的人在不读源码的情况下，理解 Requirements / Dashboard 在数据库、Domain、HTTP、前端各层的实际形态。
2. 给出每一条数据从产生（写）到展示（读）所经过的全链路。
3. 明确分层职责与禁止事项，防止把规则散落到多处实现。
4. 对照验收清单可直接对实际接口与代码进行回归。

---

## 1. 业务边界

### 1.1 主数据域：`requirements`

负责以下事实状态的**唯一权威**：

- `Requirement`：标题、描述、需求验收标准、阶段、优先级、截止日期、参与团队、创建人、completed_at。
- `Task`：标题、负责人、任务验收标准、状态、进度、优先级、截止日期、completed_at。
- `TaskDependency`：同一需求内任务的上游依赖。
- `Requirement.progress`：由任务进度聚合产生（不存外部输入，只允许由后端写）。
- `TokenSource` 关联：通过 `sessions.requirement_id` / `sessions.task_id` 派生。

### 1.2 投影域：`dashboard`

只承担"当前用户视角"的查询编排：

- "我关注的事项"投影。
- "待处理风险"投影。
- 跳转参数（`requirementId` / `taskId`）。
- 复用旧服务输出（reports / sessions / tokens）做导航入口，不写主数据。

### 1.3 共享域

- `user_follows`：跨页面通用的"用户—对象"关注关系。Requirements 页与 Dashboard 共用同一张表，没有 Dashboard 私有副本。
- `service.DeriveTaskRisks` / `service.DisplayTaskStatus` / `service.AggregateRequirementProgress` / `service.SummarizeRequirementTasks`：唯一的风险与聚合派生函数。Requirements、Task、Dashboard 三处全部经过它，不重复实现。

### 1.4 严格禁止

- 在 Dashboard 域内落业务表（无 `dashboard_*` 表，无风险表，无 Dashboard 关注表）。
- 把 `blocked` 当成 `task.status` 存储。
- 前端浏览器自行计算 `blocked` / `overdue` 或 `requirement.progress`。
- 通过 Session / Token / LLM 自动改写任务进度或状态。
- 跨需求依赖、多级任务树、人工阻塞字段、archive 字段。

---

## 2. 系统分层

### 2.1 总览（自下而上）

```
┌─────────────────────────────────────────────────────────────────────────┐
│ Layer 6  Frontend ViewModel (web)                                       │
│   features/aidashboard/requirements/pages/*                             │
│   features/aidashboard/dashboard/DashboardPage.tsx                      │
│   职责：文案、排序、Modal、URL 跳转；调用 board adapter / dashboard 直查  │
├─────────────────────────────────────────────────────────────────────────┤
│ Layer 5  Web API Adapter & Client                                       │
│   features/aidashboard/requirements/api/requirementsBoardApi.ts         │
│   features/aidashboard/api/client.ts (httpClient 封装)                   │
│   职责：DTO 与 UI 字段适配；TanStack Query 与缓存键统一                  │
├─────────────────────────────────────────────────────────────────────────┤
│ Layer 4  HTTP API DTO (api/model/models.go)                             │
│   Requirement / Task / TaskDep / RequirementTaskSummary /               │
│   RequirementRiskSummary / UserFollow / DashboardFollowItem /           │
│   DashboardRiskItem / DashboardNavigationTarget                         │
│   职责：稳定的传输契约，handler 与前端共用同一份字段                     │
├─────────────────────────────────────────────────────────────────────────┤
│ Layer 3  HTTP Handler (api/handler/*.go)                                │
│   requirement.go  task.go  follow.go  dashboard.go                      │
│   职责：鉴权 / 角色作用域 SQL / 参数校验 / 调用 Domain 派生 / 事务编排   │
├─────────────────────────────────────────────────────────────────────────┤
│ Layer 2  Domain Service (api/service/requirements.go)                   │
│   DeriveTaskRisks / DisplayTaskStatus / AggregateRequirementProgress /  │
│   SummarizeRequirementTasks                                             │
│   职责：纯函数；风险规则与聚合算法的唯一源                                │
├─────────────────────────────────────────────────────────────────────────┤
│ Layer 1  Database (api/db/migrations/*.sql)                             │
│   requirements  tasks  task_dependencies  requirement_teams             │
│   user_follows  sessions  token_usage  daily_reports  team_reports      │
│   职责：持久化事实；通过 CHECK / INDEX 约束基础合法性                    │
└─────────────────────────────────────────────────────────────────────────┘
```

### 2.2 各层禁止事项

| 层 | 禁止 |
|---|---|
| L1 DB | 存 Dashboard 卡片文案、排序、计算后的风险；不放业务派生列（display_status / risk_types）|
| L2 Domain | 直接读写 DB；保持纯函数 |
| L3 Handler | 重复实现 Domain 规则；跨域写其它资源；让前端可写 `requirement.progress` 或 `task.status=blocked` |
| L4 DTO | 暴露 DB 行的内部命名；引入仅供调试的字段 |
| L5 Adapter | 在前端推导任务的最终状态（display_status / risk）|
| L6 ViewModel | 越过 adapter 直接调用任务/需求写接口；维护独立的关注或风险集合 |

---

## 3. 数据库模型（Layer 1）

迁移：`api/db/migrations/007_requirements_p0.sql`（在 001–006 基础上叠加）。

### 3.1 `requirements`

```
id              UUID PK
title           TEXT
description     TEXT
feishu_doc_url  TEXT NULL
acceptance_criteria TEXT[]      -- 需求验收标准，可选；为空不构成风险
creator_id      UUID FK users
creator_role    TEXT
status          TEXT  CHECK IN ('todo','review','active','completed','cancelled')   -- 默认 'todo'
priority        TEXT
progress        INT  0..100     -- 由后端聚合写入，前端禁止直接写
deadline        DATE NULL
completed_at    TIMESTAMPTZ NULL    -- 新增 (007)
created_at / updated_at TIMESTAMPTZ
```

迁移行为：
- `status` CHECK 收紧为五值，默认 `todo`。
- 历史 `status='completed'` 的行回填 `completed_at = updated_at`。

### 3.2 `requirement_teams`

```
requirement_id UUID FK requirements
team_id        UUID FK teams
PRIMARY KEY (requirement_id, team_id)
```

`team_leader` 的 Requirements / Tasks / Follows 可见性都通过这张表收敛。

### 3.3 `tasks`

```
id                    UUID PK
requirement_id        UUID FK requirements
title                 TEXT
acceptance_criteria TEXT[]                -- 任务验收标准，可选；不关联需求验收项
assignee_id           UUID NULL FK users
creator_tl_id         UUID FK users      -- 创建该任务的 TL/PM
status                TEXT CHECK IN ('todo','in_progress','done')  -- 由 007 收紧；blocked 不再存储
priority              TEXT
progress              INT NOT NULL DEFAULT 0 CHECK (0..100)   -- 新增 (007)
due_date              DATE NULL
completed_at          TIMESTAMPTZ NULL                          -- 新增 (007)
created_at / updated_at TIMESTAMPTZ
```

迁移行为：
- 历史 `status='blocked'` → `in_progress`。
- 历史 `status='done'` → `progress=100, completed_at=updated_at`。

索引：`idx_tasks_due_date ON tasks (due_date) WHERE status <> 'done'`（加速超期任务风险投影）。

### 3.4 `task_dependencies`

```
task_id        UUID FK tasks
depends_on_id  UUID FK tasks
PRIMARY KEY (task_id, depends_on_id)
```

应用层在写入前用递归 CTE 防环；同需求约束由 handler 校验。

### 3.5 `user_follows`（007 新增）

```
user_id     UUID FK users ON DELETE CASCADE
target_type TEXT CHECK IN ('requirement','task')
target_id   UUID
created_at  TIMESTAMPTZ DEFAULT now()
PRIMARY KEY (user_id, target_type, target_id)

INDEX idx_user_follows_target ON user_follows (target_type, target_id)
```

- 主键即幂等保证。
- 通过 `ON DELETE CASCADE` 在用户删除时自动清理。
- 反向索引便于"是否被关注"以及"谁关注了我"类查询。

### 3.6 复用未变更表

`sessions`（含 `requirement_id` / `task_id`）、`token_usage`、`daily_reports`、`team_reports`、`users` / `teams`。Token 来源汇总通过 `sessions JOIN token_usage` 派生，不新增链接表。

### 3.7 表关系图

```
              ┌───────────────────┐
              │     users         │◄──┐
              └─────────┬─────────┘   │
                        │             │ user_id
        team_id │   creator_id        │
                ▼                     │
       ┌───────────────┐       ┌──────────────┐
       │    teams      │       │ user_follows │
       └──────┬────────┘       └──────────────┘
              │team_id              │target_type='requirement|task'
              ▼                     │target_id
   ┌────────────────────┐   ┌───────┴────────────────────┐
   │ requirement_teams  ├──►│   requirements             │
   └────────────────────┘   │ status,progress,completed  │
                            └───────────┬────────────────┘
                                        │ requirement_id
                                        ▼
                              ┌──────────────────┐
                              │      tasks       │
                              │ status,progress  │
                              └──────────┬───────┘
                                         │ task_id ⇄ depends_on_id
                                         ▼
                              ┌──────────────────┐
                              │ task_dependencies│
                              └──────────────────┘

   sessions.requirement_id / sessions.task_id  ───►  token_source_ids
```

### 3.8 验收标准口径

- `requirements.acceptance_criteria` 是需求级验收标准，可选；为空不产生风险。
- `tasks.acceptance_criteria` 是任务级验收标准，可选；为空不产生风险。
- P0 不做任务关联/覆盖需求验收项，不计算覆盖率，不暴露 `acceptance_criteria_ids`。

---

## 4. Domain 层（Layer 2）

文件：`api/service/requirements.go`，单测：`api/service/requirements_test.go`。

### 4.1 风险派生 `DeriveTaskRisks(task model.Task, now time.Time) []string`

```
if task.Status == "done"  → []
risks := []
if 任一 dependency.Status != "done"  → append("blocked")  (只追加一次)
if task.DueDate 解析失败 / 为空        → return risks
today := UTC 今日零点
if due < today          → append("overdue")
return risks
```

- 支持两种日期格式：`2006-01-02`、`time.RFC3339`（SQL `NullString` 在含时区时返回 RFC3339）。
- 不派生 `due_soon` / 临期风险；未来截止日期不产生风险。
- `blocked` 可与 `overdue` 并存。
- 永远返回非 nil 切片（handler 会再做一次 nil → `[]` 归一化，保证 JSON 输出空数组）。

### 4.2 展示状态 `DisplayTaskStatus(task model.Task) string`

```
if task.Status == "done"       → "done"
if "blocked" ∈ task.RiskTypes  → "blocked"
otherwise                      → task.Status   // todo | in_progress
```

依赖：调用前 `task.RiskTypes` 必须已由 `DeriveTaskRisks` 填好。

### 4.3 进度聚合 `AggregateRequirementProgress(tasks) int`

```
if len(tasks) == 0  → 0
else                → sum(progress) / len(tasks)   // 整数除法
```

数据库侧 `TaskHandler.updateRequirementProgress` 用 `FLOOR(AVG(progress))::int` 实现同样语义，避免每次任务写完都让 handler 反查全部任务回算。

### 4.4 汇总 `SummarizeRequirementTasks(tasks)`

返回两个结构体：

```
RequirementTaskSummary { Total, Done, Blocked }
RequirementRiskSummary { Blocked, Overdue }
```

`Blocked` 在两边都计数；`Overdue` 只出现在风险汇总。

### 4.5 单元测试断言

`api/service/requirements_test.go`：

- `blocked + overdue` 共存；
- 数据库 RFC3339 日期被识别为 `overdue`；
- 未来截止日期不产生 `due_soon` / 临期风险；
- `status=done` 永远返回空数组（即便上游未完成）；
- `AggregateRequirementProgress([25,75,100]) == 66`、空切片返回 0。

---

## 5. HTTP Handler 层（Layer 3）

文件位置：`api/handler/`。路由集中在 `api/main.go` 中的 `/api/v1` 路由组下，使用 `AuthMiddleware`。

### 5.1 RequirementHandler（`requirement.go`）

| Method / Path | Handler | 说明 |
|---|---|---|
| GET `/requirements` | `List` | 支持 `status`、`team_id`；`team_leader` 自动按 `team_id` 过滤；`ORDER BY created_at DESC` |
| POST `/requirements` | `Create` | 必填 title/description/team_ids；priority 默认 medium；需求验收标准可选，空数组按空数组保存 |
| GET `/requirements/{id}` | `Get` | 单条 |
| PUT `/requirements/{id}` | `Update` | 支持 title/description/status/priority/deadline/feishu/AC；`status=completed` 时落 `completed_at = now()`，其它阶段清空 `completed_at` |
| GET `/requirements/{id}/ac` | `GetAC` | 需求验收标准列表；P0 不计算任务覆盖或完成度 |
| POST `/requirements/{id}/regenerate-ac` | `RegenerateAC` | `director/pm/team_leader` 权限；调用 `service.AIClient` 重新生成 |

每条 Requirement 响应都经过：

1. `loadTeams(req)`：从 `requirement_teams JOIN teams` 取 `team_ids` + `team_names`。
2. `loadProjection(req, userID)`：
   - 拉取该需求下全部任务（含任务验收标准、负责人、status/progress/due_date/completed_at）。
   - 每条任务通过 `TaskHandler.enrichTask` 注入依赖、风险、display_status、token_source_ids、is_followed。
   - 调用 `service.AggregateRequirementProgress` 写回 `req.Progress`。
   - 调用 `service.SummarizeRequirementTasks` 写回 `req.TaskSummary` / `req.RiskSummary`。
   - 查询 `user_follows` 写回 `req.IsFollowed`。
   - 查询 `sessions JOIN token_usage` 拼出 `req.TokenSourceIDs`（约束 `s.requirement_id=$1 AND s.task_id IS NULL`，即"直接挂在需求而非具体任务上"的 session）。

权限分支（仅列举关键差异）：
- `team_leader`（含 `team_id`）：列表中追加 `EXISTS requirement_teams` 条件。
- `admin / director / pm`：默认无范围限制。
- `employee`：列表上游未做行级过滤；可见性由其他接口（Task / Follow）保证。

### 5.2 TaskHandler（`task.go`）

| Method / Path | Handler | 说明 |
|---|---|---|
| GET `/tasks` | `List` | 支持 `requirement_id`、`assignee_id`、`status`；按角色过滤 |
| POST `/tasks` | `Create` | `admin/director/pm/team_leader`；`team_leader` 仅能给本团队员工创建且需求需挂本团队 |
| GET `/tasks/{id}` | `Get` | 单条 |
| PUT `/tasks/{id}` | `Update` | 拒绝 `status=blocked`；`progress` 0..100；`status=done` 自动 `progress=100, completed_at=now()` |
| PUT `/tasks/{id}/status` | `UpdateStatus` | 同上的单字段语义，原子 SQL 一次性处理 |
| PUT `/tasks/{id}/progress` | `UpdateProgress` | 单字段进度；不联动 status |
| POST `/tasks/{id}/dependencies` | `AddDependency` | `validateDependency` 三重校验 |
| DELETE `/tasks/{id}/dependencies/{dep_id}` | `RemoveDependency` | 物理删除 |

#### 5.2.1 enrichTask（读路径增强）

```
enrichTask(t *Task, userID string):
  loadDeps(t)                          // 填充 t.Dependencies + t.Blocking
  t.RiskTypes     = DeriveTaskRisks(*t, time.Now())
  t.DisplayStatus = DisplayTaskStatus(*t)
  if t.RiskTypes == nil { t.RiskTypes = []string{} }   // 输出空数组而非 null
  t.TokenSourceIDs = SELECT DISTINCT s.id FROM sessions JOIN token_usage WHERE s.task_id=$1
  t.IsFollowed = EXISTS user_follows WHERE user_id=userID AND target_type='task' AND target_id=t.id
```

#### 5.2.2 写路径 → 进度回写

`Update` / `UpdateStatus` / `UpdateProgress` / `Create` / `AddDependency` 完成后调用 `updateRequirementProgress(taskID)`：

```sql
UPDATE requirements
SET progress = COALESCE((SELECT FLOOR(AVG(progress))::int
                         FROM tasks WHERE requirement_id = $1), 0),
    updated_at = now()
WHERE id = $1
```

确保读路径不需要再次聚合即能拿到一致的 `Requirement.progress`。

#### 5.2.3 依赖校验 `validateDependency(requirementID, taskID, dependsOnID)`

1. `dependsOnID` 非空。
2. 非自依赖。
3. 上游任务必须存在且 `requirement_id` 与当前一致。
4. 通过 CTE 递归遍历上游集合，禁止当前任务出现在上游链中（防环）。

```sql
WITH RECURSIVE upstream(id) AS (
  SELECT depends_on_id FROM task_dependencies WHERE task_id = $dependsOn
  UNION
  SELECT td.depends_on_id FROM task_dependencies td JOIN upstream u ON td.task_id = u.id
)
SELECT EXISTS(SELECT 1 FROM upstream WHERE id = $taskID)
```

#### 5.2.4 状态合法性

`isStoredTaskStatus(status)` 仅允许 `todo | in_progress | done`。任何尝试写入 `blocked` 的请求返回：

```
400  {"error":"invalid status; blocked is derived from dependencies"}
```

### 5.3 FollowHandler（`follow.go`）

| Method / Path | Handler | 说明 |
|---|---|---|
| GET `/follows` | `List` | 列出当前用户全部关注（含 `created_at`） |
| POST `/follows` | `Follow` | 校验 `target_type ∈ {requirement, task}` + 目标可见性；`ON CONFLICT DO NOTHING` 实现幂等 |
| DELETE `/follows/{target_type}/{target_id}` | `Unfollow` | 物理删除（无需可见性校验，允许取消已不可见的关注） |

可见性 `targetVisible`：

- `requirement` + `team_leader` → `EXISTS requirement_teams` AND `team_id = u.TeamID`
- `requirement` + 其它 → 目标存在即可
- `task` + `team_leader` → assignee 在本团队 OR `creator_tl_id = u.ID`
- `task` + 其它 → 目标存在即可

返回体统一形如：

```json
{ "favorited": true, "target_type": "requirement", "target_id": "..." }
```

便于前端按布尔统一切换 UI 状态。

### 5.4 DashboardHandler（`dashboard.go`）

| Method / Path | Handler | 说明 |
|---|---|---|
| GET `/dashboard/follows` | `Follows` | 把当前用户的 `user_follows` 投影成 `DashboardFollowItem[]` |
| GET `/dashboard/risks` | `Risks` | 计算当前用户作用域内未完成任务的风险条目 |

#### 5.4.1 `/dashboard/follows` 编排

```
SELECT target_type,target_id FROM user_follows WHERE user_id=$1 ORDER BY created_at DESC

for each (target_type, target_id):
  if requirement:
     requirementFollowItem:
        load requirement basic fields
        skip if status IN (completed, cancelled)
        RequirementHandler.loadProjection(req, userID)        // 拿到 risk_summary
        item.Status   = requirementStatusLabel(req.Status)   // 待开始/评审/进行中/已完成/已取消
        item.Risk     = requirementRiskLabel(req.RiskSummary)// 优先级：超期 > 阻塞 > 正常推进
        item.URL      = /requirements?requirementId=...
  if task:
     taskFollowItem:
        load task + requirement title
        skip if task.Status == done
        TaskHandler.enrichTask(&task, userID)
        item.Status     = taskStatusLabel(task.DisplayStatus)
        item.Risk       = taskRiskLabel(task.RiskTypes)     // 优先级：超期 > 阻塞
        item.Dependency = "" if not blocked else "未完成上游：A、B"
        item.URL        = /requirements?requirementId=...&taskId=...
```

`recentUpdateLabel` 将 `updated_at` 渲染为中文相对时间（今天 / 昨天 / N 天前），由后端统一计算，避免每个前端用户重做时区运算。

#### 5.4.2 `/dashboard/risks` 编排

SQL 基础：

```sql
SELECT ...  FROM tasks t
JOIN requirements r ON r.id = t.requirement_id
LEFT JOIN users a ON a.id = t.assignee_id
WHERE t.status <> 'done' AND r.status NOT IN ('completed','cancelled')
```

按角色叠加：

- `employee`：
  ```
  AND ( t.assignee_id = $self
        OR EXISTS user_follows WHERE target=task t.id
        OR EXISTS user_follows WHERE target=requirement r.id )
  ```
- `team_leader`（含 `team_id`）：
  ```
  AND ( a.team_id = $team
        OR t.creator_tl_id = $self
        OR EXISTS user_follows ... (同上) )
  ```
- 其它角色（admin/director/pm）：无额外过滤。

排序：`ORDER BY t.due_date NULLS LAST, t.updated_at DESC`。

对每条任务调用 `enrichTask`，然后按 `risk_types` 笛卡尔展开：一个任务可产出多条风险条目。

| risk_type | riskType / source / level / tone | reason |
|---|---|---|
| `blocked`   | `dependency_blocker` / 依赖阻塞 / 高 / red    | "等待上游任务完成：A、B" |
| `overdue`   | `deadline` / 已超期 / 高 / red               | "任务尚未完成，需要更新计划或推进状态" |

每条 item 都附带 `targetUrl` 与 `navigation`。前端只负责跳转。

### 5.5 路由注册（`api/main.go`）

```go
r.Route("/api/v1", func(r chi.Router) {
    r.Use(handler.AuthMiddleware(cfg.JWTSecret))

    r.Get("/requirements", reqH.List)
    r.Post("/requirements", reqH.Create)
    r.Get("/requirements/{id}", reqH.Get)
    r.Put("/requirements/{id}", reqH.Update)
    r.Get("/requirements/{id}/ac", reqH.GetAC)
    r.Post("/requirements/{id}/regenerate-ac", reqH.RegenerateAC)

    r.Get("/tasks", taskH.List)
    r.Post("/tasks", taskH.Create)
    r.Get("/tasks/{id}", taskH.Get)
    r.Put("/tasks/{id}", taskH.Update)
    r.Put("/tasks/{id}/status", taskH.UpdateStatus)
    r.Put("/tasks/{id}/progress", taskH.UpdateProgress)
    r.Post("/tasks/{id}/dependencies", taskH.AddDependency)
    r.Delete("/tasks/{id}/dependencies/{dep_id}", taskH.RemoveDependency)

    r.Get("/follows", followH.List)
    r.Post("/follows", followH.Follow)
    r.Delete("/follows/{target_type}/{target_id}", followH.Unfollow)
    r.Get("/dashboard/follows", dashboardH.Follows)
    r.Get("/dashboard/risks",   dashboardH.Risks)

    // ...sessions / reports / tokens / documents / teams 路由保持原状...
})
```

`auth/login`、`auth/register`、`/users`、`/health` 在认证中间件之外。

---

## 6. API DTO 层（Layer 4，`api/model/models.go`）

### 6.1 主数据

```go
type Requirement struct {
    ID, Title, Description string
    FeishuDocURL *string
    AcceptanceCriteria []string
    CreatorID, CreatorName, CreatorRole string
    Status, Priority string
    Progress int                                  // 后端聚合写入
    Deadline *string
    TeamIDs, TeamNames []string
    TokenSourceIDs []string
    TaskSummary RequirementTaskSummary             // {Total,Done,Blocked}
    RiskSummary RequirementRiskSummary             // {Blocked,Overdue}
    IsFollowed  bool
    CompletedAt *time.Time
    CreatedAt, UpdatedAt time.Time
}

type Task struct {
    ID, RequirementID, RequirementTitle, Title string
    AcceptanceCriteria []string
    AssigneeID, AssigneeName *string
    CreatorTLID string
    Status, DisplayStatus, Priority string
    Progress int
    DueDate *string
    Dependencies, Blocking []TaskDep
    RiskTypes []string                              // 永远非 nil（handler 归一化）
    TokenSourceIDs []string
    IsFollowed bool
    CompletedAt *time.Time
    CreatedAt, UpdatedAt time.Time
}

type TaskDep struct {
    TaskID, TaskTitle, Status string
}
```

### 6.2 关注 / Dashboard 投影

```go
type UserFollow struct {
    UserID, TargetType, TargetID string
    CreatedAt time.Time
}

type FollowRequest struct {
    TargetType, TargetID string
}

type DashboardNavigationTarget struct {
    RequirementID string
    TaskID *string
    URL string
}

type DashboardFollowItem struct {
    Key, Type, Title string
    Requirement string                  // 任务时回填所属需求标题
    RequirementID string
    TaskID *string
    Owner, Status, Deadline, Risk, Dependency, Activity string
    Navigation DashboardNavigationTarget
}

type DashboardRiskItem struct {
    Key string
    RiskType  string                    // "dependency_blocker" | "deadline"
    Title, Source, Target string
    RelatedObjectType string             // "task"
    RequirementID, TaskID string
    Owner, Deadline, Reason string
    Level string                         // "高" | "中"
    Tone  string                         // "red" | "orange"
    ActionText, TargetURL string
    Navigation DashboardNavigationTarget
}
```

### 6.3 写请求结构体

```go
CreateRequirementRequest { Title, Description, FeishuDocURL*, Priority, Deadline*, TeamIDs[], AcceptanceCriteria[] }
UpdateRequirementRequest { Title*, Description*, FeishuDocURL*, Priority*, Status*, Deadline*, AcceptanceCriteria*[] }
CreateTaskRequest        { RequirementID, Title, AcceptanceCriteria[], AssigneeID*, Priority, DueDate*, DependsOnIDs[] }
UpdateTaskRequest        { Title*, AcceptanceCriteria*[], AssigneeID*, Status*, Priority*, DueDate*, Progress* }
UpdateTaskStatusRequest  { Status }    // 仅允许 todo | in_progress | done
UpdateTaskProgressRequest{ Progress }  // 0..100
AddDependencyRequest     { DependsOnID }
FollowRequest            { TargetType, TargetID }
```

### 6.4 DTO 别名

```go
type RequirementListItemDTO       = Requirement
type RequirementDetailDTO         = Requirement
type RequirementTaskDTO           = Task
type TaskDependencyDTO            = TaskDep
type RequirementRiskSummaryDTO    = RequirementRiskSummary
type RequirementFollowStateDTO    = RequirementFollowState
```

前端 TS 同名同字段（见 `web/src/features/aidashboard/api/types.ts`），通过 `httpClient.normalizeApiResponse` 兼容 `{code,msg,data}` 包封装。

---

## 7. Web 客户端层（Layer 5）

### 7.1 通用 HTTP 客户端

`web/src/shared/request/httpClient.ts` 提供 `api.get/post/put/delete`，自动注入 `Authorization: Bearer <token>`，401 时清空 session 并跳转 `/login`。

### 7.2 Requirements / Dashboard API 调用

`web/src/features/aidashboard/api/client.ts` 暴露所有真实接口：

```ts
// Requirements
fetchRequirements()  fetchRequirement(id)
createRequirement(...)  updateRequirement(id, data)
fetchACStatus(id)  regenerateAC(id)

// Tasks
fetchTasks(params?)  fetchTask(id)
createTask(...)  updateTask(id, data)
updateTaskStatus(id, status)  updateTaskProgress(id, progress)
addTaskDependency(taskId, dependsOnId)  removeTaskDependency(taskId, depId)

// Follows / Dashboard 投影
fetchFollows()  followTarget(type, id)  unfollowTarget(type, id)
fetchDashboardFollows()  fetchDashboardRisks()
```

### 7.3 适配器：`requirementsBoardApi`

文件：`web/src/features/aidashboard/requirements/api/requirementsBoardApi.ts`。

作用：把后端 DTO 适配为页面所用的 `MockRequirement` / `MockTask` 形状（页面历史上是从 mock 起步的），统一调用入口：

```ts
requirementsBoardApi = {
    listRequirements()       // fetchRequirements + normalizeRequirement
    listTasks()              // fetchTasks
    listFavorites()          // fetchFollows -> 适配字段
    listTokenSources()       // 由 sessions / tokens 端点拼装

    createRequirement(input) // createRequirement + normalize
    updateRequirementStage(id, status)  // updateRequirement({status})

    createTask(input)
    updateTaskStatus(id, status)        // 只允许 stored 状态
    updateTaskProgress(id, progress)
    addTaskDependency(taskId, depId)
    removeTaskDependency(taskId, depId)

    toggleFavorite(type, id)            // 内部判断是 follow 还是 unfollow
}
```

`updateTaskStatus` 的类型签名是 `Exclude<MockTaskStatus, "blocked">`，与后端 `isStoredTaskStatus` 一致；前端连"尝试发送 blocked"都被 TS 编译器拒绝。

### 7.4 缓存键约定

| 用途 | Query Key |
|---|---|
| 需求列表/详情 | `["requirements-board", ...]` |
| Dashboard 我的关注 | `["dashboard", "follows"]` |
| Dashboard 待处理风险 | `["dashboard", "risks"]` |
| 关注关系 | `["requirements-board", "favorites"]` |

所有写操作成功回调中并行 invalidate 上述键：

```ts
queryClient.invalidateQueries({ queryKey: ["requirements-board"] });
queryClient.invalidateQueries({ queryKey: ["dashboard", "follows"] });
queryClient.invalidateQueries({ queryKey: ["dashboard", "risks"] });
```

实测覆盖点（`RequirementsListPage.tsx`、`TaskDetailPage.tsx`）：
- 状态变更（需求阶段、任务状态）
- 进度更新
- 任务创建
- 依赖增删
- Token 来源关联
- 关注切换

---

## 8. 前端 ViewModel 层（Layer 6）

### 8.1 Requirements 看板

`web/src/features/aidashboard/requirements/pages/RequirementsListPage.tsx`：
- 顶部 KPI / 阶段筛选 / 团队筛选 / 我关注切换。
- 主体：四列看板（待开始 / 评审 / 进行中 / 完成（最近 7 天，最多 10 条））。
- 卡片：标题、负责团队、负责人、进度、风险标签、关注按钮。
- 抽屉：需求详情 + 任务树 + 添加任务弹窗。

`RequirementDetailPage.tsx` / `TaskDetailPage.tsx` 提供按 ID 的深链访问，前者读取 `requirementId`，后者读取 `requirementId & taskId`。

### 8.2 Dashboard

`web/src/features/aidashboard/dashboard/DashboardPage.tsx`：
- 关注卡片直接消费 `fetchDashboardFollows()`。
- 风险卡片直接消费 `fetchDashboardRisks()`。
- 报告、Token、Session 三栏继续调用旧接口（`/reports`、`/tokens`、`/sessions`）。
- 点击关注/风险条目：使用 `item.navigation.url`（即 `/requirements?...`）跳转。

### 8.3 前端禁止事项（强约束）

- 不在浏览器中根据 dependencies/due_date 重新计算 `risk_types`、`display_status`。
- 不直接 PUT `requirement.progress`，永远经由 `updateTaskProgress`。
- 不维护 localStorage 中的"关注列表"；关注真相只在 `user_follows`。
- 不通过 Session/Token 推断或修改任务状态/进度。

---

## 9. 数据流：写路径（端到端）

### 9.1 创建需求

```
用户表单（RequirementCreatePage / RequirementsListPage 抽屉）
   └─► requirementsBoardApi.createRequirement(input)
         └─► api/client.ts: createRequirement(...)
               └─► POST /api/v1/requirements
                     └─► RequirementHandler.Create
                           ├─ 校验 title/description/team_ids
                           ├─ 需求验收标准可选，空数组不触发风险
                           ├─ INSERT requirements
                           ├─ INSERT requirement_teams 多条
                           └─ 重新 SELECT + loadTeams + loadProjection
                                 └─ AggregateRequirementProgress / SummarizeRequirementTasks
            返回 Requirement DTO
   失效缓存：requirements-board / dashboard.follows / dashboard.risks
```

### 9.2 更新任务进度

```
Slider/InputNumber
  └─► requirementsBoardApi.updateTaskProgress(id, n)
        └─► PUT /api/v1/tasks/{id}/progress  { progress: n }
              └─► TaskHandler.UpdateProgress
                    ├─ 范围校验 0..100
                    ├─ UPDATE tasks SET progress=$1, updated_at=now()
                    ├─ updateRequirementProgress(id)
                    │     └─ UPDATE requirements SET progress = FLOOR(AVG(progress))::int
                    └─ Get(w, r)  → enrichTask → DeriveTaskRisks + DisplayTaskStatus
           返回 Task DTO（含最新 display_status、risk_types）
  失效：requirements-board / dashboard.follows / dashboard.risks
```

### 9.3 添加上游依赖

```
任务详情：选择上游
  └─► addTaskDependency(taskId, depId)
        └─► POST /api/v1/tasks/{id}/dependencies
              └─► TaskHandler.AddDependency
                    ├─ 取 task.requirement_id
                    ├─ validateDependency(reqId, taskId, depId)
                    │     ├─ 非空 / 非自依赖
                    │     ├─ 同 requirement_id
                    │     └─ CTE 防环
                    ├─ INSERT task_dependencies ON CONFLICT DO NOTHING
                    └─ Get(w, r) → enrichTask
           返回 Task（含新的依赖列表与可能新增的 blocked 风险）
  失效：requirements-board / dashboard.risks
```

### 9.4 关注 / 取消关注

```
点击关注按钮
  └─► requirementsBoardApi.toggleFavorite(type, id)
        ├─ 已关注：DELETE /api/v1/follows/{type}/{id}
        └─ 未关注：POST   /api/v1/follows  { target_type, target_id }
              └─► FollowHandler.Follow
                    ├─ 校验 target_type / target_id
                    ├─ targetVisible(role, type, id) 拒绝越权
                    └─ INSERT user_follows ON CONFLICT DO NOTHING
  返回：{ favorited, target_type, target_id }
  失效：requirements-board.favorites / dashboard.follows / dashboard.risks
        + 需求/任务详情（更新 is_followed）
```

### 9.5 阶段流转 → 完成

```
需求拖动到"完成"列
  └─► requirementsBoardApi.updateRequirementStage(id, 'completed')
        └─► PUT /api/v1/requirements/{id}  { status: 'completed' }
              └─► RequirementHandler.Update
                    ├─ isRequirementStatus('completed') 校验
                    ├─ SET status='completed',
                    │       completed_at = COALESCE(completed_at, now())
                    └─ Get(w, r) → loadTeams + loadProjection
           完成列展示：前端按 completed_at 排序，截取最近 7 天且 ≤ 10 条
```

> 任务级 `done` 走 `PUT /tasks/{id}/status`：自动 `progress=100, completed_at=now()`，并触发需求进度回写；若全部任务完成而需求阶段仍为 `active`，由用户在 UI 显式拖动到完成列（P0 不强制自动完成）。

---

## 10. 数据流：读路径（端到端）

### 10.1 打开需求看板

```
RequirementsListPage 挂载
  ├─► requirementsBoardApi.listRequirements()
  │     └─► GET /api/v1/requirements
  │           └─► RequirementHandler.List
  │                 ├─ 角色作用域 SQL
  │                 ├─ ORDER BY created_at DESC
  │                 └─ 每条 loadTeams + loadProjection
  │                         · 任务 enrichTask（依赖、风险、display_status、token、is_followed）
  │                         · AggregateRequirementProgress
  │                         · SummarizeRequirementTasks
  │                         · user_follows 计算 is_followed
  │                         · token_source_ids
  ├─► requirementsBoardApi.listTasks()      // 用于任务树视图
  ├─► requirementsBoardApi.listTokenSources()
  └─► requirementsBoardApi.listFavorites()   // GET /follows
        前端按 task_summary / risk_summary / completed_at 决定看板列/排序
```

### 10.2 打开 Dashboard

```
DashboardPage 挂载
  ├─► fetchDashboardFollows()       GET /api/v1/dashboard/follows
  │     └─ DashboardHandler.Follows
  │           · 读取 user_follows
  │           · 对 requirement: loadProjection；过滤 completed/cancelled
  │           · 对 task: enrichTask；过滤 done；blocked 时拼 dependency 文本
  ├─► fetchDashboardRisks()         GET /api/v1/dashboard/risks
  │     └─ DashboardHandler.Risks
  │           · 按角色作用域 SQL 取未完成任务（所属需求未关闭）
  │           · enrichTask
  │           · 按 risk_types 展开为风险条目
  ├─► 旧接口：reports / tokens / sessions（保持现状）
  前端：仅做卡片渲染、排序、跳转。
```

### 10.3 跳转协议

```
/requirements?requirementId={rid}                    // 打开需求抽屉
/requirements?requirementId={rid}&taskId={tid}       // 打开任务抽屉
```

Requirements 页解析 query 并自动打开对应详情。Dashboard 卡片只携带 `navigation` 中的 URL，不携带业务数据。

---

## 11. 一致性与刷新策略

### 11.1 后端一致性

- 每次任务级写操作（status / progress / dependency / create）后，**同请求内**完成 `updateRequirementProgress`，保证 `Requirement.progress` 与任务进度一致。
- `display_status` / `risk_types` **不落库**，仅在读路径派生：删除依赖、上游变 done、修改 due_date 后下一次查询自然消失。
- `status='completed'` 与 `completed_at` 在 Update 内联：`COALESCE(completed_at, now())`；恢复到非 completed 阶段时清 `completed_at`。

### 11.2 前端缓存失效

写操作成功回调中需要同时 invalidate：

- `["requirements-board"]`（覆盖列表/详情/任务树）
- `["dashboard", "follows"]`
- `["dashboard", "risks"]`
- 必要时 `["requirements-board", "favorites"]`

模块独立失败：Dashboard 三栏（关注 / 风险 / 报告 / Token）的 query 失败彼此隔离，不会导致整页崩溃。

### 11.3 不做客户端推导

前端拿到 DTO 后不再重做：
- ❌ 自己根据 dependencies 推 `blocked`
- ❌ 自己根据 due_date 推 `overdue`
- ❌ 自己根据任务列表推 `requirement.progress`

任何此类逻辑出现都视为违反分层契约。

---

## 12. 权限与角色作用域

| 资源 | admin / director / pm | team_leader (含 team_id) | employee |
|---|---|---|---|
| Requirements 列表 | 全量 | `EXISTS requirement_teams (team_id=$self.team)` | 看团队全量；列表上游无行级过滤，但其它接口收敛实际可达性 |
| Tasks 列表 | 全量 | assignee 在本团队 OR `creator_tl_id=$self` | 仅自己负责（`scope=requirements` 时放宽） |
| 创建任务 | 允许 | 允许，但 assignee 必须本团队员工且需求挂本团队 | 拒绝 |
| 关注（Follow）| 允许（仅校验存在性） | 必须可见（团队范围） | 必须可见（assignee/follower 路径） |
| `/dashboard/risks` | 全量 | 自团队成员或自己创建 + 关注路径 | 自己负责 + 关注路径 |
| Admin 接口 | 仅 admin | — | — |

所有作用域条件都用 SQL 参数化写入 handler，避免依赖中间件。`admin` 在多数处与 `director` / `pm` 行为一致。

---

## 13. 接口契约速查表

### 13.1 Requirements

| Method | Path | Req Body | Resp |
|---|---|---|---|
| GET  | `/api/v1/requirements?status&team_id` | – | `Requirement[]` |
| POST | `/api/v1/requirements` | `CreateRequirementRequest` | `Requirement` |
| GET  | `/api/v1/requirements/{id}` | – | `Requirement` |
| PUT  | `/api/v1/requirements/{id}` | `UpdateRequirementRequest` | `Requirement` |
| GET  | `/api/v1/requirements/{id}/ac` | – | `ACStatus[]` |
| POST | `/api/v1/requirements/{id}/regenerate-ac` | – | `{acceptance_criteria}` |

### 13.2 Tasks

| Method | Path | Req | Resp |
|---|---|---|---|
| GET    | `/api/v1/tasks?requirement_id&assignee_id&status&scope` | – | `Task[]` |
| POST   | `/api/v1/tasks` | `CreateTaskRequest` | `{id,status}` |
| GET    | `/api/v1/tasks/{id}` | – | `Task` |
| PUT    | `/api/v1/tasks/{id}` | `UpdateTaskRequest` | `Task` |
| PUT    | `/api/v1/tasks/{id}/status` | `{status}` 仅 todo/in_progress/done | `Task` |
| PUT    | `/api/v1/tasks/{id}/progress` | `{progress}` 0..100 | `Task` |
| POST   | `/api/v1/tasks/{id}/dependencies` | `{depends_on_id}` | `Task` |
| DELETE | `/api/v1/tasks/{id}/dependencies/{dep_id}` | – | `Task` |

### 13.3 Follow + Dashboard 投影

| Method | Path | Req | Resp |
|---|---|---|---|
| GET    | `/api/v1/follows` | – | `UserFollow[]` |
| POST   | `/api/v1/follows` | `{target_type,target_id}` | `{favorited,...}` |
| DELETE | `/api/v1/follows/{target_type}/{target_id}` | – | `{favorited:false,...}` |
| GET    | `/api/v1/dashboard/follows` | – | `DashboardFollowItem[]` |
| GET    | `/api/v1/dashboard/risks`   | – | `DashboardRiskItem[]` |

### 13.4 错误状态约定

- 400：枚举非法（status / target_type）、范围越界、缺必填项、依赖不合法（自依赖 / 跨需求 / 环）。
- 401：未登录或 token 失效（中间件统一处理）。
- 403：角色不足（如非 TL/PM 创建任务、非 admin 调用 admin 接口）。
- 404：对象不存在或对当前用户不可见（Follow 写时也会用 404 表达"目标不可见"）。
- 5xx：DB / IO 异常，原始 error 文本透传到 JSON。

---

## 14. P0 明确不做

以下能力**没有数据库列、没有接口、没有前端实现**，引用现有未实现状态时应明确说明：

- 手动 archive / unarchive 与 `archived_at` 列。
- 自动归档已完成需求。
- 独立关注中心 / 风险中心 / 通知中心。
- 评论、注释、审批。
- 跨需求依赖、多级任务树、人工阻塞、阻塞原因字段。
- 周报 / 部门报告新接口、完整日报系统重构。
- 飞书发送、Skill 上传管理。
- 任务关联/覆盖需求验收项、覆盖率、`tasks.acceptance_criteria_ids` 业务链路。
- 缺少需求验收标准或任务验收标准风险。
- Token 明细页与权限管理页重构。
- Session / LLM 自动改写任务进度或状态。
- Dashboard 私有业务表。

---

## 15. 验收清单（按层）

### 15.1 数据库 (L1)

- [ ] `requirements.status` CHECK 五值；`completed_at` 列存在；`status='completed'` 历史行有 `completed_at`。
- [ ] `tasks.status` CHECK 仅三值；`progress` NOT NULL DEFAULT 0；`completed_at` 存在；`status='done'` 历史行 `progress=100, completed_at` 非空。
- [ ] `user_follows` 表与 `idx_user_follows_target` 存在。
- [ ] `idx_tasks_due_date` 部分索引存在。

### 15.2 Domain (L2)

- [ ] `DeriveTaskRisks` 对 `done` 任务返回空数组。
- [ ] `due < today` → `overdue`；未来截止日期不产生 `due_soon` / 临期风险。
- [ ] `blocked` 与 `overdue` 可共存。
- [ ] `AggregateRequirementProgress([25,75,100]) == 66`。
- [ ] `requirements_test.go` 全绿。

### 15.3 Handler (L3)

- [ ] `PUT /tasks/{id}*` 拒绝 `status=blocked` 并返回 400。
- [ ] `status=done` 自动 `progress=100, completed_at=now()`；非 done 清空 `completed_at`。
- [ ] 依赖添加遇自依赖 / 跨需求 / 环路时返回 400。
- [ ] 任意任务写操作后，对应 Requirement.progress 立即一致。
- [ ] `POST /follows` 在已存在记录时不报错（幂等）；目标不可见时 404。
- [ ] `/dashboard/follows` 过滤 `requirement.status ∈ {completed,cancelled}` 与 `task.status=done`。
- [ ] `/dashboard/risks` 按角色作用域过滤；按 `due_date NULLS LAST, updated_at DESC` 排序；多风险展开为多条。

### 15.4 前端 (L5/L6)

- [ ] 所有写操作成功回调失效 `requirements-board` + `dashboard.follows` + `dashboard.risks` 三组 key。
- [ ] 前端类型禁止 `updateTaskStatus(id, 'blocked')`。
- [ ] Dashboard 关注/风险条目跳转 URL 与 Requirements 页面参数协议一致（`requirementId` + 可选 `taskId`）。
- [ ] 前端不本地推导 `blocked` / `overdue` / `requirement.progress`。
- [ ] Dashboard 单卡片请求失败不影响其它卡片。

### 15.5 端到端

- [ ] 关注某需求 → Dashboard "我的关注"立即出现；标记需求为 completed → 立即消失。
- [ ] 给任务加未完成上游依赖 → Dashboard 风险列表出现 `dependency_blocker`；上游变 done → 下次刷新消失。
- [ ] 把任务 due_date 改到昨天 → 出现 `overdue`；改到未来日期 → 不出现 `due_soon` / 临期风险。
- [ ] 任务进度 0→100 → Requirement.progress 同步；阶段拖到"完成"→ 落 `completed_at`。
