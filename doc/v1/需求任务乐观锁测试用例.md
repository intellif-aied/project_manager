# 需求任务乐观锁测试用例

## 0. 测试目标

基于《需求任务乐观锁 P0 改造方案》，设计一套接口级真实模拟测试，用真实 HTTP API 调用验证：

- `requirements.version` 是否只保护需求本体字段。
- `tasks.version` 是否保护任务本体字段和任务依赖。
- 写接口是否必须携带 `base_version`。
- 版本冲突是否返回 `409 EDIT_CONFLICT`。
- `409` 是否包含 `current_version`。
- 是否能防止多人同时编辑时的静默覆盖。
- 任务变化是否不会误 bump 父需求 `requirements.version`。
- 业务 `409` 是否不会被误判为并发冲突。
- 成功写接口是否返回最新 `version`，避免连续操作自己触发 `409`。

本测试不是前端 E2E，不使用浏览器，只通过真实 HTTP API 模拟客户端 A/B。

---

## 1. 测试执行形态

### 1.1 执行前置条件

本文件只定义测试方案。后续 Claude Code 执行需求实现后，应先重启 API 服务，让 migration 和新 handler 代码生效，再按本文件执行接口测试。

执行前必须确认：

1. API 服务已重启并可访问 `API_BASE_URL`。
2. 数据库 migration 已执行，`requirements.version` / `tasks.version` 已存在。
3. 内置测试账号可登录，默认密码为 `123`。
4. `/teams`、`/task-assignees` 可正常返回测试数据。
5. 测试运行器只新增测试数据和读取接口状态，不修改生产业务逻辑，不手工改库。

如果任一前置条件不满足，测试运行器应输出 `SKIP all` 或将相关 setup 标记为 `SKIP`，并给出阻塞原因，不应继续执行会产生误判的用例。

### 1.2 推荐新增脚本

建议新增独立脚本：

- `scripts/test_optimistic_lock_api.py`

文档本身不修改生产业务逻辑；脚本只通过 HTTP API 创建测试数据、执行请求、读取最终状态并输出报告。

### 1.3 运行命令

示例：

```bash
API_BASE_URL=http://localhost:8080/api/v1 \
TEST_EMPLOYEE_ID=liu_tl \
TEST_PASSWORD='123' \
python3 scripts/test_optimistic_lock_api.py
```

如果已有 token：

```bash
API_BASE_URL=http://localhost:8080/api/v1 \
TEST_TOKEN='eyJ...' \
python3 scripts/test_optimistic_lock_api.py
```

清理测试数据需要显式传参，默认不清理：

```bash
API_BASE_URL=http://localhost:8080/api/v1 \
TEST_TOKEN='eyJ...' \
python3 scripts/test_optimistic_lock_api.py --cleanup
```

### 1.4 环境变量

| 变量 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- |
| `API_BASE_URL` | 否 | `http://localhost:8080/api/v1` | API base URL |
| `TEST_TOKEN` | 否 | 空 | 已登录 JWT；提供后跳过登录 |
| `TEST_EMPLOYEE_ID` | 否 | `liu_tl` | 登录工号；未提供 token 时使用，默认用 TL 账号便于创建本团队需求 / 任务 |
| `TEST_PASSWORD` | 否 | `123` | 登录密码；未提供 token 时使用当前服务内置账号默认密码 |
| `TEST_TEAM_ID` | 否 | 自动从 `/teams` 取第一个 | 创建需求用 team id |
| `TEST_ASSIGNEE_ID` | 否 | 自动从 `/task-assignees` 取第一个 | 创建任务用 assignee id |
| `TEST_TIMEOUT_SECONDS` | 否 | `20` | HTTP 超时 |
| `TEST_PREFIX` | 否 | `OL_TEST_<timestamp>` | 测试数据唯一前缀 |

### 1.5 内置测试账号

测试用例执行时优先使用当前服务内置账号，覆盖 4 类主要角色，避免因缺少可登录账号导致接口测试阻塞。

| 变量名 | employee_id | 姓名 | role | 说明 |
| --- | --- | --- | --- | --- |
| `directorUser` | `li_director` | 李总监 | `director` | 验证不再默认全局 |
| `pmUser` | `chen_pm` | 陈PM | `pm` | 验证不再默认全局 |
| `tlUser` | `liu_tl` | 刘TL | `team_leader` | 验证不再默认团队 |
| `employeeA` | `zhangsan` | 张三 | `employee` | 验证负责 / 创建 / 关注 |
| `employeeB` | `lisi` | 李四 | `employee` | 作为无关或被指派对照 |

默认密码：

```text
123
```

账号使用建议：

- 主流程用例默认使用 `tlUser` 登录；创建需求时 `team_ids` 必须使用 `tlUser.team_id`，创建任务时 `assignee_id` 优先选择 `/task-assignees` 中与 `tlUser.team_id` 相同的员工。
- 如果 `tlUser` 没有 `team_id`、没有可指派员工，或本地权限策略导致 TL 无法创建测试数据，则改用 `pmUser`；`pmUser` 可创建任意团队需求并指派员工。
- `directorUser` / `pmUser` 用于验证全局角色不再默认绕过乐观锁；不要把全局可见权限误判为无需 `base_version`。
- 权限边界类用例使用 `employeeA` / `employeeB` 做负责人与无关用户对照。
- 脚本或人工执行时仍允许通过 `TEST_EMPLOYEE_ID` / `TEST_PASSWORD` / `TEST_TOKEN` 覆盖默认账号。

认证优先级：

1. 如果设置 `TEST_TOKEN`，直接使用 `Authorization: Bearer <token>`。
2. 否则调用 `POST /auth/login`，body：
   ```json
   { "employee_id": "...", "password": "..." }
   ```
3. 不硬编码真实密码；本地默认账号密码由运行者通过环境变量提供。

### 1.6 测试数据前缀

所有测试数据标题统一使用：

```text
OL_TEST_<timestamp>_<case_id>_<suffix>
```

示例：

```text
OL_TEST_20260627_153012_B1_requirement
OL_TEST_20260627_153012_D1_task
```

默认保留测试数据，方便人工 review。`--cleanup` 仅删除脚本本次创建且可删除的数据；遇到业务关联导致不可删除时记录 `SKIP cleanup`，不得强删人工已有数据。

### 1.7 测试数据构造规则

每个用例必须独立创建自己的 Requirement / Task，避免用例之间共享资源导致 version 被前置用例递增后误判。

推荐 setup 顺序：

1. 登录主测试账号，默认 `liu_tl / 123`。
2. 调用 `GET /auth/me` 或解码登录响应，记录当前用户 `id`、`role`、`team_id`。
3. 调用 `GET /teams` 获取团队列表。
4. 调用 `GET /task-assignees` 获取可指派员工列表。
5. 选择 `team_id`：优先 `TEST_TEAM_ID`；否则优先当前登录用户 `team_id`；若当前用户没有团队，则取 `/teams` 第一个。
6. 选择 `assignee_id`：优先 `TEST_ASSIGNEE_ID`；否则优先同团队员工；否则取 `/task-assignees` 第一个。
7. 若无法选择合法 `team_id` 或 `assignee_id`，所有依赖创建需求 / 任务的用例标记 `SKIP`。

创建基础需求的建议 payload：

```json
{
  "title": "OL_TEST_<timestamp>_<case_id>_requirement",
  "description": "optimistic lock test requirement",
  "priority": "medium",
  "team_ids": ["<selected_team_id>"],
  "acceptance_criteria": ["AC 1", "AC 2"]
}
```

创建基础任务的建议 payload：

```json
{
  "requirement_id": "<requirement_id>",
  "title": "OL_TEST_<timestamp>_<case_id>_task",
  "acceptance_criteria": ["AC 1"],
  "assignee_id": "<selected_assignee_id>",
  "priority": "medium"
}
```

如果 `POST /tasks` 返回 `{id,status}`，脚本应紧接着 `GET /tasks/{id}` 获取完整任务详情；任务创建不带 `base_version`，创建接口本身不强制返回完整 Task，但详情 / 列表 / 需求投影里的新任务必须返回 `version`。

---

## 2. 当前接口梳理

### 2.1 Base URL

开发环境常用：

```text
http://localhost:8080/api/v1
```

脚本通过 `API_BASE_URL` 覆盖。

### 2.2 认证方式

登录接口：

```http
POST /auth/login
Content-Type: application/json

{
  "employee_id": "liu_tl",
  "password": "<from env, default 123>"
}
```

成功后返回 token。脚本统一请求头：

```http
Authorization: Bearer <token>
Content-Type: application/json
```

### 2.3 需求接口

| 操作 | 方法与路径 | 请求体 / 参数 |
| --- | --- | --- |
| 创建需求 | `POST /requirements` | `title`, `description`, `priority`, `team_ids`, `deadline?`, `feishu_doc_url?`, `acceptance_criteria?` |
| 需求详情 | `GET /requirements/{id}` | - |
| 需求列表 | `GET /requirements` | 可选 query |
| 更新需求 | `PUT /requirements/{id}` | 业务字段 + `base_version` |
| 取消需求 | `PUT /requirements/{id}` | `{ "status": "cancelled", "base_version": v }` |
| 恢复需求 | `PUT /requirements/{id}/restore` | `{ "base_version": v }` |
| 删除需求 | `DELETE /requirements/{id}?base_version=v` | query |
| 重新生成 AC | `POST /requirements/{id}/regenerate-ac` | `{ "base_version": v }` |

### 2.4 任务接口

| 操作 | 方法与路径 | 请求体 / 参数 |
| --- | --- | --- |
| 创建任务 | `POST /tasks` | `requirement_id`, `title`, `acceptance_criteria?`, `assignee_id`, `priority`, `due_date?`, `depends_on_ids?` |
| 任务详情 | `GET /tasks/{id}` | - |
| 任务列表 | `GET /tasks?requirement_id={id}` | query |
| 更新任务 | `PUT /tasks/{id}` | 业务字段 + `base_version` |
| 更新任务状态 | `PUT /tasks/{id}/status` | `{ "status": "in_progress", "base_version": v }` |
| 更新任务进度 | `PUT /tasks/{id}/progress` | `{ "progress": 50, "base_version": v }` |
| 删除任务 | `DELETE /tasks/{id}?base_version=v` | query |
| 新增依赖 | `POST /tasks/{id}/dependencies` | `{ "depends_on_id": "...", "base_version": v }` |
| 删除依赖 | `DELETE /tasks/{id}/dependencies/{dep_id}?base_version=v` | query |

### 2.5 当前不可测 / 条件性可测接口

| 场景 | 默认处理 |
| --- | --- |
| `regenerate-ac` 正常成功 | 如果本地没有 AI / Claude CLI / mock 能力，标记 `SKIP` |
| `regenerate-ac` 生成期间版本变化 | 需要 AI 延迟、mock、stub 或测试 hook；无能力则 `SKIP` |
| 删除任务副作用：sessions / token_usage / documents / user_follows | 如果无法通过公开 API 构造关联，只覆盖可构造部分，其余 `SKIP` |
| 跨非法范围依赖、循环依赖 | 如果当前后端没有相应校验，记录当前行为，不假装通过 |

---

## 3. 测试脚本设计

### 3.1 结果状态

每个 case 输出：

- `PASS`：实际结果符合期望。
- `FAIL`：接口返回、最终状态或副作用不符合期望。
- `SKIP`：当前环境 / 接口能力不足以执行，需输出原因。

判定口径：

- 乐观锁 P0 已实现后，核心能力缺失必须记为 `FAIL`，不能记为 `SKIP`。例如详情 / 列表 / 投影响应缺少 `version`、写接口缺少 `base_version` 仍成功、旧版本写入返回 `200`、冲突响应没有 `current_version`。
- 只有环境不可控或公开 API 无法构造的场景才记为 `SKIP`。例如 AI/Claude CLI 不可用、无法构造 sessions/token_usage/documents 关联、缺少 mock 延迟能力。
- setup 阶段无法创建基础 Requirement / Task 时，依赖该资源的用例记为 `SKIP`，原因必须包含具体失败接口、状态码和响应体。
- `POST /requirements` 创建响应必须包含 `version`。`POST /tasks` 可以只返回 `id/status`，但脚本必须紧接着 `GET /tasks/{id}` 补齐完整任务并校验 `version`；如果详情缺少 `version`，记为 `FAIL`。
- 业务 `409` 只有在 `code == "EDIT_CONFLICT"` 时才算并发冲突；其他业务 409 必须按业务错误校验。

### 3.2 前置健康检查

正式执行 case 前先执行 health/setup 检查：

1. `POST /auth/login`：确认主账号可登录。
2. `GET /auth/me`：确认 token 有效，并记录当前用户角色和团队。
3. `GET /teams`：确认至少有一个团队。
4. `GET /task-assignees`：确认至少有一个可指派员工。
5. `POST /requirements` + `GET /requirements/{id}`：确认能创建并读取测试需求。
6. `POST /tasks` + `GET /tasks/{id}`：确认能创建并读取测试任务。

health/setup 不计入业务 case 总数，但失败时必须输出阻塞详情。若只是一部分能力不可用，继续执行不依赖该能力的用例。

### 3.3 输出字段

每个用例输出一行表格：

| case_id | name | status | reason | related endpoint |
| --- | --- | --- | --- | --- |

失败用例必须输出：

- request method/path
- request body，隐藏 token
- status code
- response body
- final GET state

### 3.4 并发模拟方式

A/B 模拟规则：

1. A 和 B 都先 `GET` 同一资源，拿到相同 `version = v1`。
2. A 用 `base_version = v1` 提交成功。
3. B 仍用 `base_version = v1` 提交。
4. 校验 B 是否返回 `409 EDIT_CONFLICT`，最终状态是否没有静默覆盖。

真实并发压测型用例使用 Python `ThreadPoolExecutor`：

- 10 个请求使用同一个 `base_version` 同时提交。
- 统计成功数、冲突数、最终版本。

### 3.5 并发冲突判断函数

脚本必须区分业务 409 与并发 409：

```python
def is_edit_conflict(status_code, body):
    return status_code == 409 and isinstance(body, dict) and body.get("code") == "EDIT_CONFLICT"
```

不能只用 `status_code == 409` 判断。

### 3.6 响应版本校验函数

所有成功写接口必须校验：

```python
def assert_has_version(entity, expected=None):
    assert "version" in entity
    assert isinstance(entity["version"], int)
    if expected is not None:
        assert entity["version"] == expected
```


### 3.7 版本递增校验规则

除特别说明的幂等依赖操作外，成功修改同一实体的接口应满足：

- 需求本体修改成功：`requirements.version = base_version + 1`。
- 任务本体修改成功：`tasks.version = base_version + 1`。
- owner task 依赖集合实际发生变化：owner `tasks.version = base_version + 1`。
- 删除目标实体成功后目标行不存在，不要求返回实体 version。
- 任务新增 / 编辑 / 删除 / 状态 / 进度 / 依赖变化，不应导致父需求 `requirements.version` 递增。

校验最终状态时必须重新 `GET`，不要只相信写接口响应。

乐观 UI / 连续操作类测试必须使用上一次成功响应里的 `version` 发起下一次请求，不能重新 GET 后规避成功响应未返回最新版本的问题。


---

## 4. 可运行脚本骨架

建议脚本文件：`scripts/test_optimistic_lock_api.py`。

> 下面是可运行脚本结构要求。实现时不要修改业务逻辑；如果接口尚未完成乐观锁，应输出 FAIL / SKIP，而不是修业务代码。

```python
#!/usr/bin/env python3
import argparse
import concurrent.futures
import json
import os
import sys
import time
from dataclasses import dataclass
from typing import Any, Callable, Optional

import requests


@dataclass
class CaseResult:
    case_id: str
    name: str
    status: str
    reason: str
    endpoint: str
    details: Optional[dict[str, Any]] = None


class ApiClient:
    def __init__(self, base_url: str, token: str, timeout: int = 20):
        self.base_url = base_url.rstrip("/")
        self.timeout = timeout
        self.session = requests.Session()
        self.session.headers.update({
            "Authorization": f"Bearer {token}",
            "Content-Type": "application/json",
            "Accept": "application/json",
        })

    def request(self, method: str, path: str, body: Any = None, expected: Optional[int] = None):
        url = f"{self.base_url}{path}"
        resp = self.session.request(method, url, json=body, timeout=self.timeout)
        try:
            payload = resp.json() if resp.text else None
        except Exception:
            payload = resp.text
        if expected is not None and resp.status_code != expected:
            raise AssertionError({
                "method": method,
                "path": path,
                "body": body,
                "expected": expected,
                "status": resp.status_code,
                "response": payload,
            })
        return resp.status_code, payload


def login(base_url: str, employee_id: str, password: str, timeout: int) -> str:
    resp = requests.post(
        f"{base_url.rstrip('/')}/auth/login",
        json={"employee_id": employee_id, "password": password},
        timeout=timeout,
    )
    try:
        body = resp.json()
    except Exception:
        body = {}
    if resp.status_code >= 400:
        raise RuntimeError(f"login failed: {resp.status_code} {body}")
    token = body.get("token") or body.get("access_token") or body.get("data", {}).get("token")
    if not token:
        raise RuntimeError(f"login response has no token: {body}")
    return token


def is_edit_conflict(status: int, body: Any) -> bool:
    return status == 409 and isinstance(body, dict) and body.get("code") == "EDIT_CONFLICT"


def assert_version(entity: dict[str, Any], expected: Optional[int] = None):
    if "version" not in entity:
        raise AssertionError(f"missing version in response: {entity}")
    if not isinstance(entity["version"], int):
        raise AssertionError(f"version is not int: {entity.get('version')}")
    if expected is not None and entity["version"] != expected:
        raise AssertionError(f"version expected {expected}, got {entity['version']}")


def main():
    parser = argparse.ArgumentParser()
    parser.add_argument("--cleanup", action="store_true")
    parser.add_argument("--include-regenerate-ac", action="store_true")
    args = parser.parse_args()

    base_url = os.getenv("API_BASE_URL", "http://localhost:8080/api/v1")
    timeout = int(os.getenv("TEST_TIMEOUT_SECONDS", "20"))
    prefix = os.getenv("TEST_PREFIX", f"OL_TEST_{int(time.time())}")
    token = os.getenv("TEST_TOKEN")
    if not token:
        employee_id = os.getenv("TEST_EMPLOYEE_ID", "liu_tl")
        password = os.getenv("TEST_PASSWORD", "123")
        token = login(base_url, employee_id, password, timeout)

    api = ApiClient(base_url, token, timeout)
    results: list[CaseResult] = []

    # 实现时按本文第 5 节 case 清单注册并执行。
    # 每个 case 内部自行创建唯一测试数据，默认不清理。

    print_report(results)


def print_report(results: list[CaseResult]):
    total = len(results)
    passed = sum(1 for r in results if r.status == "PASS")
    failed = sum(1 for r in results if r.status == "FAIL")
    skipped = sum(1 for r in results if r.status == "SKIP")
    print("| case_id | name | status | reason | endpoint |")
    print("| --- | --- | --- | --- | --- |")
    for r in results:
        print(f"| {r.case_id} | {r.name} | {r.status} | {r.reason} | {r.endpoint} |")
    print()
    print(f"Total: {total}, PASS: {passed}, FAIL: {failed}, SKIP: {skipped}")
    if failed:
        print("Failed cases:")
        for r in results:
            if r.status == "FAIL":
                print(json.dumps({
                    "case_id": r.case_id,
                    "name": r.name,
                    "reason": r.reason,
                    "details": r.details,
                }, ensure_ascii=False, indent=2))
        sys.exit(1)


if __name__ == "__main__":
    main()
```

---

## 5. 测试用例清单

执行顺序建议：

1. 每个 case 独立 setup，独立创建自己的 Requirement / Task。
2. 基础能力 A 组先跑；若 A1/A2/A3/A4 中 version 基础返回失败，后续依赖 version 的用例仍可继续执行，但失败原因应明确指向缺少 version / base_version 能力。
3. B/D/G/I/J/K 是 P0 核心回归，不能因为实现尚未完成而 SKIP。
4. C/E3/G8 是条件性用例，只有环境或公开 API 无法构造时才 SKIP。
5. 清理只在全部用例结束后执行；测试过程中不要删除其他用例还可能读取的资源。

如果开启 `--cleanup`，推荐按依赖反向清理：先删除任务依赖，再删除任务，再尝试删除无关联需求。遇到业务 409 或 404 只记录 cleanup 结果，不影响测试结论。

## A. 基础 version 能力

### A1. 创建需求返回 version

**Endpoint**：`POST /requirements`

步骤：

1. 创建测试需求，title 使用 `OL_TEST_<timestamp>_A1_requirement`。
2. 校验返回体。

期望：

- 返回 `201` 或当前创建接口定义的成功码。
- response body 包含 `version`。
- 初始 `version = 1`，或符合当前实现定义。
- `created_at` / `updated_at` 正常返回。

失败判定：

- 没有 `version`：FAIL。
- version 不是数字：FAIL。

### A2. GET / LIST 需求返回 version

**Endpoint**：`GET /requirements/{id}`、`GET /requirements`

步骤：

1. 创建需求。
2. 通过详情接口读取。
3. 通过列表接口读取并定位该需求。

期望：

- 详情返回的 Requirement 包含 `version`。
- 列表中的同一 Requirement 包含 `version`。
- 同一需求 version 一致。

### A3. 创建任务后可读取 version

**Endpoint**：`POST /tasks`

步骤：

1. 创建测试需求。
2. 在该需求下创建任务。
3. 如果创建响应只包含 `{id,status}`，立即调用 `GET /tasks/{id}`。
4. 校验新任务详情。

期望：

- 创建接口成功返回任务 id。
- 通过详情接口读取的新任务包含 `version`。
- 新任务 `version = 1`，或符合当前实现定义。

注意：`POST /tasks` 不带 `base_version`，也不强制要求创建响应直接返回完整 Task；但后续客户端必须能通过详情 / 列表 / 需求投影拿到新任务 `version`。

### A4. GET / LIST 任务返回 version

**Endpoint**：`GET /tasks/{id}`、`GET /tasks?requirement_id=...`、`GET /requirements/{id}`

步骤：

1. 创建任务。
2. 通过任务详情读取。
3. 通过任务列表读取。
4. 通过需求详情投影读取任务。

期望：

- 所有返回里的 Task 都包含 `version`。
- 同一任务 version 一致。

---

## B. 需求本体乐观锁

### B1. A/B 同时改同一需求字段

**Endpoint**：`PUT /requirements/{id}`

步骤：

1. A/B 都读取同一需求，得到 `version = v1`。
2. A 用 `base_version = v1` 修改 title。
3. A 成功，返回 `version = v2`。
4. B 仍用 `base_version = v1` 修改 title。
5. GET 最终需求。

期望：

- A 返回 `200`。
- B 返回 `409`。
- B body `code = EDIT_CONFLICT`。
- B body `current_version = v2`。
- 最终 title 是 A 的值。

### B2. A 改优先级，B 改标题但提交旧整组字段

**Endpoint**：`PUT /requirements/{id}`

步骤：

1. A/B 都读取同一需求，得到 `version = v1`。
2. B 保存旧快照：`priority`、`description`、`acceptance_criteria` 等。
3. A 修改 `priority`，成功到 `version = v2`。
4. B 用 `base_version = v1` 修改 `title`，请求体同时带旧 `priority`。
5. GET 最终需求。

期望：

- B 返回 `409 EDIT_CONFLICT`。
- 最终 `priority` 保持 A 的修改。
- B 的旧 `priority` 不得覆盖 A。

### B3. A 改 acceptance_criteria，B 改 description 但带旧 AC

**Endpoint**：`PUT /requirements/{id}`

步骤：

1. A/B 都读取需求 `version = v1`。
2. A 修改 `acceptance_criteria` 成功到 `v2`。
3. B 用旧 `base_version = v1` 修改 `description`，并带旧 `acceptance_criteria`。
4. GET 最终需求。

期望：

- B 返回 `409 EDIT_CONFLICT`。
- 最终 `acceptance_criteria` 保持 A 的修改。

### B4. 需求状态流转旧版本冲突

**Endpoint**：`PUT /requirements/{id}`

步骤：

1. A/B 都读取需求 `version = v1`。
2. A 修改 `status` 成功到 `v2`。
3. B 用 `base_version = v1` 修改 `status`。
4. GET 最终需求。

期望：

- B 返回 `409 EDIT_CONFLICT`。
- 最终 status 是 A 的值。

### B5. cancelRequirement 旧版本冲突

**Endpoint**：`PUT /requirements/{id}`

步骤：

1. A/B 都读取需求 `version = v1`。
2. A 修改需求任意字段成功到 `v2`。
3. B 用 `base_version = v1` 提交 `{ "status": "cancelled" }`。
4. GET 最终需求。

期望：

- B 返回 `409 EDIT_CONFLICT`。
- 需求状态不被 B 取消。

### B6. restoreRequirement 旧版本冲突

**Endpoint**：`PUT /requirements/{id}/restore`

前置：创建一个 `cancelled` 状态需求。

步骤：

1. A/B 都读取该需求 `version = v1`。
2. A 修改该需求，例如更新 `priority`，成功到 `v2`，但保持需求仍为 `cancelled`。
3. B 用 `base_version = v1` 执行 restore。

期望：

- B 返回 `409 EDIT_CONFLICT`。
- `current_version = v2`。
- 需求仍保持 A 修改后的状态。

补充业务 409 子场景：

1. 创建一个非 `cancelled` 需求，读取当前 `version`。
2. 用正确 `base_version` 执行 restore。
3. 期望返回业务 409，例如 `not_cancelled`，且不得被 `is_edit_conflict` 判定为并发冲突。

### B7. 删除需求旧版本冲突

**Endpoint**：`DELETE /requirements/{id}?base_version=...`

步骤：

1. 创建一个可删除测试需求，`version = v1`。
2. A/B 都读取 `version = v1`。
3. A 修改 title 成功到 `v2`。
4. B 用 `base_version = v1` 删除需求。
5. GET 需求。

期望：

- B 返回 `409 EDIT_CONFLICT`。
- 需求仍存在。

### B8. 删除需求业务 409 不应误判为 EDIT_CONFLICT

**Endpoint**：`DELETE /requirements/{id}?base_version=...`

步骤：

1. 创建需求。
2. 创建关联任务，使需求不可删除。
3. 用正确 `base_version` 删除需求。

期望：

- 返回业务 `409`，例如 `has_associations`。
- `code` 不能是 `EDIT_CONFLICT`。
- `is_edit_conflict` 判断结果必须为 false。

### B9. 需求空更新返回 400

**Endpoint**：`PUT /requirements/{id}`

步骤：

1. 创建需求，读取 `version = v1`。
2. PUT body 只有 `{ "base_version": v1 }`。
3. 再次 GET。

期望：

- 返回 `400`。
- body `code = no_fields_to_update`。
- 再次 GET，version 仍为 `v1`。

### B10. base_version 缺失 / 0 / 负数 / 非数字

**Endpoint**：`PUT /requirements/{id}`、`DELETE /requirements/{id}?base_version=abc`

分别测试：

1. 缺失 `base_version`。
2. `base_version = 0`。
3. `base_version = -1`。
4. DELETE query `base_version=abc`。

期望：

- 返回 `400`。
- 不修改数据。
- version 不递增。

---

## C. regenerate-ac

### C1. regenerate-ac 正常成功递增需求 version

**Endpoint**：`POST /requirements/{id}/regenerate-ac`

步骤：

1. 创建需求 `version = v1`。
2. 调用 regenerate-ac，`base_version = v1`。

期望：

- 成功返回完整 Requirement。
- `version = v1 + 1`。
- `acceptance_criteria` 被更新。

SKIP 条件：

- 本地环境没有 AI / Claude CLI / mock 能力。
- 接口返回 AI 服务不可用。

### C2. regenerate-ac 生成前版本已过期

**Endpoint**：`POST /requirements/{id}/regenerate-ac`

步骤：

1. A 读取需求 `version = v1`。
2. B 修改需求成功到 `v2`。
3. A 调用 regenerate-ac，仍传 `base_version = v1`。

期望：

- 返回 `409 EDIT_CONFLICT`。
- `current_version = v2`。
- 不触发 AI 调用。如果无法直接断言是否调用 AI，报告中说明无法断言。
- 需求不被修改。

### C3. regenerate-ac 生成期间版本变化

**Endpoint**：`POST /requirements/{id}/regenerate-ac`

步骤：

1. 通过 mock/stub 或人工延迟让 regenerate-ac 在生成和写入之间可插入操作。
2. A 调用 regenerate-ac，`base_version = v1`。
3. B 在 A 生成期间修改需求成功到 `v2`。
4. A 写入阶段应失败。

期望：

- A 返回 `409 EDIT_CONFLICT`。
- 最终需求保留 B 的修改。
- 不保存、不合并、不返回生成草稿。

SKIP 条件：

- 当前代码无法制造生成期间并发。
- 缺少 mock AI 延迟或测试 hook。

---

## D. 任务本体乐观锁

### D1. A/B 同时改同一任务字段

**Endpoint**：`PUT /tasks/{id}`

步骤：

1. 创建任务，A/B 都读取 `version = v1`。
2. A 修改 title 成功到 `v2`。
3. B 用 `base_version = v1` 修改 title。
4. GET 最终任务。

期望：

- B 返回 `409 EDIT_CONFLICT`。
- 最终 title 是 A 的值。

### D2. A 改负责人，B 改优先级但提交旧整组字段

**Endpoint**：`PUT /tasks/{id}`

步骤：

1. A/B 都读取任务 `version = v1`。
2. 记录父需求 `requirements.version = rv1`。
3. A 修改 `assignee_id` 成功到 `v2`。
4. B 用 `base_version = v1` 修改 `priority`，并带旧 `assignee_id`。
5. GET 任务和父需求。

期望：

- B 返回 `409 EDIT_CONFLICT`。
- 最终 `assignee_id` 保持 A 的修改。
- 父需求 `requirements.version` 仍为 `rv1`。

### D3. A 改任务 AC，B 改负责人但带旧 AC

**Endpoint**：`PUT /tasks/{id}`

步骤：

1. A/B 都读取任务 `version = v1`。
2. A 修改 `acceptance_criteria` 成功到 `v2`。
3. B 用旧 `base_version = v1` 修改 `assignee_id`，并带旧 `acceptance_criteria`。

期望：

- B 返回 `409 EDIT_CONFLICT`。
- 最终 `acceptance_criteria` 保持 A 的修改。

### D4. 任务状态旧版本冲突

**Endpoint**：`PUT /tasks/{id}/status`

步骤：

1. A/B 都读取任务 `version = v1`。
2. A 修改 status 成功到 `v2`。
3. B 用 `base_version = v1` 修改 status。

期望：

- B 返回 `409 EDIT_CONFLICT`。
- 最终 status 是 A 的值。

### D5. 任务进度旧版本冲突

**Endpoint**：`PUT /tasks/{id}/progress`

步骤：

1. A/B 都读取任务 `version = v1`。
2. A 修改 progress 成功到 `v2`。
3. B 用 `base_version = v1` 修改 progress。

期望：

- B 返回 `409 EDIT_CONFLICT`。
- 最终 progress 是 A 的值。

### D6. 状态改 done 后 version 和 progress 逻辑

**Endpoint**：`PUT /tasks/{id}/status`

步骤：

1. 创建任务 `version = v1`。
2. 记录父需求 `requirements.version = rv1`。
3. 用 `base_version = v1` 把 status 改为 `done`。
4. GET 任务和父需求。

期望：

- 成功。
- task `version = v1 + 1`。
- `progress = 100`。
- `completed_at` 被设置。
- 父需求 progress / updated_at 可以刷新，但 `requirements.version` 不递增。

### D6b. Dashboard 推荐一次 updateTask 写入 status + progress

**Endpoint**：`PUT /tasks/{id}`

背景：Dashboard 应用任务推荐时会先读取当前任务，再通过一次 `updateTask` 同时提交 `status`、`progress` 和 `base_version`。该路径必须复用任务状态 `done` 的规则，且只能递增一次 version。

步骤：

1. 创建任务 `version = v1`，初始 `status = in_progress` 或 `todo`，`progress < 100`。
2. 记录父需求 `requirements.version = rv1`。
3. 用 `base_version = v1` 调用 `PUT /tasks/{id}`，body：
   ```json
   {
     "status": "done",
     "progress": 30,
     "base_version": "<v1>"
   }
   ```
4. GET 任务和父需求。

期望：

- 请求成功。
- task `version = v1 + 1`，不能因为 status 和 progress 两个字段递增两次。
- task `status = done`。
- task `progress = 100`，请求里的 `progress = 30` 不得覆盖 done 规则。
- `completed_at` 被设置。
- 父需求 progress / updated_at 可以刷新，但 `requirements.version` 不递增。

补充非 done 子场景：

1. 创建任务 `version = v1`。
2. 用 `base_version = v1` 调用 `PUT /tasks/{id}`，同时提交 `status = in_progress`、`progress = 60`。
3. 期望 task `version = v1 + 1`、`status = in_progress`、`progress = 60`。

### D7. 任务空更新返回 400

**Endpoint**：`PUT /tasks/{id}`

步骤：

1. 创建任务，读取 `version = v1`。
2. PUT body 只有 `{ "base_version": v1 }`。
3. GET 任务。

期望：

- 返回 `400`。
- `code = no_fields_to_update`。
- `tasks.version` 不递增。

### D8. 任务 base_version 缺失 / 0 / 负数

**Endpoint**：`PUT /tasks/{id}`、`PUT /tasks/{id}/status`、`PUT /tasks/{id}/progress`

分别测试 update、status、progress：

- 缺失 `base_version`
- `base_version = 0`
- `base_version = -1`

期望：

- 返回 `400`。
- 不修改任务。
- version 不递增。

---

## E. 删除任务

### E1. A 删除任务，B 后修改任务

**Endpoint**：`DELETE /tasks/{id}?base_version=...`、`PUT /tasks/{id}`

步骤：

1. A/B 都读取任务 `version = v1`。
2. 记录父需求 `requirements.version = rv1`。
3. A 用 `base_version = v1` 删除任务，成功。
4. B 用 `base_version = v1` 修改任务。
5. GET 任务和父需求。

期望：

- B 返回 `404`。
- 任务已不存在。
- 父需求 `requirements.version` 不因删除任务递增。

### E2. B 先修改任务，A 用旧版本删除任务

**Endpoint**：`PUT /tasks/{id}`、`DELETE /tasks/{id}?base_version=...`

步骤：

1. A/B 都读取任务 `version = v1`。
2. B 修改任务成功到 `v2`。
3. A 用 `base_version = v1` 删除任务。
4. GET 任务。

期望：

- A 返回 `409 EDIT_CONFLICT`。
- `current_version = v2`。
- 任务仍存在。
- B 的修改保留。

### E3. 删除任务旧版本不得产生副作用

**Endpoint**：`DELETE /tasks/{id}?base_version=...`

步骤：

1. 创建任务，并尽可能建立 `user_follows` / `sessions` / `token_usage` / `documents` 等关联。
2. A/B 都读取 `version = v1`。
3. A 修改任务成功到 `v2`。
4. B 用 `base_version = v1` 删除任务。
5. 查询可验证的关联。

期望：

- B 返回 `409 EDIT_CONFLICT`。
- 不解绑 sessions / token_usage / documents / user_follows。

SKIP 条件：

- 某些关联无法通过公开 API 构造；记录部分 SKIP。

---

## F. 任务新增

### F1. A/B 同时新增任务

**Endpoint**：`POST /tasks`

步骤：

1. A/B 在同一需求下同时 `POST /tasks`。
2. `POST /tasks` 不带 `base_version`。
3. LIST 任务。
4. GET 父需求。

期望：

- 两个创建都成功。
- 两个任务都存在。
- 新任务 `version = 1`。
- 父需求 `requirements.version` 不递增。
- P0 不阻止重复任务名。

### F2. 新增任务后父需求投影刷新但不 bump version

**Endpoint**：`POST /tasks`、`GET /requirements/{id}`

步骤：

1. 读取父需求 `version = rv1`。
2. 创建任务。
3. 再读取父需求。

期望：

- `requirements.version` 仍为 `rv1`。
- 如果现有实现会更新 `requirements.updated_at` / `progress`，校验投影正确。
- 如果不更新，记录当前行为。
- 不要求触发需求乐观锁冲突。

---

## G. 任务依赖

### G1. 新增依赖成功 bump owner task version

**Endpoint**：`POST /tasks/{id}/dependencies`

步骤：

1. 创建任务 T1、T2。
2. 读取 T1 `version = v1`，读取 T2 `version = t2v1`，读取父需求 `version = rv1`。
3. 给 T1 新增依赖 T2，`base_version = v1`。
4. GET T1、T2、父需求。

期望：

- 成功。
- T1 `version = v1 + 1`。
- 依赖存在。
- 父需求 `requirements.version` 不递增。
- T2 version 不变化。

### G2. 新增已存在依赖不 bump version

**Endpoint**：`POST /tasks/{id}/dependencies`

步骤：

1. T1 已经依赖 T2。
2. 读取 T1 `version = v1`。
3. 再次新增同一依赖，`base_version = v1`。
4. GET T1。

期望：

- 成功返回最新 Task。
- T1 version 仍为 `v1`。
- 不返回 409。
- 不重复依赖。

### G3. 删除依赖成功 bump owner task version

**Endpoint**：`DELETE /tasks/{id}/dependencies/{dep_id}?base_version=...`

步骤：

1. T1 依赖 T2。
2. 读取 T1 `version = v1`、T2 `version = t2v1`、父需求 `version = rv1`。
3. 删除该依赖，`base_version = v1`。
4. GET T1、T2、父需求。

期望：

- 成功。
- T1 `version = v1 + 1`。
- 依赖不存在。
- 父需求 `requirements.version` 不递增。
- T2 version 不变化。

### G4. 删除不存在依赖不 bump version

**Endpoint**：`DELETE /tasks/{id}/dependencies/{dep_id}?base_version=...`

步骤：

1. T1 不依赖 T2。
2. 读取 T1 `version = v1`。
3. 删除该不存在依赖，`base_version = v1`。
4. GET T1。

期望：

- 如果定义为幂等成功：返回最新 Task，T1 version 仍为 `v1`。
- 如果当前实现定义为 404：记录当前行为，但不能 bump version。

### G5. A 新增依赖，B 用旧版本删除同一依赖

**Endpoint**：`POST /tasks/{id}/dependencies`、`DELETE /tasks/{id}/dependencies/{dep_id}?base_version=...`

前置：依赖原本不存在。

步骤：

1. A/B 都读取 T1 `version = v1`。
2. A 新增 T1 -> T2，成功到 `v2`。
3. B 用 `base_version = v1` 删除 T1 -> T2。
4. GET T1。

期望：

- B 返回 `409 EDIT_CONFLICT`。
- `current_version = v2`。
- 依赖保持存在。

说明：即使删除接口被设计为“删除不存在依赖可幂等成功”，这里也不能幂等成功，因为 owner task 当前 version 已从 `v1` 变为 `v2`，必须先判定版本冲突。

### G6. A 删除依赖，B 用旧版本新增同一依赖

**Endpoint**：`DELETE /tasks/{id}/dependencies/{dep_id}?base_version=...`、`POST /tasks/{id}/dependencies`

前置：依赖原本存在。

步骤：

1. A/B 都读取 T1 `version = v1`。
2. A 删除 T1 -> T2，成功到 `v2`。
3. B 用 `base_version = v1` 新增 T1 -> T2。
4. GET T1。

期望：

- B 返回 `409 EDIT_CONFLICT`。
- `current_version = v2`。
- 依赖保持删除状态。

说明：即使新增接口被设计为“已存在依赖可幂等成功”，这里也不能幂等成功，因为 owner task 当前 version 已从 `v1` 变为 `v2`，必须先判定版本冲突。

### G7. 依赖 base_version 缺失 / 非法

**Endpoint**：`POST /tasks/{id}/dependencies`、`DELETE /tasks/{id}/dependencies/{dep_id}?base_version=...`

分别测试：

1. POST dependencies 缺失 `base_version`。
2. POST dependencies `base_version = 0`。
3. DELETE dependencies 缺失 `base_version`。
4. DELETE dependencies `base_version=abc`。

期望：

- 返回 `400`。
- 不修改依赖。
- owner task version 不递增。

### G8. 依赖合法性业务错误不能误判为 EDIT_CONFLICT

**Endpoint**：`POST /tasks/{id}/dependencies`

前置：每个子场景都必须先读取 owner task 最新 `version`，并使用正确 `base_version` 发起请求。若刻意传旧 `base_version`，接口应优先返回 `409 EDIT_CONFLICT`，不能用来验证依赖合法性业务错误。

测试场景尽量覆盖：

1. 自依赖。
2. 依赖不存在任务。
3. 依赖跨非法范围。
4. 循环依赖，如果当前有校验。

期望：

- 返回对应业务错误。
- `code` 不能是 `EDIT_CONFLICT`，除非确实是版本冲突。
- version 不递增。

---

## H. 404 与 409 区分

### H1. 更新不存在需求返回 404，不返回 EDIT_CONFLICT

**Endpoint**：`PUT /requirements/{id}`

步骤：

1. 使用不存在的 requirement id。
2. 带合法 `base_version` 更新需求。

期望：

- 返回 `404`。
- 不能返回 `409 EDIT_CONFLICT`。

### H2. 删除不存在需求返回 404

**Endpoint**：`DELETE /requirements/{id}?base_version=1`

期望：

- 返回 `404`。
- 不能返回 `409 EDIT_CONFLICT`。

### H3. 更新不存在任务返回 404

**Endpoint**：`PUT /tasks/{id}`

期望：

- 返回 `404`。
- 不能返回 `409 EDIT_CONFLICT`。

### H4. 删除不存在任务返回 404

**Endpoint**：`DELETE /tasks/{id}?base_version=1`

期望：

- 返回 `404`。
- 不能返回 `409 EDIT_CONFLICT`。

### H5. 删除不存在依赖 owner task 返回 404

**Endpoint**：`DELETE /tasks/{id}/dependencies/{dep_id}?base_version=1`

步骤：

1. owner task id 不存在。
2. DELETE dependency。

期望：

- 返回 `404`。
- 不能返回 `EDIT_CONFLICT`。

---

## I. 成功响应与连续操作

### I1. 需求连续两次操作不应自己触发 409

**Endpoint**：`PUT /requirements/{id}`

步骤：

1. 读取需求 `version = v1`。
2. 修改 title 成功，返回 `version = v2`。
3. 立即使用返回实体里的 `version = v2` 修改 priority。
4. GET 需求。

期望：

- 第二次成功。
- 最终 `version = v3`。
- 说明成功响应里的最新 version 可用于下一次操作。

### I2. 任务连续状态/进度操作不应自己触发 409

**Endpoint**：`PUT /tasks/{id}/progress`、`PUT /tasks/{id}/status`

步骤：

1. 读取任务 `version = v1`。
2. 修改 progress 成功到 `v2`。
3. 使用返回 `version = v2` 修改 status。

期望：

- 第二次成功。
- 最终 `version = v3`。

### I3. 依赖连续操作不应自己触发 409

**Endpoint**：`POST /tasks/{id}/dependencies`、`DELETE /tasks/{id}/dependencies/{dep_id}?base_version=...`

步骤：

1. 读取任务 `version = v1`。
2. 新增依赖成功到 `v2`。
3. 使用返回 `version = v2` 删除依赖。

期望：

- 删除成功到 `v3`。

---

## J. 父需求 version 不误 bump

### J1. 任务编辑不 bump 父需求 version

**Endpoint**：`PUT /tasks/{id}`、`PUT /tasks/{id}/status`、`PUT /tasks/{id}/progress`

步骤：

1. 读取父需求 `version = rv1`。
2. 修改子任务 title / status / progress / assignee。
3. 再读取父需求。

期望：

- `requirements.version` 仍为 `rv1`。
- `tasks.version` 正常递增。

### J2. 任务删除不 bump 父需求 version

**Endpoint**：`DELETE /tasks/{id}?base_version=...`

步骤：

1. 读取父需求 `version = rv1`。
2. 删除子任务。
3. 再读取父需求。

期望：

- `requirements.version` 仍为 `rv1`。

### J3. 任务依赖变化不 bump 父需求 version

**Endpoint**：`POST /tasks/{id}/dependencies`、`DELETE /tasks/{id}/dependencies/{dep_id}?base_version=...`

步骤：

1. 读取父需求 `version = rv1`。
2. 新增 / 删除任务依赖。
3. 再读取父需求。

期望：

- `requirements.version` 仍为 `rv1`。

### J4. 任务变化不影响正在保存需求本体

**Endpoint**：`PUT /tasks/{id}`、`PUT /requirements/{id}`

步骤：

1. A 读取需求 `version = rv1`。
2. B 修改该需求下某个任务，task version 递增。
3. A 用 `base_version = rv1` 修改需求 title。

期望：

- A 成功。
- 需求 version 从 `rv1` 到 `rv2`。
- 说明任务变化不会误伤需求本体编辑。

---

## K. 真实并发压测型用例

### K1. 多个客户端同时更新同一需求

**Endpoint**：`PUT /requirements/{id}`

步骤：

1. 读取需求 `version = v1`。
2. 10 个并发请求都使用同一个 `base_version = v1` 修改同一需求 title。
3. GET 最终需求。

期望：

- 只有 1 个成功。
- 其余 9 个返回 `409 EDIT_CONFLICT`。
- 最终 version 只递增 1。
- 最终 title 等于成功请求的值。

### K2. 多个客户端同时更新同一任务状态

**Endpoint**：`PUT /tasks/{id}/status`

步骤：

1. 读取任务 `version = v1`。
2. 10 个并发请求都使用同一个 `base_version = v1` 修改同一任务 status。
3. GET 最终任务。

期望：

- 只有 1 个成功。
- 其余返回 `409 EDIT_CONFLICT`。
- version 只递增 1。

### K3. 多个客户端同时新增同一依赖

**Endpoint**：`POST /tasks/{id}/dependencies`

步骤：

1. T1 不依赖 T2。
2. 读取 T1 `version = v1`。
3. 10 个并发请求都使用同一个旧快照 `base_version = v1` 给 T1 新增依赖 T2；请求之间不得在提交前重新 GET 最新 version。
4. GET T1。

期望：

- 只有 1 个请求实际新增依赖并 bump version。
- 其余 9 个请求必须返回 `409 EDIT_CONFLICT`。
- 最终必须满足：
  - 依赖只存在一条。
  - owner task version 只递增 1 次。
  - 不出现 500。

说明：已存在依赖的幂等成功只适用于调用方使用当前最新 `base_version` 的场景。多个客户端持同一个旧快照并发提交时，版本检查必须优先于幂等判断。

---

## L. 报告格式

### L1. 脚本输出摘要

脚本结束输出：

```text
Total: <n>, PASS: <n>, FAIL: <n>, SKIP: <n>
Failed cases: <case_id list>
Skipped cases: <case_id list>
```

### L2. 每个用例结果表

| case_id | name | status | reason | related endpoint |
| --- | --- | --- | --- | --- |
| A1 | 创建需求返回 version | PASS | version=1 | POST /requirements |

### L3. 失败详情

失败用例输出 JSON：

```json
{
  "case_id": "B1",
  "name": "A/B 同时改同一需求字段",
  "request": {
    "method": "PUT",
    "path": "/requirements/<id>",
    "body": {
      "title": "...",
      "base_version": 1
    }
  },
  "status_code": 200,
  "response_body": {},
  "final_get_state": {},
  "reason": "expected 409 EDIT_CONFLICT, got 200"
}
```

敏感信息要求：

- 不输出 Authorization token。
- 不输出登录密码。

### L4. 结论字段

最终报告必须回答：

- 是否存在静默覆盖。
- 是否存在误 bump 父需求 version。
- 是否存在业务 409 被误判为 `EDIT_CONFLICT`。
- 是否存在成功响应未返回最新 version 导致连续操作失败。
- 哪些场景由于环境或 mock 缺失被 SKIP。

---

## M. 实现约束

1. 不修改生产业务逻辑。
2. 不改数据库 schema；如果当前乐观锁实现缺失，只报告 FAIL / SKIP，不顺手修。
3. 不删除人工已有数据。
4. 测试数据必须使用唯一前缀。
5. 默认保留测试数据。
6. `--cleanup` 只清理本脚本本次创建的数据，且遇到业务限制不强删。
7. 不硬编码真实密码。
8. 不把所有 `409` 都当成并发冲突。
9. 不使用浏览器，不做前端 E2E。
10. 不因测试失败自动重试覆盖写入。

---

## N. 实施建议

第一阶段建议先实现并跑通以下最小集合：

- A1-A4：确认 version 基础返回能力。
- B1、B2、B8、B9、B10：确认需求乐观锁、业务 409、空更新。
- D1、D2、D4、D5、D7、D8：确认任务乐观锁。
- E1、E2：确认删除任务 404 / 409。
- G1-G7：确认依赖事务与 version。
- I1-I3：确认成功响应 version 可连续使用。
- J1-J4：确认父需求 version 不误 bump。
- K1-K3：确认真实并发下不会静默覆盖。

第二阶段再补充：

- C1-C3 regenerate-ac，需要 AI/mock 能力。
- E3 删除任务副作用，需要可构造 sessions / token_usage / documents / follows。
- G8 依赖业务错误覆盖，需要结合当前后端校验能力。
