# 日报邮件通知测试与验收方案：每日个人日报与 TL 小组汇总

> 文档状态：测试中（自动化通过，待真实链路验收）
> 日期：2026-08-06
> 需求基线：[02-日报邮件通知需求文档-20260806.md](02-日报邮件通知需求文档-20260806.md)
> 架构基线：[03-日报邮件通知架构设计-20260806.md](03-日报邮件通知架构设计-20260806.md)

## 1. 测试原则

聚焦模块公开入口、时间边界、收件人隔离、内容正确性、幂等和单封失败隔离；不连接共享数据库或真实群发邮箱。

## 2. 需求追溯矩阵

| 用例 | 需求引用 | 场景 | 输入 | 可观察结果 | 层级 |
|---|---|---|---|---|---|
| T-01 | 3.1/R-01 | 08:00 个人派发 | 两名有可信邮箱和日报的用户 | 各收到自己的前一日日报 | 单元/集成 |
| T-02 | 3.2/R-02 | TL 小组汇总 | 一个 TL、多个组员 | TL 收到含全部组员的单一表格 | 单元/集成 |
| T-03 | 3.2/R-03 | 组员缺日报 | 一名成员无日报 | 汇总保留该成员并标记缺失 | 单元 |
| T-04 | 3.3/R-04 | 单封发送失败 | 发送器对一个地址报错 | 其他邮件仍发送，失败状态被记录 | 单元/集成 |
| T-05 | 3.3/R-05 | 重复调度 | 同一日期执行两次 | 已成功业务邮件只发送一次 | 集成 |
| T-06 | 3.3/R-06 | 未确认邮箱 | 用户仅有 `users.email` | 不生成邮件 | 集成 |
| T-07 | 3.3/R-06 | 通讯录匹配异常 | 重名、不存在、邮箱冲突 | dry-run 标记且 apply 跳过 | 单元/命令 |
| T-08 | 3.1 | 北京时间边界 | 07:59、08:00、次日 | 只派发正确报告日期 | 单元 |

## 3. 架构风险验证

| 用例 | 架构决策 | 风险 | 验证方法 | 通过条件 |
|---|---|---|---|---|
| A-T01 | A-01 | 时区/重启导致漏发或重复 | 可注入时钟测试 | 当日到点后可补跑且仅成功一次 |
| A-T02 | A-03 | 多实例重复发送 | 唯一键冲突测试 | 同一业务键只允许一个成功投递 |
| A-T03 | A-04 | 邮件头注入或正文破坏 HTML | 恶意姓名/主题输入 | 头部拒绝换行、正文 HTML 转义 |
| A-T04 | A-05 | 通讯录误绑定 | 匹配表驱动测试 | 仅唯一精确姓名匹配可应用 |

## 4. 回归范围

配置未启用时 API 启动行为保持不变；日报读写、自动生成和现有 Agent 调度不变。

## 5. 真实链路验收

发布前在隔离环境使用测试 SMTP 收件箱和测试数据库：准备两名成员、一个 TL、一个缺失日报成员，触发指定日期派发并核对三类邮件与投递记录。本开发阶段不执行真实群发。

## 6. 发布门禁

- 所有产品验收项通过；
- 所有高风险架构用例通过；
- 无未解释的回归；
- 生产 SMTP 配置与通讯录导入另行获得授权。

## 7. 当前测试结果

- `go test ./internal/reportemail ./config ./cmd/report-email-recipient-import`：通过。
- `go test ./...`：通过，共执行两次最终回归。
- `git diff --check`：通过。
- 未执行：迁移真实数据库验证、真实 SMTP 投递、企业通讯录 dry-run/apply；原因是独立 worktree 不复用共享运行环境，且数据库写入和真实群发需另行授权。
