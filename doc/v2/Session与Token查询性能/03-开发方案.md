# 开发方案

## 1. 实施顺序

```text
统一 Content Reader
  -> 迁移内容消费者
  -> 停止冗余 Payload 写入
  -> 历史数据紧凑
  -> 最终回归与旧路径收尾
```

每个阶段验收通过后才进入下一阶段。状态只使用：未开始、进行中、已验收、阻断。

## 2. 已完成基线

| 交付物 | 状态 | 收尾要求 |
| --- | --- | --- |
| Report Source Catalog 与候选分页 | 已实现 | 执行最终性能回归 |
| Token Contribution、Session Family、Daily/Chunk Rollup | 已上线 | 执行最终对账回归 |
| Token API/Web/MCP Rollup/Snapshot 查询 | 已上线 | 执行最终契约回归 |

## 3. 统一 Content Reader

**状态：已验收。**

| 任务 | 代码 | 交付物 |
| --- | --- | --- |
| Reader 接口和错误模型 | `api/internal/contentreader/` | 输入/输出类型、可识别错误 |
| legacy 对象读取 | `sessions.raw_log_url` | 按 cursor 读单 JSONL 对象 |
| V2 Chunk 读取 | `session_upload_chunks.raw_object_key` | 复用 `sessionsync.ParseContentChunk` 完成单/多 Chunk 解析与跨边界读取 |
| 完整性与流控 | Reader 内部 | cursor/JSONL/SHA256 校验、取消、超时、背压 |
| 单元测试 | `api/internal/contentreader/*_test.go` | 覆盖正常、跨 Chunk、异常与大对象 |

完成定义：Reader 单元测试全部通过，内存符合流式读取要求；不改生产消费者，不停止 Payload 写入。

验收结果：`eafb603`、`6349ae2`；本地和 192.168.14.157 的 `go test ./...`、`go vet ./...` 通过，Reader 定向 race 测试通过。

## 4. 内容消费者迁移

**状态：已验收。**

| 顺序 | 任务 | 状态 | 代码 | 完成定义 |
| --- | --- | --- | --- | --- |
| 1 | Digest V2 改读 Reader | **已验收** | `api/internal/sessiondigestv2/` | 12 类新旧事件投影一致；独立测试库全链路通过；生产代码不再读 Payload |
| 2 | Digest V1 改读 Reader | **已验收** | `api/internal/sessiondigest/` | 12 类新旧事件投影和 source bytes 一致；独立测试库全链路通过；生产代码不再读 Payload |
| 3 | MCP/report full 改读 Reader | **已验收** | `api/internal/reportsource/service.go`、`api/internal/contentreader/reader.go` | NULL Payload 全链路按 100+2 分页；cursor/权限/冻结/脱敏不变；Range 读取无整对象重复扫描 |
| 4 | V2 Session 详情/导出 | **已验收** | `api/handler/session.go`、`api/internal/contentreader/reader.go`、Web Session 列表 | 真实 MinIO V2 原始 JSONL 下载、能力字段、权限及 clear 后 410 通过；legacy 路径保留 |
| 5 | 内容清理/恢复兼容 | **已验收** | `api/internal/sessionsync/content_lifecycle.go`、`api/internal/usage/metering_processor.go` | Reader 在 clearing/cleared 拒读；MinIO 删除；Projection/Digest/Catalog 失效；Token、成本、Rollup 和审计保留 |
| 6 | Payload 依赖盘点 | **已验收** | 全仓检索 `content_payload` | 生产代码只剩 Projection 写入；另一处为 migration 列定义 |

完成定义：《测试验收》的 Digest、MCP 和 Session 回归全部通过，业务读路径不再访问 Payload。

## 5. 停止冗余 Payload 写入

**状态：已验收。**

- 修改 `api/internal/sessionsync/content_projection_processor.go`，保留轻量字段并不再写 `content_payload`；
- 保持 revision、Chunk、Catalog、Digest、Usage 和 Token Job 状态流转；
- 更新 Projection 集成测试，断言新事件 Payload 为 NULL；
- 回归 Claude、Codex、Subagent 和重复增量上传。

完成定义：新上传不增加完整 Payload，其他派生数据全部就绪。本阶段不删列、不修改历史行。

验收结果：`9944789`、`4ad6275`；真实链路完成 JSONL → Projection（Payload 为 NULL）→ Reader → Digest V2，HTTP 上传与 V2 下载通过，生命周期、MCP/report、Token 回归通过。生产代码检索只剩 migration 中的历史列定义。

## 6. 历史数据紧凑

**状态：阻断；等待新写入验收和生产等量副本演练。**

| 任务 | 交付物 | 完成定义 |
| --- | --- | --- |
| MinIO 完整性盘点 | 独立命令 | legacy/V2 对象、cursor、hash 和 active revision 对账 |
| 紧凑表 migration | 新 migration | 无 Payload 表结构、约束、索引和切换语句可验证 |
| 分批复制与对账 | 迁移命令 | 默认 dry-run，显式 apply，支持进度、续跑、最终增量和对账 |
| 等量副本演练 | 演练记录 | 耗时、WAL、锁、磁盘峰值、前台影响和恢复时间达标 |
| 生产切换与空间回收 | 发布执行记录 | 旧表在回滚窗口保留，最终删除后空间符合预期 |

## 7. 最终回归与收尾

**状态：未开始。**

- Report Source 真实规模性能回归；
- Digest、MCP full、Session 详情/导出和清理/恢复回归；
- Token 三维、快照、成本、权限和 MCP ad-hoc 对账；
- 全仓检索确认无业务代码读写 `content_payload`；
- 更新测试证据和完整发布清单。

## 8. 进度与证据

| 阶段 | 状态 | Git SHA | 测试证据 | 数据证据 |
| --- | --- | --- | --- | --- |
| 统一 Content Reader | 已验收 | `eafb603`、`6349ae2` | 本地/157 全量测试、vet、定向 race 通过 | legacy、V2 多 Chunk、hash/cursor/异常矩阵通过 |
| 内容消费者迁移 | 已验收 | Digest V2：`96bcd34`～`f8e8d13`；Digest V1：`1848c2f`；MCP full：`ccf82fd`～`f78199b`；Session 导出：`6a5410e`；生命周期：`debc1de` | SQL 基线、Digest、NULL Payload 分页、真实 MinIO V2 下载、生命周期完整回归、远程 Go/TS 检查通过 | 生产读路径不再读取 Payload；清理时 Projection/Digest/Catalog 失效，Token/成本/审计保留 |
| 停止冗余 Payload 写入 | 已验收 | `9944789`、`4ad6275` | Projection、真实 Reader→Digest、HTTP+MinIO 上传、全量测试和 vet 通过 | 新事件只写轻量字段，Payload 为 NULL；生产代码无 Payload 读写 |
| 历史数据紧凑 | 未开始 | - | - | 新写入门禁已通过，等待等量副本迁移工具和演练 |
| 最终回归与收尾 | 未开始 | - | - | - |

任务只有在本表记录真实提交、测试和数据证据后，才能标记为已验收。
