# Session 与 Token 查询性能：完整发布清单（执行版）

> 本文不是原则说明，而是本专项的生产发布执行单。每次发布必须复制一份并填入真实提交、镜像、数据和测试结果；空白项不得视为通过。

## 0. 当前批次结论

| 项目 | 当前值 |
| --- | --- |
| 主分支 | `fea.0.0.1` |
| 当前代码基线 | `65db0a0`（Token/Codex 修复）；文档提交以 `fea.0.0.1` 实际 HEAD 为准 |
| 已部署 API | `20260718-ccc0de1-r5b-repair-v4` |
| 已部署 Web | `20260718-ccc0de1-r5b-web` |
| 已部署 Report Skill | `10086/aida-report@1.0.14` |
| 当前生产健康 | `/health` 返回 `{"status":"ok"}` |
| Token 历史回填 | eligible 5095，active 5081，failed 14，building 0，pending 0，dead job 0 |
| PG 完整 Payload | 仍存在，未迁移、未停止写入、未清理 |
| 本专项完整发布判定 | **阻断** |

阻断原因不是 Token Rollup，而是内容存储链路尚未完成：Digest/MCP/Session 内容读取仍未全部切换到 MinIO Reader，不能停止写入或删除 PostgreSQL `content_payload`。

## 1. 本次需求必须交付的结果

以下 8 项是一个完整版本，不允许只完成其中的 Token 项就发布“专项完成”：

| 编号 | 交付结果 | 通过标准 | 当前状态 |
| --- | --- | --- | --- |
| RSP-01 | Report Source 目录分页 | 候选查询只读 `report_source_slice_catalog` 和权限表，不读事件/Usage 明细 | 已完成/需生产证据 |
| RSP-02 | MinIO Content Reader | 按 revision/cursor 从 MinIO 流式读 JSONL，校验 hash/cursor，失败不返回空内容 | 未完成，阻断 |
| RSP-03 | 内容消费者切换 | Digest V1/V2、冻结报告、MCP full、Session 详情/导出全部使用 Reader | 未完成，阻断 |
| RSP-04 | 停止 Payload 双写 | 新上传不再写 `session_content_events.content_payload` | 未完成，阻断 |
| RSP-05 | 历史 Payload 清理 | 对象/hash/cursor/消费者全量对账后下线历史 JSONB 并回收空间 | 未执行，阻断 |
| TOK-01 | Usage Contribution | Session、Subagent、Chunk、日期和成本事实可追溯 | 已完成，需生产对账证据 |
| TOK-02 | 三维 Rollup/Snapshot | family total = daily sum = chunk sum = active Contribution sum | 已完成，生产回填已做 |
| TOK-03 | Token API/前端/MCP | `/token-analytics/*`、`/tokens*`、工作台、需求关联和 MCP ad-hoc 使用同一 Snapshot | 已发布，需完整回归证据 |

只要 RSP-02～RSP-05 任一项未通过，最终状态必须是“部分完成/禁止生产发布”。

## 2. 代码实现核对

### 2.1 已经存在并需要纳入发布的代码

| 检查项 | 代码/迁移位置 | 核对方式 | 状态 |
| --- | --- | --- | --- |
| Token Rollup | `api/db/migrations/023_token_contribution_and_rollups.sql` | 检查 Contribution、Family、Daily、Chunk、Cost、Snapshot 表和索引 | 已完成 |
| Report Source 空目录状态 | `api/db/migrations/024_report_source_catalog_empty_status.sql` | migration 024 已执行，空目录来源状态可区分 | 已合入主分支 |
| Codex null usage 处理 | `api/internal/usage/provider_parser.go` | `go test ./api/internal/usage/...` | 已完成 |
| Codex counter reset 修复 | `api/internal/usage/provider_parser.go` | parser regression tests + 生产 failed source 复核 | 已完成 |
| Token 入口 | `api/handler/token_analytics.go`、`api/handler/token_sessions.go` | 代码检索不得回退到 `session_activity_slices`/高基数明细 | 已完成，需回归 |

### 2.2 必须补齐、否则不能称为完成的代码

| 检查项 | 必须看到的实现 | 当前状态 |
| --- | --- | --- |
| Reader | 独立 Content Event Reader，输入 `revision_id + start_cursor + end_cursor`，输出事件流 | 未实现 |
| 轻量事件索引 | 只保存 cursor、时间、type、摘要、hash、MinIO object key；不保存完整 Payload | 未完成 |
| Digest V1/V2 | 不再直接查询 `session_content_events.content_payload` | 未完成 |
| MCP full | 不再直接查询 `content_payload`，分页从 MinIO Reader 读取 | 未完成 |
| Session 详情/导出 | 不再把 PostgreSQL JSONB 当内容源 | 未完成 |
| 新上传写入 | 只写 MinIO 对象和轻量索引，不新增完整 `content_payload` | 未完成 |
| 历史清理 | 只允许在前述消费者全部切换后执行，不能使用无条件批量 `UPDATE` | 未完成 |

## 3. 数据迁移清单

### 3.1 已完成的数据工作

- [x] 生产 PostgreSQL 备份已建立：`/home/luoxian/aida/backups/20260718T084206Z-pre-token-repair/aidashboard.dump`。
- [x] 备份 SHA256：`74f381d684dc1231fb019ae2d4fd2d6a47c3a392d68b1b72ed2269b737468a26`。
- [x] Token 历史回填已完成：eligible=5095、active=5081、failed=14、building=0、pending=0。
- [x] 当前 parser scope 的 `token-contribution-backfill --timeout=2m` dead_jobs=0。
- [x] 14 条 Codex counter reset 无法确认边界，保持 failed/conflict，未强行激活。

### 3.2 尚未完成、不能跳过的数据工作

| 步骤 | 操作 | 完成证据 | 状态 |
| --- | --- | --- | --- |
| DATA-01 | 盘点 `sessions.raw_log_url` 与 `session_upload_chunks.raw_object_key` | 对象数、字节数、hash、cursor 报告 | 未执行 |
| DATA-02 | 建立/回填轻量事件索引 | source/revision/cursor 全量对账 | 未执行 |
| DATA-03 | MinIO Reader 与 PG Reader 离线逐切片比较 | 事件数、顺序、hash、Digest hash 一致 | 未执行 |
| DATA-04 | 停止新写入完整 Payload | 新上传后 `content_payload` 不再增长 | 未执行 |
| DATA-05 | 历史 Payload 清理演练 | 备份恢复、WAL/锁/空间峰值报告 | 未执行 |
| DATA-06 | 生产清理 | 批次、删除对象数、PG 空间、失败重试和最终复核 | 未执行 |

Token 回填完成不等于 DATA-01～DATA-06 完成；两条数据链路必须分别验收。

## 4. 接口消费者回归清单

每一项都必须保存请求、响应摘要、数据对账结果和执行时间；只做健康检查不算通过。

| 用例 ID | 接口/功能 | 必须验证 | 当前状态 |
| --- | --- | --- | --- |
| REG-RS-001 | `/api/v1/report-source-sessions` | 第 1 页、中间页、总数不扫描事件 Payload | 已有性能修复，需生产证据 |
| REG-DIG-001 | Digest V1/V2 生成 | MinIO Reader 与旧 PG 结果事件数/hash/Digest hash 一致 | 未执行 |
| REG-DIG-002 | Digest 未就绪 | 返回 `REPORT_SOURCE_DIGEST_NOT_READY`，不现场扫 JSONL | 未执行 |
| REG-MCP-001 | MCP attached digest | 冻结 Digest 不依赖实时 PG Payload | 未执行 |
| REG-MCP-002 | MCP attached full | 多页从 MinIO Reader 读取，无重复/遗漏，cursor 可重试 | 未执行 |
| REG-MCP-003 | MCP ad-hoc `get_sessions` | 日期、权限、Token 字段和旧契约不变 | 需执行 |
| REG-SES-001 | Session 详情/导出 | 对象缺失、hash 错误、清理状态均明确失败 | 未执行 |
| REG-TOK-001 | `/token-analytics/summary` | 只读 Rollup，真实数据 p95/p99 记录 | 已有部分证据，需完整生产证据 |
| REG-TOK-002 | `/tokens/sessions` 分页 | 第 2 页复用 Snapshot，不重新扫描明细 | 已有部分证据，需完整生产证据 |
| REG-TOK-003 | 工作台/需求关联 | 不再浏览器全量拉取 Token，不重复计算 Session | 需执行 |
| REG-UP-001 | 新上传 | MinIO、Usage、Digest、Token、成本链路全部完成 | 需执行 |

## 5. 性能验收清单

不能使用“单次 curl 成功”替代性能验收。必须在真实规模数据上保存执行计划和统计窗口。

| 项目 | 通过标准 | 必须保存的证据 | 状态 |
| --- | --- | --- | --- |
| Report Source 分页 | p95 ≤ 300ms，p99 ≤ 800ms | `EXPLAIN (ANALYZE, BUFFERS)` + 真实压测窗口 | 未完成 |
| Token Summary | 不随 JSONL 字节数增长 | SQL plan、p95/p99、扫描行数 | 未完成 |
| Token Sessions | 第 N 页只读 Snapshot/Rollup | SQL plan、分页重复/遗漏检查 | 未完成 |
| Digest/Reader | 大对象流式、内存受控、可取消 | 321 万事件/7GB 等量样本报告 | 未完成 |
| 回填任务 | 有暂停阈值，不阻塞线上请求 | pending/leased/dead、锁等待、暂停次数 | 已有 Token 部分，内容迁移未完成 |

## 6. 构建与发布清单

### 6.1 发布前

- [ ] 主分支为 `fea.0.0.1`，工作区干净，发布 SHA 已记录。
- [ ] API、Web、Skill/MCP/Agent 和 migration 版本在同一发布记录中对齐。
- [ ] PostgreSQL、`.env`、Compose、Nginx、镜像 digest 已备份。
- [ ] RSP-01～RSP-05、TOK-01～TOK-03 均有证据；不是只看 Token 项。
- [ ] `REG-*` 回归项无阻断项。
- [ ] DATA-01～DATA-06 的范围和停止条件已明确。

### 6.2 部署后

- [ ] API/Web 容器运行同一发布批次的不可变镜像。
- [ ] `/health`、`/health/ready`、关键业务接口通过。
- [ ] 容器内 migration、Skill owner/version/sha256 与发布记录一致。
- [ ] 线上 5xx、队列 dead job、锁等待、慢查询在观察窗口内正常。
- [ ] 数据回填/清理任务的最终数字已写入发布记录。
- [ ] 未完成项明确写入后续任务，不能写“发布完成”。

## 7. 当前发布判定

```text
Token 性能子阶段：已完成并已发布
PG/MinIO 内容去重：未完成
Digest/MCP/Session Reader 迁移：未完成
PG JSONB 历史清理：未执行
本专项完整版本：阻断，不得再次发布
```

后续必须先完成 RSP-02～RSP-05 和对应 `REG-DIG-* / REG-MCP-* / REG-SES-*`，再重新填写本清单并进行一次完整发布。不得复用本次 Token 回填结果作为完整发布通过依据。
