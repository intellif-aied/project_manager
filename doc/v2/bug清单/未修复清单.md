# 未修复清单

> 更新时间：2026-07-18
> 范围：持续汇总当前尚未满足关闭条件的问题。已完成核心真实验证且无后续事项的问题不列入本表。

| 编号 | 优先级 | 状态 | 问题与下一步 |
| --- | --- | --- | --- |
| AIDA-BUG-20260716-001 | P0 | 代码已提交，待发布和生产历史重算 | Codex fork/subagent 继承父 Session 历史，造成 Token 与成本重复统计。代码提交为 `603d369`；关闭前仍需发布客户端/服务端、执行正式历史 revision 重算并完成生产对账。详见 [Codex-fork-Session重复Token统计事故修复方案.md](./Codex-fork-Session重复Token统计事故修复方案.md)。 |
| AIDA-BUG-20260716-003 | P0 | 代码已提交，待发布和真实上传验收 | Aida 客户端原先允许读取 8MB 单行但上传 chunk 仅 4MB，超大 JSONL 单行会在 Digest 前中断并残留 staging。代码提交为 `603d369`；关闭前仍需发布并用原失败文件验证上传、finalize、Digest 与 staging 清理。详见 [AIDA-BUG-20260716-003-Aida客户端超大JSONL单行上传中断.md](./AIDA-BUG-20260716-003-Aida客户端超大JSONL单行上传中断.md)。 |
| AIDA-BUG-20260716-004 | P0 | 已修复第一阶段，待生产验证 | 候选列表查询已改为物化候选集合、合并 count 与分页查询，并只对当前页计算 `total_tokens`；14.157 单独请求约 1～3 秒，接口契约和日报生成流程保持不变。生产发布后继续观察页面并发下的 p95/5xx；更大范围的数据流重构仍待定。详见 [AIDA-BUG-20260716-004-日报来源候选切片列表查询性能事故.md](./AIDA-BUG-20260716-004-日报来源候选切片列表查询性能事故.md)。 |
| AIDA-BUG-20260716-005 | P0 | Done/readiness 已在 14.157 真实验收，待发布和 checkpoint 隔离 | 客户端现已等待服务端权威 readiness，测试02真实上传、重复上传、数据库 projection 和报告来源候选一致；处理中、失败和当前快照不再误报 Done。关闭前仍需发布生产，并完成账号/服务端维度 checkpoint 隔离及跨账号回归。详见 [AIDA-BUG-20260716-005-Aida上传Done与服务端Session可用状态不一致.md](./AIDA-BUG-20260716-005-Aida上传Done与服务端Session可用状态不一致.md)。 |
| AIDA-BUG-20260717-006 | P1 | 已确认，待后续优化 | 内容切片 fully indexed 后会先进入候选列表，Digest 仍由 5 秒 Reconciler 与单批 Worker 异步生成；用户立即启动日报/周报时，任一 Slice Digest 未就绪都会返回 `REPORT_SOURCE_DIGEST_NOT_READY`，前端目前直接显示生成失败。后续应保留同一 Selection、显示“正在准备报告数据”并自动衔接，不能同步生成或弱化冻结门禁。详见 [AIDA-BUG-20260717-006-报告生成Digest未就绪时序竞态.md](./AIDA-BUG-20260717-006-报告生成Digest未就绪时序竞态.md)。 |
| AIDA-BUG-20260718-007 | P0 | R5A 已部署，R5B 被数据门禁阻断 | 高基数 Snapshot 和旧接口数据断层已完成代码改造；14.157 的 228 个来源已有 215 个完成 Contribution/Rollup，但仍有 8 个质量门禁失败、3 个 MinIO 对象缺失和 2 个替换覆盖回退。在人工确定修复或排除策略前不切 R5B。详见 [AIDA-BUG-20260718-007-Token查询快照超时与旧接口数据断层.md](./AIDA-BUG-20260718-007-Token查询快照超时与旧接口数据断层.md)。 |
| AIDA-BUG-20260718-008 | P2 | 未修改 | AI 生成个人日报、周报时，Report Context 与正文均成功，但报告表未写入已选来源元数据，工作台可能显示“已关联 0 条记录”。该问题与本次 PG 瘦身无直接关系；修复前需统一展示唯一 Session 数或来源切片数。详见 [AIDA-BUG-20260718-008-AI生成报告来源数量显示为0.md](./AIDA-BUG-20260718-008-AI生成报告来源数量显示为0.md)。 |
| AIDA-RUNTIME-20260716-001 | P0 | 待平台修复 | Codex 运行出现 `MCP tools seem to not be available` 或 `unsupported call` 时，可能未调用 `get_sessions`、未调用 `write_report_result`、结果为空，却被平台标记 completed。完成条件是没有真实报告写回的报告任务必须失败并保留明确错误。 |
| AIDA-OPS-20260716-001 | P2 | 持续观察 | 模型服务曾返回 high demand。它与 Session 容量和 Digest 无关，按外部服务拥塞处理；需要重试、可观测性和用户可理解的错误提示，不纳入 Digest 代码修复。 |

## 维护规则

- 每个问题只有在代码、自动化和对应真实回归均通过后才能移出本表；
- 生产数据修正必须单独备份、审计和验收，不因代码提交自动视为完成；
- 测试服或生产环境的新证据优先补到对应专项文档，本表只保留状态和入口。
