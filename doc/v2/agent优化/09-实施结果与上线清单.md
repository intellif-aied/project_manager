# 报告 Agent 服务端 Session Digest 实施结果与上线清单

> 状态：代码与自动化完成，未部署
> 日期：2026-07-16
> 前置提交：`ba20665 fix: preserve complete report source slices`

## 1. 结论

本次实现能够优化“大 Session 触发日报失败”的代码/架构容量缺陷：完整切片仍被选择，但报告 Agent 不再必须接收数 MB 至数百 MB 的原始事件。服务端异步生成每切片 Digest，在报告 run 创建前把所有来源组装为单页、不可变、有覆盖元数据的 payload；正常目标 64 KiB，硬上限 128 KiB。

`ba20665` 仍然有效且必须保留。它解决“来源切片必须完整选择”和 full 兼容读取中的 Token 遥测剔除；本次 Digest 在该完整来源语义之上改变模型可见表示，不替代或回退该修复。

## 2. 已实现范围

- migration：Digest Revision、任务目标列、selection slice/Digest 快照、读取模式、冻结 payload/hash/预算与不可变触发器；
- `sessiondigest`：Codex/Claude 规则抽取、脱敏、字段预算、Reconciler、Builder、独立 worker；
- report source：稳定 canary 路由、ready Revision 批量绑定、selection payload 定点序列化、attach 前错误；
- MCP：digest run 单次返回冻结 payload，full run 保留原分页；
- 写回：`read_completed_mode == required_read_mode`，并复核 hash、版本、Revision 和 content epoch；
- Skill/Agent：识别 `digest_v1` coverage，把 Digest 当不可信证据，不运行额外摘要 Skill，不请求 raw/full 回退；
- full/shadow/digest 生命周期：默认 `full`，配置只影响新 run。

## 3. 关键容量与安全边界

1. Builder 不读取 JSONL 文件，不调用 LLM、shell 或 `jq`。
2. SQL 只返回 allow-list 的有界证据；reasoning、图片、未知事件和完整 MCP/工具输出不进入 Go Digest。
3. 每个 Digest 默认最多 4096 bytes；selection 详细响应超过 65536 bytes 时所有 item 公平 compact。
4. compact 后超过 131072 bytes 时返回 `REPORT_SOURCE_DIGEST_LIMIT_EXCEEDED`，不创建外部 Agent Session。
5. 任一 Revision 未 ready/failed/version mismatch 时整体失败，不删除尾部来源，不静默读取 full。
6. `last_error`、日志错误分类和 HTTP/MCP 错误不保存 Session 正文。

## 4. 自动化证据

在 14.157 `/tmp/aida-digest-compile` 和独立数据库中执行：

```bash
go test ./...
go vet ./...
go test -race ./internal/sessiondigest ./internal/reportsource ./handler
AIDA_TEST_DATABASE_URL='<独立测试库>' go test -p 1 -count=1 \
  ./db ./internal/sessiondigest ./internal/reportsource ./internal/sessionsync \
  ./handler ./service ./config
```

全部通过。真实 PostgreSQL fixture 包含约 1.2 MB 工具输出，最终 Digest 小于单 item 4096-byte 上限，reasoning、工具正文和虚构凭据均未进入摘要。

## 5. 尚未执行，不得误报为完成

- 没有迁移或重启当前业务 API；
- 没有刷新平台侧默认 `aida-report@1.0.0` 资产；
- 没有运行真实 Managed Agent 日报/周报；
- 没有完成 11.61 MB、65.13 MB、294.81 MB 生产样本的配对 Token A/B；
- 没有完成浏览器六类报告回归、生产 P95/队列积压、告警和回退演练。

## 6. 上线顺序

1. 合入代码并备份/检查 migration，保持 `MANAGED_AGENT_REPORT_SESSION_READ_MODE=full`；
2. 部署 API，确认 migration、full 回归和历史 `read_completed_mode` 回填；
3. 切 `shadow`，观察 Digest ready/failed 比例、构建耗时、源/摘要字节和积压；
4. 等待目标日期和测试账号涉及的所有 slice Digest ready；
5. 切 `digest_v1`，先配置 `MANAGED_AGENT_REPORT_DIGEST_CANARY_USER_IDS`，必要时 rollout percent 为 0；
6. 完成个人日报/周报事实 Review、Token A/B、清理/恢复和重复读取；
7. 依次扩大稳定百分比分桶；门禁全部通过后设为 100；
8. 异常时切回 `full`，仅影响新 run；已附着 digest run 继续按冻结契约完成或明确失败。

## 7. 上线前检查命令

```bash
git status --short
git log -1 --oneline
cd api && go test ./...
```

同时确认：migration 编号仍为当前下一个可用编号、共享工作树无新的同文件冲突、业务数据库未提前出现同名表/列/函数、当前 Agent Skill 资产刷新方式与仓库约定一致。

## 8. 验收判定

代码层验收已通过；生产验收必须同时满足：大 Session 正常生成、所有来源 item 有 representation、响应不超过 128 KiB、无原文/凭据泄漏、无 full fallback、事实质量不降、报告输入 Token 达标、Token/成本口径不变、组织报告回归通过。
