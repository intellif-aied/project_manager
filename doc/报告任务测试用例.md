# 报告任务测试用例

## 1. 目标

本用例用于指导 Codex 基于 Aida 当前环境，对 **AI Assets / Agent 定时任务** 做一轮高覆盖、可复现的端到端测试。

本轮测试目标不是只验证“接口 200”，而是尽量完整覆盖下面四层：

- Report Agent 直接手动运行，生成 6 类 `report_type` 报告
- Report Agent 定时任务创建、编辑、启停、删除、立即运行、自动触发
- 6 类 `report_type` 在手动运行和定时任务两类入口下的调度能力
- 不同角色与 `report_type` 的允许 / 拒绝矩阵
- 执行后的 `ai_runs / schedule / report` 联动结果
- Agent 生成内容的轻度合理性检查
- 默认报告 Agent 统一引用当前环境的系统报告 Skill，且不会给普通用户创建重复副本
- 系统 Skill owner 配置异常时在修改 Agent 前失败，历史 `SKILL_NOT_FOUND` owner 回归不再出现

测试必须以 **Aida 当前真实接口** 为准，不直接调用远端 Agent 平台私有接口，不手工伪造 Report MCP 回写。

重要口径：本测试用例以本文件和 [定时任务.md](/home/intellif/dev/project_manager/doc/定时任务.md) 中定义的产品 / 技术契约作为验收基线。当前代码形态只能作为定位接口、字段和实现路径的证据，不能被视为 100% 正确。如果实际代码行为与契约冲突，应在执行记录中标注为 **实现缺陷 / 产品口径缺口 / 可观测性缺口**，不得因为“代码当前就是这样”而降低测试预期。

---

## 2. 适用范围

本用例覆盖的报告类型：

- `personal_daily`
- `personal_weekly`
- `team_daily`
- `team_weekly`
- `department_daily`
- `department_weekly`

本用例默认当前 6 类报告主流程已经实现并可正式支持。

本用例覆盖两类最常见用户入口：

- **直接手动运行 Report Agent**：用户在 AI Assets 中选择 Report Agent，选择 `report_type`、报告周期和补充要求后立即运行。
- **设置定时器运行 Report Agent**：用户创建 Agent 定时任务，由 schedule 按 daily / weekly 频率触发对应 `report_type`。

这两类入口的 period 来源不同：直接手动运行使用用户提交的 `period`；定时器运行使用 `report_type + scheduled_trigger_at` 推导。测试用例必须分别验证，不能混为一类。

---

## 3. 账号与角色

账号来源：

- [测试账号文档.md](/home/intellif/dev/project_manager/doc/测试账号文档.md)

本轮建议使用以下账号：

| 用途 | 用户 ID | username | 角色 | 说明 |
| --- | ---: | --- | --- | --- |
| 部门报告执行人 | 304 | t02 | director | 负责部门日报、部门周报 |
| 小组 A 执行人 | 305 | t03 | team_leader | 负责小组 A 日报、周报 |
| 小组 B 执行人 | 306 | t04 | team_leader | 负责小组 B 日报、周报 |
| 小组 A 成员 1 | 307 | t05 | employee | 个人日报、个人周报来源 |
| 小组 A 成员 2 | 308 | t06 | employee | 个人日报、个人周报来源 |
| 小组 B 成员 1 | 311 | t09 | employee | 个人日报、个人周报来源 |
| 小组 B 成员 2 | 312 | t10 | employee | 个人日报、个人周报来源 |
| 生命周期补充账号 | 309 | t07 | employee | 仅用于调度启停 / 自动触发 / 删除验证 |

说明：

- 本轮不强制使用 PM 账号 303 参与报告生成。
- 小组 / 部门报告必须使用对应角色账号执行，不允许用 employee 账号代替。

角色与 `report_type` 的验收口径：

| 角色 | 正向应允许的 report_type | 负向应拒绝的 report_type | 本轮主测账号 |
| --- | --- | --- | --- |
| employee | `personal_daily`, `personal_weekly` | `team_daily`, `team_weekly`, `department_daily`, `department_weekly` | 307 / 308 / 311 / 312 / 309 |
| team_leader | `personal_daily`, `personal_weekly`, `team_daily`, `team_weekly` | `department_daily`, `department_weekly` | 305 / 306 |
| director | `personal_daily`, `personal_weekly`, `department_daily`, `department_weekly` | `team_daily`, `team_weekly` | 304 |
| admin | 若环境提供 admin，可作为管理兜底账号验证全部类型；本轮不强制 | 无，除非产品另有限制 | 可选 |

本轮正向主路径按最常见职责分工执行：employee 生成个人报告，team_leader 生成小组报告，director 生成部门报告。team_leader / director 的个人报告可作为补充抽测，但不替代对应层级报告。

如果当前实现允许 employee 生成小组 / 部门报告，或允许 team_leader 生成部门报告，或允许 director 生成小组报告，应记录为 **实现缺陷或产品契约缺口**，不得因为代码当前放行而视为通过。

---

## 4. 测试原则

### 4.1 执行方式

优先顺序：

1. API 为主
2. 页面为辅

建议：

- 创建默认 Report Agent、创建定时任务、手动触发、查询结果，优先用 API 批量执行
- 页面抽样必须覆盖本文件 8.12 的字段隐藏、表单分支、预览和列表验收，不能只做“看一眼页面能打开”

### 4.2 时间原则与缺陷判定

不要直接假设“今天”一定有可用 session，也不要把历史 `seed_date` 误当成所有运行方式的报告归属日期。

权威规则以 [定时任务.md](/home/intellif/dev/project_manager/doc/定时任务.md) 为准：

- 直接手动运行 Report Agent，即 `POST /ai-assets/report-agents/{agentId}/runs`，报告周期由用户提交的 `period` 决定。
- 定时任务运行 Report Agent，即 `POST /ai-assets/agent-schedules/{id}/runs` 或 scanner 自动触发，报告周期由 `report_type + scheduled_trigger_at` 推导。
- 定时任务的 `manual` / `save_and_run` 运行，`scheduled_trigger_at` 应是用户触发时的当前时间，不应使用 `next_run_at`。
- 定时任务的 `scheduled` 自动触发，`scheduled_trigger_at` 应是 scanner 领取前的旧 `next_run_at`。

Codex 在执行前必须先做数据预检查并记录：

- `execution_date`：本轮实际触发时间在后端默认时区下的日期。
- `execution_week_start` / `execution_week_end`：`execution_date` 所在自然周，周一到周日。
- `seed_date`：最近 14 天内，`307 / 308 / 311 / 312` 都有 session 的共同日期。
- `seed_week_start` / `seed_week_end`：`seed_date` 所在自然周，周一到周日。

要求：

- 如果使用直接手动 Report Agent Run，报告期望基于请求 payload 中显式提交的 `period`，可以使用 `seed_date` / `seed_week_start` 来验证历史数据。
- 如果使用定时任务的 `manual` 或 `save_and_run` 验证报告内容，报告期望必须基于 `execution_date` / `execution_week_start`，不能硬套历史 `seed_date`。
- 如果 `execution_date` 上 307 / 308 / 311 / 312 没有共同 session，团队 / 部门“内容来源完整性”应判定为 **测试环境前置数据不足**，不要用历史 `seed_date` 冒充本次运行周期。
- 历史 `seed_date` 可用于直接手动 Report Agent Run 的显式 `period`。但对定时任务运行而言，除非测试手段能让 `scheduled_trigger_at` 合法落到该日期，否则不能作为本次定时任务写回的期望周期。
- 如果产品需要按历史日期生成报告，但当前公开接口无法表达该能力，应记录为 **产品 / 接口能力缺口**。
- 如果报告查询接口宣称支持 `date` / `week_start` 但实际忽略参数，应记录为 **实现缺陷或可观测性缺口**，不得静默改成“按当前代码行为通过”。

### 4.3 清理原则

测试前应尽量清理本地 Aida 数据，避免旧 schedule / ai_run 干扰：

- 仅清理 Aida 本地库中本轮测试账号的：
  - `managed_agent_schedules`
  - `managed_agent_profiles`
  - `ai_runs`
- 不要求清远端 Agent 平台历史资产

若远端已有未归档默认 Report Agent，可直接复用；若没有，则通过 Aida 接口创建。

---

## 5. 预检查

Codex 在正式执行前，必须完成下面预检查并记录结果。

### 5.1 服务连通性

至少验证：

- `/api/v1/auth/me`
- `/api/v1/ai-assets/agents`
- `/api/v1/ai-assets/agent-schedules`
- `/api/v1/sessions`

预期：

- 均返回 200

### 5.2 账号可用性

对下列账号逐个验证 `/auth/me`：

- 304
- 305
- 306
- 307
- 308
- 309
- 311
- 312

预期：

- token 有效
- 角色与 [测试账号文档.md](/home/intellif/dev/project_manager/doc/测试账号文档.md) 一致

### 5.3 Session 前置数据

对 307 / 308 / 311 / 312 分别查询最近 14 天 session 列表。

Codex 必须确认并记录：

- 每个账号至少有 1 条可读 session。
- 是否存在 1 个共同历史日期 `seed_date`，四个账号在该日期都有 session。
- `execution_date` 上四个账号是否都有 session。

判定口径：

- 直接手动 Report Agent Run 内容验证依赖请求中的显式 `period`。
- 定时任务 `manual` / `save_and_run` 内容验证优先依赖 `execution_date`。
- 如果只有历史 `seed_date` 有数据，而 `execution_date` 无共同 session，则主链路可以继续验证调度和写回，但团队 / 部门报告的“来源覆盖完整性”应标记为 **测试环境前置数据不足**。
- 如果测试报告周期与 `scheduled_trigger_at` 不一致，应标记为 fail；如果实现无法暴露足够字段证明周期，应标记为 **可观测性缺口**。

建议额外记录：

- 每个账号在 `execution_date` 的 session 数量。
- 每个账号在 `seed_date` 的 session 数量。
- 四个账号共同落在的 `execution_week_start` 和 `seed_week_start`。

### 5.4 默认 Report Agent

对 304 / 305 / 306 / 307 / 308 / 311 / 312 / 309：

- 先完成 5.5 的系统 Skill 预检查
- 调用 `/ai-assets/agents`
- 若无可用 `business_type=report` 的 Agent，则调用 `POST /ai-assets/report-agents/default`

预期：

- 每个参与测试的账号至少有 1 个可用 Report Agent

### 5.5 系统报告 Skill 前置条件

14.157 开发环境必须配置：

```text
MANAGED_AGENT_REPORT_SKILL_OWNER=100866
MANAGED_AGENT_REPORT_SKILL_SLUG=aida-report
MANAGED_AGENT_REPORT_SKILL_VERSION=1.0.0
```

正式创建默认 Report Agent 前必须验证：

- Agent 平台公共 Skill Registry 中存在未归档的 `100866/aida-report@1.0.0`。
- 公共列表返回的 owner 明确为 `100866`，不能是空字符串、当前测试用户名或本地用户 ID。
- 14.157 不会选中 `10086/aida-report@1.0.0`。
- 普通测试账号自己的 `scope=mine` 中不要求存在 `aida-report@1.0.0`。
- `GET /ai-assets/skills?scope=mine&include_system=true` 返回普通用户 Skill 与唯一一份开发系统报告 Skill。
- `include_system=false` 不注入系统报告 Skill。

如果系统 Skill 未发布、owner 不一致或配置为空，应在执行记录中判定为 **环境配置缺陷**，并确认 Aida 没有创建或修改默认 Agent，也没有给普通用户创建报告 Skill 副本。

---

## 6. 执行顺序

为保证小组 / 部门报告有真实来源，建议按下面顺序执行。

### 阶段 0：直接手动 Report Agent Run

目标：先覆盖用户最直接的“选择 Report Agent 立即生成报告”场景。

执行：

- 307 / 308 / 311 / 312 分别运行 `personal_daily`，period 使用 `seed_date`。
- 307 / 308 / 311 / 312 分别运行 `personal_weekly`，period 使用 `seed_week_start ~ seed_week_end`。
- 305 / 306 分别运行 `team_daily`，period 使用 `seed_date`。
- 305 / 306 分别运行 `team_weekly`，period 使用 `seed_week_start ~ seed_week_end`。
- 304 运行 `department_daily`，period 使用 `seed_date`。
- 304 运行 `department_weekly`，period 使用 `seed_week_start ~ seed_week_end`。

验证：

- 每类 `report_type` 至少有 1 次手动 Report Agent Run 成功。
- 每次 run 的 `trigger_source=manual`，且没有 `schedule_id`。
- `period` 等于请求 payload 显式提交的 period。
- `target` 按当前用户角色自动或显式推导正确。

### 阶段 A：个人日报来源

执行账号：

- 307
- 308
- 311
- 312

每个账号：

- 创建 `personal_daily` 定时任务
- 使用 `manual` 或 `save_and_run` 立即运行
- 验证个人日报生成成功

### 阶段 B：小组日报来源

执行账号：

- 305
- 306

每个账号：

- 创建 `team_daily` 定时任务
- 立即运行
- 验证小组日报生成成功

### 阶段 C：部门日报

执行账号：

- 304

执行：

- 创建 `department_daily` 定时任务
- 立即运行
- 验证部门日报生成成功

### 阶段 D：个人周报来源

执行账号：

- 307
- 308
- 311
- 312

每个账号：

- 创建 `personal_weekly` 定时任务
- 立即运行
- 验证个人周报生成成功

### 阶段 E：小组周报来源

执行账号：

- 305
- 306

每个账号：

- 创建 `team_weekly` 定时任务
- 立即运行
- 验证小组周报生成成功

### 阶段 F：部门周报

执行账号：

- 304

执行：

- 创建 `department_weekly` 定时任务
- 立即运行
- 验证部门周报生成成功

### 阶段 G：调度生命周期补充

执行账号：

- 309

执行：

- 创建一个 `personal_daily` 定时任务
- 测试编辑
- 测试停用 / 启用
- 测试自动触发
- 测试删除

---

## 7. 核心测试矩阵

| Case ID | 执行账号 | report_type | 核心目标 |
| --- | --- | --- | --- |
| TC-MR-PD-307 | 307 | personal_daily | 手动运行 Report Agent 生成个人日报 |
| TC-MR-PD-308 | 308 | personal_daily | 手动运行 Report Agent 生成个人日报，小组 A 第二成员 |
| TC-MR-PD-311 | 311 | personal_daily | 手动运行 Report Agent 生成个人日报，小组 B 成员 |
| TC-MR-PD-312 | 312 | personal_daily | 手动运行 Report Agent 生成个人日报，小组 B 第二成员 |
| TC-MR-PW-307 | 307 | personal_weekly | 手动运行 Report Agent 生成个人周报 |
| TC-MR-PW-308 | 308 | personal_weekly | 手动运行 Report Agent 生成个人周报，小组 A 第二成员 |
| TC-MR-PW-311 | 311 | personal_weekly | 手动运行 Report Agent 生成个人周报，小组 B 成员 |
| TC-MR-PW-312 | 312 | personal_weekly | 手动运行 Report Agent 生成个人周报，小组 B 第二成员 |
| TC-MR-TD-305 | 305 | team_daily | 手动运行 Report Agent 生成小组 A 日报 |
| TC-MR-TD-306 | 306 | team_daily | 手动运行 Report Agent 生成小组 B 日报 |
| TC-MR-TW-305 | 305 | team_weekly | 手动运行 Report Agent 生成小组 A 周报 |
| TC-MR-TW-306 | 306 | team_weekly | 手动运行 Report Agent 生成小组 B 周报 |
| TC-MR-DD-304 | 304 | department_daily | 手动运行 Report Agent 生成部门日报 |
| TC-MR-DW-304 | 304 | department_weekly | 手动运行 Report Agent 生成部门周报 |
| TC-PD-307 | 307 | personal_daily | 员工个人日报调度成功 |
| TC-PD-308 | 308 | personal_daily | 小组 A 第二个成员日报成功 |
| TC-PD-311 | 311 | personal_daily | 小组 B 成员日报成功 |
| TC-PD-312 | 312 | personal_daily | 小组 B 第二个成员日报成功 |
| TC-TD-305 | 305 | team_daily | 小组 A 日报汇总成功 |
| TC-TD-306 | 306 | team_daily | 小组 B 日报汇总成功 |
| TC-DD-304 | 304 | department_daily | 部门日报汇总成功 |
| TC-PW-307 | 307 | personal_weekly | 员工个人周报调度成功 |
| TC-PW-308 | 308 | personal_weekly | 小组 A 第二个成员周报成功 |
| TC-PW-311 | 311 | personal_weekly | 小组 B 成员周报成功 |
| TC-PW-312 | 312 | personal_weekly | 小组 B 第二个成员周报成功 |
| TC-TW-305 | 305 | team_weekly | 小组 A 周报汇总成功 |
| TC-TW-306 | 306 | team_weekly | 小组 B 周报汇总成功 |
| TC-DW-304 | 304 | department_weekly | 部门周报汇总成功 |
| TC-DC-PD-WEEKLY | 309 | personal_daily | weekly schedule 触发个人日报，验证 schedule_type 与 report_type 解耦 |
| TC-DC-PW-DAILY | 309 | personal_weekly | daily schedule 触发个人周报，验证 schedule_type 与 report_type 解耦 |
| TC-DC-DW-DAILY | 304 | department_weekly | daily schedule 触发部门周报，验证预览 / 周期推导不被 schedule_type 误导 |
| TC-UI-REPORT-SCHEDULE | 305 / 304 | 多类型 | Report Agent 定时任务页面字段、预览、保存 payload 验收 |
| TC-UI-GENERIC-SCHEDULE | 任一普通 Agent 账号 | generic_agent | 普通 Agent 定时任务页面不展示 Report 配置 |
| TC-LC-309 | 309 | personal_daily | 调度生命周期：编辑 / 启停 / 自动触发 / 删除 |

---

## 8. 详细用例

### 8.1 直接手动 Report Agent Run 用例

入口：

- `POST /api/v1/ai-assets/report-agents/{agentId}/runs`
- 若使用默认 Report Agent，可使用 `agentId=default` 或先查询 / 创建默认 Report Agent 后使用真实 `agent_id`。

通用 payload：

```json
{
  "report_type": "personal_daily",
  "period": { "date": "<seed_date>" },
  "target": { "type": "self" },
  "model_id": "MiniMax-M2.5",
  "message": "可选补充要求",
  "start_prompt_values": {}
}
```

周报类 payload 使用：

```json
{
  "period": {
    "week_start": "<seed_week_start>",
    "week_end": "<seed_week_end>"
  }
}
```

手动 Report Agent Run 正向矩阵：

| Case ID | 执行账号 | 角色 | report_type | period | 期望 target |
| --- | --- | --- | --- | --- | --- |
| TC-MR-PD-307 | 307 | employee | `personal_daily` | `seed_date` | 当前用户本人 |
| TC-MR-PD-308 | 308 | employee | `personal_daily` | `seed_date` | 当前用户本人 |
| TC-MR-PD-311 | 311 | employee | `personal_daily` | `seed_date` | 当前用户本人 |
| TC-MR-PD-312 | 312 | employee | `personal_daily` | `seed_date` | 当前用户本人 |
| TC-MR-PW-307 | 307 | employee | `personal_weekly` | `seed_week_start ~ seed_week_end` | 当前用户本人 |
| TC-MR-PW-308 | 308 | employee | `personal_weekly` | `seed_week_start ~ seed_week_end` | 当前用户本人 |
| TC-MR-PW-311 | 311 | employee | `personal_weekly` | `seed_week_start ~ seed_week_end` | 当前用户本人 |
| TC-MR-PW-312 | 312 | employee | `personal_weekly` | `seed_week_start ~ seed_week_end` | 当前用户本人 |
| TC-MR-TD-305 | 305 | team_leader | `team_daily` | `seed_date` | 305 所在小组 |
| TC-MR-TD-306 | 306 | team_leader | `team_daily` | `seed_date` | 306 所在小组 |
| TC-MR-TW-305 | 305 | team_leader | `team_weekly` | `seed_week_start ~ seed_week_end` | 305 所在小组 |
| TC-MR-TW-306 | 306 | team_leader | `team_weekly` | `seed_week_start ~ seed_week_end` | 306 所在小组 |
| TC-MR-DD-304 | 304 | director | `department_daily` | `seed_date` | 304 所在部门 |
| TC-MR-DW-304 | 304 | director | `department_weekly` | `seed_week_start ~ seed_week_end` | 304 所在部门 |

验证：

- `/ai-assets/agent-runs/{runId}` 最终为 `succeeded`。
- `business_type` 为 Report Agent run 对应类型。
- `trigger_source=manual`。
- 不存在 `schedule_id`，因为这不是定时任务触发。
- `report_type` 与请求一致。
- `period` 与请求 payload 一致，不应被当前日期或 `next_run_at` 覆盖。
- `target` 与账号角色一致。
- 对应报告查询接口能读到内容，且 `managed_agent_run_id` 指向本次 run。

手动 Report Agent Run 负向矩阵：

| Case ID | 执行账号 | 角色 | report_type | 预期 |
| --- | --- | --- | --- | --- |
| TC-MR-NEG-EMP-TD | 307 | employee | `team_daily` | 拒绝，不能生成小组报告 |
| TC-MR-NEG-EMP-DD | 307 | employee | `department_daily` | 拒绝，不能生成部门报告 |
| TC-MR-NEG-TL-DW | 305 | team_leader | `department_weekly` | 拒绝，不能生成部门周报 |
| TC-MR-NEG-DIR-TW | 304 | director | `team_weekly` | 拒绝，不能生成小组周报，除非产品明确允许 director 代小组生成 |

如果当前实现放行上述负向场景，应记录为 **实现缺陷或产品契约缺口**。

### 8.2 定时任务通用创建步骤

除生命周期、直接手动 Report Agent Run 和负向用例外，所有定时任务正向用例统一按下面步骤执行：

1. 用对应账号获取或确认默认 Report Agent
2. 调 `/ai-assets/agent-schedules/preview`
3. 创建定时任务 `POST /ai-assets/agent-schedules`
4. 立即运行 `POST /ai-assets/agent-schedules/{id}/runs`
5. 轮询：
   - `/ai-assets/agent-runs`
   - `/ai-assets/agent-runs/{runId}`
   - `/ai-assets/agent-schedules`
6. 再调对应报告查询接口核对写回结果

### 8.3 定时任务通用创建 payload 约束

Report Agent 定时任务 payload 必须满足：

- `run_kind = report_agent`
- `trigger_config.schedule_type` 与 `report_type` 不绑定
- `run_config.report_config.report_type` 必填
- 不提交：
  - `target`
  - `target_json`
  - `period_json`
  - `period_start`
  - `period_end`
  - `scheduled_trigger_at`
  - `run_id`
  - `mcp_url`
  - `credential`
  - `credential_slot`
  - `AIDA_REPORT_MCP_AUTH`

### 8.4 个人日报用例

用例：

- `TC-PD-307`
- `TC-PD-308`
- `TC-PD-311`
- `TC-PD-312`

建议配置：

- `schedule_type = daily`
- `time_of_day = 当前时间 + 3 分钟`
- `report_type = personal_daily`
- `model_id = MiniMax-M2.5`

立即运行后验证：

- `/ai-assets/agent-runs/{runId}` 最终为 `succeeded`。
- `input_ref_json.scheduled_trigger_at` 对应 `execution_date`。
- 个人日报查询结果存在，建议优先使用 `GET /reports/mine?from=<execution_date>&to=<execution_date>` 定位报告，再用报告详情接口或数据库核对。
- 仅当 `execution_date` 等于服务端“今天”时，可以用 `GET /reports/today` 作为补充验证。不要假设 `GET /reports/today` 携带历史 `date` 参数时一定会按历史日期查询；如果产品契约要求支持但接口忽略 `date`，应记录为 bug / 可观测性缺口。
- `managed_agent_run_id` 或关联字段指向本次 run。
- 内容非空。

### 8.5 个人周报用例

用例：

- `TC-PW-307`
- `TC-PW-308`
- `TC-PW-311`
- `TC-PW-312`

建议配置：

- `schedule_type = weekly`
- `weekdays = 与 execution_date 所在周内任意合法值`；如果只测立即运行，`weekdays` 不参与本次报告周期归属
- `report_type = personal_weekly`
- `week_start = execution_week_start`

结果查询接口：

- `GET /reports/weekly/mine/current?week_start=<execution_week_start>`

验证：

- 报告存在
- 周期与 `execution_week_start ~ execution_week_end` 一致
- 内容非空

### 8.6 小组日报用例

用例：

- `TC-TD-305`
- `TC-TD-306`

前置要求：

- 对应小组至少已有 2 个成员成功生成 `personal_daily`

结果查询接口：

- `GET /reports/team/today?date=<execution_date>`

验证：

- 报告存在
- 来源范围为对应小组
- 不应跨组引用另一组成员日报

### 8.7 小组周报用例

用例：

- `TC-TW-305`
- `TC-TW-306`

前置要求：

- 对应小组至少已有 2 个成员成功生成 `personal_weekly`

结果查询接口：

- `GET /reports/team/weekly/current?week_start=<execution_week_start>`

验证：

- 报告存在
- 周期正确
- 来源范围为对应小组

### 8.8 部门日报用例

用例：

- `TC-DD-304`

前置要求：

- `TC-TD-305`
- `TC-TD-306`

结果查询接口：

- `GET /reports/department/today?date=<execution_date>`

验证：

- 报告存在
- 应体现两个小组的汇总结果

### 8.9 部门周报用例

用例：

- `TC-DW-304`

前置要求：

- `TC-TW-305`
- `TC-TW-306`

结果查询接口：

- `GET /reports/department/weekly/current?week_start=<execution_week_start>`

验证：

- 报告存在
- 周期正确
- 能看到跨小组汇总痕迹

### 8.10 生命周期用例

用例：

- `TC-LC-309`

目标：

- 覆盖创建、编辑、启用、停用、自动触发、删除

执行步骤：

1. 创建 `personal_daily` schedule，`enabled=true`，触发时间设为当前时间后 3 分钟
2. 记录 `next_run_at`
3. 编辑 `time_of_day`，验证 `next_run_at` 重算
4. 停用，验证：
   - `enabled=false`
   - scanner 不应在到点时触发
5. 重新启用，验证 `next_run_at` 基于当前时间重算
6. 等待自动触发，验证：
   - 新增 `ai_run`
   - `trigger_source=scheduled`
   - `last_run_status` 更新
   - `next_run_at` 自动推进
7. 删除该 schedule，验证列表消失

### 8.11 schedule_type / report_type 解耦用例

目标：

- 明确验证 `schedule_type=daily / weekly` 只表示触发频率。
- 明确验证 `report_type` 才决定日报 / 周报，以及个人 / 小组 / 部门目标。
- 如果当前实现把 `daily schedule` 强制等同日报，或把 `weekly schedule` 强制等同周报，应记录为 **实现缺陷**。

建议至少覆盖：

| Case ID | 执行账号 | schedule_type | report_type | 核心验证 |
| --- | --- | --- | --- | --- |
| TC-DC-PD-WEEKLY | 309 | weekly | personal_daily | 每周触发一次个人日报，报告周期仍为触发日全天 |
| TC-DC-PW-DAILY | 309 | daily | personal_weekly | 每天刷新当前自然周个人周报，报告周期仍为自然周 |
| TC-DC-DW-DAILY | 304 | daily | department_weekly | 每天刷新当前自然周部门周报，报告对象仍为部门 |

验证：

- `POST /ai-assets/agent-schedules/preview` 返回的 `next_run_at` 只由 `schedule_type / weekdays / time_of_day` 决定。
- `period_display / period_start / period_end` 由 `report_type + scheduled_trigger_at` 决定。
- 创建 schedule 成功后，保存的 `schedule_type` 与 `report_config.report_type` 分别保持用户输入值。
- 立即运行后，`ai_runs.input_ref_json.report_type` 与创建时一致。
- 立即运行后，日报类 period 是 `execution_date` 全天，周报类 period 是 `execution_week_start ~ execution_week_end`。

### 8.12 定时任务专项 60 Case 矩阵

本节作为定时任务专项扩展用例，要求执行记录中逐条记录结果。若真实模型运行成本或时间受限，至少先跑 `P0` 用例；未执行的 `P1 / P2` 用例必须在执行记录中标记为 `not run`，不能删除。

统一执行要求：

- 每个创建类 case 都要记录 `schedule_id`、`next_run_at`、`enabled`、`run_kind`、`report_config.report_type`。
- 每个运行类 case 都要记录 `run_id`、`trigger_source`、`scheduled_trigger_at`、`input_ref_json.period`、最终 `ai_run.status`。
- 立即运行 / 保存后运行不得把 `next_run_at` 当成报告归属周期；自动触发必须使用 scanner 领取前的旧 `next_run_at`。
- 负向 case 如果当前代码放行，应记录为 **implementation bug**；如果需求不清，应记录为 **product / contract gap**。
- 为避免真实 Agent 并发互相影响，建议正向真实模型运行按账号和 report_type 串行或小批量执行。

| Case ID | 优先级 | 类别 | 执行账号 | schedule_type | report_type / run_kind | 操作 | 预期 / 核心验证 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| TC-SCH-POS-001 | P0 | 正向矩阵 | 308 employee | daily | personal_daily | preview + create + run-now | 成功创建并写回个人日报；period 为执行日全天 |
| TC-SCH-POS-002 | P1 | 正向矩阵 | 308 employee | weekly | personal_daily | preview + create + run-now | weekly schedule 可运行个人日报；period 仍为触发日全天 |
| TC-SCH-POS-003 | P0 | 正向矩阵 | 309 employee | daily | personal_weekly | preview + create + run-now | daily schedule 可运行个人周报；period 为当前自然周 |
| TC-SCH-POS-004 | P0 | 正向矩阵 | 309 employee | weekly | personal_weekly | preview + create + run-now | 成功创建并写回个人周报；period 为当前自然周 |
| TC-SCH-POS-005 | P0 | 正向矩阵 | 305 team_leader | daily | team_daily | preview + create + run-now | 成功创建并写回小组日报；target 为当前 TL 所属小组 |
| TC-SCH-POS-006 | P1 | 正向矩阵 | 305 team_leader | weekly | team_daily | preview + create + run-now | weekly schedule 可运行小组日报；target 不跨组 |
| TC-SCH-POS-007 | P1 | 正向矩阵 | 306 team_leader | daily | team_weekly | preview + create + run-now | daily schedule 可运行小组周报；period 为当前自然周 |
| TC-SCH-POS-008 | P0 | 正向矩阵 | 306 team_leader | weekly | team_weekly | preview + create + run-now | 成功创建并写回小组周报；target 为当前 TL 所属小组 |
| TC-SCH-POS-009 | P0 | 正向矩阵 | 304 director | daily | department_daily | preview + create + run-now | 成功创建并写回部门日报；target 为当前 director 部门 |
| TC-SCH-POS-010 | P1 | 正向矩阵 | 304 director | weekly | department_daily | preview + create + run-now | weekly schedule 可运行部门日报；period 为触发日全天 |
| TC-SCH-POS-011 | P1 | 正向矩阵 | 304 director | daily | department_weekly | preview + create + run-now | daily schedule 可运行部门周报；period 为当前自然周 |
| TC-SCH-POS-012 | P0 | 正向矩阵 | 304 director | weekly | department_weekly | preview + create + run-now | 成功创建并写回部门周报；target 为当前 director 部门 |
| TC-SCH-AUTO-013 | P0 | 自动触发 | 309 employee | daily | personal_daily | enabled=true，等 scanner 到点 | 自动生成 run；trigger_source=scheduled；next_run_at 推进 |
| TC-SCH-AUTO-014 | P1 | 自动触发 | 309 employee | weekly | personal_weekly | enabled=true，weekday=当天 | 自动生成个人周报；scheduled_trigger_at 等于旧 next_run_at |
| TC-SCH-AUTO-015 | P1 | 自动触发 | 305 team_leader | daily | team_daily | enabled=true，等 scanner 到点 | 自动生成小组日报；last_ai_run_id 更新 |
| TC-SCH-AUTO-016 | P1 | 自动触发 | 306 team_leader | weekly | team_weekly | enabled=true，weekday=当天 | 自动生成小组周报；next_run_at 推进到下周 |
| TC-SCH-AUTO-017 | P1 | 自动触发 | 304 director | daily | department_daily | enabled=true，等 scanner 到点 | 自动生成部门日报；period 为旧 next_run_at 当天 |
| TC-SCH-AUTO-018 | P1 | 自动触发 | 304 director | weekly | department_weekly | enabled=true，weekday=当天 | 自动生成部门周报；period 为旧 next_run_at 所在周 |
| TC-SCH-AUTO-019 | P0 | 自动触发 | 309 employee | daily | personal_daily | enabled=false，到点等待 | 不生成 run；last_ai_run_id 不变化 |
| TC-SCH-AUTO-020 | P1 | 自动触发 | 309 employee | daily | personal_daily | 上一轮 pending/running 时再次到点 | 新触发被 skip；last_skip_reason=上一轮运行尚未结束 |
| TC-SCH-LC-021 | P0 | 生命周期 | 309 employee | daily | personal_daily | create enabled=false | 创建成功；next_run_at 为空或不参与 scanner |
| TC-SCH-LC-022 | P0 | 生命周期 | 309 employee | daily | personal_daily | update enabled=true | next_run_at 基于当前时间重算 |
| TC-SCH-LC-023 | P0 | 生命周期 | 309 employee | daily | personal_daily | update time_of_day | next_run_at 随 time_of_day 重算 |
| TC-SCH-LC-024 | P1 | 生命周期 | 309 employee | weekly | personal_weekly | update weekdays | weekly weekdays 保存去重并重算 next_run_at |
| TC-SCH-LC-025 | P0 | 生命周期 | 309 employee | daily | personal_daily | disable 已启用任务 | enabled=false；scanner 不触发 |
| TC-SCH-LC-026 | P0 | 生命周期 | 309 employee | daily | personal_daily | run-now disabled schedule | 立即运行允许；trigger_source=manual；不改变 enabled |
| TC-SCH-LC-027 | P0 | 生命周期 | 309 employee | daily | personal_daily | save_and_run | trigger_source=save_and_run 或契约定义值；schedule 保存后运行 |
| TC-SCH-LC-028 | P0 | 生命周期 | 309 employee | daily | personal_daily | delete schedule | 列表不可见；run 历史不应被误删 |
| TC-SCH-LC-029 | P1 | 生命周期 | 309 employee | daily | personal_daily | list mine | 只返回当前用户 schedule，不串其他账号 |
| TC-SCH-LC-030 | P1 | 生命周期 | 305 team_leader | daily | team_daily | get/update/delete 他人 schedule_id | 返回 404/403；不能操作他人 schedule |
| TC-SCH-PERM-031 | P0 | 权限负向 | 307 employee | daily | team_daily | create | 创建阶段拒绝；不生成 schedule |
| TC-SCH-PERM-032 | P0 | 权限负向 | 307 employee | weekly | team_weekly | create | 创建阶段拒绝；不生成 schedule |
| TC-SCH-PERM-033 | P0 | 权限负向 | 307 employee | daily | department_daily | create | 创建阶段拒绝；不生成 schedule |
| TC-SCH-PERM-034 | P0 | 权限负向 | 307 employee | weekly | department_weekly | create | 创建阶段拒绝；不生成 schedule |
| TC-SCH-PERM-035 | P0 | 权限负向 | 305 team_leader | daily | department_daily | create | 创建阶段拒绝；不生成 schedule |
| TC-SCH-PERM-036 | P0 | 权限负向 | 305 team_leader | weekly | department_weekly | create | 创建阶段拒绝；不生成 schedule |
| TC-SCH-PERM-037 | P1 | 权限负向 | 304 director | daily | team_daily | create | 创建阶段拒绝，除非产品明确允许 director 代小组生成 |
| TC-SCH-PERM-038 | P1 | 权限负向 | 304 director | weekly | team_weekly | create | 创建阶段拒绝，除非产品明确允许 director 代小组生成 |
| TC-SCH-PERM-039 | P1 | 权限负向 | 307 employee | daily | team_daily | 若历史脏 schedule 存在则 run-now | 运行阶段拒绝；不写入 team_reports |
| TC-SCH-PERM-040 | P1 | 权限负向 | 305 team_leader | weekly | department_weekly | 若历史脏 schedule 存在则 run-now | 运行阶段拒绝；不写入 department_weekly_reports |
| TC-SCH-PERM-041 | P1 | 权限负向 | 无 team_id 用户 | daily | team_daily | create | 无法推导 target；明确错误 |
| TC-SCH-PERM-042 | P1 | 权限负向 | 非 director 用户 | daily | department_daily | create | 无法推导 department target；明确错误 |
| TC-SCH-VAL-043 | P0 | 输入校验 | 309 employee | weekly | personal_weekly | weekdays=[] | 创建失败；weekly 必须有 weekdays |
| TC-SCH-VAL-044 | P0 | 输入校验 | 309 employee | weekly | personal_weekly | weekdays=[0] | 创建失败；weekday 范围非法 |
| TC-SCH-VAL-045 | P0 | 输入校验 | 309 employee | weekly | personal_weekly | weekdays=[8] | 创建失败；weekday 范围非法 |
| TC-SCH-VAL-046 | P1 | 输入校验 | 309 employee | weekly | personal_weekly | weekdays=[1,1,2] | 创建成功且去重，或按契约拒绝重复；行为需明确 |
| TC-SCH-VAL-047 | P0 | 输入校验 | 309 employee | daily | personal_daily | time_of_day=24:00 | 创建失败；返回明确错误 |
| TC-SCH-VAL-048 | P0 | 输入校验 | 309 employee | daily | personal_daily | time_of_day=19:99 | 创建失败；返回明确错误 |
| TC-SCH-VAL-049 | P0 | 输入校验 | 309 employee | daily | personal_daily | time_of_day=9:00 | 创建失败，除非契约明确允许非补零 |
| TC-SCH-VAL-050 | P0 | 输入校验 | 309 employee | monthly | personal_daily | schedule_type 非 daily/weekly | 创建失败；返回明确错误 |
| TC-SCH-VAL-051 | P0 | 输入校验 | 309 employee | daily | invalid_report | report_type 非枚举 | 创建失败；返回 `REPORT_TYPE_NOT_SUPPORTED` 或等价错误 |
| TC-SCH-VAL-052 | P0 | 输入校验 | 309 employee | daily | generic_agent + Report Agent | run_kind 与 Agent 类型不匹配 | 创建失败；不能保存错误 run_kind |
| TC-SCH-VAL-053 | P0 | 输入校验 | 309 employee | daily | report_agent + 普通 Agent | run_kind 与 Agent 类型不匹配 | 创建失败；不能把普通 Agent 当 Report Agent |
| TC-SCH-VAL-054 | P0 | 输入校验 | 309 employee | daily | archived agent | agent_id 为归档 Agent | 创建失败；返回 agent archived / not found |
| TC-SCH-VAL-055 | P0 | 系统字段保护 | 309 employee | daily | personal_daily | start_prompt_values.report_type | 创建失败；保留字段不可覆盖 |
| TC-SCH-VAL-056 | P0 | 系统字段保护 | 309 employee | daily | personal_daily | start_prompt_values.scheduled_trigger_at | 创建失败；保留字段不可覆盖 |
| TC-SCH-VAL-057 | P0 | 系统字段保护 | 309 employee | daily | personal_daily | start_prompt_values.target_json | 创建失败；保留字段不可覆盖 |
| TC-SCH-VAL-058 | P0 | 系统字段保护 | 309 employee | daily | personal_daily | start_prompt_values.AIDA_REPORT_MCP_AUTH | 创建失败；凭据槽不可覆盖 |
| TC-SCH-OBS-059 | P1 | 状态与观测 | 309 employee | daily | personal_daily | Agent 提交失败或 fake agent | ai_run.status=failed；schedule.last_error 可定位原因 |
| TC-SCH-OBS-060 | P1 | 状态与观测 | 309 employee | daily | personal_daily | managed_session 长时间无写回 | ai_run 最终 timeout 或可观测；不得永久 running 无说明 |

### 8.13 定时任务并发专项 Case 矩阵

本节用于验证定时任务在多用户、多 schedule、重复触发和同周期写回时的行为。并发 case 需要记录并发度、请求时间窗口、成功/失败数量、所有 `schedule_id`、所有 `run_id`、最终 `ai_run.status`、残留 schedule 数量，以及是否出现重复触发、跨账号串写或报告元数据覆盖不符合预期。

| Case ID | 优先级 | 类别 | 并发形态 | 执行账号 | 操作 | 预期 / 核心验证 |
| --- | --- | --- | --- | --- | --- | --- |
| TC-SCH-CONC-061 | P0 | 并发创建 | 10 并发 | 309 employee | 同时创建 disabled personal_daily schedules | 全部返回 201；schedule_id 唯一；列表只显示当前用户任务；清理后残留为 0 |
| TC-SCH-CONC-062 | P0 | 并发创建 | 12 并发 | 305 team_leader | 同时创建 disabled team_daily schedules | 全部返回 201；run_kind=report_agent；report_config.report_type 不丢失；清理后残留为 0 |
| TC-SCH-CONC-063 | P1 | 跨账号创建 | 3 账号各 4 并发 | 304/305/309 | 同时创建各自 report_type schedule | schedule 只归属各自用户；list mine 不串账号 |
| TC-SCH-CONC-064 | P0 | 同一 schedule 并发 run-now | 5 并发 | 309 employee | 对同一个 disabled schedule 同时 POST runs | 不产生服务端崩溃；每个请求有明确 run 或明确 rejected；不得产生脏 schedule 状态 |
| TC-SCH-CONC-065 | P0 | 多 schedule 并发 run-now | 6 并发 | 304/305/306/308/309 | 6 类 report_type 各 1 个 schedule 同时 run-now | 6 类均可创建 run；不得出现 run_kind mismatch；input_ref.report_type/target/period 正确 |
| TC-SCH-CONC-066 | P1 | 自动触发互斥 | 1 schedule + 1 active run | 309 employee | 构造上一轮 running 后等待 scanner 到点 | 新触发被 skip；last_skip_reason 与 last_skipped_trigger_at 可观测 |
| TC-SCH-CONC-067 | P1 | 同周期重复写回 | 2 run 同周期 | 309 employee | 同一 report_type/period 连续或并发 run-now | 两个 ai_runs 都保留 output_ref/business_id；报告表允许最后一次覆盖，但不得丢 run 证据 |
| TC-SCH-CONC-068 | P1 | 更新与触发竞争 | update + scanner | 309 employee | 到点附近同时 update time_of_day/enabled 与 scanner 触发 | 不重复触发；next_run_at 保持可解释；失败时 last_error 明确 |
| TC-SCH-CONC-069 | P1 | 删除与触发竞争 | delete + scanner | 309 employee | 到点附近删除 enabled schedule | 不出现 orphan schedule；若已领取 run，run 历史保留且 schedule 删除成功 |
| TC-SCH-CONC-070 | P0 | 权限并发负向 | 8 并发 | 307 employee | 并发创建 team_daily / department_daily | 全部拒绝；不得生成任何 schedule 或报告 |
| TC-SCH-CONC-071 | P1 | 并发输入校验 | 8 并发 | 309 employee | 同时提交非法 time/weekdays/start_prompt_values | 全部按字段返回 400；不得因并发绕过校验 |
| TC-SCH-CONC-072 | P1 | 状态回收并发 | 多 running run | 309 employee | 多条 external_session_id running 超过 timeout 后同步 | 均进入 timeout 或明确终态；不得永久 running 无说明 |
| TC-SCH-CONC-073 | P0 | 同刻触发边界 | 50 个 due schedule | 304/305/306/308/309 | 50 个 enabled schedule 设置为同一 `next_run_at` 到期 | scheduler 单轮 `LIMIT 50` 边界不漏触发；50 个 schedule 均生成 `last_ai_run_id`；无重复触发；清理后残留为 0。可用 fake agent 做低成本边界模拟 |
| TC-SCH-CONC-074 | P0 | 50 真实模型同刻触发 | 50 个真实 Report Agent due schedule | 304/305/306/307/308/309 | 50 个 enabled report schedule 设置为同一 `next_run_at` 到期，使用默认 `MiniMax-M2.5` | 50 个 schedule 均创建真实 `external_session_id`；平台 session 可查询；最终 Aida 状态准确同步；成功写回的报告有 `business_id/report_id`，未写回的必须明确失败原因，不得误 timeout |
| TC-SCH-CONC-075 | P0 | 同刻触发扩容 | 100 个 due schedule | 304/305/306/307/308/309 | 100 个 enabled schedule 设置为同一 `next_run_at` 到期 | scheduler 至少分 2 轮完成领取；100 个 schedule 均生成 `last_ai_run_id`；无重复 run_id；清理后残留为 0 |
| TC-SCH-CONC-076 | P0 | 同刻触发批次边界 | 101 个 due schedule | 304/305/306/307/308/309 | 101 个 enabled schedule 设置为同一 `next_run_at` 到期 | 验证 `LIMIT 50` 后还有第 3 批；101 个 schedule 最终全部推进；不得只处理前 50 个 |
| TC-SCH-CONC-077 | P1 | 同刻触发混合频率 | 60 daily + 40 weekly | 304/305/306/308/309 | daily/weekly schedule 混合同刻到期 | 两类 schedule 均被领取；weekly `next_run_at` 推进到下一个命中 weekday |
| TC-SCH-CONC-078 | P1 | 同刻触发禁用混合 | 80 enabled + 20 disabled | 304/305/306/308/309 | enabled/disabled 混合同刻到期 | 只触发 80 个 enabled；disabled 不生成 `last_ai_run_id` |
| TC-SCH-CONC-079 | P1 | 同刻触发跨角色 | 100 due schedule | employee/TL/director | 各角色按允许 report_type 分布触发 | 每条 run 的 `input_ref_json.report_type/target` 与角色边界一致；不串角色 |
| TC-SCH-CONC-080 | P1 | 同刻触发失败可观测 | 100 fake-agent due schedule | 304/305/306/308/309 | 使用 fake agent 做失败路径观测 | 100 个 ai_run 均有明确 failed/error；schedule.last_error 可定位；不出现 pending 卡死 |
| TC-SCH-CONC-081 | P1 | 同刻触发清理恢复 | 100 due schedule | 304/305/306/308/309 | 触发完成后批量删除 schedule | schedule 残留为 0；ai_run 历史保留；删除不影响已生成 run |
| TC-SCH-CONC-082 | P0 | 并发 run-now 放大 | 20 并发 | 309 employee | 对同一 disabled schedule 同时 POST runs | 服务端无 panic；每个请求返回明确状态；不得破坏 schedule enabled/next_run_at |
| TC-SCH-CONC-083 | P0 | 多 schedule run-now 放大 | 30 并发 | 304/305/306/308/309 | 6 类 report_type 多 schedule 同时 run-now | 所有可执行请求均创建 ai_run；不得出现 run_kind mismatch；失败必须可解释 |
| TC-SCH-CONC-084 | P1 | 并发 create/update/delete | 30 并发 | 309 employee | 同时创建、更新时间、删除 disabled schedules | 最终列表无脏数据；更新不丢 run_kind/report_config；删除不影响其他账号 |
| TC-SCH-CONC-085 | P1 | 并发权限负向放大 | 30 并发 | 307 employee | 并发创建 team/department 报告 schedule | 全部拒绝；不得生成 schedule、ai_run 或报告表记录 |
| TC-SCH-CONC-086 | P1 | 并发校验负向放大 | 30 并发 | 309 employee | 非法 time/weekdays/report_type/保留字段混合提交 | 全部按字段返回 400；不得因并发绕过校验 |

### 8.14 定时任务状态同步与本轮修复定向 Case 矩阵

本节是本轮 `external_session_id` 状态同步 bug 的定向回归重点。执行时必须同时记录 Aida `ai_runs` 字段、Agent 平台 `/api/task/{external_session_id}/status` 返回、报告表写回关系，以及 syncer 最终状态。

| Case ID | 优先级 | 类别 | 初始数据 | 平台状态 | 操作 | 预期 / 核心验证 |
| --- | --- | --- | --- | --- | --- | --- |
| TC-SCH-SYNC-086 | P0 | external_session_id completed | `external_task_id=''`，`external_session_id` 有值，`business_type=scheduled_agent_run` | completed | 等 syncer tick | Aida run 从 `running` 变为 `succeeded`；`output_ref_json.session_id` 保留；不再 timeout |
| TC-SCH-SYNC-087 | P0 | report completed 且已写回 | `business_type=report_agent_run`，`business_id` 非空 | completed | 等 syncer tick | Aida run 可为 `succeeded`；不得被误判为写回缺失 |
| TC-SCH-SYNC-088 | P0 | report completed 但无写回 | `business_type=report_agent_run`，`business_id` 空，`output_ref_json.report_id` 空 | completed 且超过 2 分钟 | 等 syncer tick | Aida run 变为 `failed`；错误为 `managed agent session completed without report writeback` |
| TC-SCH-SYNC-089 | P0 | report completed 写回缓冲 | 同上 | completed 且未超过 2 分钟 | 等 syncer tick | Aida run 保持 `running`，给 MCP 写回留缓冲；不得立即失败 |
| TC-SCH-SYNC-090 | P0 | platform failed | `external_session_id` 有值 | failed 且 error 非空 | 等 syncer tick | Aida run 变为 `failed`；`error_message/output_ref_json.error` 保留平台错误 |
| TC-SCH-SYNC-091 | P1 | platform cancelled | `external_session_id` 有值 | cancelled | 等 syncer tick | Aida run 变为 `failed`；状态不再长期 running |
| TC-SCH-SYNC-092 | P1 | platform running | `external_session_id` 有值 | running | 等 syncer tick | Aida run 保持 `running`；不误标 terminal |
| TC-SCH-SYNC-093 | P1 | platform queued/pending | `external_session_id` 有值 | queued/pending | 等 syncer tick | Aida run 归一为 `running`；超时前不失败 |
| TC-SCH-SYNC-094 | P1 | platform 查询失败未超时 | `external_session_id` 有值，started_at 未超时 | HTTP 5xx/网络失败 | 等 syncer tick | Aida run 保持 pending/running；记录日志但不提前 timeout |
| TC-SCH-SYNC-095 | P0 | platform 查询失败已超时 | `external_session_id` 有值，started_at 超过 2h | HTTP 5xx/网络失败 | 等 syncer tick | Aida run 变为 `timeout`，错误包含刷新状态失败原因 |
| TC-SCH-SYNC-096 | P0 | pending submit timeout | `external_task_id=''` 且 `external_session_id=''`，status=pending 超过 10m | 无 | 等 syncer tick | Aida run 变为 `timeout`；错误为 pending submit timeout |
| TC-SCH-SYNC-097 | P0 | terminal 保护 | status 已是 `succeeded/failed/timeout` | 任意 | 等 syncer tick | syncer 不回扫 terminal run；不覆盖人工/历史结论 |
| TC-SCH-SYNC-098 | P1 | MCP 同时写回竞态 | syncer 查询期间 MCP 将 run 写为 `succeeded` | completed | 构造并发 | syncer 更新 SQL 不覆盖 terminal succeeded；最终仍为 MCP 写回的 succeeded |
| TC-SCH-SYNC-099 | P1 | output_ref report_id 兼容 | `business_id` 空但 `output_ref_json.report_id` 有值 | completed | 等 syncer tick | 判定为已写回；不得误报 writeback missing |
| TC-SCH-SYNC-100 | P0 | t02 历史复现样本 | 使用已知 completed session id 构造临时 generic run | completed | 等 syncer tick 后删除临时 run | 临时 run 成功同步为 `succeeded`；证明本轮 bug 主路径修复 |

### 8.15 定时任务规模、组合与观测扩展 Case 矩阵

| Case ID | 优先级 | 类别 | 数据规模 | 执行账号 | 操作 | 预期 / 核心验证 |
| --- | --- | --- | --- | --- | --- | --- |
| TC-SCH-SCALE-101 | P0 | 100 用例覆盖计数 | 文档层 | - | 统计 `TC-SCH-*` | 定时任务 case 数不少于 100；P0/P1 覆盖完整 |
| TC-SCH-SCALE-102 | P1 | 100 schedule 创建清理 | 100 disabled schedules | 309 employee | API 创建后批量删除 | 全部创建成功；ID 唯一；删除后残留 0 |
| TC-SCH-SCALE-103 | P1 | 100 schedule 列表分页 | 100 disabled schedules | 309 employee | list 分页/筛选 | 分页总数正确；不串其他账号；排序稳定 |
| TC-SCH-SCALE-104 | P1 | 100 schedule 更新 | 100 disabled schedules | 309 employee | 批量更新时间/启用状态 | 更新后字段一致；`next_run_at` 重算正确；无部分丢字段 |
| TC-SCH-SCALE-105 | P1 | 6 类 report_type 均衡覆盖 | 每类至少 10 条 | 304/305/306/308/309 | 创建/预览/删除 | 6 类 report_type 均至少覆盖 10 次；角色边界正确 |
| TC-SCH-SCALE-106 | P1 | 角色矩阵覆盖 | employee/TL/director | 304/305/306/307/308/309 | 正向+负向组合 | employee/TL/director 允许与禁止的 report_type 均覆盖 |
| TC-SCH-SCALE-107 | P1 | 时区固定契约 | 多 time_of_day | 309 employee | preview/create | `timezone=Asia/Shanghai` 下 next_run_at 稳定；不受服务器 UTC 展示误导 |
| TC-SCH-SCALE-108 | P1 | 周期边界 | 周日/周一 weekly | 309 employee | preview weekly | weekday 计算按 ISO 1-7；周日不被误当 0 |
| TC-SCH-SCALE-109 | P1 | 失败恢复 | fake-agent failed 后更新为有效 agent | 309 employee | 失败后更新 schedule 并 run-now | last_error 可被新运行覆盖；历史 failed run 保留 |
| TC-SCH-SCALE-110 | P1 | 报告元数据追踪 | 多 scheduled run | 304/305/309 | 检查 output_ref/input_ref | `trigger_source/scheduled_trigger_at/schedule_id/schedule_name/report_type/target/period` 均可追踪 |
| TC-SCH-SCALE-111 | P1 | 清理审计 | 全量测试后 | 全账号 | 查 schedule/ai_run/report 残留 | 测试创建的 schedule 全部清理；ai_run/report 作为历史按预期保留 |

执行优先级建议：

- `P0`：本轮必须跑，覆盖 6 类 report_type、核心权限、关键非法输入和生命周期。
- `P1`：本轮建议跑，覆盖自动触发、跨频率组合、状态观测和历史脏数据防护。
- `P2`：当前表内暂未列 P2；如后续扩展 UI 自动化、性能或多租户压测，可另起 P2。

### 8.16 前端页面抽样用例

目标：

- 验证页面没有把底层运行字段暴露给用户。
- 验证普通 Agent 与 Report Agent 的表单分支正确。
- 验证前端保存 payload 与后端契约一致。

Report Agent 新建 / 编辑页至少检查：

- 页面按“基础信息 / 选择 Agent / 定时器配置 / Agent 运行配置 / 预览”组织。
- Report Agent 展示 6 种报告类型。
- `daily schedule` 可以选择任意 `report_type`。
- `weekly schedule` 可以选择任意 `report_type`。
- 预览展示 `next_run_at`、报告对象摘要、预计报告周期。
- 不展示报告对象选择器、报告日期、报告周期规则、时区、JSON 参数编辑框、cron、队列、并发、重试。
- 不展示 `period_json / target_json / period_start / period_end / scheduled_trigger_at / run_id / mcp_url / credential / credential_slot / AIDA_REPORT_MCP_AUTH`。
- 保存 payload 不提交上述系统字段。

普通 Agent 新建 / 编辑页至少检查：

- 只展示模型、Initial Message、Start Prompt Values。
- 不展示报告类型、报告对象、报告周期、Report MCP 相关字段。
- `run_kind` 应为 `generic_agent`。

列表页至少检查：

- 展示任务名称、Agent、Agent 类型、触发规则、下次运行、最近结果、启用状态。
- 操作包含运行、编辑、停用 / 启用、删除。
- `last_skip_reason`、`failed`、`timeout` 等状态有可识别展示。

如果页面展示了禁止字段，或 UI 隐藏了字段但保存 payload 仍提交系统字段，应记录为 **实现缺陷**。

### 8.17 系统报告 Skill 与历史 Owner Bug 定向 Case

本节是系统报告 Skill 集中管理改动的 P0 回归集合。除接口状态外，必须记录 Agent 平台返回的 Skill owner、默认 Agent 实际 SkillRef，以及失败场景是否发生 Agent Create/PUT 或普通用户 Skill Create。

| Case ID | 优先级 | 类别 | 前置条件 / 操作 | 预期 / 核心验证 |
| --- | --- | --- | --- | --- |
| TC-SYS-SKILL-001 | P0 | 开发系统资产存在 | 用普通账号查询 `include_system=true` | 返回且只返回一份 `100866/aida-report@1.0.0` 系统 Skill |
| TC-SYS-SKILL-002 | P0 | 环境隔离 | 14.157 同时可见公共 `100866` 与 `10086` 资产 | 后端只注入 `100866`；不得选择生产系统 Skill |
| TC-SYS-SKILL-003 | P0 | 普通用户无副本 | 普通账号 `scope=mine` 不存在 `aida-report`，创建默认 Agent | 创建成功；用户 Registry 中仍不产生 `aida-report` |
| TC-SYS-SKILL-004 | P0 | 默认 Agent 引用 | 创建默认报告 Agent 后查询 Agent | SkillRef 严格等于 Agent 平台返回的 `100866/aida-report@1.0.0` |
| TC-SYS-SKILL-005 | P0 | 本地用户名不一致回归 | Aida 本地 username 与 Agent 平台当前用户 username 不同 | 系统 Skill 引用仍为 `100866`；不得写入当前用户名 |
| TC-SYS-SKILL-006 | P0 | 配置 owner 错误 | 临时配置不存在的 owner 后调用默认 Agent 创建 | 返回系统资源配置错误；没有 Agent Create/PUT；没有用户 Skill Create |
| TC-SYS-SKILL-007 | P0 | 平台 owner 为空 | Mock 公共 Skill 返回匹配 slug/version 但 owner 为空 | 明确失败；禁止 fallback 到当前用户名或用户 ID |
| TC-SYS-SKILL-008 | P0 | 系统 Skill 缺失 | Mock/隔离环境公共列表无精确资产 | 明确失败；禁止创建普通用户 `aida-report` 副本 |
| TC-SYS-SKILL-009 | P1 | 系统 Skill 归档 | 目标版本不出现在公共列表 | 创建、修复和运行均在写 Agent 前失败 |
| TC-SYS-SKILL-010 | P0 | 历史错误 owner 修复 | 默认 Agent 已引用 `001898/aida-report@1.0.0` | 先成功解析系统 Skill，再 PUT 修复为平台返回的 owner；随后运行成功 |
| TC-SYS-SKILL-011 | P0 | 失败不污染 | 现有 Agent 可查询，但系统 Skill 解析失败 | Agent 原 SkillRef 保持不变，不能先修复后报错 |
| TC-SYS-SKILL-012 | P0 | 手动运行 | 普通用户调用报告 Agent 手动运行 | Session 成功创建，平台不返回 `SKILL_NOT_FOUND`，报告正常写回 |
| TC-SYS-SKILL-013 | P0 | 定时运行 | 定时报告任务 run-now | 定时路径完成相同的系统 Skill 解析/修复；Session 成功且正常写回 |
| TC-SYS-SKILL-014 | P0 | 手动与定时一致 | 同一 Agent 分别手动运行和定时运行 | 两次运行使用相同系统 SkillRef，不出现一条成功一条 `SKILL_NOT_FOUND` |
| TC-SYS-SKILL-015 | P0 | Skill 内容读取 | 普通用户读取系统 Skill `skill-md` 并运行 | `skill-md` 非 404；运行时 Skill 正常物化 |
| TC-SYS-SKILL-016 | P0 | MCP 身份隔离 | 两个普通用户引用同一系统 Skill 分别生成个人报告 | MCP 只返回各自权限内数据；不得使用 `100866` Token 读取业务数据 |
| TC-SYS-SKILL-017 | P0 | 内联 MCP 边界 | 查询默认 Agent 配置 | 存在内联 `aida-report-mcp` 和 `AIDA_REPORT_MCP_AUTH`；不存在系统 Registry MCP Binding |
| TC-SYS-SKILL-018 | P1 | 重复创建幂等 | 同一用户连续调用两次默认 Agent 创建 | 复用同一个默认 Agent；不新增系统 Skill 或用户副本 |
| TC-SYS-SKILL-019 | P1 | 多用户共享 | 至少 3 个角色账号创建默认 Agent | 三个 Agent 均引用同一系统 Skill；Agent owner 和业务权限仍各自隔离 |
| TC-SYS-SKILL-020 | P1 | 50 个定时并发共享 | 50 个同刻到期报告 schedule 使用同一系统 Skill | 不因公共 Skill 查询或修复发生串 owner、重复 Skill、404/`SKILL_NOT_FOUND` |
| TC-SYS-SKILL-021 | P1 | 普通自定义 Skill | 用户创建非保留 slug 的自定义 Skill/Agent | 创建、编辑、归档和运行行为不受系统 Skill 改动影响 |
| TC-SYS-SKILL-022 | P1 | 配置缺失 | owner 配置为空后访问系统列表、创建、手动运行和定时运行 | 均返回一致、可识别的配置错误，不发生 fallback |

其中 `TC-SYS-SKILL-005`、`006`、`007`、`010`、`011`、`013`、`015` 是本轮历史 Bug 和新增调用路径的定向测试，不能只用普通成功运行代替。

---

## 9. 负向与边界用例

以下负向用例建议至少覆盖一次。

### 9.1 角色权限不匹配

用例：

- employee 手动运行或创建 schedule 使用 `team_daily`
- employee 手动运行或创建 schedule 使用 `department_daily`
- TL 手动运行或创建 schedule 使用 `department_weekly`
- director 手动运行或创建 schedule 使用 `team_weekly`，除非产品明确允许 director 代小组生成

预期：

- 手动运行应直接失败，不生成 `ai_run`，或生成失败态 `ai_run` 且错误明确。
- 创建 schedule 应直接失败，或保存后运行阶段失败且错误明确；推荐创建阶段失败。
- 返回 400 / 403 或明确业务错误信息。
- 不生成脏 schedule，不写入错误报告。

### 9.2 系统变量覆盖

对直接手动 Report Agent Run 和 Report Agent schedule 创建，故意在 `start_prompt_values` 中提交：

- `report_type`
- `period_json`
- `target_json`
- `period_start`
- `period_end`
- `scheduled_trigger_at`
- `run_id`
- `mcp_url`
- `credential`
- `credential_slot`
- `AIDA_REPORT_MCP_AUTH`

预期：

- 直接手动运行应失败，或创建 schedule 应失败。
- 返回保留字段错误。

### 9.3 Agent 不存在

使用一个假的 `agent_id` 直接手动运行 Report Agent，并使用同一个假的 `agent_id` 创建 schedule。

预期：

- 手动运行和创建 schedule 均失败。
- 返回 `agent not found` 或明确等价错误。

### 9.4 调度超时与失败展示

如果环境允许，可额外制造 1 条失败 schedule，例如：

- 先创建 schedule
- 再让其绑定的 Agent 不可用

预期：

- `last_run_status = failed` 或 `timeout`
- `last_error` 有明确原因
- 不影响其他 schedule 正常执行
- 如果失败发生在 Agent 平台提交阶段，`ai_runs.error_message` 与 `schedule.last_error` 都应可定位原因

### 9.5 调度参数与 Agent 类型边界

至少覆盖：

- `run_kind=report_agent` 绑定普通 Agent。
- `run_kind=generic_agent` 绑定 Report Agent。
- `schedule_type=weekly` 但 `weekdays` 为空。
- `weekdays` 包含 0、8 或重复值。
- `time_of_day` 非 `HH:mm`，例如 `24:00`、`9:00`、`19:99`。
- `agent_id` 为已归档 Agent。

预期：

- 创建或编辑失败。
- 返回明确错误。
- 不生成脏 schedule。
- 如果实际代码接受非法配置，应记录为实现缺陷，而不是调整测试期望。

### 9.6 报告对象无法推导

针对 Report Agent schedule，构造或查找无有效组织关系的账号，至少覆盖：

- 无 `team_id` 用户创建 `team_daily` / `team_weekly`。
- 非 director 用户创建 `department_daily` / `department_weekly`。
- director 账号无法推导所属部门或部门成员范围。

预期：

- 创建或运行阶段返回明确错误。
- 前端不应出现“报告对象选择器”让用户手工绕过。
- 如果无法通过现有测试账号构造，应记录为 **测试数据缺口**。

### 9.7 Report MCP 回写保护与幂等

本组用例优先通过真实 Report Agent run 产生的 MCP credential 或后端集成测试验证；不要在共享环境手工伪造 Report MCP 回写。

至少覆盖：

- `run_id` 不存在，`write_report_result` 被拒绝。
- `run_id` 属于其他用户，`write_report_result` 被拒绝。
- `report_type` 与 run 中记录不一致，回写被拒绝。
- `target` 与 run 中记录不一致，回写被拒绝。
- `period` 与 run 中记录不一致，回写被拒绝。
- `ai_run.status=failed` 或 `timeout` 后，回写被拒绝。
- 同一个 succeeded run 使用相同内容重复回写，返回幂等成功，不覆盖内容。
- 同一个 succeeded run 使用不同内容重复回写，返回冲突或拒绝。

如果当前公开测试手段无法触达这些验证点，应在执行记录中标记为 **可观测性 / 测试能力缺口**，不能视为通过。

### 9.8 报告内容确定性与事实边界

| Case ID | 优先级 | 场景 | 操作 | 预期 |
| --- | --- | --- | --- | --- |
| TC-CONTENT-001 | P0 | 日报星期正确 | 生成 `2026-06-08` 日报并要求显示星期 | 只能显示周一；星期来自 Aida 注入的 `calendar_context` |
| TC-CONTENT-002 | P0 | 周报周期星期正确 | 生成 `2026-06-08` 至 `2026-06-14` 周报 | 起止日期分别为周一、周日；逐日星期无错误 |
| TC-CONTENT-003 | P0 | 错误星期拒绝 | 使用有效 run_id 调用 `write_report_result`，正文传入 `2026-06-08（周日）` | 返回 `REPORT_CONTENT_INVALID`；报告和 run 状态不被破坏 |
| TC-CONTENT-004 | P0 | 无括号错误星期拒绝 | 正文传入 `2026-06-08 周日` | 同样返回 `REPORT_CONTENT_INVALID` |
| TC-CONTENT-005 | P0 | 内部字段泄漏 | 正文包含 `user_id/team_id/department_id/report_id/session_id/run_id` | 返回 `REPORT_CONTENT_INVALID` |
| TC-CONTENT-006 | P0 | 中文 ID/编号泄漏 | 正文包含“用户ID”“团队ID”“部门编号”“总监ID” | 返回 `REPORT_CONTENT_INVALID` |
| TC-CONTENT-007 | P0 | UUID 泄漏 | 正文包含用户、小组、部门或报告 UUID | 返回 `REPORT_CONTENT_INVALID` |
| TC-CONTENT-008 | P0 | 同 run 修正重试 | 先提交非法正文，再用相同 run_id 提交正确正文 | 第二次成功；若正确内容已写入则返回幂等成功 |
| TC-CONTENT-009 | P0 | 部门显示名称 | 生成部门日报和周报 | 使用 MCP `department_name`；缺失时显示“部门”，不展示部门 ID |
| TC-CONTENT-010 | P0 | 名册与活动分离 | 部门名册 10 人，其中 8 人有下级报告、2 人缺失 | 可写“名册10人、已提交8人、缺失2人”；不得写“10人全部在岗/全员参与” |
| TC-CONTENT-011 | P0 | 缺失事实传递 | 下级小组报告明确成员缺失 | 部门报告保留缺失姓名和无报告事实，不得在汇总中抹除 |
| TC-CONTENT-012 | P0 | 报告覆盖与成员活动分离 | 2/2 小组报告均已提交，但部分成员无个人报告 | 只能表述“小组报告覆盖2/2”；不得推导所有成员有活动 |

以上内容校验必须同时覆盖自动化测试和至少一轮真实模型报告。内容由模型生成不构成降低验收标准的理由；若错误正文被后端接受，应记为实现缺陷。

---

## 10. 结果验证口径

### 10.1 调度层

每个定时任务正向用例至少验证：

- schedule 创建成功。
- `next_run_at` 合法。
- `manual` / `save_and_run` 不应错误使用 `next_run_at` 作为报告归属时间。
- `scheduled` 自动触发时，`scheduled_trigger_at` 应等于触发前旧 `next_run_at`，且 `next_run_at` 应立即推进。
- 手动运行或自动触发后：
  - `last_ai_run_id` 已更新。
  - `last_run_status` 进入 `running` 或 `succeeded`。
  - 失败时 `last_error` 有明确文案。
  - disabled schedule 不应被 scanner 触发。

### 10.2 运行层

每个 run 至少验证：

- `user_id` 与当前执行用户一致。
- `agent_id` 与本次选择的 Report Agent 或 schedule 绑定 Agent 一致。
- `trigger_source` 正确：直接手动 Report Agent Run 为 `manual`；定时任务立即运行为 `manual` / `save_and_run`；自动触发为 `scheduled`。
- 直接手动 Report Agent Run 不要求 `schedule_id` 或 `scheduled_trigger_at`，但 `period` 必须等于请求 payload。
- 定时任务 run 必须有 `schedule_id` 和 `scheduled_trigger_at`，且 `period` 由 `report_type + scheduled_trigger_at` 推导。
- `report_type` 正确。
- `target` 正确：直接手动 Run 按当前用户和 payload target 推导；定时任务 run 按 `schedule.user_id + report_type` 推导。
- `external_session_id` 或 `external_task_id` 存在。
- Report Agent run 不应在可见字段中泄露原始 credential。

### 10.3 报告写回层

每个正向 case 至少验证：

- 对应报告查询接口能读到内容。
- 报告时间 / 周期与预期一致。
- `managed_agent_run_id` 或等价字段指向本次 run。
- 写回目标没有串人、串组、串部门。
- Report MCP `write_report_result` 成功后，`ai_run.status` 最终为 `succeeded`。

### 10.4 轻度质量检查

Codex 需要对生成内容做轻度合理性检查，不要求人工文学评审，但至少检查：

1. 内容非空：
   - 不应为空字符串
   - 不应只有标题
2. 无系统参数泄露：
   - 不出现 `run_id`
   - 不出现 `mcp_url`
   - 不出现 `target_json`
   - 不出现 `period_json`
   - 不出现 `AIDA_REPORT_MCP_AUTH`
3. 无明显模板残留：
   - 不出现 `{{`
   - 不出现 `}}`
   - 不出现 `TODO`
4. 范围合理：
   - 个人报告不应写成小组 / 部门口径
   - 小组报告不应写成部门口径
   - 部门报告不应只覆盖单个小组
5. 日期 / 周期合理：
   - 直接手动 Report Agent Run 应对应请求 payload 中的 `period`，可为 `seed_date` 或 `seed_week_start ~ seed_week_end`。
   - 定时任务日报应对应 `execution_date`。
   - 定时任务周报应对应 `execution_week_start ~ execution_week_end`。
6. 如果前置 session 明确存在：
   - 报告不应整篇只有“暂无记录”
7. 日期与星期确定：
   - 星期必须与报告日期匹配
   - 错误星期必须被 `write_report_result` 拒绝
8. 展示身份安全：
   - 正文不得出现内部 ID 字段、中文 ID/编号标签或 UUID
   - 小组、部门、负责人和成员只使用展示名称
9. 覆盖率事实边界：
   - 名册总数、报告提交数和有活动成员数必须分开表达
   - 下级报告存在不代表下级全员有活动
   - 缺失成员不得在上级汇总中被省略或改写为全员参与

建议额外抽查：

- 小组日报 / 周报内容中，至少出现一个该组成员昵称或工作项痕迹
- 部门日报 / 周报内容中，至少体现两个小组的汇总痕迹，例如 `小组A / 小组B`

如果内容结构基本正确，但表达较泛，可记为：

- `warning`

如果出现范围错误、日期错误、系统变量泄露、整篇空洞模板，则记为：

- `fail`

### 10.5 缺陷判定口径

执行记录中必须区分下面几类结果：

- `pass`：行为符合本测试用例和 [定时任务.md](/home/intellif/dev/project_manager/doc/定时任务.md) 的契约。
- `warning`：主链路成功，但内容质量泛、环境数据稀疏或可观测性不足。
- `implementation bug`：当前代码行为与契约不一致。
- `product / contract gap`：开发文档和实际产品需求不一致，或契约没有定义清楚。
- `test data gap`：测试账号、session、组织关系等前置数据不足。
- `observability gap`：行为可能正确，但公开 API / 日志 / 字段不足以证明。

不得把“当前代码就是这样”直接记为通过。若代码不合理，应明确记录 bug 或缺口，并保留接口返回、字段值、run id、schedule id 作为证据。

---

## 11. 建议的 Codex 执行产物

Codex 执行完本测试后，建议额外输出一份执行记录，文件建议命名为：

- `doc/报告任务测试执行记录.md`

执行记录建议包含：

- 实际使用的 `execution_date`
- 实际使用的 `execution_week_start` / `execution_week_end`
- 实际使用的 `seed_date`
- 实际使用的 `seed_week_start` / `seed_week_end`
- 每个 Case 的：
  - 执行账号
  - schedule id（定时任务 case 必填，直接手动 Report Agent Run 可为空）
  - run id
  - trigger_source
  - scheduled_trigger_at（定时任务 case 必填，直接手动 Report Agent Run 可为空）
  - 期望 report_type / target / period
  - 实际 report_type / target / period
  - 最终状态
  - 报告查询结果
  - 质量检查结论
  - 缺陷归类：implementation bug / product gap / test data gap / observability gap
- 最终结论：
  - pass
  - pass with warnings
  - fail

---

## 12. 最低通过标准

本轮测试至少满足下面条件，才可认为“Report Agent 手动与定时任务报告链路基本可用”：

1. 直接手动 Report Agent Run 覆盖 6 类 `report_type`，且全部至少成功 1 次。
2. 定时任务 Report Agent 覆盖 6 类 `report_type`，且全部至少成功 1 次。
3. 至少 1 个 personal、1 个 team 或 department schedule 完成自动触发验证。
4. 创建、编辑、启停、删除都至少通过 1 次。
5. 至少 2 个 `schedule_type / report_type` 解耦 case 通过。
6. 若本轮目标是“定时任务专项完整测试”，`TC-SCH-*` 用例至少执行 50 条，其中所有 `P0` 必须执行；未执行的 P1/P2 必须在执行记录中标记 `not run` 和原因。若包含并发专项，`TC-SCH-CONC-*` 至少执行 8 条，其中所有 `P0` 必须执行。
7. 角色 / report_type 正向矩阵和负向矩阵至少覆盖 employee、team_leader、director 三类角色。
8. 至少 2 个负向 case 返回正确错误，其中至少 1 个覆盖权限 / 目标推导。
9. 至少 1 个失败或超时 case 能正确反映 `ai_runs` 与 schedule 状态。
10. Report MCP 回写保护至少通过 1 个幂等或拒绝类验证；若无法验证，应记录可观测性缺口，不能算完全通过。
11. 所有正向 case 的报告结果都通过轻度合理性检查。
12. 不出现跨人、跨组、跨部门写回错误。
13. `TC-SYS-SKILL-*` 所有 P0 Case 必须通过；普通用户 Registry 中不得新增系统报告 Skill 副本。
14. 手动与定时运行都必须使用平台确认的同一系统 SkillRef，且不出现 Skill 404/`SKILL_NOT_FOUND`。
15. `TC-CONTENT-*` 所有 P0 Case 必须通过；错误星期、内部 ID/UUID 泄漏和名册人数过度推断任一出现都不得判定为发布通过。

如果未达到以上标准，应在执行记录中明确标注：

- 是产品 / 契约口径问题
- 是实现缺陷
- 是测试环境数据不足
- 还是可观测性 / 测试能力不足
