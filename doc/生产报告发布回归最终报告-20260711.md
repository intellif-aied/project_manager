# 生产报告发布回归最终报告

## 发布结论

- 结论：通过。
- 代码提交：`0be0f4e` (`fix: validate aggregate report coverage`)。
- 生产镜像：`20260711-0be0f4e-report-content`。
- 生产地址：`http://113.100.143.91:9180`，内外部健康检查均通过。
- 数据边界：仅使用 t05、t07、t08、t09、t10 测试账号和 `2026-05-18` 隔离日期，未使用真实员工账号。

## 资产与配置

- 生产 Skill：`10086/aida-report@1.0.0`。
- SkillID：`37630dde-98a8-4ae5-ba5e-e7c63d77bfd3`。
- Skill SHA256：`83f7cd8c3c8534998a4eb1a9c98dc2e14fee2f5f63303275cf5497879b5e8cc9`。
- API 与 Web 容器均使用最终镜像标签；PostgreSQL 和 MinIO 未重建。

## 回归过程

1. 首轮生产六类报告共执行 10 个真实模型任务，任务执行和报告写回均成功。
2. 人工内容审查发现部门日报未正确汇总成员报告，证明仅校验任务成功状态不足以判定内容正确。
3. 根因是 Agent 调用 `get_daily_reports` 时遗漏 `include_content`，MCP 默认只返回元数据，模型因而在没有源报告正文的情况下产生错误结论。
4. `d9bace0` 将 `include_content` 省略语义改为默认包含正文，显式 `false` 仍可只取元数据。
5. `d48e89a` 增加源报告数量与生成正文的一致性校验，拒绝“实际有报告但声称 0 份/无报告”。
6. `0be0f4e` 继续拒绝覆盖不完整时的“全员参与”类表述，并禁止日报夹带周报提交/缺失统计。

## 最终定向验证

- 报告类型：`department_daily`。
- Agent run ID：`82e3d7d2-11f2-460a-9482-48be2e88a951`。
- Agent platform session ID：`5af33e91-a958-4a23-a129-78895fa14b7a`。
- 结果：运行 `succeeded`，报告读回 HTTP 200，产品状态 `saved`。
- 内容校验：名册 4 人，个人日报实际提交 3 人，明确标识测试09缺失 1 人。
- 负向校验：未出现“无个人日报”、“个人日报0份”、“全员参与”、“全部在岗”或个人/小组周报缺失统计。

## 最终判定

本轮修复同时覆盖数据获取默认值、源数据一致性和聚合报告覆盖语义。最终生产真实模型定向回归通过，可作为当前生产基线。
