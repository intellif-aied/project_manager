# 系统报告 Skill 集中管理开发测试报告

## 基本信息

- 执行日期：2026-07-10
- 环境：14.157 开发环境
- Aida API：`http://192.168.14.157:18090`
- 页面代理：`http://192.168.14.157:5173`
- Agent 平台：`http://192.168.18.107:3081`
- 生产环境：未部署、未修改

## 系统资源

| 项目 | 实际值 |
| --- | --- |
| AIHub 显示名 | `aida-test` |
| AIHub username / Registry owner | `100866` |
| SkillRef | `100866/aida-report@1.0.0` |
| SkillID | `dca4fddb-68e9-4284-831f-a8e5e3a5180f` |
| SHA256 | `271bee73dc110305fc8e3ec22e5347bc1e2a56ef29f4103db5f9db1e673806aa` |
| Skill 文件读取 | HTTP 200，包含 `## Workflow` |
| 开发容器 owner 配置 | `MANAGED_AGENT_REPORT_SKILL_OWNER=100866` |

公共 Registry 中精确匹配结果：

- `100866/aida-report@1.0.0`：1 条。
- `10086/aida-report@1.0.0`：0 条。

这证明本轮只准备了开发系统 Skill，没有创建或修改生产系统 Skill。

## 自动化验证

执行：

```text
cd /home/intellif/dev/project_manager/api
go test ./... -count=1
go vet ./...
```

结果：全部通过。

新增或强化的关键断言：

- owner 配置缺失时不调用 Agent 平台。
- 公共 Skill owner 为空时拒绝 fallback。
- 系统 Skill 缺失或归档时拒绝使用。
- 解析失败时不发送 Agent PUT，现有错误 SkillRef 不被污染。
- 默认 Agent 创建不再调用用户 Skill Create。
- 资源选择器只合并配置指定的系统 Skill。
- 手动和定时运行均进入同一系统 Skill 解析/修复逻辑。

## 真实 E2E

### 普通用户 t05

资源选择器验证：

- `GET /ai-assets/skills?scope=mine&include_system=true` 返回唯一系统 Skill `100866/aida-report@1.0.0`。
- `GET /ai-assets/skills/100866/aida-report/1.0.0/skill-md` 返回 200。
- 新建默认 Agent：`aida-agent-djuynrgfrsck`。
- 连续创建两次返回相同 Agent ID，未创建重复 Agent。
- Agent SkillRef 为 `100866/aida-report@1.0.0`。
- MCP 使用内联 `aida-report-mcp` 和 `AIDA_REPORT_MCP_AUTH`，Registry MCP Binding 为空。

手动报告运行：

| 字段 | 结果 |
| --- | --- |
| run_id | `b3589ebe-c97a-4639-94bd-c7fe93321874` |
| external_session_id | `e01ee194-a468-4c45-8a72-f6ed1a22c93b` |
| 最终状态 | `succeeded` |
| report_id | `0f7ec220-7d8e-4318-b1c0-7246e8635771` |
| 报告状态 | `saved` |

定时任务 run-now：

| 字段 | 结果 |
| --- | --- |
| schedule_id | `a6262955-0146-4873-b2bf-d8bb4ea8001f`，测试后已删除 |
| run_id | `561dd0b6-4fe6-44b1-b236-a22b6b005cbb` |
| external_session_id | `836e14fb-5f42-4385-9550-b27b79285bfb` |
| 最终状态 | `succeeded` |
| report_id | `0f7ec220-7d8e-4318-b1c0-7246e8635771` |

手动与定时两条路径均未出现 Skill 404 或 `SKILL_NOT_FOUND`。

### 历史 Agent 修复账号 t03

- 旧默认报告 Agent：`aida-agent-djrmjsu9702f`。
- 修复前报告 SkillRef 缺失。
- 修复后 SkillRef：`100866/aida-report@1.0.0`。
- 真实运行 run_id：`d8a3c330-5ef2-4c18-a803-095eb9017217`。
- Agent 平台 Session：`3a4eaaf4-81fe-44d7-9415-50d877c51c37`。
- 最终状态：`succeeded`。
- report_id：`737076a1-8e13-48f7-bb6a-56716774e3ca`。

该结果覆盖历史 owner 修复后再运行的主链路。

## Case 结论

| Case 范围 | 结果 | 说明 |
| --- | --- | --- |
| `TC-SYS-SKILL-001` 至 `005` | PASS | 开发系统资产、环境 owner、默认 Agent 引用和本地用户名隔离通过 |
| `TC-SYS-SKILL-006` 至 `011` | PASS | 配置错误、owner 为空、缺失、归档、旧 owner 修复和失败不污染由自动化测试覆盖 |
| `TC-SYS-SKILL-012` 至 `017` | PASS | 手动、定时、Skill 文件、MCP 用户身份和内联边界通过 |
| `TC-SYS-SKILL-018` | PASS | 默认 Agent 创建幂等 |
| `TC-SYS-SKILL-019` | PASS | t03 与 t05 共享同一系统 Skill，报告分别写回各自用户 |
| `TC-SYS-SKILL-020` | NOT RUN | 50 个真实并发定时任务留给完整报告回归批次 |
| `TC-SYS-SKILL-021` | PASS | 现有全量 handler 测试覆盖普通自定义 Skill/Agent 路径 |
| `TC-SYS-SKILL-022` | PASS | 缺失 owner 的配置错误和禁止 fallback 已自动化验证 |

## 已知环境数据

- t03、t05 等历史测试账号在 Agent 平台仍有旧的用户自有 `aida-report` 版本，这是改动前的测试脏数据。
- 本轮代码不会再创建新的普通用户报告 Skill；资源选择器会过滤这些历史重复资产，只注入 `100866` 的系统 Skill。
- 本轮保留 t03、t05 的 Agent 和报告结果用于人工核验，只删除了临时 schedule。

## 最终结论

14.157 开发环境通过。

系统 Skill 可以由普通用户跨 owner 正常选择、读取和运行；手动与定时报告链路都成功写回，历史 owner 错配修复没有回归。生产发布前仍需先由 Registry owner `10086` 发布对应生产 Skill，再部署生产配置。
